---
phase: 04-refresh-and-anchor
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-03"]
files_modified:
  - docs/phase-2/cardano-realism-audit.md
autonomous: true
requirements: [DOC-01, DOC-03]
tags: [docs, audit-refresh, calibration-provenance, disclosure-statements]

must_haves:
  truths:
    - "The 2026-05-13 and 2026-05-14 annotation banners are stripped from the audit; the sections they invalidated are removed (single-producer disclosure prose, historical accumulator-cadence framing) per CONTEXT.md D-38"
    - "Every calibration value in the audit is presented as a `(value, source, date-retrieved YYYY-MM-DD)` triple per ROADMAP.md success criterion #1"
    - "The CIP-pasteable substrate-scope paragraph for upstream-Leios-simulator limitations is included in the audit per ROADMAP.md success criterion #1"
    - "The §'Pricing-controller calibration' section is rewritten to incorporate Plan 04-01's per-sub-knob anchor decisions; each sub-knob ANCHORED has its motivating citation cited; each DISCLOSED has a 'conditional on X' disclosure"
    - "The §'Recommended disclosure statements' section is regenerated against Phase 3 evidence per CONTEXT.md D-39: un-reserved arms outperform single-lane EIP-1559; RB-reserved arms underperform; the multiplier_floor regime-dependence; the cross-arm duplicate-job artefact"
    - "The refreshed audit reads in single authoritative voice; the audit is dual-purpose (calibration-provenance + CIP-pasteable disclosure prose) per CONTEXT.md D-39"
  artifacts:
    - path: "docs/phase-2/cardano-realism-audit.md"
      provides: "Refreshed audit; single authoritative voice; (value, source, date-retrieved) triples; Phase-3-narrated disclosure statements"
      min_lines: 300
  key_links:
    - from: "docs/phase-2/cardano-realism-audit.md §'Pricing-controller calibration'"
      to: ".planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md per-sub-knob 'Draft audit-section copy'"
      via: "Plan 04-01's draft audit-section copy is folded into this section verbatim or with editorial polish; each of the four sub-knobs has its motivating-citation or conditional-on-X copy here"
      pattern: "(window length 32|multiplier.floor 4|multiplier.floor 16|lane.signal.source)"
    - from: "docs/phase-2/cardano-realism-audit.md §'Recommended disclosure statements'"
      to: ".planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md §'Headline numerical findings'"
      via: "Plan 04-03's headline findings paragraphs (un-reserved outperform; RB-reserved underperform; multiplier_floor regime-dependence; cross-arm duplicate-job artefact) are folded into this section as CIP-pasteable disclosure prose"
      pattern: "(un-reserved.*outperform|RB-reserved.*underperform|regime-dependent|cross-arm)"
---

<objective>
Full rewrite of `docs/phase-2/cardano-realism-audit.md` per CONTEXT.md D-38: single authoritative voice (strip the two 2026-05-13 / 2026-05-14 annotation banners; remove the sections they invalidated); apply `(value, source, date-retrieved)` triple format uniformly across calibration values; include the CIP-pasteable substrate-scope paragraph; regenerate §"Recommended disclosure statements" against Phase 3 evidence per D-39; incorporate Plan 04-01's anchor-or-disclose decisions for the four un-anchored controller knobs.

Purpose: This is the authoritative calibration-provenance + CIP-paste-target document. The CIP author pastes §"Recommended disclosure statements" prose into the CIP Limitations section. Approximate length target: 410 → 300-400 lines per CONTEXT.md D-38 (the rewrite is leaner because the banners and the invalidated single-producer / historical accumulator-cadence prose come out, but the substrate-scope paragraph and the regenerated Phase-3-narrated disclosure prose go in).

Output: `docs/phase-2/cardano-realism-audit.md` refreshed in place. Phase 4's first significant document edit; consumed by Phase 5's CIP-author handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-refresh-and-anchor/04-CONTEXT.md
@CLAUDE.md
@docs/phase-2/cardano-realism-audit.md
@docs/phase-2/realism-risks-register.md
@docs/phase-2/mechanism-design.md
@docs/phase-2/calibration-fix-postmortem.md
@.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md
@.planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md
@.planning/family-b-decision-2026-05-14.md
@.planning/mechanism-welfare-impact-2026-05-14.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip banners + rewrite §'Verdict by category' + §'What lines up with mainnet' + §'What needs disclosure' (Fee structure subsection) + apply triple format</name>
  <files>docs/phase-2/cardano-realism-audit.md</files>
  <read_first>
    - docs/phase-2/cardano-realism-audit.md (full file — current 410 lines; the two banners at lines ~9-21 and ~134-148, the §"Topology and actor model" banner at lines ~209-218)
    - .planning/phases/04-refresh-and-anchor/04-CONTEXT.md §"DOC-01 / DOC-02 refresh strategy" — D-38 (full rewrite in single authoritative voice; banners stripped; sections they invalidated removed; calibration values as (value, source, date-retrieved) triples; substrate-scope paragraph included; 410 → 300-400 lines target) + Claude's Discretion §"Audit narrative ordering (DOC-01)" (default: keep existing four-section ordering)
    - docs/phase-2/calibration-fix-postmortem.md (supplies the `(value=0.05, source=docs/phase-2/calibration-fix-postmortem.md, date-retrieved=2026-05-14)` triple for `rb-generation-probability`; also supplies retrieval-date 2026-05-14 for the epoch-582 stake snapshot)
    - .planning/family-b-decision-2026-05-14.md (the chain-derived controller commit + decision memo; the historical "Update 2026-05-14" banner content folds into prose citing this memo)
    - CLAUDE.md §"Calibration choices" (every calibration value to be triple-formatted has its provenance documented here; rb-generation-probability = 0.05 cites activeSlotsCoeff; topology-realistic-100.yaml cites spike 006-curve-design + epoch-582 retrieval; etc.)
    - CLAUDE.md §"Conventions / gotchas" (abbreviation-on-first-use rule applies)
  </read_first>
  <action>
Refresh the audit's header + §"TL;DR" + §"Verdict by category" + §"What lines up with mainnet" + §"What needs disclosure" §§"Fee structure and mempool sizing" sub-section in place.

**Header** — keep the H1 title `# Cardano-realism audit — phase-2 dynamic-pricing simulator`. Update the date line to `Date: 2026-05-18` (today's date is 2026-05-18 per the system memory). Keep the Branch / Scope / Evidence lines. Strip the `> **[Annotation added 2026-05-13]**` block at lines 9-21 entirely — fold its content into the §"What needs disclosure" §"Topology and actor model" rewrite (Task 2). After the Evidence line add an abbreviation-on-first-use expansion line: "Abbreviations on first use: Cardano Improvement Proposal (CIP), Ethereum Improvement Proposal 1559 (EIP-1559), ranking block (RB), endorser block (EB), single-pool operator (SPO), Bias-corrected and accelerated (BCa) bootstrap, Inter-Quartile Range (IQR), Conference on Computer and Communications Security (CCS), Advances in Financial Technologies (AFT)."

**§"TL;DR"** — rewrite to reflect the post-banner state. The current TL;DR has three issues: (a) it claims `topology-single-producer.yaml` collapses ~3,000 mainnet SPOs to N=1 (this is no longer true — `topology-realistic-100.yaml` is operational; the banner at lines 9-21 was the historical correction); (b) it references "per-priced-block cadence" without naming chain-derived Family B (the 2026-05-14 commit landed); (c) it doesn't reflect Phase 3 findings. Rewrite to:
- Open with the chain-derived Family B framing (per CLAUDE.md §"Mechanism choice and audit trail"): the controller is EIP-1559-faithful with one step per canonical block; reorg-safe by construction.
- Name the topology accurately: `topology-realistic-100.yaml` (100-node mass-stratified mainnet stake curve from epoch 582, retrieved 2026-05-14); ~3,000 SPOs downsampled.
- Summarise the three disclosure categories: (i) fee `field` reinterpreted as maxFee envelope (refund-CIP dependency); (ii) four controller knobs anchored-or-disclosed per Plan 04-01; (iii) topology / actor-model substrate-scope.
- Close with the Phase 3 headline (un-reserved arms outperform single-lane EIP-1559; RB-reserved arms underperform; multiplier_floor regime-dependent at floor = 16) per `.planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md` §"Headline numerical findings".

**§"Verdict by category"** — refresh the table. Rows: RB cadence and capacity / Fee structure and mempool sizing / Pricing-controller calibration / Topology and actor model. Verdict + Disclosure-level columns. The Pricing-controller calibration row's Disclosure-level cell incorporates Plan 04-01's umbrella verdict outcome (MITIGATED iff all four sub-knobs ANCHORED; otherwise per-sub-knob granularity). The Topology row's Disclosure-level cell names the substrate-scope umbrella per CONTEXT.md D-38 (single-producer prose stripped; 100-node multi-producer is the operational reality).

**§"What lines up with mainnet"** — rewrite the bulleted list applying `(value, source, date-retrieved YYYY-MM-DD)` triple format to every calibration value. Specifically:
- `rb-generation-probability = 0.05` → triple: `(rb-generation-probability = 0.05, source: docs/phase-2/calibration-fix-postmortem.md citing Cardano mainnet activeSlotsCoeff, date-retrieved: 2026-05-14)`
- `rb-body-max-size-bytes = 90112` → triple: `(rb-body-max-size-bytes = 90112, source: Cardano mainnet protocol parameters since April 2022 update, date-retrieved: 2026-05-14)`
- `min-fee-a = 44`, `min-fee-b = 155381` → one triple each citing Conway-era mainnet
- `maxTxSize = 16384 bytes` → triple citing upstream config.default.yaml
- Mempool cap rule `2 × one_bearer_block` → cite mainnet rule
- EIP-1559 controller parameters `D = 8` (BASE_FEE_MAX_CHANGE_DENOMINATOR), `target = 0.5` (ELASTICITY_MULTIPLIER = 2), per-priced-block cadence → triple per parameter citing Ethereum mainnet
- Linear-Leios knobs `linear-vote-stage-length-slots = 4`, `linear-diffuse-stage-length-slots = 7`, `eb-referenced-txs-max-size-bytes = 12000000`, `eb-body-validation-cpu-time-ms-per-byte = 2.15e-5`, `n = 600`, `τ = 75%` → triple per knob citing CIP-0164 Table 7 (with the caveat that CIP-0164 is pre-deployment — preserve the existing caveat prose)

Each triple appears inline in the prose. Format consistency check: the triple appears as `(value, source: <citation>, date-retrieved: YYYY-MM-DD)` — exact format so Plan 04-07's regex audit `[a-z_-]+(-bytes|-probability|...)?:?\s*[0-9.]+\s*\(.*source:.*date-retrieved:.*\d{4}-\d{2}-\d{2}\)` matches.

**§"What needs disclosure" §§"Fee structure and mempool sizing"** — refresh the three disclosure items (Fee-field semantic reinterpretation / Mempool absolute byte cap 133× mainnet / Default `max_fee_policy = {4, 1}`). The existing prose is good shape; the rewrite applies the triple format consistently to in-prose calibration values (e.g. `(mempool-max-total-size-bytes = 2 × eb_referenced_txs_max_size_bytes = 24 MB, source: derived per CIP-0164 12 MB EB target, date-retrieved: 2026-05-14)`) and removes the historical "Spike 002 §Findings + §Verdict item N" cross-references at paragraph tails (these are internal-process pointers; the audit is CIP-pasteable so internal-process pointers are anti-conventional — but verify the existing audit's evidence section retains the spike-README links so the trail is preserved at document level).

Do NOT touch §"What needs disclosure" §§"Pricing-controller calibration" or §§"Topology and actor model" in this task (Task 2 handles them).

Word-count target for the sections refreshed in this task: 200-300 lines combined.
  </action>
  <verify>
    <automated>! grep -q "Annotation added 2026-05-13" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -q "topology-realistic-100" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -cE "date-retrieved: 20[0-9]{2}-[0-9]{2}-[0-9]{2}" docs/phase-2/cardano-realism-audit.md | awk '$1 &gt;= 5 {exit 0} {exit 1}' &amp;&amp; grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -q "Family B" docs/phase-2/cardano-realism-audit.md</automated>
  </verify>
  <done>The 2026-05-13 annotation banner is gone; topology-realistic-100 is the operational topology in TL;DR and Verdict-by-category; at least five `(value, source, date-retrieved YYYY-MM-DD)` triples present in §"What lines up with mainnet" + §"Fee structure"; abbreviation-on-first-use rule honoured in the header; Family B framing present.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite §'Pricing-controller calibration' (folding Plan 04-01 anchor decisions) + §'Topology and actor model' (stripping single-producer prose; folding substrate-scope paragraph)</name>
  <files>docs/phase-2/cardano-realism-audit.md</files>
  <read_first>
    - docs/phase-2/cardano-realism-audit.md (post-Task-1 state; the existing §"Pricing-controller calibration" lines 132-205 with the `> **Update 2026-05-14:**` banner and the four disclosure items 1-4; the existing §"Topology and actor model" lines 207-285 with the `> **[Corrected 2026-05-13]**` banner and the five disclosure items 1-5)
    - .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md (the four per-sub-knob blocks with Draft audit-section copy ready to be folded into §"Pricing-controller calibration"; the umbrella verdict recommendation)
    - .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md §"TEST-07a multiplier-floor-16 companion" + §"Headline numerical findings" — the multiplier_floor regime-dependence finding to be narrated into disclosure-item 2's rewrite per CONTEXT.md Claude's Discretion §"Multiplier-floor regime-dependence narration (DOC-01 and register)"
    - docs/phase-2/realism-risks-register.md §"RSK-substrate-scope" (lines 106-133; the load-bearing substrate-scope disclosure-paragraph drafted in Phase 1 plan-02; folds into Task 2's §"Topology and actor model" rewrite per CONTEXT.md / Phase 1 plan-02 SUMMARY — but per CONTEXT.md `<deferred>` "Replacing the audit's CIP-pasteable disclosure paragraphs with register disclosure-paragraph content verbatim" is out of scope; the audit retains its own engineering-report-voice prose but may cross-reference the register entry)
    - .planning/family-b-decision-2026-05-14.md (the chain-derived controller framing; §"What changed" supplies the by-construction reorg-safety claim)
    - CLAUDE.md §"Calibration choices" (the multiplier_floor calibration narrative; the rb-generation-probability triple)
  </read_first>
  <action>
Rewrite §"Pricing-controller calibration" and §"Topology and actor model" in place. Strip the two banners (`> **Update 2026-05-14:**` at lines ~134-148 and `> **[Corrected 2026-05-13]**` at lines ~209-218). Fold the necessary historical content into authoritative prose.

**§"Pricing-controller calibration"** — rewrite the four disclosure items (1: window-length 32; 2: multiplier-floor 4 calibration accommodation; 3: spec-default multiplier-floor 16; 4: spec-open lane-signal-source choices) using Plan 04-01's per-sub-knob Draft audit-section copy.

For each of the four sub-knobs, fold in Plan 04-01's draft audit-section copy:
- The `(value, source, date-retrieved)` triple for each sub-knob (e.g. for window-length 32 if ANCHORED via Liu et al. CCS 2022: `(window length = 32 blocks, motivating citation: Liu et al. CCS 2022, date-retrieved: YYYY-MM-DD)`)
- 1-3 sentences naming the motivating citation if ANCHORED, or the conditional-on-X frame if DISCLOSED
- Cross-reference to the umbrella-verdict in the register: "see `RSK-un-anchored-controller-knobs` in [`docs/phase-2/realism-risks-register.md`](realism-risks-register.md) for the per-sub-knob disclosure-paragraph"

Per CONTEXT.md Claude's Discretion §"Multiplier-floor regime-dependence narration (DOC-01 and register)", rewrite disclosure-item 2 to LEAD WITH the regime-dependent finding from TEST-07a (sourced from Plan 04-03's §"TEST-07a multiplier-floor-16 companion" sub-section). The new opening sentence is "multiplier-floor 4 is a calibration accommodation chosen to surface controller drift at moderate priority demand. TEST-07a (Phase 3) found that at multiplier-floor 16, the `phase-2-rb-scarcity` finding inverts ('standard dominates welfare' → 'priority captures everything; total welfare collapses 93–98%') and the `phase-2-urgency-inversion` finding weakly reverses ('mispriced > correctly priced' → 'correctly priced > mispriced by ~13%'). Welfare findings from these two suites are conditional on the multiplier-floor = 4 calibration." Keep the existing prose that names the floor-sweep coverage in 5 of 7 suites (4 / 8 / 16) as the bounding evidence.

Add a closing paragraph naming the per-sub-knob anchor decisions in summary form (per Plan 04-01's umbrella verdict): "The umbrella anchor decision per Plan 04-01: window-length 32 → {ANCHORED via Liu / Reijsbergen / DISCLOSED}; multiplier-floor 4 → {ANCHORED via spec / DISCLOSED}; multiplier-floor 16 → {ANCHORED via spec / DISCLOSED}; lane-signal-source → DISCLOSED. See [`docs/phase-2/realism-risks-register.md`](realism-risks-register.md) `RSK-un-anchored-controller-knobs` for per-sub-knob disclosure-paragraph prose." (Replace `{ANCHORED via X / DISCLOSED}` with the actual Plan 04-01 verdicts read from the 04-01-DOC-03-anchor-search.md `### Umbrella verdict` table.)

**§"Topology and actor model"** — strip the 2026-05-13 corrected banner entirely. Strip the single-producer disclosure prose (current items 1-2 frame the audit assuming `topology-single-producer.yaml`; this is no longer the operational topology). Rewrite to reflect the operational 100-node `topology-realistic-100.yaml` reality:

- New disclosure item 1: 100-node topology vs mainnet ~3,000 SPOs. Triple: `(topology = topology-realistic-100.yaml, source: epoch-582 mainnet snapshot at .planning/spikes/006-curve-design/README.md, date-retrieved: 2026-05-14)`. Cite top-1 stake share = 1.97%, Nakamoto coefficient = 35, Gini = 0.253. Cross-reference `RSK-pool-count` for the pool-count-sensitivity disclosure. Phase 3 TEST-05 disposition per Plan 04-03 (re-run not in Phase 4 scope; register entry stays DISCLOSED via Phase 1 plan-02 disclosure-paragraph) → cite the register entry rather than re-narrate.
- Replace the historical "honest-producer assumption is operationally trivial under N=1" prose with a multi-producer framing: under multi-producer the `partition_activated` bit is body-derivable risk (per `RSK-partition-activated-honest-producer`); cross-reference the register entry.
- Keep the existing item 3 (actor demand-mix order-of-magnitude correct, not bit-calibrated) — but apply triple format to demand-mix references.
- Keep the existing item 4 (`target_inclusion_blocks` defaults are mechanism-induced) with triple format.
- Keep the existing item 5 (demand non-stationarity at finer-than-2-hour scale not modelled; MEV / strategic actors absent) — but expand the strategic-actor caveat to cross-reference `RSK-substrate-scope`'s utility-maximising-actor-model sub-point and cite Chung and Shi SODA 2023.

Add a NEW closing paragraph: the substrate-scope umbrella paragraph per CONTEXT.md D-38 + ROADMAP.md success criterion #1. The paragraph is CIP-pasteable engineering-report voice; lists the three sub-points (a) floating-point arithmetic in non-pricing code paths; (b) propagation-model fidelity; (c) utility-maximising actor model. Cross-reference `RSK-substrate-scope` in the register for the load-bearing CIP-paste prose. This is the dual-purpose audit pattern per D-39 — the register's prose is the canonical CIP-paste source, but the audit also carries its own engineering-report-voice summary for self-containment.

Word-count target for this task: 100-150 lines combined.
  </action>
  <verify>
    <automated>! grep -q "Update 2026-05-14" docs/phase-2/cardano-realism-audit.md &amp;&amp; ! grep -q "Corrected 2026-05-13" docs/phase-2/cardano-realism-audit.md &amp;&amp; ! grep -q "single-producer topology (N=1)" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -q "topology-realistic-100" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -qE "(RSK-un-anchored-controller-knobs|realism-risks-register\.md)" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -qE "(regime-dependent|TEST-07a|multiplier.floor 16)" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -qE "(substrate-scope|RSK-substrate-scope)" docs/phase-2/cardano-realism-audit.md</automated>
  </verify>
  <done>The two annotation banners are stripped; single-producer prose is gone; topology-realistic-100 is named with epoch-582 stake source + 2026-05-14 retrieval date; the four sub-knob anchor decisions are reflected per Plan 04-01; the multiplier-floor regime-dependence finding leads disclosure item 2 per CONTEXT.md Claude's Discretion; substrate-scope umbrella paragraph is included.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate §'Recommended disclosure statements' against Phase 3 evidence + refresh §'Recommended next steps' + verify document is dual-purpose CIP-pasteable</name>
  <files>docs/phase-2/cardano-realism-audit.md</files>
  <read_first>
    - docs/phase-2/cardano-realism-audit.md (post-Task-2 state; the §"Recommended disclosure statements" at current lines ~294-368 with five paragraphs: fee-field semantics / controller calibration / topology / demand modelling / mempool sizing)
    - .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md §"Headline numerical findings" — un-reserved arms outperform; RB-reserved arms underperform; cross-arm duplicate-job artefact at sundaeswap_moderate × floor=4; multiplier_floor regime-dependence at floor=16
    - .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md (the four per-sub-knob blocks; the umbrella verdict)
    - .planning/phases/04-refresh-and-anchor/04-CONTEXT.md §"DOC-01 / DOC-02 refresh strategy" — D-39 (dual-purpose; "Recommended disclosure statements" regenerated against Phase 3 evidence; un-reserved arms outperform / RB-reserved arms underperform / multiplier_floor=4 regime-dependent / cross-arm duplicate-job artefact replicates) + Claude's Discretion §"Audit narrative ordering" + §"Multiplier-floor regime-dependence narration"
    - docs/phase-2/realism-risks-register.md (the load-bearing disclosure paragraphs for fee-as-maxFee-envelope / mempool-cap-magnitude / max-fee-policy-default / demand-mix-bit-calibration / target-inclusion-blocks-default — the audit's "Recommended disclosure statements" cross-references but does not deduplicate against per CONTEXT.md `<deferred>`)
  </read_first>
  <action>
Regenerate §"Recommended disclosure statements" against Phase 3 evidence per CONTEXT.md D-39. The existing five-paragraph structure is preserved (Fee-field semantics / Controller calibration / Topology / Demand modelling / Mempool sizing) plus add a sixth paragraph on the Phase-3-narrated welfare distinction.

**§"Recommended disclosure statements" preamble** — keep the opening sentence ("The following paragraphs are ready to paste into a 'Limitations and Modeling Assumptions' section of a phase-2 paper / CIP write-up. Tone is rigorous, not defensive."). Add a second sentence: "These paragraphs are dual-purpose with the per-`RSK-NN` disclosure-paragraphs in [`docs/phase-2/realism-risks-register.md`](realism-risks-register.md); the audit's paragraphs are engineering-report-voice summaries; the register's paragraphs are the per-risk canonical CIP-paste source."

**"On fee-field semantics"** — keep the existing paragraph; verify it cross-references `RSK-fee-as-maxFee-envelope` in the register. Apply triple format if any calibration values appear.

**"On controller calibration"** — rewrite to lead with the Phase 3 multiplier_floor regime-dependence finding (per CONTEXT.md Claude's Discretion §"Multiplier-floor regime-dependence narration"). The new paragraph structure:
- Opening: name the two regime-dependent findings from TEST-07a (rb-scarcity inversion and urgency-inversion weak reversal at floor=16); cite [`.planning/realism-tests/multiplier-floor-16-companion/results.md`](../../.planning/realism-tests/multiplier-floor-16-companion/results.md) as the source.
- Middle: name the four un-anchored controller knobs (window-length 32, multiplier-floor 4, multiplier-floor 16, lane-signal-source) and the anchor-or-disclose disposition per Plan 04-01's umbrella verdict; cross-reference `RSK-un-anchored-controller-knobs` for the per-sub-knob disclosure-paragraphs.
- Close: re-state the existing "core parameters match Ethereum mainnet" framing (D=8, target=0.5, per-priced-block cadence under Family B) per CLAUDE.md §"Mechanism choice and audit trail".

**"On topology"** — rewrite to reflect the operational topology. Strip the historical single-producer / N=1 framing. New paragraph: "This phase uses a 100-node mass-stratified mainnet stake-curve topology (`topology-realistic-100.yaml`); the curve is a downsample of the Cardano mainnet snapshot at epoch 582 (retrieved 2026-05-14 per [docs/phase-2/calibration-fix-postmortem.md](calibration-fix-postmortem.md)). Mainnet operates approximately 3,000 stake pool operators (SPOs); pool-count sensitivity within the 100-to-150 range is bounded by `RSK-pool-count` in [`docs/phase-2/realism-risks-register.md`](realism-risks-register.md); behaviour at deployed-mainnet pool counts (~3,000) is named there as DISCLOSED. The honest-producer assumption operates by construction in the simulator; under multi-producer with a Byzantine producer the `partition_activated` claim attack surface is not exercised — see `RSK-partition-activated-honest-producer` in the register for the disclosure paragraph." Apply triple format to the topology calibration value.

**"On demand modelling"** — keep mostly intact; verify cross-reference to `RSK-demand-mix-bit-calibration` and `RSK-demand-non-stationarity` in the register; apply triple format to any quantitative values.

**"On mempool sizing"** — keep mostly intact; verify cross-reference to `RSK-mempool-cap-magnitude`; apply triple format to the 24 MB / 180 KB values.

**NEW paragraph "On the menu-item welfare distinction"** (per CONTEXT.md D-39) — author from Plan 04-03's §"Headline numerical findings". Paragraph content: "Phase 3 multi-seed evidence (TEST-04 at N=20 seeds, sundaeswap_moderate demand, multiplier_floor = 4; results at [`.planning/realism-tests/multi-seed-variance/results.md`](../../.planning/realism-tests/multi-seed-variance/results.md)) establishes the welfare ranking among the four CIP menu options:
- **Un-reserved menu arms materially outperform single-lane EIP-1559**: priority-only un-reserved Δ retained_value = +6.66e+09 (95% BCa confidence interval [+4.28e+09, +8.49e+09]); both-dynamic un-reserved Δ = +7.95e+09 (CI [+5.65e+09, +1.09e+10]). Sign-coherence 0.90 across 20 seeds.
- **RB-reserved menu arms underperform single-lane EIP-1559 under the same calibration**: priority-only RB-reserved Δ = −4.15e+09 (CI [−6.02e+09, −1.00e+09]); both-dynamic RB-reserved (partitioned) Δ = −4.15e+09 (CI [−5.95e+09, −8.87e+08]). The pre-Phase-3 single-seed framing 'two-lane mechanisms outperform single-lane EIP-1559' held only for the un-reserved variants under this calibration.
- **The cross-arm duplicate-job artefact (partitioned ≡ RB-reserved welfare at sundaeswap_moderate × multiplier_floor = 4) replicates at N=20** because the standard quote never drifts off the multiplier floor. This is a calibration-conditional menu indistinguishability finding worth disclosing.
- Phase 3 hash-diversity gate: 17 of 17 BACKED-eligible cells pass at distinct-hash count = N. See [`docs/phase-2/coverage-check.md`](coverage-check.md) for the per-claim coverage map; see [`docs/phase-2/realism-risks-register.md`](realism-risks-register.md) `RSK-single-seed-precision` for the disclosure paragraph on precision."

**§"Recommended next steps"** — refresh the existing 6-bulleted-item list. Remove the "M6 (already on this branch)" item (the 600-pool migration is out of scope per PROJECT.md — superseded by TEST-05 which is now disclose-only per Plan 04-03). Remove the "Optional: strategic-actor demand profile" item (out of scope per PROJECT.md; adversarial / strategic-bidder modelling is in `RSK-substrate-scope`). Remove the "Optional: body-derivable `partition_activated`" item (out of scope; `RSK-partition-activated-honest-producer` disclosure covers it). KEEP the "Hard dependency to flag in any publication" item on the refund-CIP. KEEP the "Documentation residual" item on CIP-0164 Table 7. ADD a new item: "Pool-count sensitivity above 100 pools is currently disclose-only per `RSK-pool-count`; a future TEST-05 re-run at 100 vs 150 pools (recipe in [`.planning/realism-tests/pool-number-sensitivity/results.md`](../../.planning/realism-tests/pool-number-sensitivity/results.md)) would replace the disclosure with a MITIGATED verdict if Δ% on welfare metrics is within seed-IQR." ADD a new item: "Run-length / steady-state validation is similarly disclose-only per `RSK-steady-state-run-length`; future TEST-06 re-run recipe in [`.planning/realism-tests/run-length-steady-state/results.md`](../../.planning/realism-tests/run-length-steady-state/results.md)."

§"Evidence" — leave intact (spike-README citations).

Final pass: audit the entire document for the CLAUDE.md abbreviation-on-first-use rule. Every abbreviation expanded on first use somewhere in the document (Cardano Improvement Proposal (CIP), Ethereum Improvement Proposal 1559 (EIP-1559), ranking block (RB), endorser block (EB), Stake Pool Operator (SPO), Bias-corrected and accelerated (BCa) bootstrap, Inter-Quartile Range (IQR), Conference on Computer and Communications Security (CCS), Advances in Financial Technologies (AFT), Symposium on Discrete Algorithms (SODA), Maximum Extractable Value (MEV), extended unspent-transaction-output (eUTxO)). Audit for residual TBD / "as of today" / "current" / "operational" qualifiers that imply tense-fragility; replace with explicit `(date-retrieved YYYY-MM-DD)` markers.

Word-count target combined across all three tasks: final file should be 300-400 lines per CONTEXT.md D-38.
  </action>
  <verify>
    <automated>grep -cE "^## Recommended disclosure statements" docs/phase-2/cardano-realism-audit.md | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; grep -qE "(un-reserved.*outperform|outperform.*un-reserved)" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -qE "(RB-reserved.*underperform|underperform.*RB-reserved)" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -qE "(regime-dependent|TEST-07a)" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -qE "cross-arm" docs/phase-2/cardano-realism-audit.md &amp;&amp; grep -cE "RSK-(fee-as-maxFee-envelope|un-anchored-controller-knobs|pool-count|substrate-scope|partition-activated-honest-producer|steady-state-run-length)" docs/phase-2/cardano-realism-audit.md | awk '$1 &gt;= 5 {exit 0} {exit 1}' &amp;&amp; test $(wc -l &lt; docs/phase-2/cardano-realism-audit.md) -ge 300 &amp;&amp; test $(wc -l &lt; docs/phase-2/cardano-realism-audit.md) -le 500</automated>
  </verify>
  <done>§"Recommended disclosure statements" carries Phase 3 evidence (un-reserved-outperform, RB-reserved-underperform, multiplier_floor regime-dependence, cross-arm duplicate-job artefact); the section cross-references at least 5 RSK-NN entries in the register; §"Recommended next steps" refreshed; the file lands 300-500 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit prose → CIP Limitations section | The "Recommended disclosure statements" section is the CIP paste-target. Fabricated citations or inaccurate triple values would land in the CIP. |
| Plan 04-01 / 04-03 inputs → audit prose | The four sub-knob anchor decisions and the Phase 3 numerical findings are extracted in Wave 1 and consumed here verbatim; transcription errors would propagate to CIP-pasteable prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | T (Tampering) | Calibration triple values | mitigate | Each `(value, source, date-retrieved YYYY-MM-DD)` triple cites a specific source file; Plan 04-07's regex audit enforces format conformance |
| T-04-11 | I (Information Disclosure) | Phase 3 numerical findings in CIP-paste-prose | mitigate | Numbers are extracted from Plan 04-03's §"Headline numerical findings"; Plan 04-07 re-verifies against `.planning/realism-tests/multi-seed-variance/results.md` |
| T-04-12 | R (Repudiation) | Banner-stripped historical content | accept | The 2026-05-13 / 2026-05-14 corrections are folded into authoritative prose; the original banners' content is preserved in git history if a reviewer needs the audit trail |
</threat_model>

<verification>
- The two annotation banners (`> **[Annotation added 2026-05-13]**`, `> **Update 2026-05-14:**`, `> **[Corrected 2026-05-13]**`) are stripped
- `topology-realistic-100.yaml` is the operational topology in TL;DR / Verdict / What-lines-up-with-mainnet / Recommended-disclosure-statements
- At least 5 `(value, source, date-retrieved YYYY-MM-DD)` triples present in calibration-claim sections
- Phase 3 headline findings (un-reserved outperform / RB-reserved underperform / multiplier_floor regime-dependence / cross-arm duplicate-job) present in §"Recommended disclosure statements"
- §"Pricing-controller calibration" reflects Plan 04-01's per-sub-knob anchor decisions
- Substrate-scope paragraph included with cross-reference to `RSK-substrate-scope`
- At least 5 `RSK-NN` cross-references in §"Recommended disclosure statements"
- File lands between 300 and 500 lines per CONTEXT.md D-38 target (410 → 300-400 with substrate-scope paragraph and Phase 3 prose added)
- All abbreviations expand on first use
</verification>

<success_criteria>
- ROADMAP.md Phase 4 success criterion #1: `docs/phase-2/cardano-realism-audit.md` reads in authoritative voice (annotation banners removed and folded in), every calibration value as `(value, source, date-retrieved)` triple, CIP-pasteable substrate-scope paragraph included
- ROADMAP.md Phase 4 success criterion #3 (audit-side): each of the four un-anchored controller knobs has either an external anchor or a "conditional on X" disclosure paragraph (per Plan 04-01's umbrella verdict, narrated into §"Pricing-controller calibration")
</success_criteria>

<output>
After completion, create `.planning/phases/04-refresh-and-anchor/04-04-SUMMARY.md`. The SUMMARY records: final line count; banner-stripping diff (which lines went away, which sections shrunk); the per-sub-knob anchor narration outcome from Plan 04-01; the Phase 3 evidence integration outcome from Plan 04-03; any deviations from CONTEXT.md D-38 / D-39 / Claude's Discretion guidance.
</output>
