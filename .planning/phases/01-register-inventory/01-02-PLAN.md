---
phase: 01-register-inventory
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/phase-2/realism-risks-register.md
autonomous: true
requirements: [REG-02, REG-03, REG-04, REG-05]
must_haves:
  truths:
    - "Every RSK-NN entry has a final verdict from the vocabulary LIVE / DORMANT / MITIGATED / DISCLOSED (no TBD placeholders remain)"
    - "Every LIVE entry is paired with at least one EXP-NN identifier and a populated scope-of-resolution field stating the explicit hypothesis the verdict must establish or refute"
    - "RSK-pool-count carries the locked scope-of-resolution text 'Δ% < seed-IQR of same job at 100 pools establishes MITIGATED' (or semantically equivalent prose) verbatim before any Phase 3 test runs"
    - "Every DISCLOSED entry carries a CIP-pasteable disclosure-paragraph in Cardano CIP house style (engineering-report voice, abbreviations expanded on first use, no internal jargon like raw 'WR-1' without context)"
    - "The four mandatory LIVE entries from D-09 retain their LIVE verdict and have populated scope-of-resolution + EXP-NN linkage"
    - "Verification: the register is internally consistent — no dead references between Index table and per-entry sections, no orphan RSK-NN in either direction, every EXP-NN slug used has a cross-reference column to its TEST-NN REQ-ID per CONTEXT.md Claude's Discretion 'EXP-NN ↔ TEST-NN alignment'"
    - "CONTEXT.md decisions honoured: D-01 (per-threat content from validity-threats appears as RSK-NN entries; per-claim trust ratings deferred to Phase 2 coverage check), D-04 (spike READMEs and mechanism-welfare-impact-2026-05-14.md remain evidence sources cited in evidence-for/against; their verdicts are NOT used as register verdicts), D-05 (Leios RSK-NN and EXP-NN identifier convention used; EXP-NN slugs are append-only and cross-reference TEST-NN REQ-IDs per the provisional mapping EXP-pool-number → TEST-05, EXP-sign-flip-variance → TEST-03, EXP-canonical-variance → TEST-04, EXP-run-length → TEST-06, with EXP-window-length-anchor / EXP-multiplier-floor-anchor / EXP-lane-signal-source-anchor handled by Phase 4 DOC-03), D-06 (final verdicts use exactly the four-value vocabulary LIVE / DORMANT / MITIGATED / DISCLOSED with LIVE as the default for ambiguous cells), D-07 (Wohlin four-fold categorisation preserved from plan 01; multi-tagging retained), D-08 (every entry has all nine required fields finalised; disclosure-paragraph mandatory for DISCLOSED, optional placeholder for LIVE entries that may land as DISCLOSED later), D-09 (the four mandatory LIVE entries retain LIVE verdict), D-10 (RSK-pool-count carries the locked scope-of-resolution text verbatim)"
  artifacts:
    - path: "docs/phase-2/realism-risks-register.md"
      provides: "Finalised realism-risks register with all verdicts assigned, LIVE entries scoped, DISCLOSED entries CIP-pasteable, and the locked scope-of-resolution text on RSK-pool-count"
      contains: "Δ% < seed-IQR"
    - path: "docs/phase-2/realism-risks-register.md"
      provides: "Index table updated with final verdicts (no 'TBD plan 02' markers remaining)"
      contains: "## Index"
  key_links:
    - from: "docs/phase-2/realism-risks-register.md"
      to: ".planning/REQUIREMENTS.md"
      via: "EXP-NN slugs cross-reference TEST-NN REQ-IDs per the provisional mapping in CONTEXT.md Claude's Discretion"
      pattern: "TEST-"
    - from: "docs/phase-2/realism-risks-register.md disclosure-paragraph fields"
      to: "the user's CIP draft (out-of-scope this milestone)"
      via: "DISCLOSED entries' disclosure-paragraph fields are pastable into the CIP's Limitations section in Phase 5 handoff"
      pattern: "Disclosure-paragraph"
---

<objective>
Finalise `docs/phase-2/realism-risks-register.md` by replacing every `TBD plan 02` placeholder with a real value. This is the judgement-and-prose pass on top of plan 01's inventory skeleton.

Purpose: Plan 01 produced a skeleton with all RSK-NN entries inventoried and descriptive fields populated. This plan assigns verdicts based on existing evidence (no new tests run this phase), pairs every LIVE entry with at least one EXP-NN identifier and a scoped hypothesis, writes the locked `RSK-pool-count` scope-of-resolution text (the canonical PITFALLS MOD-5 prevention pattern), and writes CIP-pasteable disclosure-paragraphs for every DISCLOSED entry. Then it runs a self-consistency verification pass over the file.

Output:
- `docs/phase-2/realism-risks-register.md` is the final v1 register — no `TBD plan 02` markers remain, the Index table reflects final verdicts, the locked scope-of-resolution text appears on `RSK-pool-count`, every DISCLOSED entry has a CIP-pasteable disclosure-paragraph, every LIVE entry has at least one EXP-NN slug linking to a TEST-NN REQ-ID.
- Coverage for REG-02 (required fields populated — now including verdict and disclosure-paragraph), REG-03 (every LIVE entry paired with at least one EXP-NN), REG-04 (the four mandatory LIVE entries retain LIVE verdict), REG-05 (the locked scope-of-resolution text on `RSK-pool-count`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-register-inventory/01-CONTEXT.md
@.planning/phases/01-register-inventory/01-01-SUMMARY.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@CLAUDE.md

The register file itself is the primary input. Read the entire current state of `docs/phase-2/realism-risks-register.md` before beginning Task 1.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assign final verdicts and populate scope-of-resolution + EXP-NN for every entry</name>
  <files>
    docs/phase-2/realism-risks-register.md (MODIFY — finalise judgement fields)
  </files>
  <read_first>
    - docs/phase-2/realism-risks-register.md (the full current state — produced by plan 01; contains all RSK-NN entries with TBD plan 02 markers in the judgement fields)
    - .planning/phases/01-register-inventory/01-01-SUMMARY.md (plan 01's summary — records the clustering decision for `RSK-un-anchored-controller-knobs`, the final entry count, and any exclusions plan 02 must honour)
    - .planning/phases/01-register-inventory/01-CONTEXT.md (decisions D-06 verdict vocabulary, D-09 mandatory LIVE entries, D-10 locked scope-of-resolution text for RSK-pool-count, plus Claude's Discretion item "EXP-NN ↔ TEST-NN alignment" for the cross-reference rule)
    - .planning/REQUIREMENTS.md (the TEST-NN REQ-IDs — TEST-03 sign-flip variance, TEST-04 canonical variance, TEST-05 pool-number sensitivity, TEST-06 run-length steady-state — referenced by the provisional EXP-NN ↔ TEST-NN mapping in CONTEXT.md)
    - .planning/research/SUMMARY.md §"Phase 1: Register Inventory" (verdict-assignment guidance: "initial status from existing evidence only" — no new tests this phase)
    - .planning/research/PITFALLS.md §"MOD-5: Cheap-tests that don't actually resolve the doubt" (the canonical scope-of-resolution discipline; the locked RSK-pool-count text is the prototype-pattern)
  </read_first>
  <action>
    For every RSK-NN entry in the register, replace the `TBD plan 02` markers in the `Verdict`, `Scope-of-resolution`, and `EXP / Resolution` fields with final values. Apply the assignment rule below.

    **Verdict assignment rule (from existing evidence only, per SUMMARY.md):**
    - **LIVE** — the risk could invalidate or weaken a CIP claim and no existing evidence resolves it; a Phase 3 cheap test or Phase 4 disclosure is needed. The default for ambiguous cells per D-06.
    - **DORMANT** — the risk is real in principle but has no observed activation in the current evidence base AND no plausible activation under the demand regimes the CIP exercises. Use sparingly; if in doubt, default LIVE.
    - **MITIGATED** — existing evidence already resolves the risk (e.g. `RSK` for WR-1 is RESOLVED 2026-05-14 per REVIEW.md and Family B chain-derived design). The evidence-against field must cite the resolving artefact.
    - **DISCLOSED** — the risk is real and is acknowledged in the CIP via a disclosure paragraph rather than tested away. Substrate-scope items are the canonical case (per CRIT-4 in PITFALLS.md, the substrate is out-of-scope for re-audit per PROJECT.md Out of Scope item 3).

    **The four mandatory LIVE entries (D-09) retain LIVE verdict** — do NOT mark any of `RSK-pool-count`, `RSK-single-seed-precision`, `RSK-un-anchored-controller-knobs` (or its 4 sub-RSKs), or `RSK-substrate-scope` as anything other than LIVE in this plan. `RSK-substrate-scope` lands as LIVE-going-to-DISCLOSED — its `EXP / Resolution` field reads `Disclosed (no EXP — substrate out-of-scope per PROJECT.md)` and Task 2 writes its disclosure-paragraph. The verdict in v1 is LIVE because Phase 4 (DOC-01 / DOC-03) is what flips it to DISCLOSED after the substrate-scope CIP-pasteable paragraph lands in the refreshed audit; v1 of the register marks it LIVE with the disclosure-paragraph drafted.

    **For every LIVE entry (REG-03):** populate the `EXP / Resolution` field with at least one `EXP-<slug>` identifier and add a cross-reference to the corresponding `TEST-NN` REQ-ID per CONTEXT.md Claude's Discretion "EXP-NN ↔ TEST-NN alignment". The provisional mapping from CONTEXT.md is:
    - `EXP-pool-number` → TEST-05
    - `EXP-sign-flip-variance` → TEST-03
    - `EXP-canonical-variance` → TEST-04
    - `EXP-run-length` → TEST-06
    - `EXP-window-length-anchor`, `EXP-multiplier-floor-anchor`, `EXP-lane-signal-source-anchor` → no TEST-NN (these are Phase 4 / DOC-03 anchor-or-disclose work, not Phase 3 tests)

    Use the format: `EXP-pool-number (→ TEST-05)` so a reader can trace from register entry to milestone-level REQ-ID. For Phase-4-handled EXP slugs, use: `EXP-window-length-anchor (Phase 4 / DOC-03 anchor-or-disclose)`.

    Plan 01 may surface additional LIVE risks not yet named in CONTEXT.md (e.g. `RSK-steady-state-run-length` per MOD-2, `RSK-mempool-revalidation-cost-model` per ARCHITECTURE.md, `RSK-multiplier-floor-4-suite-coverage` per MOD-1). For each, assign a descriptive `EXP-<slug>` and cross-reference to the closest TEST-NN per REQUIREMENTS.md — typically TEST-07 (the placeholder for 3–5 additional cheap tests). Format: `EXP-multiplier-floor-4-companion-run (→ TEST-07x)` where `TEST-07x` is the placeholder.

    **For every LIVE entry: populate `Scope-of-resolution` with an explicit hypothesis statement** per MOD-5 prevention. The hypothesis names what the test must establish or refute, including the metric, the threshold, and the range over which the verdict holds. Example template: "We test &lt;X&gt; across the range &lt;Y&gt;; we expect &lt;Z&gt;; if observed, the risk is MITIGATED within range &lt;Y&gt;; the risk remains LIVE outside range &lt;Y&gt;." Avoid vague wording like "needs more seeds" or "verify steady-state" — name the metric, the threshold, and the range explicitly.

    **REG-05: locked scope-of-resolution text for `RSK-pool-count`** — this entry's `Scope-of-resolution` field MUST contain (verbatim, or in semantically-equivalent form per D-10 / CONTEXT.md `<specifics>`): "Δ% < seed-IQR of same job at 100 pools establishes MITIGATED". The threshold-before-the-test discipline is the canonical PITFALLS MOD-5 prevention. Spell out "Inter-Quartile Range (IQR)" on first use within the entry's prose if not already expanded earlier in the file.

    **For every MITIGATED entry:** populate `Scope-of-resolution` with the resolving evidence and the date of resolution (e.g. for WR-1: "RESOLVED 2026-05-14 by chain-derivation in Family B; orphan-block samples cannot contaminate the canonical controller trajectory per `.planning/REVIEW.md` Fix Status table"). For MITIGATED entries the `EXP / Resolution` field is `MITIGATED — no further test required` plus the resolving artefact citation.

    **For every DORMANT entry:** populate `Scope-of-resolution` with the no-observed-activation argument (e.g. "no demand regime in the current suite set activates this failure mode; remains DORMANT unless a future demand profile re-activates it"). `EXP / Resolution` is `DORMANT — no test until activated`.

    **For every DISCLOSED entry:** the `Scope-of-resolution` reads `DISCLOSED — see disclosure-paragraph below`. The actual disclosure paragraph is Task 2's work.

    **Update the Index table at the top of the file** so every row's Verdict and EXP / Resolution columns reflect the final values (no `TBD plan 02` markers remain in the Index table either).

    Do NOT write disclosure-paragraphs in this task — that is Task 2's job. This task's deliverable is verdicts + scope-of-resolution + EXP-NN linkage for every entry.
  </action>
  <verify>
    <automated>test -f docs/phase-2/realism-risks-register.md &amp;&amp; ! grep -q "TBD plan 02" docs/phase-2/realism-risks-register.md | grep -v "Disclosure-paragraph" &amp;&amp; grep -q "Δ% < seed-IQR" docs/phase-2/realism-risks-register.md &amp;&amp; grep -q "EXP-pool-number" docs/phase-2/realism-risks-register.md</automated>
  </verify>
  <acceptance_criteria>
    - Every `Verdict` field carries one of `LIVE`, `DORMANT`, `MITIGATED`, `DISCLOSED` (verify via `grep -v '^#' docs/phase-2/realism-risks-register.md | grep '^\*\*Verdict:\*\*' | grep -v -E 'LIVE|DORMANT|MITIGATED|DISCLOSED'` returns no hits)
    - Every `Scope-of-resolution` field is populated with non-TBD content (verify via `grep -v '^#' docs/phase-2/realism-risks-register.md | grep '^\*\*Scope-of-resolution:\*\*' | grep 'TBD plan 02'` returns no hits)
    - Every `EXP / Resolution` field is populated with non-TBD content (verify via `grep -v '^#' docs/phase-2/realism-risks-register.md | grep '^\*\*EXP / Resolution:\*\*' | grep 'TBD plan 02'` returns no hits)
    - The Index table at the top of the file contains no `TBD plan 02` markers (verify via `sed -n '/^## Index$/,/^## /p' docs/phase-2/realism-risks-register.md | grep 'TBD plan 02'` returns no hits)
    - `RSK-pool-count` retains `Verdict: LIVE` and its `Scope-of-resolution` contains `Δ% < seed-IQR` (REG-05 locked text)
    - `RSK-single-seed-precision` retains `Verdict: LIVE` and its `EXP / Resolution` references `EXP-sign-flip-variance` AND `EXP-canonical-variance` (or equivalent slugs covering both TEST-03 and TEST-04)
    - The umbrella `RSK-un-anchored-controller-knobs` entry (or its 4 sub-RSKs) retains `Verdict: LIVE` and its `EXP / Resolution` references at least three of the four anchor-or-disclose EXP slugs
    - `RSK-substrate-scope` retains `Verdict: LIVE` and its `EXP / Resolution` reads `Disclosed (no EXP — substrate out-of-scope per PROJECT.md)` or semantically equivalent
    - Every LIVE entry's `EXP / Resolution` field references at least one `EXP-<slug>` identifier (REG-03)
    - Every LIVE entry's `EXP / Resolution` field with a TEST-NN-aligned slug cross-references the TEST-NN REQ-ID in the format `EXP-... (→ TEST-NN)` or notes Phase-4 handling (verify via `grep -E "EXP-[a-z-]+ \(→ TEST-[0-9]+\)|Phase 4 / DOC" docs/phase-2/realism-risks-register.md` returns multiple hits — at least one per LIVE entry that has a TEST-NN counterpart)
    - The disclosure-paragraph field may still contain `TBD plan 02` — Task 2 finalises those
  </acceptance_criteria>
  <done>
    Every RSK-NN entry has final values in `Verdict`, `Scope-of-resolution`, and `EXP / Resolution` fields. The Index table is updated with final verdicts. The locked scope-of-resolution text for `RSK-pool-count` appears verbatim. Every LIVE entry has at least one EXP-NN identifier and the cross-reference to its TEST-NN REQ-ID where applicable. Only `Disclosure-paragraph` fields may still carry `TBD plan 02` markers — those are Task 2's work.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write CIP-pasteable disclosure-paragraphs and verify register consistency</name>
  <files>
    docs/phase-2/realism-risks-register.md (MODIFY — populate every Disclosure-paragraph field that is required and run consistency verification)
  </files>
  <read_first>
    - docs/phase-2/realism-risks-register.md (the full current state — every entry has finalised verdict + scope-of-resolution + EXP-NN linkage from Task 1)
    - .planning/phases/01-register-inventory/01-CONTEXT.md (decision D-08 — disclosure-paragraph is mandatory for DISCLOSED entries, optional placeholder for LIVE entries that may land as DISCLOSED later; plus Claude's Discretion "Disclosure-paragraph voice" — Cardano CIP house style, engineering-report register, no internal jargon like raw `WR-1` without context)
    - CLAUDE.md §"Conventions / gotchas" (abbreviation-on-first-use rule applies to disclosure-paragraph prose since these paragraphs paste into the CIP verbatim)
    - .planning/research/PITFALLS.md §"CRIT-4: Inherited-substrate limitations not disclosed" (the substrate-scope disclosure-paragraph is the most load-bearing one in this register — it covers three sub-points and pastes into the CIP's Limitations section)
    - External precedent: CIP-0164 §"Trade-offs & Limitations" — closest Cardano CIP house-style precedent. (Web URL: https://cips.cardano.org/cip/CIP-0164 — read only if not already familiar; the in-context guidance below carries the salient voice cues.)
  </read_first>
  <action>
    **Disclosure-paragraph required for two categories of entry:**
    1. Every entry whose `Verdict` is `DISCLOSED` — MUST have a complete CIP-pasteable disclosure-paragraph per D-08.
    2. The four mandatory LIVE entries from D-09 — each requires a draft disclosure-paragraph that the CIP author may use when Phase 4 / DOC-01 / DOC-03 flips the verdict to DISCLOSED. For `RSK-substrate-scope` specifically the disclosure-paragraph is load-bearing — it pastes into the CIP's Limitations section verbatim per CRIT-4.

    For other LIVE entries: disclosure-paragraph remains a placeholder (`TBD — drafted in Phase 4 if test verdict lands as DISCLOSED`). This is acceptable per D-08; do NOT force draft prose for LIVE entries whose verdict will likely flip to MITIGATED.

    **Voice cues for CIP house style** (per CONTEXT.md Claude's Discretion "Disclosure-paragraph voice"):
    - Engineering-report register, not academic-paper register. Sentences in active voice; short paragraphs; the reader is a technically competent CIP reviewer, not a domain specialist.
    - Abbreviations expanded on first use within the paragraph itself, because the CIP author pastes the paragraph in isolation. Do not assume the surrounding CIP prose has already introduced the abbreviation.
    - No internal jargon. `WR-1` becomes "the controller-state contamination risk on slot-battle reorg (resolved by Family B chain-derivation 2026-05-14 in this evidence base)" not just "WR-1". Internal-process identifiers (`WR-N`, `CR-N`) may be cited as a parenthetical pointer once the underlying issue is described in prose.
    - The paragraph names the limitation, its scope (where it applies), what the evidence base does and does not exercise, and a forward-pointer to deferred work. The CIP-0164 voice is the closest published precedent.

    **For `RSK-substrate-scope` specifically**, the disclosure-paragraph names all three sub-points (per the substrate-scope-grouping decision in CONTEXT.md Claude's Discretion):
    - (a) The Cardano Improvement Proposal's evidence base is derived from a Rust simulator that inherits f64 floating-point arithmetic in non-pricing code paths (slot lottery, propagation, distribution sampling). The pricing kernel itself is integer/rational/u128 (per CLAUDE.md "Numeric representation contract"), but the surrounding substrate is not. Reproducibility is intra-architectural (reference build environment: x86_64 / glibc); cross-architecture reproducibility is deferred future work.
    - (b) The simulator's propagation model uses round-trip-time-driven topology fidelity, not packet-level Cardano-mainnet network conditions. Welfare claims are conditional on the simulator's propagation regime; deployed-system propagation may shift magnitudes.
    - (c) The actor model is utility-maximising across multiple urgency-tagged demand components. Strategic-bidder regimes (bribery, side-contracts, miner extractable value or MEV strategies, sustained controller gaming) are out of scope for this evidence base and are disclosed as future work — citing the Chung & Shi Symposium on Discrete Algorithms (SODA) 2023 impossibility result for user-incentive-compatibility, miner-incentive-compatibility, and side-contract-proofness as the standard formal frame.
    Length: one paragraph, ~5–8 sentences. The CIP author pastes this verbatim into the Limitations section.

    **For `RSK-pool-count`**, draft a disclosure-paragraph as a fallback in case the Phase 3 / TEST-05 pool-number sensitivity test fails to land MITIGATED. Frame: "We exercise the menu across 100 and 150 pools, using the Cardano mainnet epoch-582 stake snapshot rescaled to a 100-pool topology and a parallel 150-pool topology generated by the same mass-stratified downsampling script. The CIP-0164 600-pool migration is out of scope for this evidence base; if pool-number sensitivity tests at the 100→150 range show stability within seed-Inter-Quartile-Range (IQR) noise, the qualitative findings extend to the same-order-of-magnitude regime but not to the 600-pool regime, which is disclosed as a separate item." If TEST-05 lands MITIGATED in Phase 3, the verdict flips to MITIGATED and the disclosure-paragraph becomes a forward-pointer rather than load-bearing prose.

    **For `RSK-single-seed-precision`**, draft a fallback disclosure-paragraph framing what the CIP says if multi-seed variance bands (Phase 3 / TEST-03 + TEST-04) surface a sign-flip cell that does not resolve under Paired Seed Evaluation (PSE). Frame: "Welfare claims at near-zero absolute magnitude are reported with their 95% Bias-corrected and accelerated (BCa) bootstrap confidence intervals on paired welfare deltas; cells where the confidence interval crosses zero are flagged in the coverage check as WEAK rather than BACKED."

    **For `RSK-un-anchored-controller-knobs`** (or each of its four sub-RSKs), draft a per-knob fallback disclosure-paragraph in the "conditional on X" frame per CONTEXT.md / Phase 4 DOC-03 expectation. Example for window-length: "The pricing controller uses a smoothing window of 32 blocks for capacity-varying signals. This value is a round-number choice from the EIP-1559 calibration tradition rather than a Cardano-mainnet anchor. Qualitative findings are conditional on window-length 32; an alternative such as window-length 64 (smoother, slower) or 16 (more reactive, noisier) was not exercised in this evidence base. A two-hour literature search at Phase 4 open will determine whether an external anchor (e.g. Liu et al. Conference on Computer and Communications Security (CCS) 2022, Reijsbergen et al. Advances in Financial Technologies (AFT) 2021) is available; if not, this disclosure-paragraph pastes into the CIP." Mirror the structure for the other three knobs.

    **For every other DISCLOSED entry** (those that lack mainnet evidence and whose disclosure is the resolution path — likely candidates: `RSK-mempool-revalidation-cost-model`, `RSK-determinism-cross-arch-disclosure`, `RSK-welfare-reporting-f64-precision`, depending on what plan 01 surfaced), write a one-paragraph disclosure in the same voice. Each names the limitation, its scope, and the bound argument that justifies CIP publication despite the limitation.

    **Consistency verification pass** (after disclosure-paragraphs are drafted):
    1. **Index ↔ entry sections cross-check** — every RSK-NN in the Index table has a corresponding `## RSK-NN` section, and every `## RSK-NN` section has a row in the Index table. No orphans in either direction.
    2. **Verdict consistency** — every Index-table Verdict value matches the corresponding entry's `Verdict` field.
    3. **EXP-NN format consistency** — every EXP-NN slug used in `EXP / Resolution` follows the pattern `EXP-<descriptive-slug>` (lowercase, hyphen-separated, no embedded spaces).
    4. **Mandatory-four LIVE entries** — verify `RSK-pool-count`, `RSK-single-seed-precision`, `RSK-substrate-scope`, and (`RSK-un-anchored-controller-knobs` OR the four sub-RSKs) all retain LIVE verdict.
    5. **REG-05 locked text** — verify `RSK-pool-count`'s scope-of-resolution contains `Δ% < seed-IQR` and the surrounding prose makes the verdict-threshold-before-test pattern explicit.
    6. **No TBD plan 02 markers remain anywhere in the file** — except, optionally, in disclosure-paragraph fields for LIVE entries that legitimately defer disclosure-drafting to Phase 4 (these read `TBD — drafted in Phase 4 if test verdict lands as DISCLOSED` rather than `TBD plan 02`).
    7. **Abbreviation-on-first-use** — `Cardano Improvement Proposal (CIP)`, `Inter-Quartile Range (IQR)`, `Paired Seed Evaluation (PSE)`, `Bias-corrected and accelerated (BCa) bootstrap`, `Conference on Computer and Communications Security (CCS)`, `Advances in Financial Technologies (AFT)`, `Symposium on Discrete Algorithms (SODA)` all appear in expanded form somewhere in the file.

    If any inconsistency is found, fix it before declaring the task done.
  </action>
  <verify>
    <automated>test -f docs/phase-2/realism-risks-register.md &amp;&amp; ! grep -q "TBD plan 02" docs/phase-2/realism-risks-register.md &amp;&amp; grep -q "Δ% < seed-IQR" docs/phase-2/realism-risks-register.md &amp;&amp; grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/realism-risks-register.md &amp;&amp; grep -q "Paired Seed Evaluation (PSE)" docs/phase-2/realism-risks-register.md &amp;&amp; grep -q "Bias-corrected and accelerated (BCa) bootstrap" docs/phase-2/realism-risks-register.md</automated>
  </verify>
  <acceptance_criteria>
    - No `TBD plan 02` markers remain anywhere in the file (verify via `grep -c "TBD plan 02" docs/phase-2/realism-risks-register.md` returns 0)
    - Every DISCLOSED entry has a non-placeholder disclosure-paragraph (verify by walking every `^## RSK-` section whose `^\*\*Verdict:\*\*` is `DISCLOSED` and confirming its `^\*\*Disclosure-paragraph:\*\*` field is followed by more than 200 characters of prose)
    - `RSK-substrate-scope` disclosure-paragraph mentions all three sub-points: `f64` (or "floating-point"), `propagation`, and `actor model` (or "utility-maximising") (verify via `grep -A 50 "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md | grep -i -E "f64|floating-point" &amp;&amp; grep -A 50 "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md | grep -i "propagat" &amp;&amp; grep -A 50 "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md | grep -i -E "actor|utility-maximising|strategic"`)
    - `RSK-pool-count` scope-of-resolution contains the locked text fragment `Δ% < seed-IQR` (REG-05 — verify via `grep -A 10 "^## RSK-pool-count" docs/phase-2/realism-risks-register.md | grep "Δ% < seed-IQR"`)
    - Cross-document abbreviations expanded on first use (verify each of these regexes returns at least one match):
      - `grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/realism-risks-register.md`
      - `grep -q "Inter-Quartile Range (IQR)" docs/phase-2/realism-risks-register.md`
      - `grep -q "Paired Seed Evaluation (PSE)" docs/phase-2/realism-risks-register.md`
      - `grep -q "Bias-corrected and accelerated (BCa) bootstrap" docs/phase-2/realism-risks-register.md`
      - `grep -q "Symposium on Discrete Algorithms (SODA)" docs/phase-2/realism-risks-register.md`
    - Mandatory-four LIVE entries retain `Verdict: LIVE` (verify via four `grep -A 3 "^## RSK-pool-count\|^## RSK-single-seed-precision\|^## RSK-substrate-scope\|^## RSK-un-anchored-controller-knobs" docs/phase-2/realism-risks-register.md | grep -B 1 "Verdict.*LIVE"`)
    - Index table is consistent with entry sections — for each `^## RSK-<id>` section there is exactly one Index-table row with the same RSK-NN, and vice versa (verify by extracting both lists and diffing: `diff <(grep -oE "^## (RSK-[a-z-]+)" docs/phase-2/realism-risks-register.md | sort -u) <(sed -n '/^## Index$/,/^## /p' docs/phase-2/realism-risks-register.md | grep -oE "RSK-[a-z-]+" | sort -u)` returns no diff)
  </acceptance_criteria>
  <done>
    The register file has no `TBD plan 02` markers. Every DISCLOSED entry has a CIP-pasteable disclosure-paragraph in engineering-report voice with abbreviations expanded. The four mandatory LIVE entries retain LIVE verdict and have draft disclosure-paragraphs (load-bearing for `RSK-substrate-scope`; fallback for the other three). The Index table is consistent with the entry sections — no orphans either way. REG-05's locked scope-of-resolution text appears verbatim on `RSK-pool-count`. Required abbreviations are expanded on first use. The register is in its final v1 state and ready for Phase 2 to begin (the coverage check's `related-RSK-ids` column can reference these stable identifiers).
  </done>
</task>

</tasks>

<threat_model>
This plan modifies one Markdown file under `docs/phase-2/` and reads existing in-repo documents (including the register file produced by plan 01). There is no code change, no test suite execution, no new dependency, no external service interaction. No attack surface is added or modified.

The only failure modes are documentation-quality failures (missing verdicts, drift between the Index table and per-entry sections, misclassified mandatory LIVE entries, missing locked scope-of-resolution text on RSK-pool-count, internal jargon leaking into CIP-pasteable disclosure-paragraphs). The acceptance criteria in both tasks cover these via source-assertion-style grep checks. STRIDE categories do not apply because there is no trust boundary being crossed by this change — the register is read by future planners, the CIP author, and reviewers; it is not a runtime artefact.

No `<threat_model>` STRIDE register table is needed for this plan.
</threat_model>

<verification>
After Task 2 completes, run the following final acceptance checks:

```bash
# No TBD markers remain
! grep -q "TBD plan 02" docs/phase-2/realism-risks-register.md

# Locked scope-of-resolution text for RSK-pool-count (REG-05)
grep -q "Δ% < seed-IQR" docs/phase-2/realism-risks-register.md

# Substrate-scope disclosure-paragraph names all three sub-points
grep -A 60 "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md | grep -i -E "f64|floating-point"
grep -A 60 "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md | grep -i "propagat"
grep -A 60 "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md | grep -i -E "actor|utility-maximising"

# Abbreviations expanded
grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/realism-risks-register.md
grep -q "Inter-Quartile Range (IQR)" docs/phase-2/realism-risks-register.md
grep -q "Paired Seed Evaluation (PSE)" docs/phase-2/realism-risks-register.md
grep -q "Bias-corrected and accelerated (BCa) bootstrap" docs/phase-2/realism-risks-register.md

# Index ↔ entry consistency
diff <(grep -oE "^## (RSK-[a-z-]+)" docs/phase-2/realism-risks-register.md | sed 's/^## //' | sort -u) <(sed -n '/^## Index$/,/^## RSK-/p' docs/phase-2/realism-risks-register.md | grep -oE "RSK-[a-z-]+" | sort -u)

# Four mandatory LIVE entries retain LIVE
grep -A 3 "^## RSK-pool-count" docs/phase-2/realism-risks-register.md | grep -E "^\*\*Verdict:\*\* LIVE"
grep -A 3 "^## RSK-single-seed-precision" docs/phase-2/realism-risks-register.md | grep -E "^\*\*Verdict:\*\* LIVE"
grep -A 3 "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md | grep -E "^\*\*Verdict:\*\* LIVE"
```

All checks pass before the plan is declared done.
</verification>

<success_criteria>
- No `TBD plan 02` placeholder markers remain in the register
- Every entry has a final verdict from `LIVE` / `DORMANT` / `MITIGATED` / `DISCLOSED`
- Every LIVE entry is paired with at least one `EXP-NN` slug; the cross-reference to TEST-NN REQ-IDs follows the CONTEXT.md provisional mapping where applicable (REG-03)
- The four mandatory LIVE entries from D-09 retain LIVE verdict (REG-04)
- `RSK-pool-count`'s scope-of-resolution contains the locked text `Δ% < seed-IQR of same job at 100 pools establishes MITIGATED` or semantically-equivalent prose (REG-05)
- Every DISCLOSED entry has a CIP-pasteable disclosure-paragraph in engineering-report voice
- `RSK-substrate-scope`'s disclosure-paragraph names all three sub-points (f64, propagation, actor model) per the substrate-scope-grouping decision
- Index table and per-entry sections are mutually consistent — no orphans
- Required abbreviations are expanded on first use per CLAUDE.md convention
</success_criteria>

<output>
After completion, create `.planning/phases/01-register-inventory/01-02-SUMMARY.md` per the standard summary template, recording:
- Final verdict distribution (counts of LIVE / DORMANT / MITIGATED / DISCLOSED)
- The EXP-NN ↔ TEST-NN cross-reference table actually realised (verifying the CONTEXT.md provisional mapping; record any deviations and rationale)
- Which DISCLOSED entries received load-bearing disclosure-paragraphs versus fallback / forward-pointer paragraphs
- Any RSK-NN entries that surfaced needs for additional TEST-NN sub-requirements beyond TEST-07 (Phase 3's placeholder) — feeds the Phase 3 task ordering
- Confirmation that the register is in v1 final state and Phase 2 may begin (the coverage check's `related-RSK-ids` column references these identifiers)
</output>
