---
phase: 01-viz-site-mvp
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - sim-rs/scripts/viz/build.py
  - sim-rs/scripts/viz/__init__.py
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04, VIZ-05]
must_haves:
  truths:
    - "`build.discover_suites(source, includes, excludes, warnings)` returns one entry per `manifest.json` reachable via `Path(source).rglob('manifest.json')`; `--include`/`--exclude` glob flags are applied to the relative path string (D-10)."
    - "`build.run_build(source, output, includes, excludes, warnings)` emits `data/index.json`, one `data/<suite_id>.json` per suite, and one `data/<suite_id>/<job>-<seed>.json` per (job, seed) — the three-tier D-09 layout. Output goes to `sim-rs/output/viz/` (D-06)."
    - "The script lives at `sim-rs/scripts/viz/build.py` and is Python stdlib-only (D-01 / D-02 / D-05 / D-08): no off-the-shelf framework, no Cargo binary, no `requirements.txt`, no virtualenv."
    - "Manifest reads use kebab-case keys (`suite-name`, `started-at-utc`, `jobs[job][seed].status`); run_summary reads use snake_case keys (`priority_retained_value_total`, `components`, `latency_blocks_observations`)."
    - "`latency_blocks_observations` (list per component) is reduced to `latency_blocks_mean` (scalar per component) at build time, matching the Rust accessor `ComponentSummary::latency_blocks_mean()`."
    - "`time_series.csv` is converted to long-form `{slot, lane, metric, value}` records at build time; the browser never parses CSV (D-11)."
    - "Suite identifier is derived from `manifest_path.parent.relative_to(source)` with `/` → `__`, NOT from `manifest['suite-name']`, so two `eip1559-robustness` directories under different parents stay distinct."
    - "Skip-and-warn on malformed manifest, missing run_summary, missing time_series; build never exits non-zero on data errors (D-21)."
    - "Per-suite JSON includes a `jobs[job_name].seeds[seed].headline` map AND an `aggregates` field that is empty/null when no `*.csv` exists at the suite root — the build does NOT pretend the historical `priority_only_fast_path_overall_comparison.csv` is present in phase-2 suites."
  artifacts:
    - path: sim-rs/scripts/viz/build.py
      provides: "discover_suites, load_seed, _read_time_series_long, run_build, parse_args, main"
      min_lines: 200
      exports: ["discover_suites", "load_seed", "run_build", "parse_args", "main"]
    - path: sim-rs/scripts/viz/__init__.py
      provides: "Package marker so tests/test_ingest.py can `import build`"
  key_links:
    - from: sim-rs/scripts/viz/build.py::run_build
      to: sim-rs/scripts/viz/build.py::discover_suites
      via: "iteration over discovered (manifest_path, manifest) tuples"
      pattern: "for .* in discover_suites"
    - from: sim-rs/scripts/viz/build.py::run_build
      to: sim-rs/scripts/viz/build.py::load_seed
      via: "per-(job, seed) call inside the suite loop"
      pattern: "load_seed\\("
    - from: sim-rs/scripts/viz/tests/test_ingest.py
      to: sim-rs/scripts/viz/build.py
      via: "import build with sys.path.insert"
      pattern: "import build"
---

<objective>
The ingest layer: a stdlib-only Python module that walks `sim-rs/output/` for `manifest.json` files, reads each suite's manifest plus per-(job, seed) `run_summary.json` and `time_series.csv`, and emits the three-tier JSON layout (D-09) under `--output`. This plan implements the data pipeline that Wave 0 tests target.

Purpose: Hard-pin the on-disk → JSON contract so the browser plans (01-03, 01-05) can assume the schema is stable. Encode all 8 RESEARCH.md pitfalls (kebab vs snake, path-derived suite id, no per-suite CSV in phase-2, never parse `metrics_comparison.txt`, latency-list to mean, no new gitignore entry, sequential serve order, missing time-series soft failure).

Output: `sim-rs/scripts/viz/build.py` (ingest functions + a stub `main()` for now — Plan 01-04 fleshes out argparse + serve), `sim-rs/scripts/viz/__init__.py`, and a green `python -m unittest discover -s sim-rs/scripts/viz/tests` run.

This plan splits the ingest layer into three sequential tasks within a single plan so each layer is independently reviewable: (1) discovery + identifier-derivation + warnings layer, (2) per-(job, seed) ingest layer, (3) three-tier JSON emission + CLI orchestration layer. All three tasks share the same `files_modified` (`build.py` and `__init__.py`) and depend only on Wave 0 from Plan 01-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-CONTEXT.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-VALIDATION.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-01-PLAN.md
@CLAUDE.md
@sim-rs/scripts/generate-realistic-100-topology.py
@sim-rs/scripts/analyse-phase-3.py
@sim-rs/sim-cli/src/runner.rs
@sim-rs/sim-cli/src/metrics/collector.rs
@sim-rs/sim-cli/src/metrics/time_series.rs

<interfaces>
<!-- The function signatures Plan 01-04 (argparse + serve) and Plan 01-05 (browser views) will consume. Lock them here. -->

# build.py public API (used by tests, by build.py::main, and indirectly by the browser via the emitted JSON)

def discover_suites(source: Path, includes: list[str], excludes: list[str], warnings: list[str]) -> list[dict]:
    """Return list of {id, dir, name, started_at, jobs} dicts; one per reachable manifest.json.

    Side effect: appends to `warnings` on malformed manifests / glob mismatches. Never raises.
    `id` is `str(manifest_path.parent.relative_to(source)).replace("/", "__")` (D-22 / Pitfall 2).
    """

def load_seed(seed_dir: Path, warnings: list[str]) -> dict | None:
    """Read run_summary.json + time_series.csv and return a per-(job, seed) JSON-shaped dict.

    Returns None if run_summary.json is missing (warning emitted).
    Returns dict with empty `time_series` and `peak_mempool_bytes=None` if only time_series.csv is missing.
    """

def run_build(source: Path, output: Path, includes: list[str], excludes: list[str], warnings: list[str]) -> None:
    """Top-level builder: discover_suites → per-(job, seed) JSON emission → per-suite JSON → index.json.

    Writes:
      output/data/index.json
      output/data/<suite_id>.json
      output/data/<suite_id>/<job>-<seed>.json
    """

def parse_args() -> argparse.Namespace:
    """argparse with --source, --output, --include, --exclude, --serve, --port. Plan 01-04 expands."""

def main() -> None:
    """parse_args + run_build + print warnings summary. --serve hookup lands in Plan 01-04."""

# Emitted JSON schemas

# data/index.json
{
  "generated_at": "ISO-8601 UTC",
  "source": "<absolute source path>",
  "suite_count": int,
  "suites": [
    {
      "id": "phase-2__eip1559-robustness-20260514-160045",
      "name": "eip1559-robustness",            # from manifest["suite-name"]; falls back to dir name
      "path": "phase-2/eip1559-robustness-20260514-160045",
      "started_at": "2026-05-14T16:00:45Z",
      "job_count": int,
      "seed_count": int,                        # sum of seeds across jobs
      "completed_count": int,                   # count of (job,seed) with status == "completed"
      "max_concurrent_jobs": int | null         # RESEARCH.md Open Q #1 proxy: max overlap of (started, completed) intervals across (job, seed); null when fewer than 2 timestamps are usable
    }
  ]
}

# data/<suite_id>.json
{
  "id": "...",
  "name": "...",
  "path": "...",
  "started_at": "...",
  "manifest": { ...raw manifest with kebab-case keys preserved... },
  "jobs": {
    "<job_name>": {
      "seeds": {
        "<seed>": {
          "status": "completed" | ...,
          "started_at": "...",
          "completed_at": "...",
          "headline": {                           # mirrors per-(job,seed) headline fields; lets browser overlay seeds w/o N round-trips
            "retained_value": float,
            "net_utility": float,
            "retained_value_ratio": float | null,
            "peak_mempool_bytes": int | null,
            "components": [
              {"index": int, "latency_blocks_mean": float, "priority_included": int, "standard_included": int}
            ]
          }
        }
      }
    }
  },
  "aggregates": null   # PITFALL 3 / LANDMINE #2: phase-2 emits no `*comparison.csv` at suite root.
                      # Reserved field; render-time gated by browser on null. NEVER parse metrics_comparison.txt (Pitfall 4).
                      # If a `*.csv` is later found at the suite root by a future enhancement, this field
                      # carries the parsed table; for now it is null on every phase-2 suite.
}

# data/<suite_id>/<job>-<seed>.json
{
  "suite_id": "...", "job": "...", "seed": "...",
  "retained_value": float,                            # priority_retained_value_total + standard_retained_value_total
  "priority_retained_value": float,
  "standard_retained_value": float,
  "net_utility": float,                                # sum over components of net_utility_total
  "retained_value_ratio": float | null,                # priority+standard retained / priority+standard included (lovelace); null on div-by-zero
  "total_txs_submitted": int, "total_txs_included": int, "total_txs_evicted_quote_drift": int,
  "total_fees_paid_lovelace": int, "total_refund_lovelace": int,
  "pricing_event_stream_sha256": str,
  "components": [
    {"index": int, "txs_submitted": int, "txs_included": int, "bytes_included": int,
     "retained_value": float, "net_utility": float,
     "latency_blocks_mean": float,             # computed: sum(obs)/len(obs), 0.0 on empty
     "priority_included": int, "standard_included": int}
  ],
  "time_series": [                                     # long-form, ready for Plot
    {"slot": int, "lane": "priority"|"standard"|"total", "metric": "quote_per_byte"|"mempool_bytes"|...,"value": int}
  ],
  "peak_mempool_bytes": int | null                     # max over time_series where metric=mempool_bytes, lane=total
}

LANE_FIELDS contract (the CSV → long-form mapping; PATTERNS.md Pattern F):
[
  ("c_priority",                "priority", "quote_per_byte"),
  ("c_standard",                "standard", "quote_per_byte"),
  ("mempool_bytes_total",       "total",    "mempool_bytes"),
  ("mempool_bytes_priority",    "priority", "mempool_bytes"),
  ("mempool_bytes_standard",    "standard", "mempool_bytes"),
  ("included_bytes_priority",   "priority", "included_bytes"),
  ("included_bytes_standard",   "standard", "included_bytes"),
  ("included_count_priority",   "priority", "included_count"),
  ("included_count_standard",   "standard", "included_count"),
  ("fees_paid_lovelace",        "total",    "fees_paid_lovelace"),
  ("refund_lovelace",           "total",    "refund_lovelace"),
  ("evicted_quote_drift_count", "total",    "evicted_quote_drift_count"),
]
# (util_*_window_x_1e9 columns are reserved for later — drop on the floor; the 13 entries above
#  cover every chart VIZ-04 / D-14 calls out.)
</interfaces>

<style_anchors>
<!-- Style baseline: read these files end-to-end before authoring `build.py`. They are short and the patterns transfer directly. -->
sim-rs/scripts/generate-realistic-100-topology.py     # shebang + module docstring + argparse + Path + sys.stderr style
sim-rs/scripts/analyse-phase-3.py                     # closer functional analog (consumes phase-2 artefacts under sim-rs/output/)
</style_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite-walk + identifier-derivation + warnings scaffold (`discover_suites`, `_max_concurrent_jobs`, `_iso_to_epoch`, `__init__.py`)</name>
  <files>sim-rs/scripts/viz/build.py, sim-rs/scripts/viz/__init__.py</files>
  <read_first>
    sim-rs/scripts/generate-realistic-100-topology.py (style baseline — shebang, docstring, argparse, Path types),
    sim-rs/scripts/analyse-phase-3.py (functional analog — `load_run_summary`, `collect_job`, `pathlib` walk style),
    sim-rs/sim-cli/src/runner.rs (lines 44-74 — Manifest / JobEntry / JobStatus serde shapes with `rename_all = "kebab-case"`),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (Patterns A, C — skip-and-warn, suite-id-from-path; Shared Patterns block)
  </read_first>
  <action>
    Author the scaffold of `sim-rs/scripts/viz/build.py` and create `sim-rs/scripts/viz/__init__.py`. This task lands the discovery/identifier/warnings layer; downstream tasks add the ingest and emit layers on top.

    File header (build.py): open with `#!/usr/bin/env python3`, a module docstring patterned on `generate-realistic-100-topology.py` (one-line summary; usage block showing `python3 sim-rs/scripts/viz/build.py [--serve]`; design contract noting three-tier output layout). Imports: stdlib only — `argparse`, `csv`, `dataclasses` (optional), `datetime`, `fnmatch`, `functools`, `html`, `json`, `shutil`, `sys` and `from pathlib import Path`. NO third-party imports. The abbreviation-expansion rule from CLAUDE.md applies to the docstring and any non-trivial comments (spell "Single-Page Application (SPA)", "Observable Plot", "Endorser Block (EB)" if mentioned). Do NOT add a `.gitignore` entry anywhere — Pitfall 6 / CRITICAL LANDMINE #4: the existing `sim-rs/.gitignore` `/output` rule already covers `sim-rs/output/viz/`.

    Module-level constant `LANE_FIELDS` — the 13-tuple list from the interfaces block above. Comments must note that `util_*_window_x_1e9` columns are intentionally dropped (reserved for future) and that all values cast to `int(row[col])` because every CSV column is u64 in Rust (PATTERNS.md Pattern F). The constant is defined here so the next task can use it without redefinition.

    (a) `_iso_to_epoch(s: str | None) -> int | None` — parse `"2026-05-14T16:00:45Z"` via `datetime.fromisoformat(s.replace("Z","+00:00")).timestamp()` (per RESEARCH.md `## Don't Hand-Roll`). Returns None on None input or parse failure.

    (b) `_max_concurrent_jobs(manifest: dict) -> int | None` — RESEARCH.md Open Q #1 (RESOLVED) proxy: walk every `jobs[job][seed]` entry, collect (started-at-utc, completed-at-utc) pairs that are both non-null and parseable, return the maximum number of intervals that overlap at any single point in time (a single integer). Return None if fewer than 2 usable pairs. Implementation: collect start/end events, sort by timestamp, sweep with +1/-1.

    (c) `discover_suites(source: Path, includes: list[str], excludes: list[str], warnings: list[str]) -> list[dict]` — `sorted(source.rglob('manifest.json'))` to keep emission order deterministic. For each `manifest_path`, try/except on `(json.JSONDecodeError, OSError)`, append-and-continue on failure. Suite id: `str(manifest_path.parent.relative_to(source)).replace('/', '__')` (D-22 / Pitfall 2). After computing the relative path string, apply `fnmatch.fnmatch` against each `--include` glob (if any) and `--exclude` glob; both `[]` means accept-all. Return one dict per accepted suite carrying `id`, `dir` (Path), `name` (manifest.get("suite-name", manifest_path.parent.name)), `started_at`, `jobs`, plus the raw `manifest` for downstream use. Use `manifest["suite-name"]` and `manifest["started-at-utc"]` exactly — kebab-case, square-bracket access — to make Pitfall 1 unmissable.

    Create `sim-rs/scripts/viz/__init__.py` as an empty file so `import build` from inside `tests/test_ingest.py` resolves cleanly when `sys.path.insert(0, str(Path(__file__).resolve().parent.parent))` is used.

    At the end of this task, `build.py` does not yet define `load_seed`, `_read_time_series_long`, `run_build`, `parse_args`, or `main`. Those land in Tasks 2 and 3. The grep gates in `<acceptance_criteria>` are scoped only to what THIS task contributes; full-API checks are validated in Task 3.

    Constraints repeated for emphasis:
    - **DO NOT touch any `.gitignore` file** (CRITICAL LANDMINE #4 — existing `/output` rule covers `viz/`).
    - **DO NOT use `f64`-arithmetic terminology in comments** in a way that suggests this module touches simulation-affecting state; reporting-f64 is fine here (RESEARCH.md project-constraints note).
  </action>
  <verify>
    <automated>cd sim-rs && python3 -c "import sys; sys.path.insert(0, 'scripts/viz'); import build; print('module imports OK'); assert hasattr(build, 'discover_suites'); assert hasattr(build, 'LANE_FIELDS'); assert len(build.LANE_FIELDS) >= 12, f'LANE_FIELDS too short: {len(build.LANE_FIELDS)}'; assert hasattr(build, '_iso_to_epoch'); assert hasattr(build, '_max_concurrent_jobs'); print('Task 1 API present')" && test -f sim-rs/scripts/viz/__init__.py && grep -q 'manifest\["suite-name"\]' sim-rs/scripts/viz/build.py && grep -qE 'relative_to\(source\).*replace\(.*"/".*"__"' sim-rs/scripts/viz/build.py && ! grep -E '^import (yaml|requests|pytest|jinja2|flask)' sim-rs/scripts/viz/build.py && ! grep -E '^from (yaml|requests|pytest|jinja2|flask)' sim-rs/scripts/viz/build.py && test -z "$(git diff sim-rs/.gitignore)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `sim-rs/scripts/viz/build.py` exists; `python3 -c "import build"` (with sys.path insertion) succeeds without ImportError.
    - The module exports `discover_suites`, `_iso_to_epoch`, `_max_concurrent_jobs`, and the constant `LANE_FIELDS` (>= 12 tuples).
    - `sim-rs/scripts/viz/__init__.py` exists.
    - File contains no `import yaml`, no `import requests`, no `import pytest`, no `import jinja2`, no `import flask` (grep `^import ` and `^from .* import` must show only stdlib modules).
    - File contains the literal token `manifest["suite-name"]` (Pitfall 1 enforcement of kebab-case).
    - File contains `relative_to(source)` and `.replace("/", "__")` on the same line or within one expression (Pitfall 2 / D-22).
    - `sim-rs/.gitignore` is unmodified by this plan (`git diff sim-rs/.gitignore` shows no changes).
  </acceptance_criteria>
  <done>
    Discovery layer present; module imports cleanly; `discover_suites` runs against the fixture (manual ad-hoc check); Tasks 2 and 3 can extend the same file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-(job, seed) ingest layer (`load_seed`, `_read_time_series_long`, per-component latency mean)</name>
  <files>sim-rs/scripts/viz/build.py</files>
  <read_first>
    sim-rs/scripts/viz/build.py (current state from Task 1 — extend in place),
    sim-rs/sim-cli/src/metrics/collector.rs (lines 11-160 — RunSummary / ComponentSummary snake_case shapes; `latency_blocks_mean` is a method, not a serialized field),
    sim-rs/sim-cli/src/metrics/time_series.rs (lines 1-50 — pinned 15-column CSV header),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (Patterns D, E, F — kebab/snake split, latency-list-to-mean, csv-to-long-form),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md (Common Pitfalls #3 — landmine #3: per-component latency mean)
  </read_first>
  <action>
    Extend `build.py` with the per-(job, seed) ingest helpers. This layer reads `run_summary.json` and `time_series.csv` and returns a JSON-shaped dict per seed.

    (a) `_read_time_series_long(csv_path: Path)` (generator) — exactly the body from PATTERNS.md Pattern F: `with open(csv_path, newline="") as f: for row in csv.DictReader(f): slot = int(row["slot"]); for col, lane, metric in LANE_FIELDS: yield {"slot": slot, "lane": lane, "metric": metric, "value": int(row[col])}`. No defensive `.get`; the header is pinned by the Rust writer (CSV column names come from `time_series.rs`). Uses the `LANE_FIELDS` constant defined in Task 1.

    (b) `load_seed(seed_dir: Path, warnings: list[str]) -> dict | None` — read `run_summary.json` via `with open() as f: json.load(f)`. Return None and append warning if file missing. Build the headline dict per the per-(job, seed) JSON schema in the interfaces block:
      - `retained_value = rs["priority_retained_value_total"] + rs["standard_retained_value_total"]`
      - `net_utility = sum(c["net_utility_total"] for c in rs["components"])`
      - `retained_value_ratio = retained_value / (rs["priority_included_value_total"] + rs["standard_included_value_total"]) if (denom > 0) else None`

      Per-component dict carries `latency_blocks_mean` computed as `sum(c["latency_blocks_observations"]) / len(c["latency_blocks_observations"]) if c["latency_blocks_observations"] else 0.0` — Pitfall 5 / RESEARCH.md landmine #3, mirrors the Rust accessor `ComponentSummary::latency_blocks_mean()`.

      Use `.get(key, default)` only for the M6-noise-metric fields documented as defensive in `## Verified Schemas`. For all required snake_case fields, use square-bracket access so a schema regression surfaces as a clear KeyError (caught at the call site in Task 3's `run_build`).

      If `time_series.csv` exists, set `time_series = list(_read_time_series_long(ts_path))` and compute `peak_mempool_bytes = max((r["value"] for r in time_series if r["metric"] == "mempool_bytes" and r["lane"] == "total"), default=0)`. If missing, set `time_series = []`, `peak_mempool_bytes = None`, append warning per Pitfall 8.

    Constraints repeated for emphasis:
    - The Rust struct stores `latency_blocks_observations` (list); our JSON carries `latency_blocks_mean` (scalar). Pitfall 5: do not forget the reduction.
    - All snake_case keys come from `RunSummary` / `ComponentSummary` (collector.rs). Kebab vs snake split: this task touches only snake-case JSON; no `manifest[...]` access in this task (that's Task 3's `run_build`).
  </action>
  <verify>
    <automated>cd sim-rs && python3 -c "import sys; sys.path.insert(0, 'scripts/viz'); import build; assert hasattr(build, 'load_seed'); assert hasattr(build, '_read_time_series_long'); print('Task 2 API present')" && grep -q 'latency_blocks_observations' sim-rs/scripts/viz/build.py && grep -q 'latency_blocks_mean' sim-rs/scripts/viz/build.py && grep -q '_read_time_series_long' sim-rs/scripts/viz/build.py && grep -q 'priority_retained_value_total' sim-rs/scripts/viz/build.py && grep -q 'mempool_bytes' sim-rs/scripts/viz/build.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `build.py` defines `load_seed` and `_read_time_series_long` (importable from the module).
    - File contains the literal tokens `latency_blocks_observations` (the per-component list field) AND `latency_blocks_mean` (the computed scalar).
    - File contains `priority_retained_value_total` (snake_case from collector.rs).
    - `load_seed` uses `with open() as f: json.load(f)` for `run_summary.json` (not `json.loads(path.read_text())`; PATTERNS.md style).
    - `_read_time_series_long` is a generator (contains `yield`), not a list-builder.
    - When called against the fixture's seed directory, `load_seed(fixture_seed_dir, [])` returns a dict with non-empty `time_series` and `components` carrying `latency_blocks_mean` floats.
  </acceptance_criteria>
  <done>
    Ingest layer can read a single (job, seed) from disk and emit the per-seed dict shape locked in the interfaces block; downstream Task 3 wires this into the top-level builder.
  </done>
</task>

<task type="auto">
  <name>Task 3: Three-tier JSON emit + CLI orchestration (`_build_suite_json`, `_emit_seed_json`, `run_build`, `parse_args`, `main`)</name>
  <files>sim-rs/scripts/viz/build.py</files>
  <read_first>
    sim-rs/scripts/viz/build.py (current state from Tasks 1+2 — extend in place),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (Pattern B — argparse style; Shared Patterns / "no priority_only CSV in phase-2"),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md (Common Pitfalls #3, #4 — no per-suite CSV in phase-2; never parse metrics_comparison.txt),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-CONTEXT.md (D-09 three-tier layout; D-21 skip-and-warn; D-22 path-derived suite id)
  </read_first>
  <action>
    Wire Tasks 1 and 2 together with the three-tier emit logic and the CLI surface. After this task, `build.py` is complete (Plan 01-04 will later add `--serve` and `copy_static_assets`).

    (a) `_build_suite_json(suite: dict, source: Path, warnings: list[str]) -> dict` — iterate the suite's `manifest["jobs"]` (kebab on outer, but the suite's `jobs` is a dict-of-dicts as in the schema). For each `(job_name, seeds)`, for each `(seed, job_entry)`, compute `seed_dir = suite["dir"] / job_name / seed` (the manifest's `output-path` is optional and not always reliable; the seed dir is conventionally `<suite_dir>/<job_name>/<seed>`). Call `load_seed(seed_dir, warnings)`. If None, skip the seed (no headline) but still record `status`, `started_at`, `completed_at` from the manifest entry. The output `jobs[job_name].seeds[seed].headline` carries only the fields the cross-seed overlay needs (a subset of the per-(job, seed) JSON): retained_value, net_utility, retained_value_ratio, peak_mempool_bytes, per-component latency means.

    Set `aggregates = None` unconditionally for v1 — the per-suite `*.csv` does not exist in phase-2 (CRITICAL LANDMINE #2 / Pitfall 3). Add a comment quoting Pitfall 3: "Pitfall 3: phase-2's metrics writer emits only metrics_comparison.txt (prose-Markdown). The historical priority_only_fast_path_overall_comparison.csv lives under sim-rs/output/analysis/ from older work, not in any phase-2 suite. NEVER parse metrics_comparison.txt (Pitfall 4) — every field it carries is also in run_summary.json. If a future iteration emits suite-level CSVs, populate this field then; do not pretend it exists today."

    (b) `_emit_seed_json(suite_id: str, job_name: str, seed: str, seed_data: dict, output: Path) -> None` — `path = output / "data" / suite_id / f"{job_name}-{seed}.json"`, `path.parent.mkdir(parents=True, exist_ok=True)`, `json.dump(seed_data, path.open("w"), indent=2, sort_keys=True)`.

    (c) `run_build(source: Path, output: Path, includes: list[str], excludes: list[str], warnings: list[str]) -> None` — Resolve source to an absolute path. Discover suites. Build the per-suite JSON for each, emitting per-(job, seed) JSONs as we go. Build the index from the same suites (id, name, path, started_at, job_count, seed_count, completed_count, max_concurrent_jobs). Write `output/data/index.json`. Use `json.dump(..., indent=2, sort_keys=True)` everywhere so emission is deterministic across runs (lets future diff-based testing work cleanly). Do not raise on per-suite failures; every error path appends to `warnings`.

    (d) `parse_args() -> argparse.Namespace` — minimal stub for now: `--source` (Path, default `Path("sim-rs/output")`), `--output` (Path, default `Path("sim-rs/output/viz")`), `--include` (action="append", default=[]), `--exclude` (action="append", default=[]). Plan 01-04 expands with `--serve` and `--port`.

    (e) `main() -> None` — parse_args, call run_build, print warnings to `sys.stderr` (one bullet per warning, prefixed `  - `), print `"Wrote {output}/data/index.json with N suites"` to stderr. Don't add the `--serve` branch yet; Plan 01-04 lands it.

    (f) `if __name__ == "__main__": main()` guard at file end.

    Constraints repeated for emphasis:
    - **DO NOT parse `metrics_comparison.txt`** anywhere in build.py (Pitfall 4).
    - **DO NOT bind to `0.0.0.0`** when the serve helper eventually lands (Plan 01-04).
    - `aggregates: null` is unconditional for phase-2 (LANDMINE #2).

    Task 3 also runs the full Wave 0 unit test suite as part of its verification — the Wave 0 tests target the full public API and only land green once all three tasks of this plan are in place.
  </action>
  <verify>
    <automated>cd sim-rs && python3 -c "import sys; sys.path.insert(0, 'scripts/viz'); import build; print('module imports OK'); assert hasattr(build, 'discover_suites'); assert hasattr(build, 'load_seed'); assert hasattr(build, 'run_build'); assert hasattr(build, 'parse_args'); assert hasattr(build, 'main'); assert hasattr(build, 'LANE_FIELDS'); assert len(build.LANE_FIELDS) >= 12, f'LANE_FIELDS too short: {len(build.LANE_FIELDS)}'; print('full public API present')" && cd sim-rs && python3 -m unittest discover -s scripts/viz/tests -t scripts/viz -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - The module exports the five public names: `discover_suites`, `load_seed`, `run_build`, `parse_args`, `main`, and the constant `LANE_FIELDS` (>= 12 tuples).
    - File contains a comment containing "Pitfall 3" or "priority_only_fast_path_overall_comparison.csv" explaining that no suite-level CSV is emitted in phase-2. Grep: `grep -E 'Pitfall 3|priority_only_fast_path_overall_comparison' sim-rs/scripts/viz/build.py | grep -v '^\s*$'` returns >= 1 line.
    - File does NOT contain `metrics_comparison.txt` referenced as an open() target (Pitfall 4). Grep: `grep -E "open\\(.*metrics_comparison\\.txt" sim-rs/scripts/viz/build.py` returns nothing.
    - `python -m unittest discover -s sim-rs/scripts/viz/tests -t sim-rs/scripts/viz` runs all Wave 0 tests; the named tests pass (no skips, no errors). Specifically: `test_index_lists_all_manifests`, `test_jobs_match_manifest`, `test_headline_fields_present`, `test_time_series_long_form`, `test_seed_grouping_present`, `test_kebab_case_manifest_snake_case_run_summary`, `test_latency_blocks_observations_aggregated_to_mean`, `test_missing_time_series_csv_returns_empty_list_with_warning`, `test_phase_2_has_no_priority_only_fast_path_csv`, `test_malformed_manifest_skipped_with_warning`, `test_suite_id_derived_from_path_not_suite_name` are all green.
    - Running `python sim-rs/scripts/viz/build.py --source sim-rs/scripts/viz/tests/fixtures/mini-suite --output /tmp/viz-out-$$` (no `--serve` flag yet) produces `/tmp/viz-out-$$/data/index.json` with `suite_count >= 1` and at least one `/tmp/viz-out-$$/data/<suite_id>/<job>-<seed>.json`.
    - Every per-suite JSON emitted from the fixture has `aggregates: null` (CRITICAL LANDMINE #2 enforced).
  </acceptance_criteria>
  <done>
    All Wave 0 tests from Plan 01-01 are green; the build module's public API is in place; ad-hoc CLI run against the mini-suite fixture emits the three-tier JSON tree.
  </done>
</task>

</tasks>

<verification>

```bash
cd sim-rs
# Module imports + API surface
python3 -c "import sys; sys.path.insert(0,'scripts/viz'); import build; print(sorted(n for n in dir(build) if not n.startswith('_')))"

# All Wave 0 tests green
python3 -m unittest discover -s scripts/viz/tests -t scripts/viz -v

# End-to-end against the fixture
rm -rf /tmp/viz-out-check && python3 scripts/viz/build.py --source scripts/viz/tests/fixtures/mini-suite --output /tmp/viz-out-check
python3 -c "import json; idx = json.load(open('/tmp/viz-out-check/data/index.json')); print('suites:', len(idx['suites']))"
ls /tmp/viz-out-check/data/
```

</verification>

<success_criteria>
- All Wave 0 tests green (10 tests; 0 failures, 0 errors, 0 skips).
- `build.py` is stdlib-only, exports the locked public API, and emits the three-tier JSON layout for the mini-suite fixture in <1s.
- No `.gitignore` edits anywhere in the tree.
- `aggregates: null` in every per-suite JSON emitted from the fixture (CRITICAL LANDMINE #2 enforced).
- Suite IDs path-derived (D-22 / Pitfall 2 enforced).
- Latency observations reduced to means at build time (Pitfall 5 enforced).
</success_criteria>

<output>
After completion, create `.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-02-SUMMARY.md` per the standard summary template. Surface: the public API, the LANE_FIELDS mapping, which Pitfall (1-8) each function guards, and the line count of build.py (sanity-check target ~250-450 LOC).
</output>
</output>
