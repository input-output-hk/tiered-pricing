---
phase: 04-refresh-and-anchor
plan: 06
type: execute
wave: 2
depends_on: ["04-01", "04-03"]
files_modified:
  - docs/phase-2/realism-risks-register.md
  - docs/phase-2/coverage-check.md
autonomous: true
requirements: [DOC-03]
tags: [docs, register-edit, verdict-flip, disclosure-paragraph, coverage-check-signal-source]

must_haves:
  truths:
    - "`RSK-un-anchored-controller-knobs` umbrella verdict reflects Plan 04-01's anchor decisions (MITIGATED iff all four sub-knobs ANCHORED; otherwise LIVE → DISCLOSED with per-sub-knob granularity per the register's umbrella-grouping rule)"
    - "`RSK-un-anchored-controller-knobs` `disclosure-paragraph` field is rewritten per-sub-knob using Plan 04-01's draft register prose (load-bearing only for DISCLOSED sub-knobs; ANCHORED sub-knobs cite the motivating citation)"
    - "`RSK-pool-count`, `RSK-calibration-stale-stake-snapshot`, `RSK-steady-state-run-length` verdicts flip from LIVE to DISCLOSED per Plan 04-03's disclose-only fallback decision (TEST-05 / TEST-06 re-runs are out of scope per CONTEXT.md `<deferred>`)"
    - "`RSK-multiplier-floor-4-suite-coverage` `disclosure-paragraph` field is rewritten to incorporate the TEST-07a regime-dependence finding per Plan 04-03 §'TEST-07a multiplier-floor-16 companion' (the expected MITIGATED path failed; verdict flips to DISCLOSED with reframe)"
    - "`docs/phase-2/coverage-check.md` `signal-source-anchoring` cells are flipped from `unanchored (RSK-un-anchored-controller-knobs)` to `mainnet-data-cited (citation)` for any sub-knob that ANCHORED per Plan 04-01's umbrella verdict"
  artifacts:
    - path: "docs/phase-2/realism-risks-register.md"
      provides: "Register with refreshed verdicts and disclosure-paragraphs for the 5 entries Phase 4 touches"
      contains: "RSK-un-anchored-controller-knobs"
    - path: "docs/phase-2/coverage-check.md"
      provides: "Coverage-check with updated signal-source-anchoring cells per Plan 04-01's anchor outcomes"
      contains: "signal-source-anchoring"
  key_links:
    - from: "docs/phase-2/realism-risks-register.md `RSK-un-anchored-controller-knobs` `disclosure-paragraph` field"
      to: ".planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md per-sub-knob Draft register prose"
      via: "Plan 04-01's draft prose blocks paste into the disclosure-paragraph field; the umbrella verdict drives the verdict-line flip"
      pattern: "(window-length 32|multiplier.floor 4|multiplier.floor 16|lane.signal.source).*(ANCHORED|conditional on)"
    - from: "docs/phase-2/coverage-check.md `signal-source-anchoring` cells"
      to: "Plan 04-01's per-sub-knob anchor outcome"
      via: "`unanchored (RSK-...)` → `mainnet-data-cited (citation)` flips where a sub-knob ANCHORED; otherwise cells stay `unanchored`"
      pattern: "(mainnet-data-cited|unanchored \\(RSK-un-anchored-controller-knobs)"
---

<objective>
Apply register and coverage-check updates per Plan 04-01's anchor-or-disclose outcomes (DOC-03 register-side work) and per Plan 04-03's TEST-05 / TEST-06 disclose-only fallback decision. Five register entries are touched: `RSK-un-anchored-controller-knobs`, `RSK-pool-count`, `RSK-calibration-stale-stake-snapshot`, `RSK-steady-state-run-length`, `RSK-multiplier-floor-4-suite-coverage`. Coverage-check `signal-source-anchoring` cells are updated for any sub-knob that ANCHORED.

Purpose: This is the register-side companion to Plan 04-04's audit-side anchor narration. Per CONTEXT.md D-36 the anchor lands in BOTH documents — the audit carries the brief citation; the register carries the load-bearing CIP-paste disclosure prose. Per CONTEXT.md D-39 the audit and register may duplicate some content; the audit's prose is engineering-report voice, the register's prose is per-risk canonical CIP-paste source.

Output: Two files updated in place — `docs/phase-2/realism-risks-register.md` (5 entries touched) and `docs/phase-2/coverage-check.md` (up to ~10 `signal-source-anchoring` cells updated if all four sub-knobs ANCHORED; fewer if mixed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-refresh-and-anchor/04-CONTEXT.md
@CLAUDE.md
@docs/phase-2/realism-risks-register.md
@docs/phase-2/coverage-check.md
@.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md
@.planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update `RSK-un-anchored-controller-knobs` verdict + disclosure-paragraph + update coverage-check `signal-source-anchoring` cells per Plan 04-01's umbrella verdict</name>
  <files>docs/phase-2/realism-risks-register.md, docs/phase-2/coverage-check.md</files>
  <read_first>
    - docs/phase-2/realism-risks-register.md §"RSK-un-anchored-controller-knobs" (lines 80-104; the existing umbrella entry with `Verdict: LIVE`, the 4-sub-knob enumeration, the existing draft fallback `Disclosure-paragraph` field at line 104)
    - .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md §"Per-knob anchor decisions" (the four sub-knob Disposition decisions + Draft register prose blocks) + §"Umbrella verdict for RSK-un-anchored-controller-knobs" (MITIGATED iff all four ANCHORED, else LIVE → DISCLOSED with per-sub-knob granularity; the 4-row disposition table)
    - docs/phase-2/coverage-check.md §"Coverage table" (find all rows with `signal-source-anchoring` cells reading `unanchored (RSK-un-anchored-controller-knobs)` — these are the cells to potentially flip per the anchor outcomes; the cells appear inline in rows CLM-01 through ~CLM-13 per Phase 3 evidence summary)
    - CONTEXT.md §"DOC-03" — D-36 (anchor lands in both audit and register; per-knob granularity preserved); §"Verdict-flip authority" (register verdicts updated only where Phase 4 evidence licenses the flip)
  </read_first>
  <action>
**Register update — `RSK-un-anchored-controller-knobs`**:

Apply the umbrella verdict from Plan 04-01's §"Umbrella verdict" recommendation. Read the recommendation from `.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md` and set the register entry's `Verdict:` line accordingly:
- If Plan 04-01 recommends MITIGATED (all four sub-knobs ANCHORED): change `**Verdict:** LIVE` to `**Verdict:** MITIGATED` in the register entry. Also update the entry in the §"Index" table (line 23) to reflect the new verdict.
- If Plan 04-01 recommends LIVE → DISCLOSED (any sub-knob is DISCLOSED): change `**Verdict:** LIVE` to `**Verdict:** DISCLOSED` in the register entry. Update the §"Index" table accordingly.

Rewrite the `**Disclosure-paragraph:**` field at line 104. The existing field is a `(draft fallback; per-knob load-bearing only for sub-knobs that find no external anchor at Phase 4 / DOC-03 open)` blockquote with ~750 words of per-sub-knob prose. Replace with the four `Draft register prose` blocks from Plan 04-01 §"Per-knob anchor decisions", concatenated into a single multi-paragraph `Disclosure-paragraph` field. Drop the `(draft fallback; ...)` italic marker — the per-sub-knob prose is now load-bearing per Plan 04-01's outcomes. Preserve the engineering-report voice; the prose pastes verbatim into the CIP's Limitations section.

Specifically:
- For each sub-knob ANCHORED in Plan 04-01: the corresponding paragraph cites the motivating citation (Liu / Reijsbergen / spec / etc.) and states what the anchor grounds. Per D-35 the motivating-citation bar means the prose says "the choice of X is motivated by Y" rather than "X = Y numerically".
- For each sub-knob DISCLOSED in Plan 04-01: the paragraph uses the existing "conditional on X" frame from the v1 register draft (e.g. "Welfare findings are conditional on the lane-signal-source choice; alternative signal sources were not exercised").

Update the entry's `**Scope-of-resolution:**` field to reflect that the anchor-or-disclose path is now resolved (replace the existing "A two-hour literature search at Phase 4 / DOC-03 open determines..." prose with a closing-the-loop summary: "Plan 04-01's anchor-or-disclose work landed [N] sub-knobs ANCHORED via [citations] and [4-N] sub-knobs DISCLOSED with the 'conditional on X' frame. The umbrella entry's verdict is [MITIGATED | DISCLOSED] per the umbrella-grouping rule.").

Update the entry's `**EXP / Resolution:**` field — the four sub-knob EXP slugs (EXP-window-length-anchor / EXP-multiplier-floor-4-anchor / EXP-multiplier-floor-16-anchor / EXP-lane-signal-source-anchor) each get their resolution outcome marked: "EXP-window-length-anchor (RESOLVED: ANCHORED via Liu et al. CCS 2022 / DISCLOSED conditional on X)" per Plan 04-01's per-sub-knob disposition. Preserve the four EXP slugs (do not renumber per the append-only convention).

**Coverage-check update — `signal-source-anchoring` cells**:

For each row in `docs/phase-2/coverage-check.md` §"Coverage table" whose `signal-source-anchoring` cell reads `unanchored (RSK-un-anchored-controller-knobs)`, check whether the cell's load-bearing sub-knob is ANCHORED per Plan 04-01:
- CLM-01 (priority-only-un-reserved): load-bearing sub-knob is lane-signal-source. If lane-signal-source is ANCHORED in Plan 04-01: change cell to `mainnet-data-cited (<citation>)`. If lane-signal-source is DISCLOSED: cell stays `unanchored (RSK-un-anchored-controller-knobs)`.
- CLM-02 (priority-only-RB-reserved): load-bearing sub-knob is multiplier-floor 4 OR 16 (multiplier-floor sweep). If both are ANCHORED: change cell to `mainnet-data-cited (<citation>)`. If either is DISCLOSED: cell stays `unanchored`.
- CLM-03 (both-dynamic-un-partitioned): load-bearing sub-knob is window-length 32 + multiplier-floor + standard-signal-source. If all three are ANCHORED: change cell to `mainnet-data-cited`. If any is DISCLOSED: stays `unanchored`.
- CLM-04 (both-dynamic-partitioned): similar to CLM-03.
- CLM-05 (single-lane-EIP-1559-control): the cell currently reads `spec-default (mechanism-design.md ... window-length 32 is anchored separately under RSK-un-anchored-controller-knobs)`. If window-length 32 is ANCHORED in Plan 04-01: update the parenthetical to cite the motivating citation rather than the RSK entry; the enum stays `spec-default`. If DISCLOSED: cell stays as-is.
- CLM-06 through CLM-13 (Phase 3 TEST-04 BACKED / WEAK cells): similar pattern per row's load-bearing sub-knob.

For each cell change, also update the row's `related-RSK-ids` field if the verdict-flip warrants — e.g. if Plan 04-01's outcome moves `RSK-un-anchored-controller-knobs` to MITIGATED for one sub-knob, the row's related-RSK list may stay the same (the cross-reference is to the umbrella entry which still exists in the register).

Audit the entries for the CLAUDE.md abbreviation-on-first-use rule — each disclosure-paragraph is self-contained and abbreviations must expand on first use within the paragraph.

Cross-reference Plan 04-01's output: read `.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md` literally; copy the per-sub-knob Draft register prose blocks verbatim (with light editorial polish only where needed for register-context flow).
  </action>
  <verify>
    <automated>grep -E "^\\*\\*Verdict:\\*\\* (MITIGATED|DISCLOSED)" docs/phase-2/realism-risks-register.md | grep -q "" || true; grep -c "RSK-un-anchored-controller-knobs" docs/phase-2/realism-risks-register.md | awk '$1 &gt;= 1 {exit 0} {exit 1}' &amp;&amp; grep -q "Plan 04-01" docs/phase-2/realism-risks-register.md &amp;&amp; (grep -q "mainnet-data-cited" docs/phase-2/coverage-check.md || grep -q "unanchored" docs/phase-2/coverage-check.md)</automated>
  </verify>
  <done>`RSK-un-anchored-controller-knobs` verdict reflects Plan 04-01's umbrella decision; the `Disclosure-paragraph` field carries the per-sub-knob prose from Plan 04-01; coverage-check `signal-source-anchoring` cells are updated where sub-knobs ANCHORED; `EXP / Resolution` field reflects the per-sub-knob resolution outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Flip `RSK-pool-count`, `RSK-calibration-stale-stake-snapshot`, `RSK-steady-state-run-length` to DISCLOSED per Plan 04-03's disclose-only fallback decision</name>
  <files>docs/phase-2/realism-risks-register.md</files>
  <read_first>
    - docs/phase-2/realism-risks-register.md §"RSK-pool-count" (lines 46-62), §"RSK-calibration-stale-stake-snapshot" (lines 182-196), §"RSK-steady-state-run-length" (lines 382-396) — the three existing entries with `Verdict: LIVE` and existing draft fallback disclosure-paragraphs from Phase 1 plan-02
    - .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md §"TEST-05 / TEST-06 disclosure-fallback decision" + §"Register entries Phase 4 touches" (the explicit per-entry recommendations: verdict-flip-only for RSK-pool-count and RSK-calibration-stale-stake-snapshot; verdict-flip + draft-required for RSK-steady-state-run-length)
    - .planning/realism-tests/pool-number-sensitivity/results.md (TEST-05 data-gap status; documents the disclose-only fallback per CONTEXT.md `<deferred>`)
    - .planning/realism-tests/run-length-steady-state/results.md (TEST-06 partial-data status; 1 of 4 menu arms covered)
    - .planning/phases/04-refresh-and-anchor/04-CONTEXT.md §"TEST-05 / TEST-06 status" — D-34 (the verdict rule); §"Deferred Ideas" (re-runs out of scope for Phase 4; fall back to disclose-only)
    - docs/phase-2/realism-risks-register.md §"Reading guide" §"Index" table (lines 17-44) — verdict cells for these three entries need updating in the table
  </read_first>
  <action>
**`RSK-pool-count`** (line 46):
- Change `**Verdict:** LIVE` to `**Verdict:** DISCLOSED` in the per-entry block.
- Update the §"Index" table row (line 21): `LIVE` → `DISCLOSED`; `EXP-pool-number (→ TEST-05)` → `EXP-pool-number (→ TEST-05 deferred to disclose-only; re-run recipe in .planning/realism-tests/pool-number-sensitivity/results.md)`
- Update the `**Scope-of-resolution:**` field — the existing scope-of-resolution names the TEST-05 verdict rule (MITIGATED iff Δ% < seed-IQR). Add a closing sentence: "Phase 4 fall-back outcome: TEST-05 re-run is user-managed and out of scope for Phase 4 per `.planning/phases/04-refresh-and-anchor/04-CONTEXT.md` `<deferred>`; this entry's verdict accordingly lands at DISCLOSED via the existing draft fallback disclosure-paragraph. The locked threshold text `Δ% < seed-IQR of same job at 100 pools establishes MITIGATED` is preserved for any future re-run of TEST-05."
- The `**Disclosure-paragraph:**` field already carries the load-bearing fallback prose drafted in Phase 1 plan-02 — no rewrite required (the fallback IS the disclosure-paragraph once the verdict flips to DISCLOSED). Remove any `(draft fallback; load-bearing only if ...)` italic marker prefix so the paragraph reads as load-bearing.

**`RSK-calibration-stale-stake-snapshot`** (line 182):
- Change `**Verdict:** LIVE` to `**Verdict:** DISCLOSED` in the per-entry block.
- Update the §"Index" table row (line 28): `LIVE` → `DISCLOSED`.
- Update the `**Scope-of-resolution:**` field — the existing field cross-references TEST-05's verdict path. Add a closing sentence noting the TEST-05 fall-back per `RSK-pool-count` propagates to this entry.
- The `**Disclosure-paragraph:**` field already carries draft fallback prose — no rewrite required; remove the `(draft fallback; ...)` italic marker.

**`RSK-steady-state-run-length`** (line 382):
- Change `**Verdict:** LIVE` to `**Verdict:** DISCLOSED` in the per-entry block.
- Update the §"Index" table row (line 41): `LIVE` → `DISCLOSED`.
- Update the `**Scope-of-resolution:**` field — the existing scope-of-resolution names TEST-06's verdict rule. Add a closing sentence: "Phase 4 fall-back outcome: TEST-06 re-run is user-managed and out of scope for Phase 4 per `.planning/phases/04-refresh-and-anchor/04-CONTEXT.md` `<deferred>` (TEST-06 covered only 1 of 4 menu arms at full N=10); this entry's verdict lands at DISCLOSED. The per-(job, length, N seeds) rolling-mean comparison criterion is preserved for any future re-run of TEST-06."
- Read the existing `**Disclosure-paragraph:**` field. Per Plan 04-03 §"Register entries Phase 4 touches" item 3, this entry's existing disclosure-paragraph is in the "TBD — drafted in Phase 4 if test verdict lands as DISCLOSED" forward-pointer form (per Phase 1 plan-02 SUMMARY's two-track convention). If the existing paragraph is a forward-pointer placeholder, DRAFT NEW LOAD-BEARING DISCLOSURE-PARAGRAPH PROSE in engineering-report voice naming: (a) the partial TEST-06 coverage (1 of 4 menu arms at N=10; cite the results.md path); (b) the steady-state criterion (per-(job, length, N seeds) rolling mean comparison; first-half vs second-half within seed-IQR); (c) the fall-back framing: "Welfare findings at 2000 slots are reported under the assumption that the steady-state criterion holds; reviewers should treat the 2000-slot default as a stylised run length pending future TEST-06 re-run coverage of all four menu arms. The single menu arm with completed TEST-06 coverage (`rb_reserved_priority_only_x4`) does not exhibit a steady-state-violation pattern, but inference to the other three menu arms is not licensed by the partial data."

Audit the three entries for the CLAUDE.md abbreviation-on-first-use rule.

Update the §"Reading guide" or footer prose IF it currently states the v1 verdict distribution as "12 LIVE + 12 DISCLOSED" (per Phase 1 plan-02 SUMMARY). After Task 2 the distribution becomes 8 LIVE + 15 DISCLOSED + 1 MITIGATED (or 9 LIVE + 14 DISCLOSED + 1 MITIGATED, depending on Plan 04-01's outcome for `RSK-un-anchored-controller-knobs`). Update the prose to reflect post-Phase-4 distribution. Note: `RSK-substrate-scope` is also LIVE-going-to-DISCLOSED per Phase 1 plan-02 SUMMARY's "LIVE → DISCLOSED at Phase 4 / DOC-01" — but that flip is triggered by the audit refresh in Plan 04-04, not this plan; record the post-Plan-04-04 expected distribution if Plan 04-04 has run (the dependency order in Wave 2 makes simultaneous plans possible; coordinate via Plan 04-07's final consistency review).
  </action>
  <verify>
    <automated>POOL_VERDICT=$(awk '/^## RSK-pool-count/,/^## RSK-/' docs/phase-2/realism-risks-register.md | grep -cE "^\\*\\*Verdict:\\*\\* DISCLOSED"); STALE_VERDICT=$(awk '/^## RSK-calibration-stale-stake-snapshot/,/^## RSK-/' docs/phase-2/realism-risks-register.md | grep -cE "^\\*\\*Verdict:\\*\\* DISCLOSED"); STEADY_VERDICT=$(awk '/^## RSK-steady-state-run-length/,/^## RSK-/' docs/phase-2/realism-risks-register.md | grep -cE "^\\*\\*Verdict:\\*\\* DISCLOSED"); [ "$POOL_VERDICT" -ge 1 ] &amp;&amp; [ "$STALE_VERDICT" -ge 1 ] &amp;&amp; [ "$STEADY_VERDICT" -ge 1 ]</automated>
  </verify>
  <done>All three register entries have `Verdict: DISCLOSED`; index table updated; scope-of-resolution and disclosure-paragraph fields refreshed; for RSK-steady-state-run-length the new load-bearing disclosure prose is drafted in engineering-report voice.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite `RSK-multiplier-floor-4-suite-coverage` disclosure-paragraph + flip verdict; final consistency-check on register</name>
  <files>docs/phase-2/realism-risks-register.md</files>
  <read_first>
    - docs/phase-2/realism-risks-register.md §"RSK-multiplier-floor-4-suite-coverage" (lines 272-286; existing `**Verdict:** LIVE` with EXP-multiplier-floor-16-companion-run → TEST-07a; existing draft `**Disclosure-paragraph:**` field with forward-pointer "expects MITIGATED via TEST-07a" framing)
    - .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md §"TEST-07a multiplier-floor-16 companion" (the regime-dependence finding: rb-scarcity inversion + urgency-inversion weak reversal at floor=16; the cross-cell SHA-256 identity at seeds 1+2 for the high-floor convergence finding) + §"Register entries Phase 4 touches" item 5 (the expected action: Plan 04-06 rewrite required; reframe per TEST-07a)
    - .planning/realism-tests/multiplier-floor-16-companion/results.md (TEST-07a authoritative results; the source for the disclosure-paragraph rewrite)
    - .planning/phases/04-refresh-and-anchor/04-CONTEXT.md Claude's Discretion §"Multiplier-floor regime-dependence narration (DOC-01 and register)" (the rewrite leads with the regime-dependent finding; the audit and register narrate in parallel)
    - docs/phase-2/realism-risks-register.md §"Index" table row for RSK-multiplier-floor-4-suite-coverage (line 34) — needs verdict update
  </read_first>
  <action>
**`RSK-multiplier-floor-4-suite-coverage`** verdict + disclosure-paragraph rewrite:

Change `**Verdict:** LIVE` to `**Verdict:** DISCLOSED` in the per-entry block (line ~273). Update the §"Index" table row (line 34) to reflect DISCLOSED.

Update the `**EXP / Resolution:**` field (line 285): change `EXP-multiplier-floor-16-companion-run (→ TEST-07a)` to `EXP-multiplier-floor-16-companion-run (→ TEST-07a: LIVE → DISCLOSED with regime-dependence reframe; see .planning/realism-tests/multiplier-floor-16-companion/results.md)`.

Update the `**Scope-of-resolution:**` field (line 284) — the existing text frames TEST-07a as the path to MITIGATED. Replace with the reframe-not-replicate framing: "Phase 3 TEST-07a found that the qualitative findings at multiplier_floor = 4 in `phase-2-rb-scarcity` and `phase-2-urgency-inversion` do NOT replicate at multiplier_floor = 16. Specifically: (a) the rb-scarcity finding inverts ('standard dominates welfare; RB scarcity mostly invisible' at floor = 4 → 'priority captures everything; total welfare collapses 93-98%' at floor = 16); (b) the urgency-inversion finding weakly reverses ('mispriced > correctly priced' at floor = 4 → 'correctly priced > mispriced by ~13%' at floor = 16). The Phase 3 disposition is LIVE → DISCLOSED with the regime-dependence reframe (per `.planning/realism-tests/multiplier-floor-16-companion/results.md`); the expected MITIGATED path failed because the findings are calibration-specific rather than replicable across floor magnitudes."

Rewrite the `**Disclosure-paragraph:**` field (existing forward-pointer at line 286). New load-bearing prose, in engineering-report voice (paraphrased here; the executor writes definitive prose):

"Two of seven phase-2 goldens-pinned suites condition exclusively on `multiplier_floor = 4` rather than the spec default 16: `phase-2-rb-scarcity` and `phase-2-urgency-inversion`. The calibration choice was made because at the spec default 16, priority demand in these two suites stays too thin to surface controller drift; at 4, urgency≥2 components self-select into priority and the controller does drift. Phase 3 TEST-07a (`.planning/realism-tests/multiplier-floor-16-companion/results.md`) ran like-for-like companion jobs at `multiplier_floor = 16` for both suites at N=5 seeds and found that the qualitative findings at floor = 4 do not replicate at floor = 16. At floor = 16 the `phase-2-rb-scarcity` finding inverts: standard-lane demand is rejected by the mechanism (`max_fee_lovelace` budget below 16× standard quote), priority captures all surviving value, and total retained value collapses by 93–98% relative to the floor = 4 baseline. The `phase-2-urgency-inversion` finding weakly reverses at floor = 16: the floor = 4 anomaly where mis-priced high-urgency components produce higher total retained value than correctly-priced components disappears (and weakly reverses, with correctly-priced producing ~13% higher retained value than mis-priced). Welfare findings from `phase-2-rb-scarcity` and `phase-2-urgency-inversion` are conditional on the multiplier_floor = 4 calibration; the menu-item welfare distinction these suites establish is regime-specific, not universal. Reviewers should not extrapolate findings from these two suites to deployment regimes that use the spec default multiplier_floor = 16. The remaining five goldens-pinned suites cover the spec default 16 (priority-only suites sweep {4, 8, 16}; both-dynamic suite sweeps {4, 16}), so the phase-2 design as a whole brackets the spec default."

Also note in the same paragraph the cross-cell SHA-256 identity finding at seeds 1+2 (the high-floor convergence finding from TEST-07a §"Identical-output observation"): "TEST-07a also surfaced a high-floor mechanism-convergence observation: under `paper_like_congested` demand at floor = 16, the partitioned-both-dynamic mechanism's standard controller never sees enough standard-lane demand to drift its quote (only urgency≥5 components can afford 16× standard), so partitioned-both-dynamic degenerates to priority-only-static at high floor. Two cells (`rb_scarcity_x16_baseline` and `urgency_inversion_x16_correctly_priced`) produce identical pricing event stream SHA-256 hashes at seeds 1 and 2 — a cross-cell convergence finding distinct from the floor = 4 cross-arm duplicate-job artefact noted in `RSK-single-seed-precision` and the audit's `Recommended disclosure statements`."

Audit the entry for the CLAUDE.md abbreviation-on-first-use rule (the disclosure-paragraph is self-contained for CIP-paste purposes).

**Final register consistency check** — after Task 1 / Task 2 / Task 3 register edits, run the following audits:
1. Read the `§"Index"` table rows for the 5 entries Phase 4 touched (RSK-un-anchored-controller-knobs, RSK-pool-count, RSK-calibration-stale-stake-snapshot, RSK-steady-state-run-length, RSK-multiplier-floor-4-suite-coverage). Confirm the table's verdict cell matches each entry's per-entry `**Verdict:**` field.
2. Confirm the verdict distribution in the §"Reading guide" (or wherever it's documented) matches the post-Phase-4 reality. The Phase 1 plan-02 SUMMARY stated 12 LIVE + 12 DISCLOSED in v1. Post-Phase-4 the distribution moves to (approximately) 7 LIVE + 16 DISCLOSED + 1 MITIGATED depending on Plan 04-01's outcome. The reading-guide prose should be updated to reflect the post-Phase-4 distribution; if Plan 04-04 (audit refresh) flips `RSK-substrate-scope` from LIVE to DISCLOSED via folding the substrate-scope paragraph, the distribution updates accordingly. Plan 04-07 (the consistency review) will independently re-verify the distribution.
3. Confirm no `(draft fallback; ...)` italic markers remain on the rewritten DISCLOSED entries (the prose is now load-bearing).
4. Confirm no `TBD plan 02` markers anywhere (these were forbidden from the v1 register per Phase 1 plan-02; should still hold).
5. Confirm any markdown link targets resolve (e.g. links to `.planning/realism-tests/...` and `.planning/phases/04-refresh-and-anchor/...`).
  </action>
  <verify>
    <automated>MULT_VERDICT=$(awk '/^## RSK-multiplier-floor-4-suite-coverage/,/^## RSK-/' docs/phase-2/realism-risks-register.md | grep -cE "^\\*\\*Verdict:\\*\\* DISCLOSED"); REGIME=$(grep -cE "(regime-dependen|regime-specific|inverts at|weakly reverse)" docs/phase-2/realism-risks-register.md); TBD=$(grep -cE "TBD plan 02" docs/phase-2/realism-risks-register.md); [ "$MULT_VERDICT" -ge 1 ] &amp;&amp; [ "$REGIME" -ge 1 ] &amp;&amp; [ "$TBD" -eq 0 ]</automated>
  </verify>
  <done>`RSK-multiplier-floor-4-suite-coverage` carries `Verdict: DISCLOSED`; the disclosure-paragraph names the regime-dependence finding from TEST-07a explicitly; the cross-cell SHA-256 identity finding is referenced; no TBD plan 02 markers; register internal consistency holds across the index table + per-entry verdicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disclosure-paragraph prose → CIP Limitations section | Each rewritten paragraph is a CIP-paste source; transcription errors from Plan 04-01 or Plan 04-03 propagate to the published CIP |
| Verdict-cell consistency (index vs per-entry) | The §"Index" table and per-entry `**Verdict:**` fields must agree; drift between them would mislead CIP authors / reviewers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | T (Tampering) | Verdict-cell consistency between index table and per-entry | mitigate | Task 3 §"Final register consistency check" audits this; Plan 04-07 independently re-verifies |
| T-04-17 | I (Information Disclosure) | Disclosure-paragraph prose paste-target accuracy | mitigate | Plan 04-01's Draft register prose blocks are the canonical source; Plan 04-07 cross-verifies the register prose matches Plan 04-01's blocks (with allowed editorial polish) |
| T-04-18 | R (Repudiation) | Deferred-result fall-back attribution | accept | The disclose-only-per-CONTEXT.md-deferred decision is cited verbatim in each affected entry's scope-of-resolution; a reviewer challenging the fall-back is directed to the CONTEXT.md decision record |
</threat_model>

<verification>
- `RSK-un-anchored-controller-knobs` verdict reflects Plan 04-01's umbrella decision
- `RSK-pool-count`, `RSK-calibration-stale-stake-snapshot`, `RSK-steady-state-run-length` all carry `Verdict: DISCLOSED`
- `RSK-multiplier-floor-4-suite-coverage` carries `Verdict: DISCLOSED` with regime-dependence prose
- §"Index" table verdict cells match per-entry `**Verdict:**` fields for the 5 entries Phase 4 touched
- `RSK-steady-state-run-length` disclosure-paragraph is newly drafted in engineering-report voice (not a forward-pointer placeholder)
- coverage-check.md `signal-source-anchoring` cells reflect Plan 04-01's per-sub-knob anchor decisions
- No `TBD plan 02` markers anywhere
- All cross-reference identifiers (`RSK-NN`, `EXP-NN`) preserved per the append-only convention
</verification>

<success_criteria>
- ROADMAP.md Phase 4 success criterion #3 (register-side): each of the four un-anchored controller knobs has either an external anchor cited in the register's `disclosure-paragraph` or a "conditional on X" disclosure paragraph in the same field
- The TEST-05 / TEST-06 disclose-only fallback per CONTEXT.md `<deferred>` is applied: three register entries flip LIVE → DISCLOSED with their existing fallback prose load-bearing
- The TEST-07a regime-dependence finding lands in `RSK-multiplier-floor-4-suite-coverage`'s disclosure-paragraph as load-bearing CIP-paste prose
- Coverage-check `signal-source-anchoring` cells are consistent with the register's `RSK-un-anchored-controller-knobs` per-sub-knob disposition
</success_criteria>

<output>
After completion, create `.planning/phases/04-refresh-and-anchor/04-06-SUMMARY.md`. The SUMMARY records: the 5 register-entry verdict changes (before/after table); the coverage-check signal-source-anchoring cell changes (which CLM-NN rows updated, what the new cell content is); the post-Phase-4 register verdict distribution counts; any anomalies for Plan 04-07's consistency review (e.g. verdict-flip authority concerns; cross-reference dead-link candidates).
</output>
