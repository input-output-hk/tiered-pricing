---
phase: 04-refresh-and-anchor
plan: 05
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - docs/phase-2/validity-threats.md
autonomous: true
requirements: [DOC-02]
tags: [docs, validity-threats, per-suite-trust, rsk-cross-reference, clm-cross-reference]

must_haves:
  truths:
    - "The 'Resolved 2026-05-13' and 'Resolved 2026-05-14' historical sections are folded into per-suite prose per CONTEXT.md D-40 (refresh in place; banners stripped)"
    - "Each of the 19 per-suite blocks acquires an explicit `Related RSK:` cross-reference field listing the `RSK-NN` identifiers from `docs/phase-2/realism-risks-register.md` that apply to that suite"
    - "Per-suite trust verdicts are reconciled with the register's RSK-derived verdicts; the 4 formerly-UNRESOLVED suites get refreshed verdicts derived from Phase 2's output-read (now BACKED/WEAK rows in coverage-check.md)"
    - "Menu-item-trade-off claims identified by `docs/phase-2/coverage-check.md` are added as CLM-NN cross-references in the appropriate per-suite Trust sub-section per CONTEXT.md D-40"
    - "The per-suite matrix structure is preserved per CONTEXT.md D-40 (not flattened to a thin pointer; full per-suite reviewability retained as an accountability artefact)"
  artifacts:
    - path: "docs/phase-2/validity-threats.md"
      provides: "Refreshed per-suite trust matrix; RSK-NN cross-references per suite; CLM-NN cross-references where applicable; historical banners folded inline"
      min_lines: 500
      contains: "Per-suite claims and trust ratings"
  key_links:
    - from: "docs/phase-2/validity-threats.md per-suite blocks"
      to: "docs/phase-2/realism-risks-register.md `RSK-NN` entries"
      via: "explicit 'Related RSK:' field per suite block listing applicable RSK-NN identifiers"
      pattern: "Related RSK: ?RSK-"
    - from: "docs/phase-2/validity-threats.md per-suite Trust sub-sections"
      to: "docs/phase-2/coverage-check.md `CLM-NN` rows"
      via: "CLM-NN cross-references where the suite is named in `backing-suite` cells"
      pattern: "CLM-[0-9]+"
---

<objective>
In-place refresh of `docs/phase-2/validity-threats.md` per CONTEXT.md D-40: retain the per-suite trust matrix; strip the 2026-05-13 and 2026-05-14 historical banners; fold their content into per-suite prose; reconcile per-suite verdicts with the register; add `RSK-NN` and `CLM-NN` cross-references per suite block.

Purpose: This is the per-suite accountability artefact. Per CONTEXT.md D-40 this deliberately deviates from Phase 1 D-01's "validity-threats becomes a thin pointer" prescription — the matrix is preserved because per-suite reviewability is load-bearing for any reviewer drilling into a specific suite's evidence. Three documents (register / coverage-check / validity-threats) coexist with some duplicated content; the register holds the per-risk inventory, the coverage-check holds the per-claim coverage, the validity-threats holds the per-suite trust matrix.

Output: `docs/phase-2/validity-threats.md` refreshed in place. Approximate length target: 713 → 600-800 lines per CONTEXT.md D-40 (refreshed but not restructured).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-refresh-and-anchor/04-CONTEXT.md
@CLAUDE.md
@docs/phase-2/validity-threats.md
@docs/phase-2/realism-risks-register.md
@docs/phase-2/coverage-check.md
@.planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md
@.planning/phases/01-register-inventory/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip historical banner sections + refresh header / TL;DR / Family-B-decision / Trust framework sections</name>
  <files>docs/phase-2/validity-threats.md</files>
  <read_first>
    - docs/phase-2/validity-threats.md (full file at 713 lines; specifically lines 1-184: header / TL;DR (lines 11-24) / `## Resolved 2026-05-13` section (lines 26-40) / `## Resolved 2026-05-14` section (lines 41-99) / `## Family B decision` section (lines 100-142) / `## Trust framework` section (lines 143-183))
    - .planning/phases/04-refresh-and-anchor/04-CONTEXT.md §"DOC-01 / DOC-02 refresh strategy" — D-40 (per-suite matrix retained; banners folded inline; verdicts reconciled with register; CLM cross-references added; 713 → 600-800 lines target) + Claude's Discretion §"Per-suite trust-matrix refresh scope (DOC-02)" (keep all 19; the 4 formerly-UNRESOLVED suites get refreshed verdicts derived from Phase 2's output-read)
    - .planning/family-b-decision-2026-05-14.md (the chain-derived commit memo)
    - .planning/mechanism-welfare-impact-2026-05-14.md (the welfare-impact characterisation)
    - .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md (the Phase 3 headline finding to be reflected in the refreshed TL;DR)
  </read_first>
  <action>
Refresh header + §"TL;DR" + strip-and-fold `## Resolved 2026-05-13` + strip-and-fold `## Resolved 2026-05-14` + refresh §"Family B decision" + retain §"Trust framework" (lightly polished for tense / abbreviation conformance).

Header — Keep the H1 title `# Validity threats — phase-2 dynamic-pricing simulator`. Update the date line to `Date: 2026-05-18`. Keep Branch / Scope / Companion-to lines. Add an abbreviation-on-first-use line per CLAUDE.md: "Abbreviations on first use: Cardano Improvement Proposal (CIP), Ethereum Improvement Proposal 1559 (EIP-1559), ranking block (RB), endorser block (EB), Realism Risk (RSK) identifier in `docs/phase-2/realism-risks-register.md`, claim identifier (CLM) in `docs/phase-2/coverage-check.md`, Inter-Quartile Range (IQR), Bias-corrected and accelerated (BCa) bootstrap, Maximum Extractable Value (MEV)."

§"TL;DR" — rewrite to reflect the post-banner state. The current TL;DR carries two issues: (a) it warns about `topology.default.yaml` vs `topology-single-producer.yaml` mismatch (closed by the 2026-05-13 banner — fold the close-state into the TL;DR); (b) it doesn't reflect Phase 3 findings. Rewrite to: open with the operational state ("Of 19 phase-2 suite YAMLs in `parameters/phase-2-sweep/suites/`, this document gives a per-suite trust assessment for the CIP author who wants to know which claims each suite can fairly license"); state aggregate counts as a placeholder noting Task 2 fills in; state the topology-realistic-100 operational truth with epoch-582 / 2026-05-14 source; state the WR-1 RESOLVED disposition citing the chain-derived Family B commit; state the Phase 3 headline (un-reserved arms outperform single-lane EIP-1559; RB-reserved arms underperform; multiplier_floor calibration regime-dependent at floor=16) cross-referencing `.planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md`.

Strip `## Resolved 2026-05-13` section (lines 26-40) — remove entirely. Its content (topology-correction history) is now part of the operational state described in the refreshed TL;DR.

Strip `## Resolved 2026-05-14` section (lines 41-99) — remove the blockquote framing entirely. Fold the load-bearing content (the WR-1 / Family-B-faithfulness resolution + the four sign-flip cells named: `eip1559_d4_t50_w32`, `eip1559_d8_t25_w32`, `rb_reserved_x4_rb_quarter`, `partitioned_x4_rb_quarter`) into the refreshed §"Family B decision" section. The per-suite blockquote on which suites should report which flips is folded into individual per-suite blocks in Task 2.

§"Family B decision" — refresh in place. Current content (lines 100-142) is good shape but tense-fragile (uses "today", "now", "as of 2026-05-14"). Rewrite to declarative authoritative voice naming the chain-derived commit memo `.planning/family-b-decision-2026-05-14.md`. Reduce length to 20-30 lines; the Phase 3 N=20 evidence supersedes the N=1 33-job-sundaeswap characterisation as the authoritative welfare-distinction source (cite Plan 04-03's summary for the multi-seed evidence; cite `.planning/mechanism-welfare-impact-2026-05-14.md` for the broader 33-job sundaeswap context).

§"Trust framework" — keep largely intact (lines 143-183 define HIGH / MEDIUM / LOW / UNRESOLVED rating semantics). Update the "UNRESOLVED" definition to note that the 4 formerly-UNRESOLVED non-pinned suites have been resolved via Phase 2's output-read (Phase 2 SUMMARY 02-02 walked the output directories and promoted UNBACKED to WEAK / BACKED per the data). Per CONTEXT.md Claude's Discretion §"Per-suite trust-matrix refresh scope (DOC-02)" keep all 19 suite blocks in Task 2; the 4 formerly-UNRESOLVED suites get refreshed verdicts derived from Phase 2's output-read.

Audit the refreshed sections for the abbreviation-on-first-use rule.

Word-count target for this task: 90-130 lines combined.
  </action>
  <verify>
    <automated>(! grep -q "^## Resolved 2026-05-13" docs/phase-2/validity-threats.md) &amp;&amp; (! grep -q "^## Resolved 2026-05-14" docs/phase-2/validity-threats.md) &amp;&amp; grep -q "topology-realistic-100" docs/phase-2/validity-threats.md &amp;&amp; grep -q "Family B" docs/phase-2/validity-threats.md &amp;&amp; grep -q "Realism Risk (RSK)" docs/phase-2/validity-threats.md &amp;&amp; grep -q "claim identifier (CLM)" docs/phase-2/validity-threats.md</automated>
  </verify>
  <done>The `Resolved 2026-05-13` and `Resolved 2026-05-14` section headings are gone; the historical content is folded into the refreshed TL;DR and §"Family B decision"; the abbreviation-on-first-use rule covers RSK and CLM in the header.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh all 19 per-suite blocks — add Related-RSK + Related-CLM fields; reconcile verdicts with register; resolve the 4 formerly-UNRESOLVED suites</name>
  <files>docs/phase-2/validity-threats.md</files>
  <read_first>
    - docs/phase-2/validity-threats.md (post-Task-1 state; §"Per-suite claims and trust ratings" at lines ~184-563; 19 suite blocks structured as `#### <suite>.yaml` + Demand / Question / Claim it would license / Internal threats / External threats / Statistical / Trust / Caveats sub-fields)
    - docs/phase-2/realism-risks-register.md (full 24 RSK-NN entries; for each suite block, identify which RSK-NN entries apply based on the suite's content — e.g. RB-reserved suites cite `RSK-partition-activated-honest-producer`; multiplier_floor = 4 conditional suites cite `RSK-multiplier-floor-4-suite-coverage`; un-reserved suites cite `RSK-un-anchored-controller-knobs` for the lane-signal-source sub-knob)
    - docs/phase-2/coverage-check.md (the CLM-NN rows; for each suite block, identify which CLM-NN rows have that suite in their `backing-suite` cell — there are explicit `backing-suite` cells citing each suite's YAML path)
    - .planning/phases/01-register-inventory/01-02-SUMMARY.md (the 4 formerly-UNRESOLVED non-pinned suites: `phase-2-moderate-priority-only`, `phase-2-moderate-both-dynamic`, `phase-2-realistic-both-dynamic`, `phase-2-sundaeswap-both-dynamic`; their post-Phase-2 verdicts are derivable from `docs/phase-2/coverage-check.md` rows that cite them)
    - .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md (the Phase 3 per-cell verdicts — TEST-03 + TEST-04 cells correspond to specific suites: phase-2-priority-only-rb-reserved, phase-2-priority-only-unreserved, phase-2-two-lane-both-dynamic, phase-2-eip1559-robustness, phase-2-eip1559-smoothing; these suites' per-suite Trust verdicts may move based on the N=20 BCa CI evidence)
  </read_first>
  <action>
For each of the 19 per-suite blocks under §"Per-suite claims and trust ratings", apply the following refresh:

Add a new `Related RSK:` field between `External threats:` and `Statistical:` (or where natural in the block). The field is a comma-separated list of `RSK-NN` identifiers from `docs/phase-2/realism-risks-register.md` that bear on this suite's claims. The selection rule per suite:
- Every suite with multiplier_floor sweep: `RSK-un-anchored-controller-knobs` (multiplier-floor 4 / 16 sub-knobs)
- Every suite with EIP-1559 D / target / window sweep: `RSK-un-anchored-controller-knobs` (window-length 32 sub-knob)
- RB-reserved suites: `RSK-partition-activated-honest-producer`
- Un-reserved suites with priority signal source: `RSK-un-anchored-controller-knobs` (lane-signal-source sub-knob)
- Both-dynamic suites: `RSK-un-anchored-controller-knobs` (both-dynamic standard signal source sub-knob); `RSK-standard-user-fee-drift-exposure`
- Suites conditioning on multiplier_floor = 4 exclusively (`phase-2-rb-scarcity`, `phase-2-urgency-inversion`): `RSK-multiplier-floor-4-suite-coverage`
- Every suite with 3-seed-default statistical scope: `RSK-three-seed-statistical-power` (mitigated for the 5 jobs reborn at N=20 via TEST-04; cite the mitigation if applicable)
- Every suite using sundaeswap demand profile: `RSK-sundaeswap-demand-staleness`
- Every suite with paper_like_mispriced: `RSK-max-fee-policy-default`
- All suites: `RSK-single-seed-precision` (mitigated to BACKED only for the cells TEST-03 / TEST-04 covered)
- All suites: `RSK-substrate-scope` (universal; the substrate-scope umbrella applies to every suite by construction since the substrate is shared)

The list should be 3-8 RSK-NN per suite (not a complete cross-product). Use judgement — only include entries where the connection is non-trivial.

Add a new `Related CLM:` field after `Related RSK:`. The field is a comma-separated list of `CLM-NN` identifiers from `docs/phase-2/coverage-check.md` that have the current suite in their `backing-suite` cell. Search the coverage table for the suite's YAML path; record the matching `CLM-NN` IDs. Coverage-check rows that cite the suite get cross-referenced here.

Reconcile per-suite Trust verdict with the register's RSK-derived verdicts per CONTEXT.md D-40. The reconciliation rule:
- A suite's Trust verdict should be consistent with the verdicts of its `Related RSK:` entries.
- If a suite's Trust is currently MEDIUM but a Related-RSK entry has flipped to MITIGATED (e.g. `RSK-single-seed-precision` is MITIGATED for the TEST-04 cells at N=20), the Trust may be upgraded to HIGH for those specific claim-shapes — but document the upgrade with rationale tied to the specific TEST-NN evidence.
- If a suite's Trust is currently LOW (e.g. `phase-2-rb-scarcity` and `phase-2-urgency-inversion` are currently LOW because they condition on multiplier_floor = 4), the Trust stays LOW per CONTEXT.md (the regime-dependence finding from TEST-07a is now in `RSK-multiplier-floor-4-suite-coverage`'s disclosure-paragraph, but the suites still license claims conditional on a calibration choice the register marks as load-bearing).
- For the 4 formerly-UNRESOLVED suites (`phase-2-moderate-priority-only`, `phase-2-moderate-both-dynamic`, `phase-2-realistic-both-dynamic`, `phase-2-sundaeswap-both-dynamic`): replace the UNRESOLVED verdict with a verdict derived from Phase 2's output-read — look up the suite's CLM rows in `docs/phase-2/coverage-check.md`; if WEAK rows dominate the suite's coverage, the verdict is MEDIUM with annotation "Phase 2 output-read promoted the originally-UNRESOLVED rows to WEAK; further upgrade requires multi-seed evidence"; if mixed BACKED/WEAK, the verdict is MEDIUM. Cross-reference the relevant CLM-NN rows.

Update per-suite Caveats to fold the historical "WR-1 (rollback)" caveats into a single closing line per block: "WR-1 (controller contamination on slot-battle reorg) is RESOLVED by the chain-derived Family B commit on 2026-05-14 per `.planning/family-b-decision-2026-05-14.md`; the historical WR-1 caveat in this block is preserved as audit trail but no longer constrains the rating." (Per CONTEXT.md D-40 the per-suite WR-1 mentions are preserved as historical context but do not constrain the rating today.)

For the 5 suites with Phase 3 N=20 evidence (`phase-2-priority-only-rb-reserved`, `phase-2-priority-only-unreserved`, `phase-2-two-lane-both-dynamic`, `phase-2-eip1559-robustness`, `phase-2-eip1559-smoothing`), add a new `Phase 3 evidence:` sub-field after `Statistical:` summarising the relevant TEST-03 / TEST-04 / TEST-07a cell verdicts per Plan 04-03's evidence summary. For `phase-2-rb-scarcity` and `phase-2-urgency-inversion`, add the TEST-07a regime-dependence finding to the `Phase 3 evidence:` sub-field.

Aggregate trust summary table (existing at lines ~564-577) — recompute counts after the per-suite verdict refresh. The table tallies HIGH / MEDIUM / LOW counts (the UNRESOLVED row is removed since the 4 formerly-UNRESOLVED suites now carry MEDIUM verdicts).

Cross-cutting threats (lines ~578-616) — light-touch refresh: any references to "topology-single-producer.yaml" → "topology-realistic-100.yaml"; any references to "WR-1 (dormant)" or "WR-1 (live)" → "WR-1 (RESOLVED via Family B)"; apply triple format to any in-prose calibration values.

Recommendations to raise trust (lines ~617-661) — refresh: remove items the milestone has now resolved (the Phase 2 output-read on the 4 UNRESOLVED suites; the multi-seed Phase 3 work); update items still open per Phase 4's disclose-only fallback for TEST-05 / TEST-06.

Recommended publication framing per claim category (lines ~663 to end) — light-touch refresh; ensure consistency with the audit's §"Recommended disclosure statements" produced by Plan 04-04 (read the post-Task-3 state of cardano-realism-audit.md to verify the framing is consistent). Specifically the menu-item welfare distinction (un-reserved arms outperform / RB-reserved arms underperform) should appear here as a per-suite recommendation.

Word-count target for this task: 450-650 lines across all 19 suite blocks + aggregate sections.

Final pass: audit the entire document for the CLAUDE.md abbreviation-on-first-use rule.
  </action>
  <verify>
    <automated>RSK_COUNT=$(grep -cE "Related RSK:" docs/phase-2/validity-threats.md); CLM_COUNT=$(grep -cE "Related CLM:" docs/phase-2/validity-threats.md); SUITE_COUNT=$(grep -cE "^#### phase-2-.*\.yaml$" docs/phase-2/validity-threats.md); TEST_COUNT=$(grep -cE "(TEST-03|TEST-04|TEST-07a)" docs/phase-2/validity-threats.md); LINES=$(wc -l &lt; docs/phase-2/validity-threats.md); [ "$SUITE_COUNT" -eq 19 ] &amp;&amp; [ "$RSK_COUNT" -ge 15 ] &amp;&amp; [ "$CLM_COUNT" -ge 10 ] &amp;&amp; [ "$TEST_COUNT" -ge 5 ] &amp;&amp; [ "$LINES" -ge 500 ] &amp;&amp; [ "$LINES" -le 850 ]</automated>
  </verify>
  <done>All 19 per-suite blocks have `Related RSK:` and `Related CLM:` fields where applicable; Phase 3 evidence sub-fields present in the 7 suites Phase 3 directly touched; aggregate trust summary recomputed; document lands in 500-850 line range.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-suite Trust verdict → CIP reviewer claim-attribution | A reviewer cites a per-suite Trust verdict to attribute a CIP claim's strength; if the verdict is stale (e.g. still UNRESOLVED for a suite Phase 2 actually resolved), reviewers receive incorrect signal |
| RSK-NN / CLM-NN cross-reference dead-links | A `Related RSK: RSK-foo` pointing to a non-existent register entry would survive into the document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | T (Tampering) | Cross-reference integrity (RSK + CLM) | mitigate | Plan 04-07's consistency review runs cross-reference resolution; every RSK-NN in validity-threats must exist in realism-risks-register.md; every CLM-NN must exist in coverage-check.md |
| T-04-14 | I (Information Disclosure) | Verdict reconciliation accuracy | mitigate | The reconciliation rule in Task 2 is explicit; Plan 04-07 audits the verdict consistency between register / coverage-check / validity-threats |
| T-04-15 | R (Repudiation) | Phase 3 evidence sub-field accuracy | mitigate | The TEST-03 / TEST-04 / TEST-07a citations sourced from Plan 04-03's evidence summary; Plan 04-07 re-verifies the numerical citations |
</threat_model>

<verification>
- The `## Resolved 2026-05-13` and `## Resolved 2026-05-14` section headings are stripped
- All 19 `#### phase-2-*.yaml` per-suite blocks remain
- At least 15 of the 19 per-suite blocks carry a `Related RSK:` field
- At least 10 per-suite blocks carry a `Related CLM:` field (suites whose claims appear in coverage-check.md)
- At least 5 per-suite blocks carry a `Phase 3 evidence:` sub-field citing TEST-03 / TEST-04 / TEST-07a numerical evidence
- Aggregate trust summary recomputed; the table tallies HIGH / MEDIUM / LOW counts
- Cross-cutting threats / Recommendations / Recommended-publication-framing sections lightly refreshed for consistency with the audit's §"Recommended disclosure statements"
- File lands between 500 and 850 lines per CONTEXT.md D-40 target (713 → 600-800)
- Abbreviations CIP / EIP-1559 / RB / EB / RSK / CLM / IQR / BCa / MEV expand on first use somewhere in the document
</verification>

<success_criteria>
- ROADMAP.md Phase 4 success criterion #2: `docs/phase-2/validity-threats.md` is refreshed so per-suite trust ratings are cross-referenced to `RSK-NN` identifiers, verdicts are consistent with the register, and any menu-item-trade-off claims identified by the coverage check are added
- The 19-suite matrix is preserved (per-suite reviewability retained as an accountability artefact per CONTEXT.md D-40)
- The 4 formerly-UNRESOLVED suites have refreshed verdicts derived from Phase 2's output-read
</success_criteria>

<output>
After completion, create `.planning/phases/04-refresh-and-anchor/04-05-SUMMARY.md`. The SUMMARY records: final aggregate trust summary table; the per-suite verdict changes vs the pre-refresh state; the 4 formerly-UNRESOLVED suites' new verdicts; any RSK-NN / CLM-NN cross-references that were difficult to assign (so Plan 04-07's consistency review knows where to focus); final line count.
</output>
