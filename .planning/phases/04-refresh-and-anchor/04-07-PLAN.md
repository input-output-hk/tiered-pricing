---
phase: 04-refresh-and-anchor
plan: 07
type: execute
wave: 3
depends_on: ["04-04", "04-05", "04-06"]
files_modified:
  - .planning/phases/04-refresh-and-anchor/04-SUMMARY.md
  - docs/phase-2/cardano-realism-audit.md
  - docs/phase-2/validity-threats.md
  - docs/phase-2/realism-risks-register.md
  - docs/phase-2/coverage-check.md
  - docs/phase-2/methodology-overview.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]
tags: [docs, consistency-review, cross-reference-integrity, phase-summary]

must_haves:
  truths:
    - "Every `RSK-NN` reference in `cardano-realism-audit.md`, `validity-threats.md`, `coverage-check.md`, `methodology-overview.md` resolves to an existing entry in `realism-risks-register.md`"
    - "Every `CLM-NN` reference in `validity-threats.md`, `methodology-overview.md` resolves to an existing entry in `coverage-check.md`"
    - "Verdict consistency holds between `realism-risks-register.md` (per-entry `**Verdict:**` field) and `validity-threats.md` (per-suite Trust verdict): when both documents mention the same risk, the dispositions align"
    - "The abbreviation-on-first-use rule is honoured across all four refreshed documents and the new methodology-overview.md"
    - "Every `(value, source, date-retrieved YYYY-MM-DD)` triple in `cardano-realism-audit.md` follows the canonical format"
    - "All markdown links in the four refreshed documents + methodology-overview.md resolve to existing files"
    - "Phase 4 SUMMARY at `.planning/phases/04-refresh-and-anchor/04-SUMMARY.md` exists for gsd-verify-phase consumption"
  artifacts:
    - path: ".planning/phases/04-refresh-and-anchor/04-SUMMARY.md"
      provides: "Phase 4 SUMMARY consumed by gsd-verify-phase / Phase 5 handoff"
      min_lines: 80
    - path: ".planning/phases/04-refresh-and-anchor/04-07-consistency-report.md"
      provides: "Per-document audit results: dead-link counts; verdict-consistency report; abbreviation-on-first-use audit; triple-format conformance report"
      min_lines: 50
  key_links:
    - from: ".planning/phases/04-refresh-and-anchor/04-SUMMARY.md"
      to: "gsd-verify-phase (Phase 5 entry condition)"
      via: "consolidated Phase 4 outcomes table; per-deliverable acceptance evidence"
      pattern: "(DOC-01|DOC-02|DOC-03|DOC-04).*?(PASS|FAIL)"
---

<objective>
Final consistency review across the four refreshed documents (`cardano-realism-audit.md`, `validity-threats.md`, `realism-risks-register.md`, `coverage-check.md`) and the new `methodology-overview.md`. Verify cross-reference integrity (RSK / CLM / EXP); verdict reconciliation between register and validity-threats; abbreviation-on-first-use audit; (value, source, date-retrieved) triple format conformance; markdown link resolution. Write the Phase 4 SUMMARY for gsd-verify-phase consumption.

Purpose: Wave 2 plans (04-04, 04-05, 04-06) executed in parallel; this Wave 3 review catches inconsistencies that emerge only when the documents are read against each other. Without this review, Phase 5 / HAND-02 (the milestone-close consistency review) would be the first place dead identifier references or verdict mismatches surface — and HAND-02 is the final gate before git-tagging.

Output: One consistency report (`04-07-consistency-report.md`) + the Phase 4 SUMMARY (`04-SUMMARY.md`). Any defects found are fixed in place in the four refreshed documents before the SUMMARY is finalised (the SUMMARY records "fixed in plan 04-07" notes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-refresh-and-anchor/04-CONTEXT.md
@CLAUDE.md
@docs/phase-2/cardano-realism-audit.md
@docs/phase-2/validity-threats.md
@docs/phase-2/realism-risks-register.md
@docs/phase-2/coverage-check.md
@docs/phase-2/methodology-overview.md
@.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md
@.planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md
@.planning/phases/04-refresh-and-anchor/04-04-SUMMARY.md
@.planning/phases/04-refresh-and-anchor/04-05-SUMMARY.md
@.planning/phases/04-refresh-and-anchor/04-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-reference integrity audit + abbreviation audit + triple-format conformance audit + markdown link audit; fix defects in place</name>
  <files>.planning/phases/04-refresh-and-anchor/04-07-consistency-report.md, docs/phase-2/cardano-realism-audit.md, docs/phase-2/validity-threats.md, docs/phase-2/realism-risks-register.md, docs/phase-2/coverage-check.md, docs/phase-2/methodology-overview.md</files>
  <read_first>
    - docs/phase-2/cardano-realism-audit.md (post-Plan-04-04 final state)
    - docs/phase-2/validity-threats.md (post-Plan-04-05 final state)
    - docs/phase-2/realism-risks-register.md (post-Plan-04-06 final state)
    - docs/phase-2/coverage-check.md (post-Plan-04-06 final state)
    - docs/phase-2/methodology-overview.md (post-Plan-04-02 final state)
    - .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md (Plan 04-01 outcomes; verify Plan 04-04 §"Pricing-controller calibration" and Plan 04-06 register `RSK-un-anchored-controller-knobs` are consistent with these)
    - .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md (verify Phase 3 numerical citations in Plans 04-04 / 04-05 / 04-06 match)
    - .planning/phases/04-refresh-and-anchor/04-04-SUMMARY.md, 04-05-SUMMARY.md, 04-06-SUMMARY.md (the per-plan reports of what was changed)
    - CLAUDE.md §"Conventions / gotchas" (abbreviation-on-first-use rule)
  </read_first>
  <action>
Create `.planning/phases/04-refresh-and-anchor/04-07-consistency-report.md` with the following audit sections. For each audit, RUN the audit, record the result, fix any defects found in the source documents (Edit tool), then re-run the audit to confirm.

§"RSK-NN cross-reference integrity audit":
1. Extract the canonical set of `RSK-NN` identifiers from `docs/phase-2/realism-risks-register.md`: `grep -oE 'RSK-[a-z0-9-]+' docs/phase-2/realism-risks-register.md | sort -u`. Record as `REGISTER_RSK_SET`.
2. For each of the four refreshed documents (`cardano-realism-audit.md`, `validity-threats.md`, `coverage-check.md`, `methodology-overview.md`): extract all `RSK-NN` references via `grep -oE 'RSK-[a-z0-9-]+' <doc> | sort -u`. For each extracted reference, verify it appears in `REGISTER_RSK_SET`.
3. Any orphaned `RSK-NN` reference (cited but not present in the register) is a dead link. Fix by either: (a) correcting the citing document if the reference is a typo; or (b) confirming the citing document is correct and the register entry is missing (escalate as a defect — Plan 04-01 / 04-06 forgot to retain the entry; restore from the pre-Phase-4 state of the register).
4. Record the count of cross-references checked and the count of defects found / fixed.

§"CLM-NN cross-reference integrity audit":
1. Extract `CLM-NN` identifiers from `docs/phase-2/coverage-check.md`: `grep -oE 'CLM-[0-9]+' docs/phase-2/coverage-check.md | sort -u`. Record as `COVERAGE_CLM_SET`.
2. For `validity-threats.md` and `methodology-overview.md` (the documents that may cross-reference CLM-NN): extract `CLM-NN` references; verify each appears in `COVERAGE_CLM_SET`.
3. Fix dead references in place; record counts.

§"EXP-NN cross-reference integrity audit":
1. Extract `EXP-NN` identifiers from `docs/phase-2/realism-risks-register.md` (this is the canonical register of EXP slugs). Record as `REGISTER_EXP_SET`.
2. For each of the other documents (audit / validity-threats / coverage-check / methodology-overview), extract `EXP-NN` references and verify each appears in `REGISTER_EXP_SET`. Note: the register's append-only convention means EXP slugs never renumber.
3. Fix dead references; record counts.

§"Register verdict ↔ Index table consistency audit":
1. For each `## RSK-NN: ...` block in `docs/phase-2/realism-risks-register.md`, extract the `**Verdict:** <value>` line. Build a `PER_ENTRY_VERDICT_MAP`.
2. For the §"Index" table rows, extract the Verdict cell value. Build an `INDEX_TABLE_VERDICT_MAP`.
3. Verify the two maps agree for every RSK-NN identifier (the 5 entries Phase 4 touched are at highest risk of drift: RSK-un-anchored-controller-knobs, RSK-pool-count, RSK-calibration-stale-stake-snapshot, RSK-steady-state-run-length, RSK-multiplier-floor-4-suite-coverage). Note: `RSK-substrate-scope` may also have flipped from LIVE to DISCLOSED via Plan 04-04's audit refresh (the audit folds the substrate-scope disclosure-paragraph in); if so, the register's `RSK-substrate-scope` per-entry verdict should also flip — verify this consistency.
4. Fix any verdict-drift in place. Record the post-Phase-4 verdict distribution counts (LIVE / DORMANT / MITIGATED / DISCLOSED across all 24 entries) and update the §"Reading guide" prose in the register to match if needed.

§"Register ↔ validity-threats verdict reconciliation audit":
1. For each per-suite block in `validity-threats.md` with a `Related RSK:` field, read the `RSK-NN` references and the suite's Trust verdict.
2. Reconcile: a suite's Trust verdict should be consistent with its Related-RSK entries' register verdicts (no formal mapping, but qualitative consistency expected — e.g. a suite whose Related-RSK is all DISCLOSED should not carry an UNRESOLVED Trust; a suite whose Related-RSK contains a LIVE entry should not carry HIGH Trust without explicit rationale).
3. Record any inconsistencies; recommend fixes but do not auto-fix Trust verdicts (verdict re-derivation is Plan 04-05's prerogative; Plan 04-07 surfaces conflict but defers to Plan 04-05's reading).

§"Abbreviation-on-first-use audit":
1. For each of the four refreshed documents and `methodology-overview.md`, run a check: every abbreviation introduced via parentheses must be the first use of that abbreviation in the document. The audit pattern is: extract every `<expansion> (<abbr>)` form; for each, search backward in the document for any prior occurrence of `<abbr>` standalone (without the expansion). Any prior standalone occurrence is a violation — the first use must include the expansion.
2. Common abbreviations to check: CIP, EIP-1559, RB, EB, RSK, CLM, EXP, IQR, BCa, CCS, AFT, SODA, MEV, SPO, eUTxO, ODD, JASSS, BCa, σ, SHA-256.
3. Fix violations in place; record violation count.

§"(value, source, date-retrieved) triple format conformance audit" (audit document only):
1. For `docs/phase-2/cardano-realism-audit.md`, extract every calibration-value-shaped expression. The canonical format per ROADMAP.md success criterion #1 is `(<value>, source: <citation>, date-retrieved: YYYY-MM-DD)`.
2. Audit pattern: `grep -oE '\([^()]*source:[^()]*date-retrieved:[^()]*\)' docs/phase-2/cardano-realism-audit.md`. Each match must follow the format `(<value>, source: <something>, date-retrieved: YYYY-MM-DD)`.
3. Count format conformance; record the count. The ROADMAP.md success criterion #1 requires "every calibration value" to be in triple form — count the triple instances and confirm the post-refresh audit has at least the calibration value count from the pre-refresh audit (the rewrite preserves but does not lose triples; it adds the date-retrieved field uniformly).
4. Fix non-conforming triples in place.

§"Markdown link resolution audit":
1. For each of the four refreshed documents + `methodology-overview.md`, extract every markdown link of the form `[<text>](<url>)`. For `<url>` values that are local file paths (relative paths starting with `.`, `..`, or `/`), test whether the file exists.
2. Record any unresolved links. Fix by either correcting the path (typo) or removing the link (the cited file no longer exists — e.g. `.planning/chain-derived-bug2-investigation.md` may have been deleted; if so, replace the citation with one to a still-present document like `.planning/family-b-decision-2026-05-14.md`).

§"Audit document constraints" (per CONTEXT.md `<planner_guidance>`):
1. Confirm `docs/phase-2/cardano-realism-audit.md` does NOT contain the string `2026-05-13` outside the §References / footer area (banners stripped per D-38). Check: `grep -n '2026-05-13' docs/phase-2/cardano-realism-audit.md`; if any matches are in non-footer prose, the banner was incompletely stripped — fix in place.
2. Confirm `docs/phase-2/cardano-realism-audit.md` is between 300 and 500 lines.
3. Confirm `docs/phase-2/validity-threats.md` is between 500 and 850 lines.
4. Confirm `docs/phase-2/methodology-overview.md` has the seven `### ` headings matching the regex `^### (Purpose|State variables|Process overview|Design concepts|Initialisation|Input data|Submodels)$` plus the seven `### Worked example: ...` headings.
5. Confirm `docs/phase-2/realism-risks-register.md` has no `TBD plan 02` markers, no `(draft fallback; ...)` markers on the 5 entries Phase 4 touched.

After running every audit, write `04-07-consistency-report.md` summarising the results: per-audit pass/fail; per-defect count; per-defect resolution (fixed in plan / escalated / accepted). The report is read by Plan 04's SUMMARY (Task 2) and by Phase 5 / HAND-02's final consistency review.
  </action>
  <verify>
    <automated>test -s .planning/phases/04-refresh-and-anchor/04-07-consistency-report.md &amp;&amp; grep -cE "^## (RSK-NN|CLM-NN|EXP-NN|Register verdict|Register ↔ validity-threats|Abbreviation-on-first-use|.*triple format|Markdown link|Audit document constraints)" .planning/phases/04-refresh-and-anchor/04-07-consistency-report.md | awk '$1 &gt;= 7 {exit 0} {exit 1}' &amp;&amp; (! grep -nE "\\bTBD plan 02\\b" docs/phase-2/realism-risks-register.md) &amp;&amp; LINES_AUDIT=$(wc -l &lt; docs/phase-2/cardano-realism-audit.md); LINES_VT=$(wc -l &lt; docs/phase-2/validity-threats.md); [ "$LINES_AUDIT" -ge 300 ] &amp;&amp; [ "$LINES_AUDIT" -le 500 ] &amp;&amp; [ "$LINES_VT" -ge 500 ] &amp;&amp; [ "$LINES_VT" -le 850 ]</automated>
  </verify>
  <done>The consistency report exists with at least 7 audit sections; all defects found are fixed in place in the source documents; no `TBD plan 02` markers in the register; the audit and validity-threats documents land in their target line-count ranges; the abbreviation-on-first-use rule holds throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Author Phase 4 SUMMARY for gsd-verify-phase consumption</name>
  <files>.planning/phases/04-refresh-and-anchor/04-SUMMARY.md</files>
  <read_first>
    - All seven Wave-1, Wave-2, Wave-3 plan SUMMARY files: `.planning/phases/04-refresh-and-anchor/04-{01,02,03,04,05,06}-SUMMARY.md` (each plan's individual SUMMARY)
    - `.planning/phases/04-refresh-and-anchor/04-07-consistency-report.md` (Task 1 output)
    - The four refreshed documents + methodology-overview.md in final state
    - .planning/ROADMAP.md §"Phase 4: Refresh and Anchor" — the 4 success criteria
    - $HOME/.claude/get-shit-done/templates/summary.md (the SUMMARY template)
    - .planning/phases/03-targeted-cheap-tests/03-SUMMARY.md (the Phase 3 SUMMARY shape; Phase 4's SUMMARY follows the same shape for gsd-verify-phase consumption)
  </read_first>
  <action>
Author `.planning/phases/04-refresh-and-anchor/04-SUMMARY.md` for gsd-verify-phase consumption, following the project's SUMMARY template ($HOME/.claude/get-shit-done/templates/summary.md). The SUMMARY consolidates Phase 4's outcomes.

Structure (following the Phase 3 SUMMARY shape):

**Frontmatter** — phase / status (complete) / date (2026-05-18) / requirements ([DOC-01, DOC-02, DOC-03, DOC-04]) / plans ([04-01, 04-02, 04-03, 04-04, 04-05, 04-06, 04-07]) / deliverables_complete / deliverables_partial / deliverables_total.

**§"Goal"** — quote the ROADMAP.md Phase 4 goal verbatim.

**§"Outcome"** — one-paragraph summary of what landed. Acknowledge the TEST-05 / TEST-06 disclose-only fallback (per CONTEXT.md `<deferred>`) as an explicit Phase 4 decision rather than a gap; the gap-shape is Phase 3's outcome carried forward.

**§"Deliverables status"** — table mapping each DOC-NN requirement to its delivery state:
| Deliverable | Where | Status |
|---|---|---|
| DOC-01 refreshed audit | docs/phase-2/cardano-realism-audit.md | Complete (Plan 04-04); N lines |
| DOC-02 refreshed validity-threats | docs/phase-2/validity-threats.md | Complete (Plan 04-05); N lines |
| DOC-03 anchor-or-disclose (audit + register) | docs/phase-2/cardano-realism-audit.md §"Pricing-controller calibration" + docs/phase-2/realism-risks-register.md `RSK-un-anchored-controller-knobs` | Complete (Plans 04-01, 04-04, 04-06); umbrella verdict: [MITIGATED / DISCLOSED] per Plan 04-01 |
| DOC-04 methodology-overview | docs/phase-2/methodology-overview.md | Complete (Plan 04-02); N lines |
| Register `disclosure-paragraph` edits + verdict flips | docs/phase-2/realism-risks-register.md | Complete (Plan 04-06); 5 entries updated |
| Coverage-check `signal-source-anchoring` updates | docs/phase-2/coverage-check.md | Complete (Plan 04-06); [N] cells updated |
| Consistency review | .planning/phases/04-refresh-and-anchor/04-07-consistency-report.md | Complete (Plan 04-07) |

**§"What landed in tree"** — table of files created or modified during Phase 4:
| File | Lines | Purpose |
|---|---|---|
| docs/phase-2/cardano-realism-audit.md | N | Refreshed; banners stripped; triple format applied; Phase 3 narrative incorporated |
| docs/phase-2/validity-threats.md | N | Refreshed; per-suite RSK + CLM cross-references; 4 formerly-UNRESOLVED suites resolved |
| docs/phase-2/realism-risks-register.md | N | 5 entries' verdicts and disclosure-paragraphs updated |
| docs/phase-2/coverage-check.md | N | signal-source-anchoring cells updated per Plan 04-01 anchor outcomes |
| docs/phase-2/methodology-overview.md | N | New ODD methodology overview + worked example |
| .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md | N | Plan 04-01 anchor decisions + rejected-citations list |
| .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md | N | Consolidated Phase 3 evidence for Wave 2 plans |
| .planning/phases/04-refresh-and-anchor/04-07-consistency-report.md | N | Plan 04-07 consistency audit results |

**§"Determinism + invariants"** — note that Phase 4 is documentation-only; no simulator code modified; M2/M3 unit-test goldens unchanged; M5 suite-level goldens unchanged. `cargo test --workspace` is unperturbed.

**§"Phase 5 inputs"** — what Phase 4 hands to Phase 5 (CIP author handoff). The four refreshed documents + methodology-overview.md are the four primary CIP-cited artefacts. The register's load-bearing disclosure-paragraphs (DISCLOSED entries) paste into the CIP's Limitations section per Phase 5 / HAND-01.

**§"Open questions for Phase 5"** — gaps Phase 5 must address:
- TEST-05 re-run is user-managed and out of Phase 4 scope per CONTEXT.md `<deferred>`. `RSK-pool-count` and `RSK-calibration-stale-stake-snapshot` lands at DISCLOSED via the existing fallback prose. If the user re-runs TEST-05 between Phase 4 close and CIP publication, Phase 5 can incorporate the data as a verdict-flip patch.
- TEST-06 re-run similar.
- The CIP author summary (HAND-01) is Phase 5 work; identifies which `disclosure-paragraph` blocks paste into which CIP section.

**§"Verification"** — quote each ROADMAP.md success criterion + the per-criterion PASS / FAIL determination per Task 1's audit. All four success criteria should be PASS (DOC-01 / DOC-02 / DOC-03 / DOC-04).

**§"Abbreviations on first use"** — list of abbreviations introduced in the SUMMARY (CIP / EIP-1559 / RB / EB / RSK / CLM / EXP / ODD / IQR / BCa / etc.).

The SUMMARY is the file gsd-verify-phase reads; the Phase 5 entry condition depends on this file's existence and content matching the requirements traceability.

Run a final sanity check: confirm STATE.md, ROADMAP.md, and REQUIREMENTS.md are updated per the standard execute-plan.md post-task updates (Plan 04-07's executor handles this; not part of this Task 2's action). The user's project memory prohibits auto-committing; the executor leaves all changes in the working tree for the user to commit.
  </action>
  <verify>
    <automated>test -s .planning/phases/04-refresh-and-anchor/04-SUMMARY.md &amp;&amp; grep -cE "^# (Phase 04|Phase 4)" .planning/phases/04-refresh-and-anchor/04-SUMMARY.md | awk '$1 &gt;= 1 {exit 0} {exit 1}' &amp;&amp; grep -cE "(DOC-01|DOC-02|DOC-03|DOC-04)" .planning/phases/04-refresh-and-anchor/04-SUMMARY.md | awk '$1 &gt;= 8 {exit 0} {exit 1}' &amp;&amp; grep -cE "^(## |---)" .planning/phases/04-refresh-and-anchor/04-SUMMARY.md | awk '$1 &gt;= 6 {exit 0} {exit 1}'</automated>
  </verify>
  <done>04-SUMMARY.md exists with required sections (Goal / Outcome / Deliverables status / What landed / Determinism / Phase 5 inputs / Open questions / Verification / Abbreviations); each of the four DOC-NN requirements is referenced; gsd-verify-phase can consume this file as the Phase 4 entry artefact for Phase 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 4 SUMMARY → gsd-verify-phase entry condition | Phase 5 / HAND-02 depends on Phase 4 closure; the SUMMARY records the closure state |
| Cross-reference integrity → CIP citation chain | Dead RSK / CLM / EXP references would survive into the published CIP if Plan 04-07 doesn't catch them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | T (Tampering) | Cross-reference integrity (RSK/CLM/EXP) across 5 docs | mitigate | Task 1 §"RSK-NN cross-reference integrity audit", §"CLM-NN", §"EXP-NN" run a complete pass over each pair of documents; defects fixed in place |
| T-04-20 | I (Information Disclosure) | Banner-residue or pre-refresh prose surviving into refreshed docs | mitigate | Task 1 §"Audit document constraints" explicitly checks for `2026-05-13` residue outside the footer; the line-count check catches under-rewrites |
| T-04-21 | R (Repudiation) | Verdict consistency between register and validity-threats | mitigate | Task 1 §"Register ↔ validity-threats verdict reconciliation audit" surfaces conflicts; Plan 04-05 has the verdict-revision authority per CONTEXT.md so Task 1 reports rather than auto-fixes Trust verdicts |
</threat_model>

<verification>
- `.planning/phases/04-refresh-and-anchor/04-07-consistency-report.md` exists with at least 7 audit sections
- `.planning/phases/04-refresh-and-anchor/04-SUMMARY.md` exists matching the Phase 3 SUMMARY shape
- All RSK-NN / CLM-NN / EXP-NN cross-references in the 5 documents resolve to canonical sources
- Register §"Index" verdict cells match per-entry `**Verdict:**` fields for all 24 entries
- Abbreviation-on-first-use rule holds in all 5 documents
- (value, source, date-retrieved YYYY-MM-DD) triples in cardano-realism-audit.md conform to canonical format
- Markdown links in all 5 documents resolve to existing files
- Audit document has no `2026-05-13` references outside the footer / references area
- Audit document is between 300 and 500 lines
- Validity-threats document is between 500 and 850 lines
- Methodology-overview document has the 14 expected H3 headings (7 ODD elements + 7 worked-example traces)
- Register has no `TBD plan 02` markers
- All four ROADMAP.md Phase 4 success criteria pass
</verification>

<success_criteria>
Phase 4 is verifiably complete by gsd-verify-phase:
- DOC-01 PASS: `docs/phase-2/cardano-realism-audit.md` in authoritative voice, triple format applied, substrate-scope paragraph included
- DOC-02 PASS: `docs/phase-2/validity-threats.md` per-suite Trust ratings cross-referenced to `RSK-NN`; verdicts reconciled
- DOC-03 PASS: Each of the four un-anchored controller knobs has either an external anchor (cited in audit + register) or a "conditional on X" disclosure-paragraph (in register `RSK-un-anchored-controller-knobs` per-sub-knob prose)
- DOC-04 PASS: `docs/phase-2/methodology-overview.md` exists as ODD index + per-element prose + worked example
- Cross-reference integrity holds across all four refreshed documents + methodology-overview
- 04-SUMMARY.md is the gsd-verify-phase entry-condition artefact for Phase 5
</success_criteria>

<output>
The artefacts this plan produces are:
1. `.planning/phases/04-refresh-and-anchor/04-07-consistency-report.md` — per-audit results + defect-resolution log
2. `.planning/phases/04-refresh-and-anchor/04-SUMMARY.md` — the Phase 4 SUMMARY for gsd-verify-phase

After completion, also produce the standard per-plan SUMMARY for Plan 04-07 itself at `.planning/phases/04-refresh-and-anchor/04-07-SUMMARY.md` (the plan-level SUMMARY consumed by gsd-verify-plan).
</output>
