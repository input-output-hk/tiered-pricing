---
phase: 04-refresh-and-anchor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/phase-2/methodology-overview.md
autonomous: true
requirements: [DOC-04]
tags: [docs, methodology, odd, onboarding]

must_haves:
  truths:
    - "A new document `docs/phase-2/methodology-overview.md` exists at the path REQUIREMENTS.md mandates (per CONTEXT.md D-41)"
    - "The document contains an ODD index table mapping the seven elements (Purpose, State variables, Process overview, Design concepts, Initialisation, Input data, Submodels) to in-repo locations"
    - "Brief prose per ODD element exists (~4-6 sentences each) summarising what that element looks like in phase-2 with file-path links"
    - "One worked example traces a single canonical (job, seed) pair end-to-end through the seven ODD elements"
    - "The CIP can cite this document by repo URL rather than embedding methodology prose"
  artifacts:
    - path: "docs/phase-2/methodology-overview.md"
      provides: "ODD methodology index + per-element prose + one worked example"
      min_lines: 200
      contains: "Overview, Design concepts, Details (ODD)"
  key_links:
    - from: "docs/phase-2/methodology-overview.md §'Index'"
      to: "CLAUDE.md, docs/phase-2/mechanism-design.md, sim-rs/parameters/phase-2-sweep/suites/*.yaml, sim-rs/sim-core/src/tx_pricing/"
      via: "file-path / section anchor links per ODD element"
      pattern: "\\[.*\\]\\((CLAUDE\\.md|\\.\\./.*\\.md|\\.\\./\\.\\./sim-rs/.*)\\)"
    - from: "docs/phase-2/methodology-overview.md §'Worked example'"
      to: "phase-3 canonical (job, seed) result — phase-2-priority-only-unreserved or phase-2-two-lane-both-dynamic at seed=1"
      via: "end-to-end trace through seven ODD elements"
      pattern: "(priority-only-unreserved|both-dynamic|seed.{0,3}=.{0,3}1)"
---

<objective>
Author `docs/phase-2/methodology-overview.md` as a one-page Overview, Design concepts, Details (ODD) methodology index per ROADMAP.md Phase 4 success criterion 4 and CONTEXT.md D-41 / D-42. The shape is: index table at top (seven rows × three columns) + brief prose per ODD element (~4-6 sentences each) + one worked example tracing a single canonical (job, seed) end-to-end through the seven ODD elements.

Purpose: The CIP cites this document by repo Uniform Resource Locator (URL) rather than embedding methodology prose. Per D-42, prose is summary-only — content lives in the linked files; the methodology overview orients the reader without duplicating the linked file's prose. The worked example doubles as onboarding documentation for new contributors.

Output: One new markdown file at `docs/phase-2/methodology-overview.md` (expected ~500+ lines per D-42; index ~7 rows, prose ~4-6 sentences × 7 = ~30-50 paragraph-lines, worked example the bulk of the document at ~300-400 lines walking through seven ODD elements for one canonical run).

This plan has no upstream dependency and runs in Wave 1 parallel to Plans 04-01 and 04-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-refresh-and-anchor/04-CONTEXT.md
@CLAUDE.md
@docs/phase-2/mechanism-design.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- ODD protocol structure consumed verbatim by the document. -->
<!-- Source: Grimm et al. (2006, 2010, 2020). Seven elements, in this exact order. -->

Seven ODD elements (the seven sections):
1. Purpose
2. State variables
3. Process overview
4. Design concepts
5. Initialisation
6. Input data
7. Submodels

Index-table columns (per D-42):
| ODD element | In-repo location | One-line description |

Repo landmarks for the in-repo location column (NOT exhaustive; the executor selects the most authoritative anchor per element):
- CLAUDE.md §"Mechanism abstractions", §"Calibration choices", §"Running the suites", §"Numeric representation contract"
- docs/phase-2/mechanism-design.md (the spec)
- sim-rs/sim-core/src/tx_pricing/ (the controller policy)
- sim-rs/sim-core/src/sim/linear_leios.rs (block production + endorsement)
- sim-rs/sim-core/src/sim/mempool_gate.rs (admission/eviction/inclusion)
- sim-rs/sim-core/src/tx_actors.rs (actor model)
- sim-rs/sim-core/src/config.rs (configuration deserialisation)
- sim-rs/parameters/phase-2-sweep/protocol-base.yaml + topology-realistic-100.yaml + demand/*.yaml + pricing/*.yaml + suites/*.yaml
- sim-rs/sim-cli/src/runner.rs + suite.rs + metrics/
- sim-rs/sim-cli/src/bin/experiment-suite/ (driver)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header + seven-row ODD index table + per-element prose (~4-6 sentences each)</name>
  <files>docs/phase-2/methodology-overview.md</files>
  <read_first>
    - CLAUDE.md (full file — §"Repository layout", §"Mechanism abstractions", §"Numeric representation contract", §"Determinism scope", §"Calibration choices", §"Mechanism choice and audit trail", §"Running the suites", §"Conventions / gotchas" — the abbreviation-on-first-use rule applies; calibration-choice narrative supplies anchors)
    - docs/phase-2/mechanism-design.md (the spec; supplies the canonical Purpose framing + Process overview content)
    - .planning/codebase/STRUCTURE.md (file-path landmarks for the in-repo location column)
    - .planning/codebase/CONVENTIONS.md (idioms / patterns the prose references)
    - .planning/phases/04-refresh-and-anchor/04-CONTEXT.md §"DOC-04 ODD methodology overview" — D-41 (path: docs/phase-2/methodology-overview.md; not .planning/), D-42 (shape: index table + brief prose + one worked example)
    - The Grimm et al. ODD references in 04-CONTEXT.md §"ODD methodology reference (DOC-04)" — Grimm et al. (2006, 2010) Ecological Modelling; Grimm et al. (2020) JASSS "The ODD Protocol… A Second Update"
  </read_first>
  <action>
Create `docs/phase-2/methodology-overview.md` with the following structure (no fenced code blocks; just Markdown headings, prose, and a Markdown table).

Header: H1 title `Methodology overview — phase-2 dynamic-pricing simulator` + status / scope / date / abbreviation-on-first-use lines. Status: "Phase-2 CIP evidence base methodology index". Scope: "ODD (Overview, Design concepts, Details) index for the phase-2 dynamic-pricing simulator. The Cardano Improvement Proposal (CIP) cites this document by repository URL rather than embedding methodology prose." Cite Grimm et al. (2006, 2010, 2020) as the ODD canonical reference.

§"Reading guide" — 1 paragraph explaining the document's role: index + per-element prose + worked example; CIP citation-target; cross-reference to the four other docs/phase-2/*.md files (mechanism-design.md = spec; cardano-realism-audit.md = calibration-provenance; validity-threats.md = per-suite trust; realism-risks-register.md = unresolved risks; coverage-check.md = per-claim coverage). No verbatim duplication of those documents' content — methodology-overview.md orients, the linked files contain.

§"Index" — Markdown table with three columns: `ODD element | In-repo location | One-line description`. Exactly seven data rows (one per ODD element). The rows MUST appear in this order (matches the Grimm et al. canonical order):
1. **Purpose** | docs/phase-2/mechanism-design.md + .planning/research/SUMMARY.md | One-line: phase-2 evaluates four CIP menu options against single-lane EIP-1559 control on welfare metrics
2. **State variables** | CLAUDE.md §"Mechanism abstractions" + sim-rs/sim-core/src/model.rs | One-line: Transaction / EB / RB / ledger / PerLaneQuote / WindowAggregate
3. **Process overview** | CLAUDE.md §"Mechanism abstractions" §"Calibration choices" + sim-rs/sim-core/src/sim/linear_leios.rs | One-line: slot lottery → block production → endorsement → admission → inclusion → controller derives next quote
4. **Design concepts** | CLAUDE.md §"Numeric representation contract" §"Determinism scope" + docs/phase-2/mechanism-design.md | One-line: chain-derived stateless controller; integer/rational/u128 arithmetic in simulation paths; f64 only in reporting
5. **Initialisation** | sim-rs/parameters/phase-2-sweep/protocol-base.yaml + topology-realistic-100.yaml + demand/*.yaml + pricing/*.yaml | One-line: configuration YAML hierarchy supplies protocol / topology / demand / pricing
6. **Input data** | sim-rs/parameters/phase-2-sweep/topology-realistic-100.yaml header + docs/phase-2/calibration-fix-postmortem.md | One-line: epoch-582 mainnet stake snapshot (retrieved 2026-05-14); rb-prob = activeSlotsCoeff = 0.05
7. **Submodels** | sim-rs/sim-core/src/tx_pricing/ + sim-rs/sim-core/src/sim/mempool_gate.rs + sim-rs/sim-core/src/tx_actors.rs | One-line: PricingBackend trait (BaselinePricing / Eip1559Pricing / TwoLanePricing); MempoolGate; ActorComponent + MaxFeePolicy

§"Per-element prose" — one H3 heading per ODD element in the same canonical order: `### Purpose`, `### State variables`, `### Process overview`, `### Design concepts`, `### Initialisation`, `### Input data`, `### Submodels`. Each heading text MUST be exactly the element name (case-sensitive, no annotations) so the consistency-check regex `^### (Purpose|State variables|Process overview|Design concepts|Initialisation|Input data|Submodels)$` matches all seven.

Each H3 section contains a 4-6 sentence paragraph that:
- Summarises what that element looks like in phase-2
- Includes inline file-path links to canonical content (e.g. `[CLAUDE.md §"Mechanism abstractions"](../../CLAUDE.md)`, `[sim-rs/sim-core/src/tx_pricing/](../../sim-rs/sim-core/src/tx_pricing/)`)
- Does NOT duplicate the linked file's prose — orient only
- Expands abbreviations on first use per CLAUDE.md §"Conventions / gotchas" (ODD, CIP, EIP-1559, RB, EB, ranking block, endorser block, etc.)
- Names the specific suite YAMLs / source files / spec sections per the index table

Example shape for `### Purpose` (do not paste this verbatim; the executor writes definitive prose):
"Phase-2 evaluates four Cardano Improvement Proposal (CIP) menu options — priority-only-RB-reserved, priority-only-un-reserved, both-dynamic-partitioned, both-dynamic-un-partitioned — against a single-lane Ethereum Improvement Proposal 1559 (EIP-1559) control on welfare metrics under a Family-B chain-derived controller (see [docs/phase-2/mechanism-design.md](mechanism-design.md) §'Pricing controllers' for the spec, and [.planning/research/SUMMARY.md](../../.planning/research/SUMMARY.md) for the upstream CPS-0023 ('Urgency Signaling') problem framing this work responds to). The welfare metric is per-(job, seed) retained_value totalled across priority and standard lanes (see [CLAUDE.md §'Numeric representation contract'](../../CLAUDE.md) for the f64-reporting convention). The four menu options share the underlying linear-Leios protocol; the differences are localised to lane structure, RB partitioning, and which lane's quote is dynamic. No new on-chain primitive is introduced; the existing transaction fee field is reinterpreted as a maximum-fee envelope (see [docs/phase-2/mechanism-design.md §'Fee field'](mechanism-design.md) for the reinterpretation, and disclosure paragraph in `RSK-fee-as-maxFee-envelope` of the [realism-risks register](realism-risks-register.md) for the hard external dependency on the refund-CIP)."

Repeat for each of the remaining six ODD elements following the same shape (~4-6 sentences, inline file-path links, no duplication of linked prose, abbreviations expanded on first use).

After writing, audit the file for the abbreviation-on-first-use rule per CLAUDE.md §"Conventions / gotchas": every acronym (ODD, CIP, EIP-1559, RB, EB, IQR, BCa, SPO, MEV, eUTxO, AFT, CCS, SODA, σ, JASSS) is expanded on first use somewhere in the document — preferably in the §"Reading guide" header where multiple expansions can be grouped. Specifically: Overview, Design concepts, Details (ODD); Cardano Improvement Proposal (CIP); Ethereum Improvement Proposal 1559 (EIP-1559); ranking block (RB); endorser block (EB); Journal of Artificial Societies and Social Simulation (JASSS).
  </action>
  <verify>
    <automated>test -s docs/phase-2/methodology-overview.md &amp;&amp; grep -cE "^### (Purpose|State variables|Process overview|Design concepts|Initialisation|Input data|Submodels)$" docs/phase-2/methodology-overview.md | awk '$1 == 7 {exit 0} {exit 1}' &amp;&amp; grep -q "Overview, Design concepts, Details (ODD)" docs/phase-2/methodology-overview.md &amp;&amp; grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/methodology-overview.md &amp;&amp; grep -cE "^## (Reading guide|Index|Per-element prose)" docs/phase-2/methodology-overview.md | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <done>File `docs/phase-2/methodology-overview.md` exists; contains §"Reading guide", §"Index" with 7-row table, §"Per-element prose" with seven H3 sub-sections named exactly `Purpose`, `State variables`, `Process overview`, `Design concepts`, `Initialisation`, `Input data`, `Submodels`; ODD and CIP abbreviations expanded on first use.</done>
</task>

<task type="auto">
  <name>Task 2: Worked example — single canonical (job, seed) end-to-end through seven ODD elements</name>
  <files>docs/phase-2/methodology-overview.md</files>
  <read_first>
    - The Task 1 output of `docs/phase-2/methodology-overview.md` (the header / index / per-element prose written in Task 1; Task 2 appends the worked example)
    - .planning/realism-tests/multi-seed-variance/results.md (TEST-04 canonical menu cells; the worked example MUST match a Phase 3 TEST-04 canonical cell per CONTEXT.md Claude's Discretion §"Worked-example job choice (DOC-04)")
    - sim-rs/parameters/phase-2-sweep/suites/phase-2-priority-only-unreserved.yaml or phase-2-two-lane-both-dynamic.yaml (the two suites whose TEST-04 cells show the un-reserved-arms-outperform-single-lane-EIP-1559 headline; the executor chooses one)
    - sim-rs/parameters/phase-2-sweep/protocol-base.yaml (Initialisation source)
    - sim-rs/parameters/phase-2-sweep/topology-realistic-100.yaml (Input data source — header explains epoch-582 retrieval)
    - sim-rs/parameters/phase-2-sweep/pricing/*.yaml (the pricing YAML used by the chosen job)
    - sim-rs/parameters/phase-2-sweep/demand/*.yaml (the demand YAML used by the chosen job)
    - CLAUDE.md §"Mechanism abstractions" + §"Calibration choices" + §"Running the suites" (process overview, parameter sources, run mechanics)
    - docs/phase-2/mechanism-design.md (spec content the worked example references)
    - .planning/phases/04-refresh-and-anchor/04-CONTEXT.md Claude's Discretion §"Worked-example job choice (DOC-04)" (seed=1; ~2000 slots; match a Phase 3 TEST-04 canonical cell)
  </read_first>
  <action>
Append §"Worked example" to `docs/phase-2/methodology-overview.md`. The worked example traces a single canonical (job, seed) pair end-to-end through the seven Overview, Design concepts, Details (ODD) elements.

Choose ONE Phase 3 TEST-04 canonical cell as the worked-example target (per CONTEXT.md Claude's Discretion):
- Preferred: `menu_unreserved_priority_only_static_x4` (from phase-3-canonical-variance.yaml; the un-reserved priority-only arm at multiplier_floor = 4 under sundaeswap_moderate demand; CI [+4.28e+09, +8.49e+09] median Δ = +6.66e+09 sign-coh = 0.90 distinct-hash 20/20 — this is the CIP-cited "un-reserved arms outperform" headline cell)
- Alternative: `menu_unreserved_both_dynamic_x4` (the other un-reserved arm; same suite header narrative)

Document the choice in a one-paragraph "Worked-example target" preamble naming the cell, the suite YAML, the seed (seed = 1), the slot length (~2000 slots), and the Phase 3 CI / sign-coherence as the cited welfare outcome.

Then walk through the seven ODD elements in order (one H3 per element under §"Worked example"):

`### Worked example: Purpose` — what the Purpose looks like for this specific run: the welfare comparison this job answers ("does priority-only un-reserved at multiplier_floor = 4 produce positive welfare delta vs single-lane EIP-1559 under sundaeswap_moderate demand at seed = 1?"); name the explicit metric (`retained_value` total across priority + standard); cite the Phase 3 CI as the result.

`### Worked example: State variables` — the State variables present at slot 0: the (job, seed) seeds an empty mempool, an initial PerLaneQuote pinned at minFeeA = 44 per lane, an empty chain, and a slot lottery seeded by the seed value. Cite the specific in-tree types (Transaction, PerLaneQuote, WindowAggregate, LinearRankingBlock per CLAUDE.md §"Mechanism abstractions").

`### Worked example: Process overview` — one block-production cycle in detail: at slot N, the lottery elects a producer; the producer reads the chain tip's WindowAggregate; calls `compute_derived_quote(parent_quote, parent_aggregate, parent_samples, evicted_samples)` to derive the next PerLaneQuote (pure function — see [sim-rs/sim-core/src/tx_pricing/](../../sim-rs/sim-core/src/tx_pricing/) for the EIP-1559 step); selects transactions from the mempool against the new quote (lane-aware admission per [sim-rs/sim-core/src/sim/mempool_gate.rs](../../sim-rs/sim-core/src/sim/mempool_gate.rs)); the new block carries `derived_quote` as a header field. Walk through one priority-lane tx admission, one standard-lane tx admission, one revalidation-eviction example, and one inclusion event with `actual_fee` charged.

`### Worked example: Design concepts` — the design concepts this run exercises: chain-derivation (no node-local mutable controller state; reorg-safe by construction; cite WR-1 resolution per CLAUDE.md §"Mechanism choice and audit trail"); integer-rational discipline (u128 sums in WindowAggregate; bit-stable across architectures); multiplier-floor invariant (`c_priority ≥ multiplier_floor × c_standard`) enforced post-update with u128 intermediates. For the un-reserved variant specifically, no RB-partition rule applies — priority is producer-side `priority_first` block-build ordering.

`### Worked example: Initialisation` — the configuration the run inherits: name the specific YAMLs the (job, seed) loads at startup — `protocol-base.yaml` (protocol knobs: rb-prob = 0.05, default-slots = 2000, etc.), `topology-realistic-100.yaml` (100 nodes, mass-stratified mainnet stakes at epoch 582), `pricing/two_lane_priority_only_static_x4.yaml` (or whichever pricing YAML the cell uses; the executor inspects the cell's `phase-3-canonical-variance.yaml` to find the exact name), `demand/sundaeswap_moderate.yaml`. Cite the exact file paths and the key knobs each YAML overrides.

`### Worked example: Input data` — the empirical anchors the run uses: the epoch-582 stake snapshot retrieved 2026-05-14 (per [sim-rs/parameters/phase-2-sweep/topology-realistic-100.yaml](../../sim-rs/parameters/phase-2-sweep/topology-realistic-100.yaml) header); the mainnet `activeSlotsCoeff = 0.05` retrieved from Cardano on-chain state at the same date (cite [docs/phase-2/calibration-fix-postmortem.md](calibration-fix-postmortem.md)); the demand YAMLs' Q1 2026 mainnet transaction mix order-of-magnitude calibration (cite [docs/phase-2/cardano-realism-audit.md §"What lines up with mainnet"](cardano-realism-audit.md)).

`### Worked example: Submodels` — the submodels invoked in one block-production cycle for this job: TwoLanePricing's `compute_derived_quote` (un-reserved variant — only priority quote moves; standard quote stays at floor); MempoolGate's admission rule (`minFeeB + quote × bytes ≤ max_fee_lovelace`); ActorComponent's MaxFeePolicy::ScaledOverLaneQuote{4,1} setting `max_fee_lovelace`; ActorComponent's LanePolicy::UtilityMaximising deciding which lane each new tx posts to. For each submodel, cite the exact source file path within sim-rs/sim-core/src/.

After writing, add a closing §"Where to go next" sub-section with bulleted forward-pointers to the other docs/phase-2 documents (mechanism-design.md spec; cardano-realism-audit.md calibration audit; validity-threats.md per-suite trust; realism-risks-register.md unresolved risks; coverage-check.md per-claim coverage). Re-audit the worked-example prose for the abbreviation-on-first-use rule — each abbreviation expanded the first time it appears in the §"Worked example" block in isolation (since the example is self-contained as a learning device for new contributors per D-42).
  </action>
  <verify>
    <automated>grep -cE "^## Worked example" docs/phase-2/methodology-overview.md | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; grep -cE "^### Worked example: (Purpose|State variables|Process overview|Design concepts|Initialisation|Input data|Submodels)$" docs/phase-2/methodology-overview.md | awk '$1 == 7 {exit 0} {exit 1}' &amp;&amp; grep -qE "(menu_unreserved_priority_only_static_x4|menu_unreserved_both_dynamic_x4|phase-2-priority-only-unreserved|phase-2-two-lane-both-dynamic)" docs/phase-2/methodology-overview.md &amp;&amp; grep -qE "seed.{0,3}=.{0,3}1|seed = 1|seed=1" docs/phase-2/methodology-overview.md &amp;&amp; test $(wc -l &lt; docs/phase-2/methodology-overview.md) -ge 200</automated>
  </verify>
  <done>File `docs/phase-2/methodology-overview.md` has §"Worked example" with seven `### Worked example: <element>` sub-sections matching the canonical ODD element names; the chosen TEST-04 canonical cell is named explicitly; seed = 1 is named; file is at least 200 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Linked file references → CIP-cited URL | The CIP cites this document by repo URL; every cross-reference inside the doc must resolve to an existing file path. Dead links would survive into the CIP. |
| Worked-example numerical claims → Phase 3 evidence | The worked example cites the TEST-04 CI as the run outcome; the numbers must match `.planning/realism-tests/multi-seed-variance/results.md` exactly — fabricated numbers would land in CIP-cited prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | I (Information Disclosure) | Worked-example numerical claims | mitigate | The worked example cites the exact TEST-04 CI from `.planning/realism-tests/multi-seed-variance/results.md`; Plan 04-07's consistency review re-verifies the cited numbers against the source artefact |
| T-04-05 | T (Tampering) | Dead markdown links | mitigate | Plan 04-07 runs a markdown-link-resolution check across this file; broken links are caught before Phase 5 |
| T-04-06 | R (Repudiation) | ODD element naming | accept | The seven canonical ODD element names are pinned in the regex acceptance check; any drift from Grimm et al.'s canonical order or capitalisation fails the gate |
</threat_model>

<verification>
- `docs/phase-2/methodology-overview.md` exists with §"Reading guide", §"Index", §"Per-element prose", §"Worked example", §"Where to go next"
- Exactly 7 H3 headings match `^### (Purpose|State variables|Process overview|Design concepts|Initialisation|Input data|Submodels)$` in §"Per-element prose"
- Exactly 7 H3 headings match `^### Worked example: (Purpose|State variables|Process overview|Design concepts|Initialisation|Input data|Submodels)$` in §"Worked example"
- Worked example names a specific Phase 3 TEST-04 canonical cell (`menu_unreserved_priority_only_static_x4` or `menu_unreserved_both_dynamic_x4`)
- Worked example names seed = 1
- File is at least 200 lines
- Abbreviations Overview, Design concepts, Details (ODD), Cardano Improvement Proposal (CIP), Ethereum Improvement Proposal 1559 (EIP-1559), ranking block (RB), endorser block (EB) expand on first use somewhere in the document
</verification>

<success_criteria>
- ROADMAP.md Phase 4 success criterion #4: A document at `docs/phase-2/methodology-overview.md` exists as a one-page ODD methodology index mapping the seven ODD elements to in-repo locations, with brief prose per element and one worked example tracing a single (job, seed) end-to-end
- The CIP can cite this document by repo URL rather than embedding methodology prose (the worked example walks a CIP-cited Phase 3 result, demonstrating the document is self-sufficient as a methodology reference)
</success_criteria>

<output>
After completion, create `.planning/phases/04-refresh-and-anchor/04-02-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. The SUMMARY records: the canonical cell chosen for the worked example; the file's final line count; any deviations from D-42's shape (e.g. additional sub-sections beyond the 5 required); cross-references that Plan 04-07's link-check should verify.
</output>
