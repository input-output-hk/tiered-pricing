---
phase: 01-viz-site-mvp
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - sim-rs/scripts/viz/static/index.html
  - sim-rs/scripts/viz/static/style.css
  - sim-rs/scripts/viz/static/main.js
  - sim-rs/scripts/viz/static/plot.min.js
  - sim-rs/scripts/viz/static/PLOT_VERSION.txt
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04, VIZ-06]
must_haves:
  truths:
    - "Opening `index.html` over local HTTP loads `main.js` as an ES module, which imports Observable Plot (D-03) from a relative path (vendored locally per RESEARCH.md Open Q #3 / A3 and D-19); no network request to a CDN is made at page load."
    - "Hash-based routing matches three URL shapes: `#/` (suite list), `#/suite/<suite_id>` (drill-down), `#/job/<suite_id>/<job>/<seed>` (per-(job, seed) detail)."
    - "Every DOM insertion of suite/job/seed strings uses `textContent` (never `innerHTML`) — Pitfall security landmine."
    - "CSS is minimal: < 100 lines, no design system, no dark mode (D-23). The site is a developer tool."
    - "`plot.min.js` is committed to the repo at version 0.6.17 from jsDelivr; a sibling `PLOT_VERSION.txt` records the exact version + retrieval date for refresh tracking."
  artifacts:
    - path: sim-rs/scripts/viz/static/index.html
      provides: "SPA shell that loads main.js as a module and contains `<div id=\"app\"></div>` mount point"
      contains: "<script type=\"module\" src=\"static/main.js\">"
    - path: sim-rs/scripts/viz/static/style.css
      provides: "Minimal styling for table rows, headline metric cards, and chart pane gaps"
    - path: sim-rs/scripts/viz/static/main.js
      provides: "Router with `route()`, `renderHome`, `renderSuite`, `renderJob` stubs that draw stub markup so plumbing is testable before the chart-rendering Plan 01-05 lands"
      exports: ["route"]
    - path: sim-rs/scripts/viz/static/plot.min.js
      provides: "Vendored Observable Plot 0.6.x bundle (UMD or ESM — Task 1 chooses)"
      min_lines: 1
    - path: sim-rs/scripts/viz/static/PLOT_VERSION.txt
      provides: "Single-line `@observablehq/plot@<version> retrieved <YYYY-MM-DD>` record"
  key_links:
    - from: sim-rs/scripts/viz/static/index.html
      to: sim-rs/scripts/viz/static/main.js
      via: "<script type=\"module\" src=\"static/main.js\">"
      pattern: "type=\"module\".*static/main\\.js"
    - from: sim-rs/scripts/viz/static/main.js
      to: sim-rs/scripts/viz/static/plot.min.js
      via: "ES-module import or `<script>` tag in index.html"
      pattern: "(import.*plot\\.min\\.js|src=\"static/plot\\.min\\.js\")"
    - from: sim-rs/scripts/viz/static/main.js
      to: data/index.json
      via: "fetch('data/index.json') in renderHome"
      pattern: "fetch\\([\"']data/index\\.json[\"']\\)"
---

<objective>
The static browser bundle: a Single-Page Application (SPA) shell, vendored Observable Plot, minimal CSS, and a hash router with stub view-renderers that draw placeholder markup against the three-tier JSON contract.

Purpose: Let the data pipeline (Plan 01-02) and the entry-point/serve glue (Plan 01-04) be exercised end-to-end before the full chart-rendering logic (Plan 01-05) lands. Locks the index.html ↔ main.js ↔ plot.min.js wiring early so downstream plans can focus on view content. Also fulfils the "Local-first: must work without internet" promise (PROJECT.md) by vendoring Observable Plot under `static/` per RESEARCH.md Open Q #3 / A3.

Output: `sim-rs/scripts/viz/static/` populated with `index.html`, `style.css`, `main.js`, vendored `plot.min.js`, and `PLOT_VERSION.txt`. `main.js` includes a router that dispatches to placeholder render functions; Plan 01-05 fleshes the renderers out.

Autonomy: This plan is fully autonomous — automated grep gates and (where `node` is available) `node --check` validate the bundle. The end-to-end browser-load human-verify checkpoint that lived here previously has been relocated to Plan 01-04 as a pre-flight checkpoint (Task 0), so it runs after `build.py --serve` exists and against the live `--serve` invocation rather than a hand-assembled `python3 -m http.server` bundle copy. This keeps Plan 03 in Wave 2 with no cross-wave dependency on `build.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-CONTEXT.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Locked JSON contract from Plan 01-02. Plan 01-03's main.js fetches against these endpoints. -->

# GET data/index.json → see Plan 01-02 schema (suites: [{id, name, path, started_at, job_count, seed_count, completed_count, max_concurrent_jobs}])
# GET data/<suite_id>.json → see Plan 01-02 schema (manifest + jobs[job].seeds[seed].headline + aggregates: null)
# GET data/<suite_id>/<job>-<seed>.json → see Plan 01-02 schema (retained_value, components, time_series long-form, peak_mempool_bytes)

# main.js exports (used by Plan 01-05 to extend without breaking the router)
async function route()                  // reads location.hash, dispatches to render*()
async function renderHome()             // GET data/index.json → table of suites
async function renderSuite(suiteId)     // GET data/<suiteId>.json → manifest + (job,seed) table + cross-seed overlay button
async function renderJob(suiteId, job, seed)   // GET data/<suiteId>/<job>-<seed>.json → headline strip + 3 chart panes

# Three hash shapes
"#/"
"#/suite/<suite_id>"
"#/job/<suite_id>/<job>/<seed>"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor Observable Plot 0.6.x and record its version</name>
  <files>sim-rs/scripts/viz/static/plot.min.js, sim-rs/scripts/viz/static/PLOT_VERSION.txt</files>
  <read_first>
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md (Open Questions #3, Environment Availability, Assumptions Log A3),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (`### sim-rs/scripts/viz/static/plot.min.js (vendored Observable Plot 0.6.x)` section)
  </read_first>
  <action>
    Vendor Observable Plot 0.6.x into the repo so the site is fully offline-capable per PROJECT.md "Local-first: must work without internet."

    Use `curl -fsSL` (or `python3 -m urllib.request`) to download from jsDelivr. Choose the UMD bundle for simplicity (it exposes a global `Plot` and works inside both ES-module and classic-script tags). The URL is `https://cdn.jsdelivr.net/npm/@observablehq/plot@0.6/dist/plot.umd.min.js`. Save to `sim-rs/scripts/viz/static/plot.min.js`.

    Resolve the exact patch version at download time (jsDelivr serves the latest 0.6.x under `@0.6`). Capture it via `curl -fsSL https://cdn.jsdelivr.net/npm/@observablehq/plot@0.6/package.json | python3 -c "import json, sys; print(json.load(sys.stdin)['version'])"` (or equivalent). Write `sim-rs/scripts/viz/static/PLOT_VERSION.txt` containing exactly one line:

        @observablehq/plot@<version> retrieved <YYYY-MM-DD> from https://cdn.jsdelivr.net/npm/@observablehq/plot@0.6/dist/plot.umd.min.js

    The version expected at planning time per RESEARCH.md is 0.6.17 (verified 2026-05-20); accept anything in the 0.6.x range that jsDelivr serves.

    Do NOT add either file to `.gitignore` (existing `sim-rs/.gitignore` `/output` does not cover `sim-rs/scripts/`, so committed assets remain tracked — which is the intent). Do NOT add an ESM build; the UMD form is enough for v1 (Plot's getting-started docs treat ESM as preferred but UMD as a fully-supported alternative).

    If the network fetch fails (no internet, jsDelivr 5xx), surface the error clearly: this is the single network step in the entire phase. Do not write a stub `plot.min.js`; either the real bundle is in place or this task fails.
  </action>
  <verify>
    <automated>test -f sim-rs/scripts/viz/static/plot.min.js && test -s sim-rs/scripts/viz/static/plot.min.js && test "$(wc -c < sim-rs/scripts/viz/static/plot.min.js)" -gt 50000 && grep -qE 'observablehq/plot@0\.6\.[0-9]+ retrieved [0-9]{4}-[0-9]{2}-[0-9]{2}' sim-rs/scripts/viz/static/PLOT_VERSION.txt && head -c 200 sim-rs/scripts/viz/static/plot.min.js | grep -qiE '(observable|plot)' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `sim-rs/scripts/viz/static/plot.min.js` exists and is >= 50 KB (Plot's UMD bundle is ~150 KB; significantly smaller indicates an incomplete download).
    - The first 200 bytes of the file contain the word `observable` or `plot` (sanity check that we got Plot, not an HTML error page).
    - `sim-rs/scripts/viz/static/PLOT_VERSION.txt` exists and contains a line matching `@observablehq/plot@0.6.[0-9]+ retrieved YYYY-MM-DD`.
    - No `.gitignore` modification (`git diff sim-rs/.gitignore` empty, no new lines in repo-root `.gitignore` referencing `plot.min.js`).
  </acceptance_criteria>
  <done>
    Two files on disk; verification command prints OK.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author `index.html`, `style.css`, and `main.js` with router + stub renderers</name>
  <files>sim-rs/scripts/viz/static/index.html, sim-rs/scripts/viz/static/style.css, sim-rs/scripts/viz/static/main.js</files>
  <read_first>
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md (Architecture Patterns / Pattern 2 — hash router; Security Domain — bind 127.0.0.1, textContent never innerHTML),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (`### sim-rs/scripts/viz/static/index.html`, `### sim-rs/scripts/viz/static/main.js`, `### sim-rs/scripts/viz/static/style.css` sections; Shared Patterns / "textContent, never innerHTML"),
    sim-rs/output/tiered_plot.html (CONTEXT.md `<canonical_refs>` flags this reference-only — do NOT copy code; chart taxonomy is informational only)
  </read_first>
  <action>
    Author three files in `sim-rs/scripts/viz/static/`. All are greenfield; no in-repo Python or Rust analog. Use RESEARCH.md `## Architecture Patterns / Pattern 2` and PATTERNS.md `### sim-rs/scripts/viz/static/main.js` as the templates.

    (a) `index.html` — minimal SPA shell. `<!doctype html>`, `<html lang="en">`, `<meta charset="utf-8">`, `<title>phase-2 sim viz</title>`, link to `static/style.css`. The Plot bundle is loaded via classic `<script src="static/plot.min.js"></script>` BEFORE the module script (UMD exposes `window.Plot` so the module can reference it as a global; this is simpler than re-importing ESM and Plot's UMD docs use this pattern). Mount point: `<div id="app"></div>`. Module entry: `<script type="module" src="static/main.js"></script>`. Optionally a `<header>` with the title text and a link to `#/` (so the user can return home from any view via a static link). No external network references (no Google Fonts, no CDN), no analytics, no favicon link (or use a base64 data URL favicon to silence the 404). The page title and any human-visible text must comply with CLAUDE.md's abbreviation-expansion rule: spell "Single-Page Application (SPA)" if it ever appears; "Observable Plot" if mentioned in user-visible text.

    (b) `style.css` — minimal CSS per D-23. Aim for under ~100 lines total. Cover only: body font (system font stack, no web font fetches), max-width container (~960px), table row striping, headline-metric "cards" (simple bordered divs in a flex row), chart-pane vertical spacing, link colour, a `.muted` class for secondary metadata. No CSS variables theme, no dark-mode media query, no responsive breakpoints. Comments at the top: "Minimal developer-tool styling per D-23. Not a polished product surface." Use 2-space indentation and lowercase class names with hyphens.

    (c) `main.js` — ES module with the hash router. Follow PATTERNS.md `### sim-rs/scripts/viz/static/main.js` exactly:
      - At the top, a one-line comment noting that Observable Plot is loaded by the preceding `<script>` tag in index.html and is available as the global `window.Plot`. If a future iteration wants ESM import, swap the index.html line and add `import * as Plot from "./plot.min.js"` here.
      - `async function route()` reads `location.hash`, regex-matches `^#\/(?:(suite|job)\/(.+))?$`, dispatches to `renderHome()`, `renderSuite(suiteId)`, or `renderJob(suiteId, job, seed)`. The `#/job/<suite>/<job>/<seed>` shape unpacks via `.split("/")`.
      - `window.addEventListener("hashchange", route)` and `window.addEventListener("DOMContentLoaded", route)`.
      - Stub `renderHome()`: `fetch("data/index.json").then(r => r.json())`, replace `#app` children with a `<table>` whose `<thead>` is `name | path | started_at | jobs | seeds | completed | max_concurrent`. For each suite, build the row using `document.createElement("tr")`, create each `<td>` and set `cell.textContent = value` — NEVER `cell.innerHTML = value`. Each row's first cell wraps the suite name in an `<a href="#/suite/${suite.id}">` link. Empty-state per D-20: when `suites.length === 0`, render a `<p>` with `textContent = "No suites found under <source>. Re-run the build after suites land under sim-rs/output/."`.
      - Stub `renderSuite(suiteId)`: `fetch("data/${encodeURIComponent(suiteId)}.json")` — handle 404 with a friendly empty-state. Render a heading (suite name from JSON, `textContent`), a `<dl>` with raw manifest summary (started_at, job count, seed count), and a `<table>` listing `(job, seed)` rows with links to `#/job/<suite_id>/<job>/<seed>`. For each row, render a placeholder headline-metric cell (the full headline content lands in Plan 01-05). If `data.aggregates === null`, render nothing for aggregates (the field is null on every phase-2 suite per CRITICAL LANDMINE #2 / Plan 01-02 schema); add a `<!-- aggregates: null — no priority_only_fast_path_overall_comparison.csv in phase-2 -->` comment to the rendered output so a future executor reading DevTools knows why the section is empty.
      - Stub `renderJob(suiteId, job, seed)`: `fetch("data/${encodeURIComponent(suiteId)}/${encodeURIComponent(job)}-${encodeURIComponent(seed)}.json")`. Render a heading (job, seed via textContent), a headline-strip placeholder, and three empty `<div class="chart-pane">` containers labelled "controller quote per lane", "mempool bytes", "derived_quote per block" (the last is a stub; the time-series CSV doesn't carry `derived_quote` per block today — RESEARCH.md `## System Architecture Diagram` notes the available columns. Document this in a comment: "TODO Plan 01-05 / VIZ-04: the 'derived_quote per block' label corresponds to no single column in time_series.csv; render `c_priority` + `c_standard` here for v1 — same controller quote, lane-coloured."). For the latency-by-component label use exactly "Latency by demand component (blocks)" — NEVER "latency by lane" (CRITICAL LANDMINE #3 / Pitfall 5).

      Helper utilities at file top:
        function $app() { return document.getElementById("app"); }
        function el(tag, opts = {}, ...children) {
          const node = document.createElement(tag);
          if (opts.class) node.className = opts.class;
          if (opts.text !== undefined) node.textContent = opts.text;  // textContent only
          if (opts.href) node.href = opts.href;
          for (const c of children) node.append(c);
          return node;
        }
        function setView(node) { const a = $app(); a.replaceChildren(node); }

      Use `el(...)` everywhere in the stub renderers so the "textContent only" rule is structurally enforced — `innerHTML` does not appear anywhere in the file. Grep enforcement is part of the acceptance criteria.

      Module-level constant: `const HEADLINE_LATENCY_LABEL = "Latency by demand component (blocks)";`. Reference this constant in every place a latency UI label is needed; never spell the string inline.
  </action>
  <verify>
    <automated>test -f sim-rs/scripts/viz/static/index.html && test -f sim-rs/scripts/viz/static/style.css && test -f sim-rs/scripts/viz/static/main.js && grep -q '<script type="module" src="static/main.js">' sim-rs/scripts/viz/static/index.html && grep -q 'src="static/plot.min.js"' sim-rs/scripts/viz/static/index.html && grep -q '<div id="app">' sim-rs/scripts/viz/static/index.html && ! grep -q 'innerHTML' sim-rs/scripts/viz/static/main.js && grep -q 'textContent' sim-rs/scripts/viz/static/main.js && grep -q 'hashchange' sim-rs/scripts/viz/static/main.js && grep -q "fetch.*data/index\\.json" sim-rs/scripts/viz/static/main.js && grep -q 'Latency by demand component (blocks)' sim-rs/scripts/viz/static/main.js && ! grep -qi 'latency by lane' sim-rs/scripts/viz/static/main.js && test "$(grep -v '^\\s*$' sim-rs/scripts/viz/static/style.css | grep -v '^\\s*/\\*' | wc -l)" -lt 200 && node --check sim-rs/scripts/viz/static/main.js 2>&1 | tail -3 || (command -v node >/dev/null && echo 'node check skipped: no node available; relying on browser load in Plan 04 pre-flight checkpoint') && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `index.html`, `style.css`, `main.js` all exist under `sim-rs/scripts/viz/static/`.
    - `index.html` contains the literal substring `<script type="module" src="static/main.js">`.
    - `index.html` contains the literal substring `src="static/plot.min.js"` (UMD load BEFORE the module script).
    - `index.html` contains `<div id="app">` (mount point).
    - `main.js` contains NO occurrence of `innerHTML` (grep returns nothing — CRITICAL LANDMINE #7 security rule).
    - `main.js` contains at least one occurrence of `textContent`.
    - `main.js` contains a `hashchange` event listener and a `DOMContentLoaded` event listener.
    - `main.js` contains a `fetch("data/index.json"...)` call (the renderHome data load).
    - `main.js` contains the literal string `Latency by demand component (blocks)` (CRITICAL LANDMINE #3 / Pitfall 5 — UI label is per-component, not per-lane).
    - `main.js` contains NO occurrence of the case-insensitive string `latency by lane` (regression guard).
    - `style.css` is under 200 non-blank non-comment lines (D-23 minimalism).
    - If `node` is on PATH: `node --check sim-rs/scripts/viz/static/main.js` exits 0 (syntactic JS validity). If not: deferred to the Plan 04 pre-flight human-verify checkpoint.
  </acceptance_criteria>
  <done>
    Three files on disk; the automated verification command prints OK. The actual visual / network behaviour is confirmed by the pre-flight human-verify checkpoint at the head of Plan 01-04 (which runs once `build.py --serve` exists end-to-end).
  </done>
</task>

</tasks>

<verification>

```bash
# All static assets present and structurally clean
ls -la sim-rs/scripts/viz/static/
file sim-rs/scripts/viz/static/plot.min.js
wc -l sim-rs/scripts/viz/static/*.{html,css,js}

# No innerHTML, no "latency by lane" anywhere in main.js
grep -nE 'innerHTML|latency by lane' sim-rs/scripts/viz/static/main.js  # should print nothing
grep -nE 'textContent|Latency by demand component' sim-rs/scripts/viz/static/main.js  # should print >=2 lines

# Bundle structurally valid
node --check sim-rs/scripts/viz/static/main.js  # if node available
```

</verification>

<success_criteria>
- Static bundle in place, `plot.min.js` vendored and versioned.
- Router shape matches the three hash URLs in the locked interfaces.
- Security rules enforced structurally (no `innerHTML`, no `latency by lane`).
- End-to-end browser-load confirmation is performed by Plan 01-04's pre-flight checkpoint (Task 0), which exercises the live `build.py --serve` invocation rather than a hand-assembled `python3 -m http.server` bundle copy.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → browser DOM | Untrusted text (suite names from `manifest["suite-name"]`) is read from disk-resident JSON and injected into the DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | static/main.js DOM insertion sites | mitigate | All suite/job/seed strings inserted via `textContent`, never `innerHTML`. Grep gate in acceptance criteria of Task 2 forbids the substring `innerHTML` anywhere in `main.js`. |
| T-01-03-02 | Information disclosure | local HTTP serve binding | mitigate (deferred to Plan 01-04) | Plan 01-04 binds `ThreadingHTTPServer(("127.0.0.1", port), handler)` explicitly, and its pre-flight Task 0 (human-verify checkpoint) confirms no external host appears in the browser network panel against the live `build.py --serve` invocation. |
| T-01-03-03 | Tampering | vendored plot.min.js integrity | accept | We download once from jsDelivr (the upstream Observable Plot distribution). No SRI hash captured — overkill for a developer-tool MVP whose threat surface is the developer's own machine. `PLOT_VERSION.txt` records the version; refresh annually with a one-line PR. |
</threat_model>

<output>
After completion, create `.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-03-SUMMARY.md` per the standard summary template. Include the resolved Plot version, the line counts for index.html/style.css/main.js, and explicit confirmation that the security grep gates (no `innerHTML`, no `latency by lane`) are green.
</output>
