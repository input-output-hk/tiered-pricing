---
phase: 05-handoff
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - docs/phase-2/cip-author-summary.md
  - .planning/phases/05-handoff/05-CONSISTENCY-REPORT.md
autonomous: false
requirements: [HAND-01, HAND-03]
tags: [handoff, cip-author-summary, paste-guide, headline-claim-derivation, tag-message-draft]
must_haves:
  truths:
    - "`docs/phase-2/cip-author-summary.md` exists and is the single paste-guide artefact for the Cardano Improvement Proposal (CIP) author"
    - "The summary's headline CIP claim list (4-8 claims per D-46) is derived from the Phase 3 / Phase 4 evidence base and each claim names its backing CLM-NN row(s)"
    - "The summary's paste-target table maps every CIP section (Limitations, Evidence, Methodology, Calibration, Trust matrix) to its source artefact and the specific paragraphs/rows to paste — implements D-44's hybrid shape (paste-target table → per-CIP-section recommendations → pinned references block) with the document landing in 300-450 lines"
    - "The summary's pinned-references block names: (a) the suggested git tag `phase-2-cip-evidence-v1`; (b) the milestone-close commit SHA (or a `(tag pending: ... commit <SHA>)` placeholder if the user has not yet applied the tag); (c) the epoch-582 Cardano mainnet stake snapshot reference (retrieved 2026-05-14); (d) the `.planning/phases/05-handoff/verify-consistency.sh` script path and the `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md` report path"
    - "Tiered inline-vs-reference treatment per D-45: substrate-scope umbrella + top-3-5 Limitations paragraphs + headline-claim mappings inlined verbatim; long-tail paragraphs and supporting CLM rows referenced by RSK-NN + path + line range"
    - "A tag-message draft (~12-15 lines per CONTEXT.md Claude's Discretion) is embedded in the summary's pinned-references block for the user to apply via `git tag -a phase-2-cip-evidence-v1 -m '<message>'`"
    - "After writing the summary, the Plan-05-02 verification script is re-run; the consistency report is appended with a post-summary verification block recording the final numbers (including the cip-author-summary.md row, which now has live references to verify)"
  artifacts:
    - path: "docs/phase-2/cip-author-summary.md"
      provides: "Hybrid-shape CIP-author paste guide: paste-target table + per-CIP-section recommendations + pinned-references block"
      min_lines: 300
      max_lines: 500
      contains: "Paste-target table; Per-CIP-section recommendations; Pinned references; Tag message draft; Headline CIP claim list"
    - path: ".planning/phases/05-handoff/05-CONSISTENCY-REPORT.md"
      provides: "Plan-05-02 four-check audit log appended with post-Plan-05-03 verification numbers covering the new cip-author-summary.md"
      contains: "Post-Plan-05-03 verification"
  key_links:
    - from: "docs/phase-2/cip-author-summary.md §\"Paste-target table\""
      to: "docs/phase-2/realism-risks-register.md `## RSK-substrate-scope` + the top-3-5 Limitations RSK-NN entries"
      via: "table-cell references to each RSK-NN identifier + line range"
      pattern: "Limitations row cites RSK-NN and disclosure-paragraph line range in realism-risks-register.md"
    - from: "docs/phase-2/cip-author-summary.md §\"Headline CIP claim list\""
      to: "docs/phase-2/coverage-check.md `CLM-06` / `CLM-07` / `CLM-08` / `CLM-09` (the four TEST-04 N=20 canonical menu-item rows)"
      via: "per-claim CLM-NN citation"
      pattern: "Each headline claim names its backing CLM-NN row(s)"
    - from: "docs/phase-2/cip-author-summary.md §\"Pinned references\""
      to: "`.planning/phases/05-handoff/verify-consistency.sh` + `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md`"
      via: "explicit path + invocation command for future CIP-reviewer reproducibility"
      pattern: "Pinned references block names the script + report by path"
---

<objective>
Create the hybrid-shape `docs/phase-2/cip-author-summary.md` paste guide for the Cardano Improvement Proposal (CIP) author. Derive the 4-8 headline CIP claims from Phase 3 / Phase 4 evidence per D-46, lay out the paste-target table per D-44, apply tiered inline-vs-reference treatment per D-45, and embed the tag-message draft for HAND-03 (user-executed; per the don't-auto-commit memory, Claude drafts the message but does NOT run `git tag`).

After the summary is written, re-run `.planning/phases/05-handoff/verify-consistency.sh` and append a post-summary verification block to `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md` recording the final cross-reference numbers (which now include the new cip-author-summary.md file in the in-scope set).

The git-tag command itself is **NOT executed by this plan** — the user runs it after Phase 5 close. The pinned-references block in the summary names the tag with a `(tag pending: applied by user via `git tag -a phase-2-cip-evidence-v1 -m '<message>'` against commit `<SHA-or-HEAD>`)` annotation if the tag has not yet landed.

Purpose: Phase 5 / HAND-01 hands the CIP author a single paste guide that names exactly which artefact, paragraph, or row to copy into each CIP section, the post-Phase-5 register state (24 DISCLOSED), and the citable git tag.

Output: `docs/phase-2/cip-author-summary.md` (new file; 300-500 lines) + updated `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md` (post-Plan-05-03 verification block appended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-handoff/05-CONTEXT.md
@.planning/phases/04-refresh-and-anchor/04-SUMMARY.md
@.planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md
@docs/phase-2/realism-risks-register.md
@docs/phase-2/coverage-check.md
@docs/phase-2/cardano-realism-audit.md
@docs/phase-2/validity-threats.md
@docs/phase-2/methodology-overview.md
@CLAUDE.md

<interfaces>
<!-- Hybrid summary shape per D-44 (top → middle → bottom): -->
<!--   1. Top: paste-target table (one page) mapping (CIP section → source artefact → paste content). -->
<!--      Five CIP sections in Cardano CIP house style: -->
<!--      - Methodology  (cites methodology-overview.md by repo URL) -->
<!--      - Calibration  (cites cardano-realism-audit.md value/source/date-retrieved triples) -->
<!--      - Trust matrix (cites validity-threats.md per-suite ratings) -->
<!--      - Evidence     (cites coverage-check.md CLM rows, especially CLM-06..09 N=20 + CLM-10..11 sign-flip + CLM-12..13 WEAK) -->
<!--      - Limitations  (cites realism-risks-register.md disclosure-paragraphs; the 24 DISCLOSED entries are paste-targets) -->
<!--   2. Middle: per-CIP-section recommendations (paste order, caveats, tier-1 inline list + tier-2 reference list) -->
<!--   3. Bottom: pinned-references block (tag name, commit SHA, epoch-582 snapshot, script + report paths, tag-message draft) -->

<!-- Tiered inline-vs-reference per D-45: -->
<!--   INLINE VERBATIM in the summary: -->
<!--     - substrate-scope umbrella disclosure-paragraph from RSK-substrate-scope -->
<!--     - top-3 to top-5 Limitations disclosure-paragraphs in paste order (Claude's Discretion default order per CONTEXT.md <specifics>) -->
<!--     - headline Evidence-section claim-to-CLM mappings (4-8 claims) -->
<!--   REFERENCE-ONLY in the summary: -->
<!--     - long-tail Limitations paragraphs (cited by RSK-NN + path + line range) -->
<!--     - supporting CLM rows beyond the headline claims -->
<!--     - calibration-triple references in cardano-realism-audit.md -->
<!--     - validity-threats per-suite trust matrix -->

<!-- Headline CIP claim list draft (4 claims minimum per D-46; planner refines/extends): -->
<!--   1. "Un-reserved two-lane mechanisms outperform single-lane EIP-1559 on welfare at N=20 BCa 95% CI" -->
<!--      → backed by CLM-07 (priority-only-un-reserved; Δ=+6.66e+09; CI [+4.28e+09, +8.49e+09]; sign-coh=0.90; distinct-hash=20/20) -->
<!--      → backed by CLM-09 (both-dynamic-un-partitioned; Δ=+7.95e+09; CI [+5.65e+09, +1.09e+10]; sign-coh=0.90; distinct-hash=20/20) -->
<!--      → paired baseline: single-lane EIP-1559 control via CLM-05 -->
<!--   2. "RB-reserved two-lane mechanisms underperform single-lane EIP-1559 under same calibration" -->
<!--      → backed by CLM-06 (priority-only-RB-reserved; Δ=-4.15e+09; CI [-6.02e+09, -1.00e+09]; sign-coh=0.65) -->
<!--      → backed by CLM-08 (both-dynamic-partitioned; Δ=-4.15e+09; CI [-5.95e+09, -8.87e+08]; sign-coh=0.65) -->
<!--   3. "multiplier_floor=4 calibration is regime-dependent at multiplier_floor=16" -->
<!--      → backed by TEST-07a CLM row(s) (sourced from 04-03-phase3-evidence-summary.md §"TEST-07a multiplier-floor-16 companion") -->
<!--   4. "partitioned ≡ RB-reserved welfare at sundaeswap_moderate × multiplier_floor=4 replicates at N=20" -->
<!--      → backed by TEST-04 canonical-cells CLM row (cross-arm duplicate-job artefact) -->
<!--   Optional extensions (planner picks up to 4 more if Phase 3 evidence licenses): -->
<!--   5. Sign-flip cell variance findings at N=20 (CLM-10..13) -->
<!--   6. Reorg-safety by construction (CLM-19..23) -->
<!--   7. Calibration anchors (CLM-24..27 RB cadence; CLM-05 EIP-1559 window-length anchor) -->
<!--   8. Hash-diversity gate 17/17 (cites COV-05 + RSK-hash-diversity-policy DISCLOSED) -->

<!-- Limitations paste-order default per CONTEXT.md <specifics> (Claude's Discretion; planner may reorder): -->
<!--   1. RSK-substrate-scope (umbrella; sets substrate boundary) -->
<!--   2. RSK-cross-arch-determinism (substrate-scope cross-arch corollary) -->
<!--   3. RSK-leios-spec-pre-deployment (substrate-scope spec-immaturity corollary) -->
<!--   4. RSK-pool-count + RSK-calibration-stale-stake-snapshot (topology / calibration freshness) -->
<!--   5. RSK-steady-state-run-length (simulation run-length boundary) -->
<!--   6. Semantic-reinterpretation cluster (RSK-fee-as-maxFee-envelope, RSK-mempool-cap-magnitude, -->
<!--      RSK-max-fee-policy-default, RSK-target-inclusion-blocks-default) -->
<!--   7. Demand-modelling cluster (RSK-demand-mix-bit-calibration, RSK-demand-non-stationarity, -->
<!--      RSK-sundaeswap-demand-staleness) -->
<!--   8. RSK-un-anchored-controller-knobs (controller-knob anchor-or-disclose; 1 ANCHORED + 3 DISCLOSED sub-knobs) -->
<!--   9. RSK-multiplier-floor-4-suite-coverage (suite-coverage corollary of un-anchored knobs) -->
<!--  10. RSK-partition-activated-honest-producer (honest-producer-assumption boundary) -->
<!--  11. Reporting-precision cluster (RSK-admission-rejection-attribution, RSK-welfare-as-f64-reporting) -->
<!--  12. Five Plan-05-01 prose-promotion entries: RSK-single-seed-precision, RSK-three-seed-statistical-power, -->
<!--      RSK-unresolved-suite-claims, RSK-standard-user-fee-drift-exposure, RSK-menu-collapse-to-advocacy -->
<!--  13. RSK-hash-diversity-policy (closes the section by reinforcing BACKED claims meet a known gate) -->

<!-- Tag message template (~12-15 lines per CONTEXT.md): -->
<!--   phase-2-cip-evidence-v1 — milestone-close tag -->
<!--   -->
<!--   Citable reference for the Cardano Improvement Proposal (CIP) -->
<!--   responding to CPS-0023 ("Urgency Signaling"). -->
<!--   -->
<!--   CIP-cited artefacts: -->
<!--   - docs/phase-2/cardano-realism-audit.md -->
<!--   - docs/phase-2/validity-threats.md -->
<!--   - docs/phase-2/realism-risks-register.md (24 DISCLOSED + 0 LIVE) -->
<!--   - docs/phase-2/coverage-check.md -->
<!--   - docs/phase-2/methodology-overview.md -->
<!--   - docs/phase-2/cip-author-summary.md (paste guide) -->
<!--   -->
<!--   Topology snapshot: Cardano mainnet, epoch 582, retrieved 2026-05-14. -->
<!--   Consistency audit: .planning/phases/05-handoff/05-CONSISTENCY-REPORT.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the headline CIP claim list + author the cip-author-summary.md hybrid paste guide</name>
  <files>docs/phase-2/cip-author-summary.md</files>
  <read_first>
    - .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md (full file; the headline numerical findings + the four-claim-minimum derivation source per D-46)
    - .planning/realism-tests/multi-seed-variance/results.md (TEST-03 + TEST-04 N=20 BCa CIs; cell-level numerical findings)
    - .planning/realism-tests/multiplier-floor-16-companion/results.md (TEST-07a regime-dependence finding for headline claim #3)
    - .planning/realism-tests/hash-diversity-gate/results.md (17/17 BACKED-eligible cells pass; for optional headline claim #8)
    - docs/phase-2/realism-risks-register.md (post-Plan-05-01 24 DISCLOSED state; source for the inline substrate-scope umbrella + top-3-5 Limitations paragraphs)
    - docs/phase-2/coverage-check.md (CLM-06..09 N=20 canonical, CLM-10..13 sign-flip, CLM-05 EIP-1559 control; backing-CLM identifiers for each headline claim)
    - docs/phase-2/cardano-realism-audit.md §"Recommended disclosure statements" (the engineering-report voice the summary's inline paragraphs match)
    - docs/phase-2/validity-threats.md §"Aggregate trust summary" (the trust-matrix-source-of-truth for the summary's Trust matrix paste-target row)
    - docs/phase-2/methodology-overview.md (cited as the CIP Methodology section's primary URL target)
    - .planning/phases/05-handoff/05-CONTEXT.md (full file; D-44 / D-45 / D-46 / D-47 / D-50 / D-51 / D-52 and the <specifics> Limitations paste-order draft)
    - CLAUDE.md §"Conventions / gotchas" (abbreviation-on-first-use; the document is long enough to warrant a consolidated header expansion block)
  </read_first>
  <action>
Create `docs/phase-2/cip-author-summary.md`. Target 300-500 lines. The document follows the hybrid shape per D-44: top paste-target table → middle per-CIP-section recommendations → bottom pinned-references block. Apply tiered inline-vs-reference treatment per D-45 throughout.

**Step A — Header block** (lines 1-30):
- H1 title: `# Cardano Improvement Proposal (CIP) Author Summary — Phase-2 Evidence Base`.
- Status fields: `**Status:**` (post-Phase-5 close; references citable git tag `phase-2-cip-evidence-v1`), `**Scope:**` (paste guide for the CIP responding to CPS-0023 "Urgency Signaling"), `**Identifier convention:**` (RSK-NN realism-risks; CLM-NN coverage-check claims; both append-only), `**Verdict vocabulary:**` (register: LIVE / DORMANT / MITIGATED / DISCLOSED — post-Phase-5: 24 DISCLOSED + 0 LIVE; coverage-check: BACKED / WEAK / UNBACKED / OUT-OF-SCOPE).
- Consolidated `**Abbreviations on first use**` block listing the abbreviations introduced in the document (CIP, CPS, ODD, RSK, CLM, EXP, BCa, IQR, RB, EB, EIP-1559, SHA-256, AFT, CCS, SODA, MEV, URL, YAML, SPO, repo).
- Reading-guide paragraph: the summary is hybrid — paste-target table at top, per-CIP-section recommendations in the middle, pinned-references at the bottom. Inline-verbatim items are highlighted; reference-only items name their source-of-truth path + line range. The CIP author may edit any inline content; reference-only items should be pasted from their source-of-truth without modification.

**Step B — Paste-target table** (lines 30-60):
A markdown table with five rows (one per CIP section) and columns: `| CIP Section | Source Artefact | Paste Content | Inline / Reference |`. Per D-44:
- **Methodology** → `docs/phase-2/methodology-overview.md` → "Full document by repo URL (Overview, Design concepts, Details (ODD) index + per-element prose + worked example)" → Reference
- **Calibration** → `docs/phase-2/cardano-realism-audit.md` → "Seventeen `(value, source, date-retrieved)` triples in §\"Topology and actor model\", §\"Pricing-controller calibration\", §\"What lines up with mainnet\", §\"RB cadence\"" → Reference (CIP names specific values; cites the audit by repo URL)
- **Trust matrix** → `docs/phase-2/validity-threats.md` → "Aggregate trust matrix: 2 HIGH + 13 MEDIUM + 4 LOW + 0 UNRESOLVED across 19 per-suite blocks; per-suite Related-RSK + Related-CLM cross-references" → Reference
- **Evidence** → `docs/phase-2/coverage-check.md` → "Headline CIP claims backed by specific CLM-NN rows (see §\"Headline CIP claim list\" below for the 4-8 row-to-claim mapping); supporting CLM rows referenced by identifier + table-row line range" → Mixed (4-8 headline claims inline; long-tail CLM rows reference-only)
- **Limitations** → `docs/phase-2/realism-risks-register.md` → "Twenty-four DISCLOSED `disclosure-paragraph` blocks pastable verbatim; substrate-scope umbrella + top-3 to top-5 Limitations paragraphs inlined below in §\"Limitations paste order\"; long-tail paragraphs referenced by RSK-NN + path + line range" → Mixed

**Step C — Per-CIP-section recommendations** (lines 60-180):
Five subsections, one per CIP section. Each follows a fixed pattern:
1. Sub-heading: `## CIP Section: <Name>`.
2. Source-of-truth pointer (the artefact + path).
3. Paste order narrative.
4. Inline tier-1 content list (if any).
5. Reference tier-2 content list (RSK-NN / CLM-NN identifier + path + approximate line range).

**§"CIP Section: Methodology"**: name `docs/phase-2/methodology-overview.md` as the primary URL-by-reference target. The methodology overview is itself a one-page ODD index; the CIP cites it by repo URL rather than pasting content. Reference-only.

**§"CIP Section: Calibration"**: name `docs/phase-2/cardano-realism-audit.md`'s 17 triples in `(value, source, date-retrieved)` format. The CIP cites specific calibration values by triple but does not paste the full audit; it links to the audit by repo URL. Reference-only.

**§"CIP Section: Trust matrix"**: name `docs/phase-2/validity-threats.md`'s aggregate trust 2 HIGH + 13 MEDIUM + 4 LOW + 0 UNRESOLVED summary; the CIP cites this aggregate plus the per-suite breakdown by suite name. Reference-only.

**§"CIP Section: Evidence — Headline CIP claim list"**: inline 4-8 headline claims per D-46. Use the exact numerical findings from `.planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md` §"Headline numerical findings for Plan 04-04 (audit refresh)". For each headline claim, format as:

> **Headline Claim N:** "<one-sentence claim>"
> **Backed by:**
> - CLM-NN (`<menu-option>`): BCa 95% CI = `[<lo>, <hi>]`; median Δ = `<med>`; sign-coherence = `<coh>`; distinct-hash = N/N; backing-job = `<job-slug>` in `<suite-yaml-path>`
> - (additional CLM rows as applicable)
> **Source-of-truth:** `.planning/realism-tests/<test-name>/results.md` + `docs/phase-2/coverage-check.md` row reference

The four required headline claims per D-46:
1. Un-reserved menu arms outperform single-lane EIP-1559 (CLM-07 + CLM-09; control via CLM-05).
2. RB-reserved menu arms underperform single-lane EIP-1559 under same calibration (CLM-06 + CLM-08).
3. multiplier_floor=4 calibration is regime-dependent at multiplier_floor=16 (cite the relevant CLM row from coverage-check.md for TEST-07a; if no canonical CLM row exists, cite the TEST-07a results.md path + `RSK-multiplier-floor-4-suite-coverage` disclosure-paragraph as the load-bearing source).
4. partitioned ≡ RB-reserved welfare at sundaeswap_moderate × multiplier_floor=4 replicates at N=20 (cross-arm duplicate-job artefact; cite CLM-06 + CLM-08 + the cross-cell observation in TEST-04 results).

Optional headline claims 5-8 (planner picks up to 4 more): sign-flip cells (CLM-10..13), reorg-safety (CLM-19..23), calibration anchors (CLM-24..27 + CLM-05 window-length-32 anchor), hash-diversity gate (cites the RSK-hash-diversity-policy disclosure-paragraph + 17/17 result). Include them if they meet the bar "load-bearing for the CIP's menu-option recommendation"; skip them if they would dilute the headline list below the 8-claim cap.

**§"CIP Section: Limitations — Limitations paste order"**: inline the top-3 to top-5 Limitations disclosure-paragraphs verbatim per D-45 + the substrate-scope umbrella. Use the Claude's Discretion default paste order from CONTEXT.md `<specifics>` (substrate-scope first, then category-grouped). Paste verbatim:
- `RSK-substrate-scope` disclosure-paragraph (the substrate-scope umbrella; load-bearing).
- The next 3-4 highest-priority entries by paste order. Recommended: `RSK-cross-arch-determinism` + `RSK-leios-spec-pre-deployment` (substrate-scope corollaries) + `RSK-un-anchored-controller-knobs` (controller-knob anchor-or-disclose, with 1 ANCHORED + 3 DISCLOSED sub-knob granularity). The planner may select different top-5 entries if a different reading flow serves the CIP better — document the choice in this section's framing prose.

After the inline top-5: a reference-only table listing the remaining 19 RSK-NN entries by identifier + path + approximate line range. Format:

> | RSK-NN | Title | Source-of-truth |
> |--------|-------|-----------------|
> | RSK-pool-count | Pool-count sensitivity above 100 pools | `docs/phase-2/realism-risks-register.md` lines 47-62 |
> | ... | ... | ... |

**Step D — Pinned references block** (lines 180-240):
A clearly-marked section at the end of the document containing:

1. **Citable git tag:** `phase-2-cip-evidence-v1`. State that this tag is the canonical citable reference for the CIP. If the tag has not yet been applied at Plan-05-03 close (the don't-auto-commit memory means Claude does not run `git tag`), use the placeholder text: `(tag pending: to be applied by user via `git tag -a phase-2-cip-evidence-v1 -m '<see message draft below>'` against commit `<current HEAD SHA at Phase 5 close; resolve via 'git rev-parse HEAD'>`)`.

2. **Milestone-close commit SHA:** resolve via `git rev-parse HEAD` at the moment Plan 05-03 Task 1 runs. Annotation: "Commit `<full-40-char-SHA>` is the post-Plan-05-03 commit (after `docs/phase-2/cip-author-summary.md` was committed by the user)."

3. **Epoch-582 stake snapshot reference:** "Cardano mainnet, epoch 582, retrieved 2026-05-14, per `docs/phase-2/cardano-realism-audit.md` §\"Topology and actor model\" + `.planning/spikes/006-curve-design/README.md`."

4. **Consistency audit reproducibility:** "The consistency of cross-references across the six in-scope documents was audited by `.planning/phases/05-handoff/verify-consistency.sh` and recorded in `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md`. Future CIP peer reviewers may re-run the script independently to verify the consistency claims at the tagged commit or at any later commit."

5. **Tag message draft (annotated tag):** verbatim ~12-15 lines per CONTEXT.md `<specifics>`:

   ```
   phase-2-cip-evidence-v1 — milestone-close tag

   Citable reference for the Cardano Improvement Proposal (CIP)
   responding to CPS-0023 ("Urgency Signaling").

   CIP-cited artefacts:
   - docs/phase-2/cardano-realism-audit.md
   - docs/phase-2/validity-threats.md
   - docs/phase-2/realism-risks-register.md (24 DISCLOSED + 0 LIVE)
   - docs/phase-2/coverage-check.md
   - docs/phase-2/methodology-overview.md
   - docs/phase-2/cip-author-summary.md (paste guide)

   Topology snapshot: Cardano mainnet, epoch 582, retrieved 2026-05-14.
   Consistency audit: .planning/phases/05-handoff/05-CONSISTENCY-REPORT.md
   ```

6. **HAND-03 execution note:** explicit text marking the tag as user-executed, not Claude-executed: "Per the project's don't-auto-commit convention, Plan 05-03 draft-only the tag message; the user runs `git tag -a phase-2-cip-evidence-v1 -m '<paste the message draft above>'` after Phase 5 close and pushes the tag with `git push origin phase-2-cip-evidence-v1` if a remote citable reference is required."

**Step E — Closing footer** (lines 240-260):
- One paragraph: "Plan 05-03 closes Phase 5 — the milestone-close phase. The Cardano Improvement Proposal (CIP) author may now copy from this summary into the CIP draft; the underlying source-of-truth artefacts (cardano-realism-audit.md, validity-threats.md, realism-risks-register.md, coverage-check.md, methodology-overview.md) are stable at the post-`phase-2-cip-evidence-v1` tag. Subsequent edits to the evidence base require a new tagged version (e.g. `phase-2-cip-evidence-v2`) and a corresponding re-run of `.planning/phases/05-handoff/verify-consistency.sh`."
- Cross-reference: "See `.planning/phases/05-handoff/05-SUMMARY.md` for the Phase 5 SUMMARY consumed by `gsd-verify-phase`."

**Length discipline:** Target 300-450 lines per CONTEXT.md D-44. If the document exceeds 500 lines, compress reference-only tables; if below 300, expand the per-CIP-section recommendations.

**Abbreviation-on-first-use discipline:** The consolidated header expansion block (Step A) covers most abbreviations. Within each inlined disclosure-paragraph (Step C §"Limitations" inline content), preserve the source paragraph's own first-use expansions — those are pasted verbatim from the register and the register's disclosure-paragraphs are already CIP-pasteable per CLAUDE.md.
  </action>
  <verify>
    <automated>bash -c "set -e; cd /home/will/git/arc-tiered-pricing; test -f docs/phase-2/cip-author-summary.md && echo 'PASS: file exists'; line_count=$(wc -l < docs/phase-2/cip-author-summary.md); test $line_count -ge 250 && test $line_count -le 550 && echo \"PASS: file is $line_count lines (target 300-450; tolerance 250-550)\" || echo \"WARN: file is $line_count lines outside 250-550 tolerance\"; for kw in 'Paste-target table' 'CIP Section: Methodology' 'CIP Section: Calibration' 'CIP Section: Trust matrix' 'CIP Section: Evidence' 'CIP Section: Limitations' 'Headline' 'Pinned references' 'phase-2-cip-evidence-v1' 'epoch 582' '2026-05-14' 'verify-consistency.sh' '05-CONSISTENCY-REPORT.md' 'Tag message draft' 'CPS-0023'; do grep -qF \"$kw\" docs/phase-2/cip-author-summary.md && echo \"PASS: keyword '$kw' present\" || { echo \"FAIL: keyword '$kw' missing\"; exit 1; }; done; headline_count=$(grep -c '^>\\? *\\*\\*Headline Claim' docs/phase-2/cip-author-summary.md || grep -c '^### Headline Claim' docs/phase-2/cip-author-summary.md || true); test $headline_count -ge 4 && echo \"PASS: $headline_count headline claims (>= 4)\" || echo \"WARN: only $headline_count headline claims found (expected >=4)\"; grep -qE 'CLM-0[6-9]' docs/phase-2/cip-author-summary.md && echo 'PASS: CLM-06..09 cited (the TEST-04 N=20 canonical menu-item rows)'; grep -qF 'RSK-substrate-scope' docs/phase-2/cip-author-summary.md && echo 'PASS: substrate-scope umbrella referenced'; grep -qF '24 DISCLOSED' docs/phase-2/cip-author-summary.md && echo 'PASS: post-Phase-5 register distribution (24 DISCLOSED) recorded'"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/phase-2/cip-author-summary.md` exists.
    - Line count is in 250-550 (target 300-450 per CONTEXT.md D-44).
    - Document opens with H1 title naming "Cardano Improvement Proposal (CIP) Author Summary" + Status + consolidated abbreviation block.
    - Top-of-document paste-target table has five rows (one per CIP section: Methodology, Calibration, Trust matrix, Evidence, Limitations).
    - Middle of document has five `## CIP Section: <Name>` subsections.
    - The §"Evidence — Headline CIP claim list" subsection contains at least 4 headline claims (4-8 per D-46), each naming its backing CLM-NN row(s) with BCa CI numerics.
    - The §"Limitations — Limitations paste order" subsection inlines at least the substrate-scope umbrella + 3-4 other top-priority disclosure-paragraphs verbatim from `realism-risks-register.md`.
    - Bottom-of-document pinned-references block names: (a) the tag `phase-2-cip-evidence-v1`, (b) the milestone-close commit SHA (or pending-placeholder), (c) the epoch-582 mainnet stake snapshot reference dated 2026-05-14, (d) the `verify-consistency.sh` path + `05-CONSISTENCY-REPORT.md` path.
    - Tag message draft (~12-15 lines) is embedded in the pinned-references block.
    - HAND-03 execution note explicitly states the tag is user-executed.
    - Abbreviations expand on first use per CLAUDE.md §"Conventions / gotchas".
  </acceptance_criteria>
  <done>The hybrid-shape CIP-author paste guide is on disk with all five CIP-section subsections, the headline-claim list, inline top-5 Limitations paragraphs, and the pinned-references block including the tag message draft.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run verify-consistency.sh against the six-document corpus (now including cip-author-summary.md) and append post-Plan-05-03 verification block to 05-CONSISTENCY-REPORT.md</name>
  <files>.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md</files>
  <read_first>
    - .planning/phases/05-handoff/verify-consistency.sh (the script Plan 05-02 Task 1 created)
    - .planning/phases/05-handoff/05-CONSISTENCY-REPORT.md (Plan 05-02 Task 2's initial state; append-mode edit)
    - docs/phase-2/cip-author-summary.md (Plan 05-03 Task 1's output; now in the six-document in-scope corpus with live RSK-NN / CLM-NN references)
  </read_first>
  <action>
Re-run the consistency-verification script with the cip-author-summary.md now present on disk:

```
bash .planning/phases/05-handoff/verify-consistency.sh > /tmp/verify-post-summary.txt 2>&1
```

Capture the exit code and the full stdout. Then append a new section to `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md` titled `## Post-Plan-05-03 verification` (or insert as a final section before the closing footer if a footer is present). The new section follows the same four-check structure as the Plan-05-02 sections but covers the post-cip-author-summary.md corpus:

- `### Re-run motivation`: one paragraph stating that Plan 05-03 created `docs/phase-2/cip-author-summary.md` (the sixth in-scope document) and that the script re-run verifies the new file's cross-references resolve to canonical identifiers + the seven goldens-pinned suite YAMLs + the existing markdown link targets.
- `### Re-run command`: the exact invocation: `bash .planning/phases/05-handoff/verify-consistency.sh`. Note the script's output is reproducible at the current commit by any reviewer.
- `### Re-run results`: four sub-sections (one per check) recording the post-summary numbers. Each sub-section has the same structured table as the corresponding Plan-05-02 section but with the cip-author-summary.md row populated with live numbers instead of `n/a (pending Plan 05-03)`.
- `### Post-Plan-05-03 outcome`: PASS / FAIL line matching the script's exit code.

If the script reports any defects in `cip-author-summary.md` (dead RSK-NN / CLM-NN / EXP-NN references; broken markdown links; etc.), fix them in place in `cip-author-summary.md` (re-load Task 1's file to edit) and re-run the script. Iterate until PASS or until a defect is outside Plan-05-03 deviation-rule scope; in the latter case, list the defect under `## Open for user review` in the report.

**Update the report's opening Status block** (Plan-05-02 Task 2 wrote this initially; now refine):
- `**Outcome:**`: update from the Plan-05-02 outcome to the final post-Plan-05-03 outcome (PASS or FAIL).
- Add a new line: `**Final verification (post-Plan-05-03):**` recording the script's exit code and the date.

**Length discipline:** the Post-Plan-05-03 verification section adds ~30-50 lines. Total report length lands at ~150-230 lines (Plan-05-02 baseline + Plan-05-03 appendix). Within tolerance per CONTEXT.md verbosity default.
  </action>
  <verify>
    <automated>bash -c "set -e; cd /home/will/git/arc-tiered-pricing; grep -qF 'Post-Plan-05-03 verification' .planning/phases/05-handoff/05-CONSISTENCY-REPORT.md && echo 'PASS: post-Plan-05-03 verification section present'; grep -qF 'cip-author-summary.md' .planning/phases/05-handoff/05-CONSISTENCY-REPORT.md && echo 'PASS: cip-author-summary.md referenced in report'; grep -qE 'Outcome:\\*?\\*?\\s*PASS|Outcome:\\*?\\*?\\s*FAIL' .planning/phases/05-handoff/05-CONSISTENCY-REPORT.md && echo 'PASS: outcome line present'; bash .planning/phases/05-handoff/verify-consistency.sh > /tmp/final-verify.txt 2>&1; final_exit=$?; if [ $final_exit -eq 0 ]; then echo 'PASS: verify-consistency.sh final run exits 0'; else echo \"WARN: verify-consistency.sh exits $final_exit; check report Open-for-user-review section\"; fi; line_count=$(wc -l < .planning/phases/05-handoff/05-CONSISTENCY-REPORT.md); test $line_count -ge 150 && test $line_count -le 280 && echo \"PASS: report is $line_count lines (target 150-230; tolerance 150-280)\" || echo \"WARN: report is $line_count lines outside 150-280 tolerance\""</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md` contains a `## Post-Plan-05-03 verification` (or similarly-named) section appended after the Plan-05-02 sections.
    - The new section references `docs/phase-2/cip-author-summary.md` as the file Plan 05-03 added to the in-scope corpus.
    - The new section's four-check sub-tables show populated numbers for the cip-author-summary.md row (RSK/CLM/EXP refs, markdown link counts).
    - The new section's `### Post-Plan-05-03 outcome` line is PASS if `bash .planning/phases/05-handoff/verify-consistency.sh` exits 0, FAIL otherwise.
    - The report's opening Status block is updated with a `**Final verification (post-Plan-05-03):**` line.
    - If any defect was found in `docs/phase-2/cip-author-summary.md` during the re-run, it was fixed in place (within Plan-05-03 deviation-rule scope) or flagged as "open for user review".
    - Total report length is in 150-280 lines (target 150-230).
  </acceptance_criteria>
  <done>The consistency report is finalised with a Post-Plan-05-03 verification block; the verification script runs clean (or any residual defects are flagged for user review).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User applies the annotated git tag `phase-2-cip-evidence-v1`</name>
  <what-built>
    Plan 05-03 Tasks 1 and 2 completed: `docs/phase-2/cip-author-summary.md` is on disk (300-500 lines, hybrid shape per D-44, with the tag-message draft embedded in the pinned-references block); `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md` carries the Post-Plan-05-03 verification block (final outcome PASS or open-for-user-review).
    Per the don't-auto-commit user-memory + CONTEXT.md HAND-03 Claude's-Discretion, the tag itself is **user-executed** — Claude drafts the tag message but does not run `git tag`.
  </what-built>
  <how-to-verify>
    1. Inspect `docs/phase-2/cip-author-summary.md` §"Pinned references" §"Tag message draft" and copy the annotated tag-message block (~12-15 lines).
    2. Run, from the repo root:
       ```
       git tag -a phase-2-cip-evidence-v1 -m "$(cat <<'EOF'
       <paste the message draft block from §"Tag message draft" verbatim>
       EOF
       )"
       ```
    3. Confirm the tag landed:
       ```
       git tag --list 'phase-2-cip-evidence-v1' && git show phase-2-cip-evidence-v1 | head -20
       ```
    4. (Optional) If a remote citable reference is required:
       ```
       git push origin phase-2-cip-evidence-v1
       ```
    5. Edit `docs/phase-2/cip-author-summary.md` §"Pinned references" to replace the `(tag pending: ...)` placeholder text with the live tag-applied-at-commit annotation, e.g. `Tag applied: phase-2-cip-evidence-v1 at commit <full-SHA>`.
    6. Re-run the consistency-verification script once more after the cip-author-summary.md edit to confirm the placeholder swap did not introduce any dead references:
       ```
       bash .planning/phases/05-handoff/verify-consistency.sh
       ```
       Expected: exit code 0; no dead refs.
    7. Append a final line to `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md` §"Post-Plan-05-03 verification" recording the tag-application date and the post-tag commit SHA.
  </how-to-verify>
  <resume-signal>Type "tagged" (with the tag SHA optionally included) or describe any issues encountered during tag application.</resume-signal>
</task>

</tasks>

<verification>
After all three tasks:
- `docs/phase-2/cip-author-summary.md` exists with hybrid shape per D-44 and 300-500 lines.
- `.planning/phases/05-handoff/05-CONSISTENCY-REPORT.md` carries a Post-Plan-05-03 verification block.
- `bash .planning/phases/05-handoff/verify-consistency.sh` exits 0 (or any residual defect is recorded in the report's Open-for-user-review section).
- The git tag `phase-2-cip-evidence-v1` is applied by the user (Task 3 checkpoint).
- The cip-author-summary.md §"Pinned references" no longer carries the `(tag pending: ...)` placeholder after the user runs Task 3.
</verification>

<success_criteria>
1. `docs/phase-2/cip-author-summary.md` exists as the single CIP-author paste guide with hybrid shape (paste-target table → per-CIP-section recommendations → pinned-references block) per D-44.
2. The summary's headline CIP claim list contains 4-8 claims per D-46, each backed by specific CLM-NN row(s) with BCa CI numerics.
3. The summary's Limitations paste-order subsection inlines the substrate-scope umbrella + top-3 to top-5 Limitations disclosure-paragraphs verbatim per D-45.
4. The summary's pinned-references block names the tag, commit SHA, epoch-582 snapshot, verification script + report, and embeds the tag-message draft.
5. The consistency report's Post-Plan-05-03 verification block records the final four-check outcome (PASS or open-for-user-review).
6. The git tag `phase-2-cip-evidence-v1` is applied by the user at Task 3's checkpoint (or flagged as pending in the cip-author-summary's pinned-references block if the user defers).
7. All cross-references in the summary resolve to canonical RSK-NN / CLM-NN identifiers and to existing file paths on disk.
</success_criteria>

<output>
After completion, create `.planning/phases/05-handoff/05-03-SUMMARY.md` per the GSD summary template. Then create `.planning/phases/05-handoff/05-SUMMARY.md` as the Phase 5 SUMMARY consumed by `gsd-verify-phase` (the standard Phase-N SUMMARY pattern, summarising plans 05-01 / 05-02 / 05-03 and stating Phase 5 success criteria PASS / FAIL against the three ROADMAP success criteria for HAND-01 / HAND-02 / HAND-03).
</output>
