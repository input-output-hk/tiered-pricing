---
phase: 01-viz-site-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sim-rs/scripts/viz/tests/__init__.py
  - sim-rs/scripts/viz/tests/fixtures/mini-suite/manifest.json
  - sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/1/run_summary.json
  - sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/1/time_series.csv
  - sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/2/run_summary.json
  - sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/2/time_series.csv
  - sim-rs/scripts/viz/tests/fixtures/malformed-suite/manifest.json
  - sim-rs/scripts/viz/tests/fixtures/no-time-series/manifest.json
  - sim-rs/scripts/viz/tests/fixtures/no-time-series/job_a/1/run_summary.json
  - sim-rs/scripts/viz/tests/test_ingest.py
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04, VIZ-05]
must_haves:
  truths:
    - "Synthetic fixtures exist that exercise the kebab-case manifest schema and the snake_case run_summary schema in the same test."
    - "A test stub asserts that `latency_blocks_observations` (a list per component) is reduced to `latency_blocks_mean` (a scalar) by the ingest layer."
    - "A malformed manifest fixture exists so the skip-and-warn behavior (D-21) is testable."
    - "A no-time-series fixture exists so the missing-CSV soft-failure path (Pitfall 8) is testable."
    - "A duplicate-suite-name fixture would be unnecessary because fixtures live under one root; the suite-identifier-from-path landmine is exercised by a path-derived id assertion."
  artifacts:
    - path: sim-rs/scripts/viz/tests/__init__.py
      provides: Python package marker
    - path: sim-rs/scripts/viz/tests/fixtures/mini-suite/manifest.json
      provides: Minimal valid suite manifest with kebab-case keys and 1 job × 2 seeds
      contains: "suite-name"
    - path: sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/1/run_summary.json
      provides: Minimal valid run_summary with snake_case keys including `latency_blocks_observations`
      contains: "latency_blocks_observations"
    - path: sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/1/time_series.csv
      provides: 15-column header + a handful of integer rows
      contains: "slot,c_priority,c_standard,util_priority_window_x_1e9"
    - path: sim-rs/scripts/viz/tests/fixtures/malformed-suite/manifest.json
      provides: Truncated/invalid JSON to trigger skip-and-warn path
    - path: sim-rs/scripts/viz/tests/fixtures/no-time-series/job_a/1/run_summary.json
      provides: A seed dir with `run_summary.json` present but `time_series.csv` absent
    - path: sim-rs/scripts/viz/tests/test_ingest.py
      provides: Failing test scaffolds for every Wave 0 test case in RESEARCH.md Phase Requirements → Test Map
      contains: "class IndexBuildTest"
  key_links:
    - from: sim-rs/scripts/viz/tests/test_ingest.py
      to: sim-rs/scripts/viz/build.py
      via: "import build" with sys.path insertion
      pattern: "sys.path.insert"
---

<objective>
Wave 0 test harness: synthetic fixtures and failing test scaffolds that downstream waves implement against.

Purpose: Pin the per-task verification map (VALIDATION.md) before any business code lands. The fixtures encode the kebab-vs-snake casing landmine, the latency-list-to-mean landmine, and the skip-and-warn failure paths so the ingest plan in Wave 1 cannot regress them silently.

Output: A `sim-rs/scripts/viz/tests/` directory with `__init__.py`, three fixture trees, and `test_ingest.py` populated with failing tests that match the Phase Requirements → Test Map from RESEARCH.md `## Validation Architecture`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/viz-website/PROJECT.md
@.planning/workstreams/viz-website/ROADMAP.md
@.planning/workstreams/viz-website/REQUIREMENTS.md
@.planning/workstreams/viz-website/STATE.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-CONTEXT.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-VALIDATION.md

<interfaces>
<!-- Verified schemas (from RESEARCH.md `## Verified Schemas` and PATTERNS.md). The fixtures MUST follow these exact key namings. -->

manifest.json (kebab-case — RESEARCH.md Verified Schemas):
{
  "suite-name": String,
  "started-at-utc": "YYYY-MM-DDTHH:MM:SSZ",
  "jobs": {
    "<job_name>": {
      "<seed_string>": {
        "status": "completed" | "running" | "pending" | "failed",
        "started-at-utc": optional,
        "completed-at-utc": optional,
        "output-path": optional
      }
    }
  }
}

run_summary.json (snake_case — RESEARCH.md Verified Schemas):
{
  "components": [{
    "component_index": u32,
    "txs_submitted": u64,
    "txs_included": u64,
    "txs_evicted_quote_drift": u64,
    "bytes_included": u64,
    "fees_paid_lovelace": u64,
    "refund_lovelace": u64,
    "retained_value_total": f64,
    "net_utility_total": f64,
    "included_value_lovelace_total": number,
    "priority_included": u64,
    "standard_included": u64,
    "latency_blocks_observations": [f64]   // LIST, not scalar
  }],
  "total_txs_submitted": u64,
  "total_txs_included": u64,
  "total_txs_evicted_quote_drift": u64,
  "total_fees_paid_lovelace": u64,
  "total_refund_lovelace": u64,
  "priority_retained_value_total": f64,
  "standard_retained_value_total": f64,
  "priority_included_value_total": number,
  "standard_included_value_total": number,
  "block_generation_probability": f64,
  "multiplier_floor_breaches": u64,
  "min_priority_over_standard_ratio": f64,
  "max_priority_over_standard_ratio": f64,
  "pricing_ticks": u64,
  "pricing_event_stream_sha256": String
}

time_series.csv header (pinned in `sim-rs/sim-cli/src/metrics/time_series.rs` lines 16-20):
slot,c_priority,c_standard,util_priority_window_x_1e9,util_standard_window_x_1e9,mempool_bytes_total,mempool_bytes_priority,mempool_bytes_standard,included_bytes_priority,included_bytes_standard,included_count_priority,included_count_standard,evicted_quote_drift_count,fees_paid_lovelace,refund_lovelace

All columns are u64; no quoting; trailing newline. Plain `csv.DictReader(newline="")` reads correctly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three fixture trees with verified-schema files</name>
  <files>
    sim-rs/scripts/viz/tests/__init__.py,
    sim-rs/scripts/viz/tests/fixtures/mini-suite/manifest.json,
    sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/1/run_summary.json,
    sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/1/time_series.csv,
    sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/2/run_summary.json,
    sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/2/time_series.csv,
    sim-rs/scripts/viz/tests/fixtures/malformed-suite/manifest.json,
    sim-rs/scripts/viz/tests/fixtures/no-time-series/manifest.json,
    sim-rs/scripts/viz/tests/fixtures/no-time-series/job_a/1/run_summary.json
  </files>
  <read_first>
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md (Verified Schemas section — exact key names),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (Pattern D for kebab-vs-snake landmine, Pattern E for latency-list landmine),
    sim-rs/output/phase-2/eip1559-robustness-20260514-160045/manifest.json (one real example to confirm exact key spellings),
    sim-rs/sim-cli/src/metrics/time_series.rs (lines 16-20 — the pinned CSV header)
  </read_first>
  <action>
    Create the empty Python package marker `tests/__init__.py`. Author three fixture trees that downstream tests exercise:

    (a) `fixtures/mini-suite/` — a valid suite with kebab-case `manifest.json` (`suite-name`, `started-at-utc`, `jobs[job][seed]` → `{status: "completed", started-at-utc, completed-at-utc, output-path}`), one job `d8_target0.5_window32`, two seeds `1` and `2`. Each seed dir holds a snake_case `run_summary.json` (every top-level field from the verified schema, with `components` as a length-2 list — one priority-dominant, one standard-dominant — each carrying a `latency_blocks_observations` list of 5-10 distinct floats so the mean computation is non-trivial) and a `time_series.csv` with the pinned 15-column header and 3-5 integer rows. Use different numeric values across seeds 1 and 2 so cross-seed overlay tests can assert non-collapsed lines.

    (b) `fixtures/malformed-suite/manifest.json` — a truncated/invalid-JSON file (e.g. just the opening brace `{`) so `json.JSONDecodeError` is the failure mode the ingest layer must handle per D-21.

    (c) `fixtures/no-time-series/` — a valid `manifest.json` plus `job_a/1/run_summary.json`, but NO `time_series.csv` file. Exercises Pitfall 8 (phase-3 suites without time-series) and the missing-CSV soft-failure path.

    All fixture sizes must stay under ~5 KB total to keep the test suite fast and the repo tree tidy. All `manifest.json` keys are kebab-case; all `run_summary.json` keys are snake_case; do not paraphrase column names in `time_series.csv` (copy them verbatim from `time_series.rs`).

    These fixtures are checked-in data files, not Python code, so the abbreviation-expansion rule from CLAUDE.md does not apply to JSON/CSV contents.
  </action>
  <verify>
    <automated>python3 -c "import json; from pathlib import Path; m = json.loads(Path('sim-rs/scripts/viz/tests/fixtures/mini-suite/manifest.json').read_text()); assert 'suite-name' in m and 'started-at-utc' in m and 'jobs' in m; rs = json.loads(Path('sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/1/run_summary.json').read_text()); assert isinstance(rs['components'][0]['latency_blocks_observations'], list); assert 'priority_retained_value_total' in rs; header = Path('sim-rs/scripts/viz/tests/fixtures/mini-suite/d8_target0.5_window32/1/time_series.csv').read_text().splitlines()[0]; assert header == 'slot,c_priority,c_standard,util_priority_window_x_1e9,util_standard_window_x_1e9,mempool_bytes_total,mempool_bytes_priority,mempool_bytes_standard,included_bytes_priority,included_bytes_standard,included_count_priority,included_count_standard,evicted_quote_drift_count,fees_paid_lovelace,refund_lovelace', f'header mismatch: {header}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `sim-rs/scripts/viz/tests/__init__.py` exists (may be empty).
    - `mini-suite/manifest.json` parses as JSON and contains the keys `suite-name`, `started-at-utc`, `jobs`; `jobs` has the key `d8_target0.5_window32` whose value has keys `"1"` and `"2"`.
    - `mini-suite/d8_target0.5_window32/1/run_summary.json` parses as JSON and contains `components` (list), `priority_retained_value_total`, `standard_retained_value_total`, `total_txs_submitted`. Each component has `latency_blocks_observations` as a list of >= 2 float values.
    - `mini-suite/d8_target0.5_window32/1/time_series.csv` has the exact 15-column header from `time_series.rs` lines 16-20 and >= 3 data rows whose values parse as integers via `int(row[col])` for every column.
    - `mini-suite/d8_target0.5_window32/2/` has the same two files with different numeric values from seed 1 (overlay tests need distinct lines).
    - `malformed-suite/manifest.json` exists and raises `json.JSONDecodeError` when read with `json.loads`.
    - `no-time-series/job_a/1/run_summary.json` exists; `no-time-series/job_a/1/time_series.csv` does NOT exist.
    - `find sim-rs/scripts/viz/tests/fixtures -type f | wc -l` is between 7 and 12 (exact count depends on fixture choices but should not balloon).
  </acceptance_criteria>
  <done>
    Three fixture trees on disk; the verification command above prints `OK` and exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write failing test scaffolds in `test_ingest.py`</name>
  <files>sim-rs/scripts/viz/tests/test_ingest.py</files>
  <read_first>
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md (`## Validation Architecture` Phase Requirements → Test Map),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (Pattern at `## Code Examples / Walk for manifests`),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <action>
    Author `sim-rs/scripts/viz/tests/test_ingest.py` using stdlib `unittest`. The file imports the not-yet-existent `build` module via `sys.path.insert(0, str(Path(__file__).resolve().parent.parent))` followed by `import build`. Tests are expected to fail at import (or skipped) until Plan 01-02 lands the module; this is intentional Wave 0 scaffolding.

    Implement exactly the test classes and methods listed in RESEARCH.md Phase Requirements → Test Map and PATTERNS.md `### Test cases the plan must cover`:

    - `IndexBuildTest::test_index_lists_all_manifests` — call `build.run_build(source=<fixtures/mini-suite parent>, output=<tmp>, includes=[], excludes=[], warnings=[])`, then `json.loads((tmp / "data" / "index.json").read_text())` and assert `len(result["suites"]) >= 1`.

    - `SuiteJsonTest::test_jobs_match_manifest` — after build, the per-suite JSON at `data/<suite_id>.json` lists exactly the same `(job, seed)` pairs as the source manifest.

    - `SuiteJsonTest::test_seed_grouping_present` — the per-suite JSON exposes a `jobs[job_name].seeds[seed].headline` map so the browser can build the cross-seed overlay without re-walking.

    - `SeedJsonTest::test_headline_fields_present` — the per-(job, seed) JSON contains every locked headline metric: `retained_value`, `net_utility`, `retained_value_ratio`, `latency_blocks_mean` (per component), and `peak_mempool_bytes`.

    - `SeedJsonTest::test_time_series_long_form` — the per-(job, seed) JSON's `time_series` field is a list of `{slot: int, lane: str, metric: str, value: int}` records (NOT wide CSV-shaped). Assert presence of at least the lanes `"priority"`, `"standard"`, `"total"` and the metrics `"quote_per_byte"`, `"mempool_bytes"`.

    - `IngestTest::test_kebab_case_manifest_snake_case_run_summary` — explicitly read the fixture manifest with `m["suite-name"]` (kebab) and the run_summary with `rs["priority_retained_value_total"]` (snake) and assert both succeed. This codifies Pitfall 1.

    - `IngestTest::test_latency_blocks_observations_aggregated_to_mean` — assert that the per-(job, seed) output's per-component `latency_blocks_mean` equals `sum(observations) / len(observations)` computed from the fixture (Pitfall 5).

    - `IngestTest::test_missing_time_series_csv_returns_empty_list_with_warning` — run build against `fixtures/no-time-series/`; assert the per-(job, seed) JSON has `time_series == []` and `peak_mempool_bytes is None`, AND that a warning string mentioning "time_series.csv" is in the warnings list (Pitfall 8).

    - `IngestTest::test_phase_2_has_no_priority_only_fast_path_csv` — assert that for a fixture suite directory with no `*.csv` at the suite root, the suite JSON's `aggregates` field is either absent or an empty list / null. This codifies CRITICAL LANDMINE #2 (the file does not exist in phase-2; the build must NOT pretend it does). Reference comment: "Pitfall 3 / VIZ-05 — per-suite aggregate CSV does not exist in phase-2 suite tree; render only when present."

    - `ErrorHandlingTest::test_malformed_manifest_skipped_with_warning` — run build against `fixtures/malformed-suite/` (and `fixtures/mini-suite/` to confirm valid suite still ingested); assert exit code is normal (no exception escapes), warnings list contains an entry mentioning the malformed suite path, and `index.json` does NOT list the malformed suite.

    - `IngestTest::test_suite_id_derived_from_path_not_suite_name` — codifies Pitfall 2 / D-22: create a transient tmp dir with two subdirs both containing a manifest whose `"suite-name"` field is the same string but whose parent paths differ; assert both appear distinctly in `index.json`, identified by a path-derived id (e.g. containing `__` as the directory separator after sanitisation).

    For each test, scaffold the test body using `tempfile.TemporaryDirectory()` plus `shutil.copytree` from the fixture trees. Where the test asserts a feature the build module has not yet implemented, write the assertion as-if it works; the test will fail at import or assertion until Plan 01-02 lands. Add a module-level `unittest.skipIf` ONLY if the import fails (so the file remains discoverable; `python -m unittest discover` should print N failures, not an import error). Use the standard pattern:

        try:
            import build
            BUILD_AVAILABLE = True
        except ImportError:
            BUILD_AVAILABLE = False

        @unittest.skipUnless(BUILD_AVAILABLE, "build module not yet implemented (Wave 1)")
        class IndexBuildTest(unittest.TestCase): ...

    Include a module docstring noting that Wave 0 scaffolding is expected to be red until Plan 01-02 completes. CLAUDE.md abbreviation rule applies to Python source files: spell "Single-Page Application (SPA)" or "Observable Plot" or "Plot" on first use if mentioned, etc.
  </action>
  <verify>
    <automated>python3 -m unittest discover -s sim-rs/scripts/viz/tests -t sim-rs/scripts/viz -v 2>&1 | grep -E '(test_|Ran|OK|FAIL|ERROR|skipped)' | head -40 && python3 -c "import ast; tree = ast.parse(open('sim-rs/scripts/viz/tests/test_ingest.py').read()); classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]; required = {'IndexBuildTest','SuiteJsonTest','SeedJsonTest','IngestTest','ErrorHandlingTest'}; missing = required - set(classes); assert not missing, f'missing test classes: {missing}'; methods = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]; assert 'test_index_lists_all_manifests' in methods; assert 'test_kebab_case_manifest_snake_case_run_summary' in methods; assert 'test_latency_blocks_observations_aggregated_to_mean' in methods; assert 'test_missing_time_series_csv_returns_empty_list_with_warning' in methods; assert 'test_phase_2_has_no_priority_only_fast_path_csv' in methods; assert 'test_malformed_manifest_skipped_with_warning' in methods; assert 'test_suite_id_derived_from_path_not_suite_name' in methods; print(f'OK — {len(classes)} classes, {len(methods)} test methods')"</automated>
  </verify>
  <acceptance_criteria>
    - File `sim-rs/scripts/viz/tests/test_ingest.py` exists and is syntactically valid Python (AST parse succeeds).
    - All five required test classes are present: `IndexBuildTest`, `SuiteJsonTest`, `SeedJsonTest`, `IngestTest`, `ErrorHandlingTest`.
    - At minimum the 10 test methods listed in the action are defined (names exact).
    - The file imports `build` guarded by try/except so `python -m unittest discover` does not produce an import error; either tests are skipped (Wave 0 state) or they fail with assertion errors (after Wave 1 partial implementation).
    - Module docstring states that Wave 0 scaffolding is red until Plan 01-02 lands the build module.
    - No test uses `subprocess` to call `python sim-rs/scripts/viz/build.py` (that lives in Plan 01-06's serve smoke); these are pure unit tests against the `build` module's functions.
  </acceptance_criteria>
  <done>
    `test_ingest.py` is in place; the AST-validation command above prints `OK — N classes, M test methods` where N >= 5 and M >= 10; `python -m unittest discover` discovers the tests without crashing on import.
  </done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Fixtures discoverable and well-formed
find sim-rs/scripts/viz/tests/fixtures -type f | sort
python3 -c "import json; json.loads(open('sim-rs/scripts/viz/tests/fixtures/mini-suite/manifest.json').read())"

# Tests discover and either skip or fail (not crash on import)
cd sim-rs && python3 -m unittest discover -s scripts/viz/tests -t scripts/viz -v 2>&1 | tail -20
```

Wave 0 is complete when discovery prints test names and the test count matches the AST-extracted methods.
</verification>

<success_criteria>
- Fixtures encode every landmine the ingest layer must respect (kebab-vs-snake, latency-list-to-mean, malformed manifest, missing time-series, no priority_only CSV in phase-2).
- `test_ingest.py` has named, AST-discoverable tests for each VIZ-NN requirement and each documented landmine.
- Running `python -m unittest discover -s sim-rs/scripts/viz/tests` produces a non-zero failure/skip count but no import errors. (Green-bar comes after Plan 01-02.)
</success_criteria>

<output>
After completion, create `.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-01-SUMMARY.md` per the standard summary template, listing the fixture files created, the test method names, and explicitly noting which Pitfall (1-8 from RESEARCH.md) each test codifies.
</output>
