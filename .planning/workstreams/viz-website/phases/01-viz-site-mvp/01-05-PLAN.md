---
phase: 01-viz-site-mvp
plan: 05
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - sim-rs/scripts/viz/static/main.js
autonomous: false
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04, VIZ-05]
must_haves:
  truths:
    - "Home view (`#/`) is the landing page: a sortable table of every suite from `data/index.json` (D-12); default sort is `started_at` descending (D-18); clicking the suite name navigates to `#/suite/<id>`."
    - "Suite view (`#/suite/<id>`) is the drill-down (D-13): manifest summary + sortable per-(job, seed) table with the headline-metric columns (retained_value, net_utility, retained_value_ratio, peak_mempool_bytes, per-component latency means), and — when the user picks a job — an in-suite cross-seed time-series overlay (VIZ-05 / D-15)."
    - "Job view (`#/job/<id>/<job>/<seed>`) is a single scrollable page (D-14): headline-metric strip at top, then three vertically-stacked time-series panes via Observable Plot — controller `quote_per_byte` per lane (VIZ-04), mempool bytes per lane, fees/refunds. No tabs, no side-by-side layout. Each pane uses long-form filtering on `metric`+`lane` (no client-side melt)."
    - "Every chart pane uses `Plot.line(records, {x: 'slot', y: 'value', stroke: 'lane'})` for lane-coloured multi-line series; the cross-seed overlay uses `stroke: 'seed'` (RESEARCH.md `## Code Examples`)."
    - "Latency UI labels read exactly `Latency by demand component (blocks)` — NEVER `latency by lane` (CRITICAL LANDMINE #3 / Pitfall 5). Each component row also displays `priority_included` / `standard_included` counts so the user can see which lane dominates."
    - "When `data.aggregates === null` on the suite view, the aggregates section is omitted entirely (no empty header). The corresponding HTML comment cites Pitfall 3 so a future executor reading DevTools sees why."
    - "Every text injected into the DOM (suite names, job names, seed strings, numbers formatted as text) uses `textContent` only; `innerHTML` does not appear in `main.js`."
  artifacts:
    - path: sim-rs/scripts/viz/static/main.js
      provides: "Full implementations of renderHome, renderSuite, renderJob; helper for table sorting; helper for Plot chart panes; helper for cross-seed overlay; number-formatting helpers."
      contains: "Plot.line"
  key_links:
    - from: sim-rs/scripts/viz/static/main.js::renderJob
      to: window.Plot
      via: "Plot.plot({marks: [Plot.line(...)]}) per chart pane"
      pattern: "Plot\\.plot\\(.*Plot\\.line"
    - from: sim-rs/scripts/viz/static/main.js::renderSuite
      to: data/<suite_id>/<job>-<seed>.json
      via: "fetch when user clicks the per-job overlay button; aggregates multiple seeds into a stroke:'seed' Plot"
      pattern: "fetch.*\\$\\{suiteId\\}/.*\\$\\{job\\}-.*"
---

<objective>
The browser-side render layer: complete implementations of `renderHome`, `renderSuite`, and `renderJob` against the JSON contracts pinned in Plan 01-02, using Observable Plot patterns from RESEARCH.md `## Code Examples`.

Purpose: This is the load-bearing UI plan. After this lands, VIZ-01..VIZ-05 are observably true in the browser. Plan 01-04 already serves the bundle; Plan 01-06 documents the entry-point. This plan turns the stub renderers from Plan 01-03 into real renderings.

Output: A single modified file, `sim-rs/scripts/viz/static/main.js`, with table-sorting, chart-pane rendering, cross-seed overlay, and headline-strip rendering. CSS is intentionally untouched (D-23 minimalism).

Autonomy: One human-verify checkpoint at the end — the visual gate for VIZ-01..VIZ-05. This is the gate that "the same numbers in `metrics_comparison.txt` are rendered in a readable layout" (VIZ-03 acceptance criterion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-CONTEXT.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-VALIDATION.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-02-PLAN.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-03-PLAN.md
@sim-rs/scripts/viz/static/main.js

<interfaces>
<!-- JSON shapes consumed by each view (from Plan 01-02). Keep these as a code comment block at the top of main.js so future executors don't have to chase the schema. -->

// data/index.json:
//   { generated_at, source, suite_count, suites: [
//       {id, name, path, started_at, job_count, seed_count, completed_count, max_concurrent_jobs}
//   ] }

// data/<suite_id>.json:
//   { id, name, path, started_at, manifest, aggregates: null|object, jobs: {
//       <job_name>: { seeds: {
//           <seed>: { status, started_at, completed_at, headline: {
//               retained_value, net_utility, retained_value_ratio, peak_mempool_bytes,
//               components: [{index, latency_blocks_mean, priority_included, standard_included}]
//           } }
//       } }
//   } }

// data/<suite_id>/<job>-<seed>.json:
//   { suite_id, job, seed,
//     retained_value, priority_retained_value, standard_retained_value,
//     net_utility, retained_value_ratio,
//     total_txs_submitted, total_txs_included, total_txs_evicted_quote_drift,
//     total_fees_paid_lovelace, total_refund_lovelace,
//     pricing_event_stream_sha256,
//     components: [...],
//     time_series: [ {slot, lane, metric, value}, ... ],
//     peak_mempool_bytes }

// Long-form metric/lane combinations available in time_series:
//   metric ∈ {quote_per_byte, mempool_bytes, included_bytes, included_count, fees_paid_lovelace, refund_lovelace, evicted_quote_drift_count}
//   lane   ∈ {priority, standard, total}
//   Chart panes filter by (metric, lane-set) — never the whole array.
</interfaces>

<code_excerpts>
<!-- These two excerpts are RESEARCH.md / PATTERNS.md verbatim — copy the shape into renderJob and renderSuite. -->

// VIZ-04 per-pane chart (from RESEARCH.md `## Code Examples / Browser-side Plot rendering for VIZ-04`)
function renderQuoteChart(records, container) {
  const filtered = records.filter(r => r.metric === "quote_per_byte");
  const chart = Plot.plot({
    width: 800, height: 240,
    color: { legend: true },
    x: { label: "slot" },
    y: { label: "controller quote (lovelace/byte)" },
    marks: [
      Plot.ruleY([0]),
      Plot.line(filtered, { x: "slot", y: "value", stroke: "lane" }),
    ],
  });
  container.replaceChildren(chart);
}

// VIZ-05 cross-seed overlay (from RESEARCH.md `## Code Examples / Browser-side cross-seed overlay for VIZ-05`)
function renderCrossSeedOverlay(seedRecords, container) {
  // seedRecords: [{seed, records: [{slot, value}, ...]}, ...]
  const flat = seedRecords.flatMap(({seed, records}) =>
    records.map(r => ({...r, seed: String(seed)})));
  const chart = Plot.plot({
    color: { legend: true, type: "ordinal" },
    marks: [Plot.line(flat, {x: "slot", y: "value", stroke: "seed"})],
  });
  container.replaceChildren(chart);
}
</code_excerpts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flesh out `renderHome` and `renderSuite` with sortable tables and aggregates handling</name>
  <files>sim-rs/scripts/viz/static/main.js</files>
  <read_first>
    sim-rs/scripts/viz/static/main.js (current stub state from Plan 01-03 — the `el()` helper, router, HEADLINE_LATENCY_LABEL constant),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (Shared Patterns / "textContent, never innerHTML"; `### sim-rs/scripts/viz/static/main.js`),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md (Phase Requirements / VIZ-01, VIZ-02, VIZ-05 rows; Open Questions #1 max_concurrent_jobs proxy; Common Pitfalls / Pitfall 3 — aggregates null in phase-2)
  </read_first>
  <action>
    Replace the stub `renderHome` and `renderSuite` in `sim-rs/scripts/viz/static/main.js` with real implementations. Continue to use the `el(...)` helper and `textContent`-only DOM insertions.

    (a) `renderHome()`:
    - GET `data/index.json` and parse.
    - Empty state (D-20): if `suites.length === 0`, render `<p class="muted">No suites found under <source>. Re-run the build after suites land under sim-rs/output/.</p>` using `textContent`. Substitute the actual `data.source` value from the JSON.
    - Title / header: a `<header>` element with `<h1>` containing "phase-2 simulator visualisation" and a `<p class="muted">` showing `<N> suites discovered from <source> at <generated_at>` (textContent only).
    - Table with the columns: `name`, `path`, `started_at` (ISO formatted), `jobs`, `seeds`, `completed`, `max-concurrent-jobs`, `id`. Each `<th>` gets a click handler that re-sorts the rows by that column ascending; second click flips to descending. Highlight the active sort column with a `[class~="sort-active"]` indicator.
    - Initial sort key: `started_at` descending (D-18). Implement sort by mutating an in-memory copy of `suites`, then `tbody.replaceChildren(...newRows)`.
    - Each suite-name cell is an `<a href="#/suite/<id>">`. Use `encodeURIComponent(suite.id)` to be safe with path-derived ids that contain `__`. Set link text via `textContent = suite.name` (Pitfall security rule).
    - For `max_concurrent_jobs`: when null, render the literal string `—` (em dash), not the JS string "null". Document inline: "// max_concurrent_jobs is the RESEARCH.md Open Q #1 proxy: max overlap of (started, completed) intervals across (job, seed). null means too few timestamps to compute."

    (b) `renderSuite(suiteId)`:
    - GET `data/<suiteId>.json`. On 404 or fetch failure, render an empty-state `<p>` with `textContent` explaining the suite was not found and a `<a href="#/">` back-link.
    - Header: `<h1>` with `data.name` (textContent), `<p class="muted">` with `data.path` (textContent), and a `<a href="#/">` "← back to all suites" link.
    - Manifest summary: a `<dl>` with `<dt>started-at-utc</dt><dd>...</dd>`, `<dt>suite-name (manifest)</dt>`, `<dt>job count</dt>`, `<dt>seed count</dt>`. These come from `data.manifest` (kebab-case keys preserved!) and the precomputed counts.
    - Per-(job, seed) table: one row per `(job_name, seed)`. Columns: `job`, `seed`, `status`, `retained_value`, `net_utility`, `retained_value_ratio`, `peak_mempool_bytes`, `latency_blocks_mean (per-component)`. Latency cell shows up to 3 per-component means rounded to 2 decimals, separated by `/` (e.g. `0.93 / 4.21 / 1.10`); attribute it `title="Latency by demand component (blocks)"` (CRITICAL LANDMINE #3 — the per-component label, never "latency by lane"). Format `retained_value` and `net_utility` with `Intl.NumberFormat('en-US', {maximumFractionDigits: 0})`; format `retained_value_ratio` with `Intl.NumberFormat('en-US', {maximumFractionDigits: 4})`. Rows sortable like in `renderHome`.
    - Make each `(job, seed)` row's first cell an `<a href="#/job/<suiteId>/<job>/<seed>">` link.
    - Aggregates section (CRITICAL LANDMINE #2 / Pitfall 3): if `data.aggregates === null` (phase-2 expected), render NOTHING — no header, no empty-state placeholder. After the table, inject an HTML comment via `document.createComment("aggregates: null — per Pitfall 3 / Plan 01-02 schema, phase-2 metrics writer emits no suite-level *.csv; the historical priority_only_fast_path_overall_comparison.csv lives under sim-rs/output/analysis/ and is not generated by current phase-2 suites. Render only when aggregates is non-null.")` and append it to the view root so DevTools observers see the rationale. If `data.aggregates` is non-null (a future enhancement), render it as an `<h2>Suite aggregates</h2>` followed by a table.
    - Cross-seed overlay (VIZ-05 / D-15): below the (job, seed) table, render a `<section>` with `<h2>Cross-seed time-series overlay</h2>` and a `<select>` listing each `job_name`. When the user picks a job, fetch every `data/<suiteId>/<job>-<seed>.json` for that job's seeds in parallel via `Promise.all`, then call the `renderCrossSeedOverlay` helper from the Task 2 sketch with the `quote_per_byte` filter (priority lane by default; a second `<select>` exposes the lane choice). The button/select pair is the only place this view needs interactive state — keep it simple, store the chosen job+lane in module-level `let` variables, no framework. Use the `el()` helper.

    Helpers to add at the top of the file (above the route function):
    - `function fmtInt(n) { return new Intl.NumberFormat("en-US", {maximumFractionDigits: 0}).format(n); }`
    - `function fmtRatio(n) { return n == null ? "—" : new Intl.NumberFormat("en-US", {maximumFractionDigits: 4}).format(n); }`
    - `function fmtComponents(components) { return components.map(c => c.latency_blocks_mean.toFixed(2)).join(" / "); }`
    - `function sortBy(rows, key, dir) { return [...rows].sort((a, b) => { const av = a[key], bv = b[key]; if (av == null) return 1; if (bv == null) return -1; return dir === "desc" ? (bv > av ? 1 : bv < av ? -1 : 0) : (av > bv ? 1 : av < bv ? -1 : 0); }); }`

    Reference the constant `HEADLINE_LATENCY_LABEL` from Plan 01-03 in any tooltip / aria-label / column header where the latency value is shown. NEVER spell "latency by lane" anywhere.

    Module-level state to share across renders (keep small): a `Map<string, Promise>` cache for `data/<suite>.json` fetches so re-visiting a suite via the back button is instant.
  </action>
  <verify>
    <automated>! grep -n 'innerHTML' sim-rs/scripts/viz/static/main.js && ! grep -niE 'latency by lane' sim-rs/scripts/viz/static/main.js && grep -q 'Latency by demand component (blocks)' sim-rs/scripts/viz/static/main.js && grep -q 'renderHome' sim-rs/scripts/viz/static/main.js && grep -q 'renderSuite' sim-rs/scripts/viz/static/main.js && grep -q 'renderJob' sim-rs/scripts/viz/static/main.js && grep -q 'Pitfall 3' sim-rs/scripts/viz/static/main.js && grep -q "fetch.*data/index\\.json" sim-rs/scripts/viz/static/main.js && (command -v node >/dev/null && node --check sim-rs/scripts/viz/static/main.js || echo 'node --check skipped — defer to checkpoint') && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `renderHome` builds a table with the columns named above; default sort is `started_at` descending (verify by reading the function body — should call `sortBy(suites, "started_at", "desc")` before initial render).
    - `renderSuite` reads `data.aggregates`; when null, no `<h2>Suite aggregates</h2>` heading is rendered. An HTML comment is appended with the Pitfall 3 rationale.
    - `renderSuite` uses `HEADLINE_LATENCY_LABEL` constant (or the exact literal string "Latency by demand component (blocks)") for every latency UI element.
    - `main.js` contains NO occurrence of `innerHTML` (grep).
    - `main.js` contains NO case-insensitive `latency by lane` (grep).
    - `main.js` is syntactically valid JS (`node --check` exits 0 if node present; otherwise deferred to Task 3 checkpoint).
    - Sort handlers use `replaceChildren` (no `innerHTML += ...`).
  </acceptance_criteria>
  <done>
    `main.js` contains real `renderHome` and `renderSuite` implementations; grep gates green; rendered output (when served against the fixture) shows the suite list and drill-down.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flesh out `renderJob` — headline strip + three Observable Plot chart panes</name>
  <files>sim-rs/scripts/viz/static/main.js</files>
  <read_first>
    sim-rs/scripts/viz/static/main.js (current state after Task 1),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md (`## Code Examples / Browser-side Plot rendering for VIZ-04`; Phase Requirements / VIZ-03, VIZ-04 rows; Open Questions #2 — latency-by-component vs latency-by-lane),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (Pattern F LANE_FIELDS table — the lane/metric combinations available in time_series records)
  </read_first>
  <action>
    Implement `renderJob(suiteId, job, seed)` in `sim-rs/scripts/viz/static/main.js`. Fetch `data/<suiteId>/<job>-<seed>.json` (use `encodeURIComponent` on each path segment) and render against the schema in the interfaces block above.

    Top-of-view header: `<h1>` with `${job} · seed ${seed}` (textContent), `<p class="muted">` with `${data.suite_id}` and a back link `<a href="#/suite/${suiteId}">← back to suite</a>`.

    Headline-strip section (VIZ-03): a `<section class="headline-strip">` with one card per metric. Cards (each a `<div class="metric-card">`):
    - retained_value (formatted as an integer; show priority + standard subtotals below the main number in `<small class="muted">`).
    - net_utility (integer).
    - retained_value_ratio (4 decimals; show `—` if null).
    - peak_mempool_bytes (integer with thousands separators).
    - total_txs_included / total_txs_submitted (small "included of submitted" ratio card).
    - pricing_event_stream_sha256: render only the first 8 hex chars in a card titled "Event-stream hash" (audit trail; full hash on hover via `title=` attribute, set via setAttribute, NOT innerHTML).

    Per-component latency block (VIZ-03, the "latency by lane" criterion sanitised per Pitfall 5): a `<section><h2>` whose textContent is the constant `HEADLINE_LATENCY_LABEL` ("Latency by demand component (blocks)"). Inside, a `<table>` with one row per component: `index`, `latency_blocks_mean` (2 decimals), `priority_included`, `standard_included`, and a derived "dominant lane" column = `c.priority_included > c.standard_included ? "priority" : c.standard_included > c.priority_included ? "standard" : "tied"`. Per RESEARCH.md Open Q #2 recommendation: this is the load-bearing way to surface lane info without misrepresenting the underlying mixed observations.

    Time-series charts (VIZ-04): three `<section>` blocks with `<h2>` headings and a `<div>` container each.

    Chart 1 — Controller quote per lane:
      - filter `data.time_series` to `r.metric === "quote_per_byte"` (lanes priority + standard)
      - render with `Plot.plot({width: 800, height: 240, color: {legend: true}, x: {label: "slot"}, y: {label: "controller quote (lovelace/byte)"}, marks: [Plot.ruleY([0]), Plot.line(filtered, {x: "slot", y: "value", stroke: "lane"})]})`
      - heading: `<h2>Controller quote per lane (c_priority, c_standard)</h2>` — show both column names so a future executor reading DevTools can correlate UI to CSV columns.

    Chart 2 — Mempool bytes per lane:
      - filter to `r.metric === "mempool_bytes"` (lanes priority + standard + total)
      - same Plot.plot shape, y label "mempool bytes"
      - heading: `<h2>Mempool bytes (priority, standard, total)</h2>`.

    Chart 3 — Fees and refunds per slot:
      - filter to `r.metric === "fees_paid_lovelace"` OR `r.metric === "refund_lovelace"` (both are lane="total")
      - shape: `marks: [Plot.line(filtered, {x: "slot", y: "value", stroke: "metric"})]`
      - heading: `<h2>Fees paid + refunds per slot (lovelace)</h2>`.

    Empty time-series (Pitfall 8 / phase-3 suites): if `data.time_series.length === 0`, render a single `<p class="muted">` with textContent `"(no time-series available for this seed — see build warnings)"` inside the chart-pane area, skip the three Plot calls.

    Each chart container: `container.replaceChildren(chart)` per RESEARCH.md `## Code Examples`. Wrap each chart's render in a try/catch that, on error, replaces the container's children with a `<p class="muted">` carrying the error message — this prevents one broken pane from killing the whole view.

    Update the import-or-script-tag comment at the top of `main.js` to confirm that `Plot` is the global from `static/plot.min.js`. Add a one-line comment near the first `Plot.plot` call: `// Observable Plot 0.6.x: see static/PLOT_VERSION.txt for the vendored version.` (Abbreviation expansion for CLAUDE.md: first use of "Plot" should be "Observable Plot" if not already done in Plan 01-03's stub.)

    Do NOT add a dependency on the `derived_quote per block` label from Plan 01-03's stub. The label was a temporary placeholder; this task's three chart panes are the real VIZ-04 deliverable. Remove the stub label from `renderJob` if it's still there.
  </action>
  <verify>
    <automated>! grep -n 'innerHTML' sim-rs/scripts/viz/static/main.js && ! grep -niE 'latency by lane' sim-rs/scripts/viz/static/main.js && grep -q 'Plot\\.line' sim-rs/scripts/viz/static/main.js && grep -q "stroke:.*lane" sim-rs/scripts/viz/static/main.js && grep -q "stroke:.*seed" sim-rs/scripts/viz/static/main.js && grep -q 'pricing_event_stream_sha256' sim-rs/scripts/viz/static/main.js && grep -q 'priority_included' sim-rs/scripts/viz/static/main.js && grep -q 'no time-series available' sim-rs/scripts/viz/static/main.js && (command -v node >/dev/null && node --check sim-rs/scripts/viz/static/main.js || echo 'node --check skipped — defer to checkpoint') && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `renderJob` issues at least three `Plot.plot({... Plot.line(...) ...})` calls (one per chart pane).
    - At least one chart uses `stroke: "lane"` (lane-coloured multi-line for VIZ-04).
    - At least one chart uses `stroke: "seed"` (cross-seed overlay for VIZ-05 — either in renderSuite from Task 1 or referenced from renderJob's helper).
    - Empty-state path exists for `data.time_series.length === 0` (Pitfall 8 soft failure).
    - Pricing-event-stream hash is shown in a card titled "Event-stream hash" (full sha256 via `title=` attribute / `setAttribute("title", ...)`, not innerHTML).
    - Per-component latency table includes `priority_included`, `standard_included`, and a `dominant lane` column (RESEARCH.md Open Q #2 recommendation).
    - `main.js` still passes the security grep gates: no `innerHTML`, no `latency by lane`.
    - `node --check` exits 0 if node available; else deferred to Task 3 checkpoint.
  </acceptance_criteria>
  <done>
    `renderJob` complete; grep gates green; visual rendering deferred to Task 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify all three views render correctly against the real `sim-rs/output/` tree</name>
  <what-built>
    Three fully-rendered views: home/suite-list, per-suite drill-down with cross-seed overlay, per-(job, seed) detail with three Plot chart panes and a headline strip. All against the live `sim-rs/output/` tree (not just the fixture).
  </what-built>
  <how-to-verify>
    1. `cd sim-rs && python3 scripts/viz/build.py --serve --port 8765`
    2. Open `http://127.0.0.1:8765/` in a browser. DevTools open, Console tab visible.

    Per VIZ requirement:

    **VIZ-01:** Confirm the home page lists every suite under `sim-rs/output/`. Cross-check the count: `find sim-rs/output -name manifest.json | wc -l` should equal the row count on the page. Click a column header — rows re-sort. Default sort is `started_at` descending (the newest suite is at top).

    **VIZ-02:** Click any suite name. Confirm the drill-down view shows the manifest summary (started_at, suite-name, jobs, seeds) and a sortable (job, seed) table with status + headline metrics.

    **VIZ-03:** Click into a (job, seed). Confirm the headline strip shows all five locked metrics: retained_value, net_utility, retained_value_ratio, peak_mempool_bytes, latency-by-component. Confirm the latency label reads "Latency by demand component (blocks)" — NEVER "latency by lane". Cross-check one or two numbers against the source: `cat sim-rs/output/phase-2/<suite>/<job>/<seed>/run_summary.json | python3 -m json.tool | head -30`.

    **VIZ-04:** Confirm three chart panes render below the headline strip. The controller-quote pane shows two coloured lines (c_priority, c_standard) with a legend. The mempool pane shows three coloured lines (priority, standard, total). The fees+refunds pane shows two lines. Confirm legends are present and lane colours are distinguishable.

    **VIZ-05:** Back in the suite view, scroll to the "Cross-seed time-series overlay" section. Pick a job from the `<select>`. Confirm a chart appears with one coloured line per seed (typically 1-3 seeds per job in phase-2 suites). Confirm `data.aggregates === null` causes NO "Suite aggregates" heading to appear (open DevTools → Elements and search for "aggregates" — only the HTML comment should be present, no `<h2>`).

    **No regressions:**
    - DevTools Console: no red errors.
    - DevTools Network: no requests to external hosts (only `127.0.0.1:8765` entries).
    - Any phase-3 suite (if present) loads with an "(no time-series available)" placeholder where the charts would be, instead of crashing.

    If any of the above fail, describe the failure (URL, view, console error, missing element).
  </how-to-verify>
  <resume-signal>Type "approved" if all six VIZ requirements observably pass, otherwise describe the failure.</resume-signal>
</task>

</tasks>

<verification>

```bash
cd sim-rs

# Build + serve and exercise via curl (smoke test confirms backend; visual confirmation in checkpoint)
python3 scripts/viz/build.py --serve --port 8888 &
SERVE_PID=$!
sleep 2
curl -sI http://127.0.0.1:8888/ | head -1
curl -sI http://127.0.0.1:8888/data/index.json | head -1
curl -s http://127.0.0.1:8888/static/main.js | grep -c 'Plot.line'
kill $SERVE_PID

# Security gates
grep -nE 'innerHTML|latency by lane' scripts/viz/static/main.js   # must be empty
```

</verification>

<success_criteria>
- All six VIZ requirements observable in the browser against the live tree (confirmed via human-verify checkpoint).
- `main.js` security gates green: no `innerHTML`, no "latency by lane".
- Three Plot chart panes render with lane colouring (VIZ-04) and cross-seed overlay renders with seed colouring (VIZ-05).
- Aggregates section is absent on every phase-2 suite (CRITICAL LANDMINE #2 enforced).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/*.json` text fields → DOM | Suite names, job names, seed strings, manifest text are read from JSON and rendered in the browser. |
| `Plot.line` input arrays → SVG | Numeric values from `time_series` are read by Observable Plot and rendered as SVG path data. Plot escapes its own SVG attribute construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | DOM insertion of suite/job/seed names | mitigate | `textContent` only; `el()` helper enforces it; grep gate (no `innerHTML`) in acceptance criteria. |
| T-01-05-02 | Tampering | Plot's SVG path generation | accept | Plot 0.6.x is the upstream library. We trust its escaping. No SRI hash on the vendored bundle; refresh annually per Plan 01-03. |
| T-01-05-03 | Information disclosure | Exposing `data/*.json` over LAN | mitigate (via Plan 01-04) | Server is bound to `127.0.0.1` by Plan 01-04's serve(). This plan does not change the bind. |
</threat_model>

<output>
After completion, create `.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-05-SUMMARY.md` per the standard summary template. Surface: line count of `main.js`, the three chart-pane shapes (filter, marks, axes), confirmation that aggregates section omits its heading when null, and a one-line summary of the cross-seed overlay UX.
</output>
