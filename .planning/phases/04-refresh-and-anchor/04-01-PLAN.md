---
phase: 04-refresh-and-anchor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md
autonomous: true
requirements: [DOC-03]
tags: [docs, anchor-or-disclose, literature-search, controller-calibration]

must_haves:
  truths:
    - "A literature-search artefact records the candidates consulted, the per-knob anchor decision (ANCHORED / DISCLOSED), and the per-knob rationale for the four un-anchored controller knobs"
    - "Each of the four sub-knobs (window-length 32; multiplier-floor 4; multiplier-floor 16; lane-signal-source) has a per-knob disposition recorded"
    - "Rejected citations are listed for reviewer traceability"
    - "Draft prose blocks exist for each sub-knob, ready to be folded into the cardano-realism-audit refresh (Plan 04-04 §'Pricing-controller calibration') and the register's `RSK-un-anchored-controller-knobs` `disclosure-paragraph` field (Plan 04-06)"
  artifacts:
    - path: ".planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md"
      provides: "Literature-search log + per-knob anchor decisions + draft prose blocks + rejected-citations list"
      min_lines: 80
  key_links:
    - from: ".planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md §'Per-knob anchor decisions'"
      to: "docs/phase-2/realism-risks-register.md `RSK-un-anchored-controller-knobs` (Plan 04-06 consumes)"
      via: "draft `disclosure-paragraph` prose blocks per sub-knob; verdict flip recommendation"
      pattern: "(window-length 32|multiplier-floor 4|multiplier-floor 16|lane-signal-source).*?(ANCHORED|DISCLOSED)"
    - from: ".planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md §'Audit-section copy'"
      to: "docs/phase-2/cardano-realism-audit.md §'Pricing-controller calibration' (Plan 04-04 consumes)"
      via: "draft `(value, source, date-retrieved)` triple lines + brief citation prose"
      pattern: "(Liu et al\\.|Reijsbergen et al\\.|Leonardos et al\\.|date-retrieved:)"
---

<objective>
Resolve the four un-anchored controller knobs (window-length 32, multiplier-floor 4, multiplier-floor 16, lane-signal-source) via an open-ended literature search under the "motivating citation suffices" bar (per CONTEXT.md D-35), recording per-knob disposition (ANCHORED / DISCLOSED), drafting both the brief audit-section copy and the full register `disclosure-paragraph` prose for downstream Wave-2 plans to consume.

Purpose: DOC-03 is the Wave-1 critical path for Plan 04-04 (audit `§Pricing-controller calibration` refresh) and Plan 04-06 (register `RSK-un-anchored-controller-knobs` verdict flip + disclosure-paragraph rewrite). The per-knob decisions made here propagate to two documents in Wave 2, so the search artefact must be definitive (each sub-knob ANCHORED or DISCLOSED — never TBD).

Output: One markdown artefact under the phase directory (`04-01-DOC-03-anchor-search.md`) carrying the literature-search log, per-knob decisions, draft audit-section copy, draft register `disclosure-paragraph` per sub-knob, and a rejected-citations list. This file is read by Plans 04-04 and 04-06 in Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-refresh-and-anchor/04-CONTEXT.md
@CLAUDE.md
@docs/phase-2/realism-risks-register.md
@docs/phase-2/mechanism-design.md
@docs/phase-2/cardano-realism-audit.md
@.planning/spikes/003-pricing-controller-calibration/README.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Per-sub-knob disposition shape consumed by Plans 04-04 (audit) and 04-06 (register).
     The artefact MUST follow this shape so downstream plans can paste verbatim. -->

Per-knob block (one per sub-knob in §"Per-knob anchor decisions"):

  ### Sub-knob {1..4}: {window-length 32 | multiplier-floor 4 | multiplier-floor 16 | lane-signal-source}

  - **Disposition:** ANCHORED | DISCLOSED
  - **Citations consulted:**
    - {citation 1; outcome: anchor-supporting | rejected with reason}
    - {citation 2; outcome: ...}
    - ...
  - **Decision rationale:** 2-4 sentence prose; the cut-off decision and why marginal-anchor-unlikely
  - **Draft audit-section copy** (paste-target: docs/phase-2/cardano-realism-audit.md §"Pricing-controller calibration"):
    `({value}, {source-citation or "no external anchor"}, {date-retrieved YYYY-MM-DD or "—"})` + 1-3 sentences naming the motivating citation if ANCHORED, or the conditional-on-X frame if DISCLOSED.
  - **Draft register prose** (paste-target: docs/phase-2/realism-risks-register.md `RSK-un-anchored-controller-knobs` `disclosure-paragraph`):
    A self-contained engineering-report-voice paragraph (~4-8 sentences) that pastes verbatim into the per-sub-knob position in the umbrella entry's disclosure-paragraph. ANCHORED forms cite the anchor and explain what it grounds; DISCLOSED forms use the "conditional on X" frame.

Umbrella verdict recommendation (one block at the artefact tail):

  ### Umbrella verdict for RSK-un-anchored-controller-knobs

  Per the register's umbrella-grouping rule (RSK entry §"Scope-of-resolution"): MITIGATED iff all four sub-knobs are ANCHORED; otherwise LIVE → DISCLOSED with per-sub-knob granularity.

  - **Recommendation:** {MITIGATED | LIVE → DISCLOSED}
  - **Per-sub-knob disposition table:** 4-row table summarising the four decisions above
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Literature search across the four sub-knobs + record per-knob decisions</name>
  <files>.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md</files>
  <read_first>
    - docs/phase-2/realism-risks-register.md §"RSK-un-anchored-controller-knobs" (lines 80-104; this is the umbrella entry with the existing draft fallback disclosure-paragraph; the four sub-knobs are enumerated there exactly as they appear in this plan)
    - docs/phase-2/mechanism-design.md §"Open calibration choices" + lines 207-211 (the three lane-signal-source options; the simulator picks option 1)
    - docs/phase-2/cardano-realism-audit.md §"Pricing-controller calibration" (lines 132-205; the four existing disclosure items 1-4 supply the historical framing being refreshed)
    - .planning/spikes/003-pricing-controller-calibration/README.md (the NEEDS-DISCLOSURE verdict that licenses this work)
    - .planning/phases/04-refresh-and-anchor/04-CONTEXT.md §"Anchor-or-disclose bar (DOC-03)" — D-35 (motivating citation suffices), D-37 (open-ended budget, asymmetric per knob), Claude's Discretion §"DOC-03 literature-search scope" (Liu CCS 2022, Reijsbergen AFT 2021, Leonardos AFT 2021 as minimum reads; 2024-2026 arXiv pass optional)
    - CLAUDE.md §"Conventions / gotchas" (abbreviation-on-first-use rule applies to every prose block produced)
  </read_first>
  <action>
Create `.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md`. Conduct an open-ended literature search across the four sub-knobs of `RSK-un-anchored-controller-knobs` per D-35 (motivating-citation bar — a knob is ANCHORED if a published paper or deployed-system reference motivates the *kind* of choice the simulator makes, even without a numerical match) and D-37 (open-ended budget, asymmetric per knob: window-length most likely to anchor; lane-signal-source likely stays DISCLOSED).

Read at minimum:
- Liu, Yulin et al. (2022) "Empirical Analysis of EIP-1559: Transaction Fees, Waiting Times, and Consensus Security" — CCS 2022. Use WebFetch to retrieve abstract / introduction / relevant sections from a publicly available link (ACM Digital Library, arXiv preprint, author homepage).
- Reijsbergen, Daniël et al. (2021) "Transaction Fees on a Honeymoon: Ethereum's EIP-1559 One Month Later" — AFT 2021. Same retrieval pattern.
- Leonardos, Stefanos et al. (2021) "Dynamical Analysis of the EIP-1559 Ethereum Fee Market" — AFT 2021. Same pattern.

Optional: one 2024-2026 arXiv pass for follow-up EIP-1559 controller-stability literature (search terms: "EIP-1559 controller stability", "base fee oscillation", "EIP-1559 window length"). Exit the search when the marginal new citation adds nothing to the current anchor / disclose set per D-37.

For each of the four sub-knobs, decide ANCHORED or DISCLOSED per the asymmetric expectations from D-35:
1. **Window length 32 (capacity-varying signals)** — most likely to anchor. Plausibly anchors via Liu et al. CCS 2022 or Reijsbergen et al. AFT 2021 (Ethereum EIP-1559 short-term oscillation motivating any window > 1). Reach a definitive disposition.
2. **Multiplier-floor 4 (`phase-2-rb-scarcity` and `phase-2-urgency-inversion`)** — plausibly anchors via the spec's own rationale (`docs/phase-2/mechanism-design.md` §"Open calibration choices" + CLAUDE.md calibration-choice narrative). Note that TEST-07a (`.planning/realism-tests/multiplier-floor-16-companion/results.md`) showed the calibration is regime-dependent; this anchor work is independent of the regime-dependence finding (Plan 04-06 narrates that into `RSK-multiplier-floor-4-suite-coverage`).
3. **Multiplier-floor 16 (the spec default)** — plausibly anchors via the spec's own rationale (mechanism-design.md treats it as the spec default).
4. **Lane-signal-source choices** — likely stays DISCLOSED per D-35. Three open candidates in `mechanism-design.md` lines 207-211; option 1 chosen without a motivating external reference. The search should still consult the three primary citations to confirm no Ethereum-comparable signal-source guidance exists; record consultation as rejected with reason.

Write one §"Per-knob anchor decisions" sub-section per sub-knob, each following the `<interfaces>` shape (Disposition / Citations consulted / Decision rationale / Draft audit-section copy / Draft register prose). For ANCHORED dispositions, the draft audit-section copy MUST include the `(value, source, date-retrieved YYYY-MM-DD)` triple format per ROADMAP.md success criterion #1 — e.g. `(window length = 32 blocks, motivating citation: Liu et al. CCS 2022 §"Base-fee oscillation analysis", date-retrieved: YYYY-MM-DD)`. For DISCLOSED dispositions, use the "conditional on X" frame matching the existing fallback prose in the register entry's disclosure-paragraph field.

For every consulted citation that did NOT yield an anchor, record it under §"Rejected citations" with a one-line reason (e.g. "Liu et al. CCS 2022 — analyses EIP-1559 base-fee oscillation but does not motivate any specific window length; rejected as window-length anchor"). Be explicit about cut-off decisions per D-37.

Use the Conventions / gotchas abbreviation-on-first-use rule (per CLAUDE.md): expand CCS, AFT, CIP, EIP-1559 in this document on first use even though it's a planning artefact — the draft prose blocks are quoted verbatim into CIP-pasteable register / audit content.
  </action>
  <verify>
    <automated>test -s .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md &amp;&amp; grep -cE "^### Sub-knob [1-4]:" .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md | awk '$1 == 4 {exit 0} {exit 1}' &amp;&amp; grep -cE "Disposition:\\*?\\*? (ANCHORED|DISCLOSED)" .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md | awk '$1 >= 4 {exit 0} {exit 1}'</automated>
  </verify>
  <done>The artefact exists with four `### Sub-knob N:` sub-sections, each with an explicit ANCHORED or DISCLOSED Disposition, citations-consulted list, decision rationale, draft audit copy, and draft register prose; no TBD markers anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Umbrella verdict recommendation + rejected-citations list + draft prose audit</name>
  <files>.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md</files>
  <read_first>
    - The Task 1 output (.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md) — the four per-sub-knob blocks
    - docs/phase-2/realism-risks-register.md §"RSK-un-anchored-controller-knobs" §"Scope-of-resolution" (lines 102; the umbrella-grouping rule: MITIGATED iff all four sub-knobs ANCHORED; otherwise LIVE → DISCLOSED with per-sub-knob granularity)
    - .planning/phases/04-refresh-and-anchor/04-CONTEXT.md §"Anchor-or-disclose bar (DOC-03)" — D-36 (anchor lands in BOTH audit and register; per-knob granularity preserved in register if partial)
    - CLAUDE.md §"Conventions / gotchas" (abbreviation-on-first-use rule)
  </read_first>
  <action>
Append to `.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md` two tail sections:

1. §"Umbrella verdict for RSK-un-anchored-controller-knobs" — per the umbrella-grouping rule, the verdict recommendation is MITIGATED iff all four sub-knobs land ANCHORED, else LIVE → DISCLOSED with per-sub-knob granularity. State the recommendation explicitly. Include a 4-row per-sub-knob disposition table (columns: Sub-knob | Disposition | One-line rationale). This is the umbrella-verdict-recommendation block Plan 04-06 reads to decide whether to flip the register entry's verdict from LIVE.

2. §"Rejected citations" — consolidated list across the four sub-knobs (citations that were consulted but did not yield an anchor). Format: one bullet per rejected citation, one-line reason. The list traces reviewer auditability per D-37. Include the 2024-2026 arXiv pass outcomes if performed (could be "no new anchor candidates surfaced; search exited").

Re-read every paragraph in the document. Audit for the CLAUDE.md abbreviation-on-first-use rule: Cardano Improvement Proposal (CIP), Ethereum Improvement Proposal 1559 (EIP-1559), Conference on Computer and Communications Security (CCS), Advances in Financial Technologies (AFT), ranking block (RB), endorser block (EB) — each expanded on first use within the document. Fix any non-conforming abbreviations.

Re-confirm the §"Per-knob anchor decisions" draft register prose blocks are self-contained (a reader who pastes ONLY that prose into the CIP must understand the limitation without reading the rest of the planning artefact); abbreviations expand on first use within each prose block in isolation per the same rule the register applies to its own disclosure-paragraphs.
  </action>
  <verify>
    <automated>grep -cE "^## Umbrella verdict for RSK-un-anchored-controller-knobs" .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; grep -cE "^## Rejected citations" .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; grep -cE "(MITIGATED|LIVE → DISCLOSED)" .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md | awk '$1 >= 1 {exit 0} {exit 1}' &amp;&amp; ! grep -nE "\\bTBD\\b" .planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md</automated>
  </verify>
  <done>The artefact has an §"Umbrella verdict" section with explicit MITIGATED-or-LIVE-→-DISCLOSED recommendation + 4-row table, and a §"Rejected citations" section; no TBD markers; abbreviations expand on first use throughout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External literature → CIP-pasteable prose | Citations consulted via WebFetch must be reproducible (publicly retrievable identifier); fabricated references would land in CIP-pasteable text |
| Per-knob disposition → downstream plans | Plan 04-04 and Plan 04-06 consume the per-knob decisions verbatim; an ambiguous TBD here propagates to two CIP-facing documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Information Disclosure) | Citation prose pasted into register / audit | mitigate | Every consulted citation must be retrievable via a public identifier (DOI, arXiv ID, conference proceedings URL); the Rejected-citations list records the identifier per citation so a reviewer can verify the consultation actually occurred |
| T-04-02 | T (Tampering) | Per-knob disposition reaches Wave 2 plans | mitigate | The artefact's §"Umbrella verdict" + 4-row table is the single canonical entry point Plans 04-04 / 04-06 consume; Plan 04-07's consistency review audits cross-document conformance |
| T-04-03 | R (Repudiation) | Search cut-off decision | mitigate | The "marginal anchor unlikely" cut-off per D-37 is recorded explicitly per sub-knob, so a reviewer can challenge the cut-off (Plan 04-07 also re-verifies) |
</threat_model>

<verification>
- Task 1 emits four `### Sub-knob N:` blocks, each with Disposition (ANCHORED/DISCLOSED), Citations-consulted list, Decision rationale, Draft audit-section copy, Draft register prose
- Task 2 emits §"Umbrella verdict" with MITIGATED-or-LIVE-→-DISCLOSED recommendation + 4-row per-sub-knob table
- Task 2 emits §"Rejected citations" listing every consulted-but-not-anchoring citation with a one-line reason
- No TBD markers anywhere in the file
- Abbreviations CIP / EIP-1559 / CCS / AFT / RB / EB expand on first use in every prose block in isolation (the per-sub-knob draft register prose pastes verbatim into the register, so first-use rule applies within each block)
</verification>

<success_criteria>
The artefact `.planning/phases/04-refresh-and-anchor/04-01-DOC-03-anchor-search.md` exists, contains:
1. Four per-sub-knob blocks (window-length 32; multiplier-floor 4; multiplier-floor 16; lane-signal-source) with definitive ANCHORED-or-DISCLOSED disposition per sub-knob
2. Per-sub-knob draft audit-section copy in `(value, source, date-retrieved)` triple format ready for Plan 04-04 to fold into cardano-realism-audit.md §"Pricing-controller calibration"
3. Per-sub-knob draft register prose ready for Plan 04-06 to fold into the `RSK-un-anchored-controller-knobs` `disclosure-paragraph` field
4. An §"Umbrella verdict" recommendation (MITIGATED iff all four sub-knobs ANCHORED; otherwise LIVE → DISCLOSED) + 4-row disposition summary table
5. §"Rejected citations" list with one-line reasons per rejected citation for reviewer traceability
</success_criteria>

<output>
After completion, create `.planning/phases/04-refresh-and-anchor/04-01-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. The SUMMARY records: per-sub-knob dispositions; consulted-vs-rejected citation counts; the umbrella verdict recommendation Plan 04-06 will act on; any anomalies for Plan 04-07's consistency review.
</output>
