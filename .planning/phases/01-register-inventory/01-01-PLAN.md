---
phase: 01-register-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/phase-2/realism-risks-register.md
autonomous: true
requirements: [REG-01, REG-02, REG-04]
must_haves:
  truths:
    - "docs/phase-2/realism-risks-register.md exists with a header, index table, and per-entry RSK-NN sections"
    - "Every risk-shaped statement across the six source documents and seven spike READMEs is represented by at least one RSK-NN entry"
    - "Entries are de-duplicated into thematic clusters (target 20–30 entries total, not ~60 per-source-doc statements)"
    - "Every RSK-NN entry populates all required fields (id, title, category, description, evidence-for, evidence-against) with at minimum a placeholder for verdict/scope-of-resolution/disclosure-paragraph for plan 02 to finalise"
    - "The four mandatory LIVE entries from research (D-09) are present as distinct RSK-NN entries: RSK-pool-count, RSK-single-seed-precision, RSK-un-anchored-controller-knobs (or 4 sub-entries), RSK-substrate-scope"
    - "RSK-substrate-scope (D-09 + Claude's Discretion substrate-scope-grouping) is one entry whose description names three sub-points: upstream f64 in non-pricing hot paths, propagation-model fidelity, utility-maximising actor model"
    - "CONTEXT.md decisions honoured: D-01 (validity-threats per-threat content → register; per-claim trust ratings deferred to Phase 2 coverage check), D-02 (cardano-realism-audit risk-shaped sections move into register; audit doc itself not modified in this phase — refresh is Phase 4 DOC-01), D-03 (REVIEW.md and CONCERNS.md sourced via evidence-for/against, never modified), D-04 (mechanism-welfare-impact-2026-05-14.md and seven spike READMEs are evidence sources only; their verdicts cited in evidence-for/against but NOT lifted into register verdicts), D-05 (Leios-style RSK-NN and EXP-NN identifier convention used throughout; append-only, never renumbered), D-07 (Wohlin four-fold categorisation construct/internal/external/conclusion applied; multi-category tagging allowed for borderline cases), D-08 (every RSK-NN entry populates all nine required fields per REG-02 with at minimum a placeholder for verdict/scope-of-resolution/disclosure-paragraph for plan 02 to finalise), D-09 (the four mandatory LIVE entries are present)"
  artifacts:
    - path: "docs/phase-2/realism-risks-register.md"
      provides: "Realism-risks register skeleton with all RSK-NN entries inventoried and fields populated (verdict/scope-of-resolution/disclosure-paragraph may be placeholder for plan 02)"
      contains: "## RSK-pool-count"
    - path: "docs/phase-2/realism-risks-register.md"
      provides: "Index table listing every RSK-NN with title and (initial) category"
      contains: "## Index"
  key_links:
    - from: "docs/phase-2/realism-risks-register.md"
      to: "docs/phase-2/cardano-realism-audit.md"
      via: "evidence-for / evidence-against fields cite the audit's NEEDS-DISCLOSURE sections"
      pattern: "cardano-realism-audit.md"
    - from: "docs/phase-2/realism-risks-register.md"
      to: "docs/phase-2/validity-threats.md"
      via: "evidence-for / evidence-against fields cite per-threat content split out of validity-threats per D-01"
      pattern: "validity-threats.md"
    - from: "docs/phase-2/realism-risks-register.md"
      to: ".planning/REVIEW.md"
      via: "evidence-for / evidence-against fields cite WR-2, WR-7 and other LIVE / DEFERRED findings per D-03"
      pattern: "REVIEW.md"
---

<objective>
Build the inventory skeleton of `docs/phase-2/realism-risks-register.md` — the single source of truth for unresolved realism risks affecting the Cardano Improvement Proposal (CIP).

Purpose: This plan does the **inventory and de-duplication** work. It reads every source document, extracts every risk-shaped statement, clusters them into ~20–30 thematic `RSK-NN` entries, and writes the register file with full schema for every entry. The descriptive fields (id, title, category, description, evidence-for, evidence-against) are populated in full. The judgement fields (verdict, scope-of-resolution, EXP-NN linkage, disclosure-paragraph) are populated with **placeholders** that plan 02 will finalise. Plan 02 cannot start its judgement work without this skeleton existing.

Output:
- One new file: `docs/phase-2/realism-risks-register.md` with an index table, per-entry RSK-NN sections (full schema), and ~20–30 thematic entries covering every risk-shaped statement in the audit trail.
- Coverage for REG-01 (single register file de-duplicating across six source docs + seven spikes), REG-02 (required fields populated — verdict/scope-of-resolution/disclosure-paragraph as placeholders for plan 02), REG-04 (the four mandatory LIVE entries appear).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-register-inventory/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory pass — read all six source documents and seven spike READMEs, extract every risk-shaped statement with provenance</name>
  <files>
    No file is written in this task. The deliverable is a working set of risk-shaped statements held in context, ready for de-duplication in Task 2. Do NOT write a separate inventory file — the register itself is the output, and plan 02 must not have to read a transient intermediate doc.
  </files>
  <read_first>
    Required source documents (the inventory sweep covers every risk-shaped statement in these):
    - docs/phase-2/cardano-realism-audit.md (the 410-line existing audit; harvest NEEDS-DISCLOSURE sections, fee-as-maxFee-envelope reinterpretation, the four un-anchored controller knobs — window-length 32, multiplier-floor 4, multiplier-floor 16, lane-signal-source — calibration-stale parameters per CRIT-5)
    - docs/phase-2/validity-threats.md (the 713-line spike-005 output; harvest per-threat risk descriptions for `RSK-NN` per decision D-01. The per-claim trust ratings stay in validity-threats for Phase 4's pointer-doc refresh and Phase 2's coverage check — do NOT extract those here)
    - .planning/REVIEW.md (Fix Status table; harvest WR-2, WR-7 specifically and any LIVE / DEFERRED findings per decision D-03)
    - .planning/codebase/CONCERNS.md (comprehensive tech-debt / fragility inventory; harvest risk-shaped items that are CIP-facing, not internal-only — internal-only items can be referenced via evidence-for but do not become RSK-NN entries on their own per D-03)
    - .planning/mechanism-welfare-impact-2026-05-14.md (Family B vs accumulator cadence evidence; harvest the four sign-flip cells `d4_t50_w32`, `d8_t25_w32`, `x4_rb_quarter` × 2 arms as evidence for `RSK-single-seed-precision`)
    Spike READMEs (audit-trail evidence sources per D-04, not register inputs — read for context only; their verdicts are evidence-for citations, not register verdicts):
    - .planning/spikes/MANIFEST.md (index)
    - .planning/spikes/001-rb-cadence-and-capacity/README.md (VALIDATED)
    - .planning/spikes/002-fee-structure-and-mempool-sizing/README.md (NEEDS-DISCLOSURE — sources `RSK-fee-as-maxFee-envelope` or similar)
    - .planning/spikes/003-pricing-controller-calibration/README.md (NEEDS-DISCLOSURE — sources `RSK-un-anchored-controller-knobs` and its sub-RSKs)
    - .planning/spikes/004-topology-and-actor-model/README.md (NEEDS-DISCLOSURE — sources substrate-scope sub-points (b) propagation, (c) actor model)
    - .planning/spikes/005-validity-threats/README.md (RESOLVED, points at validity-threats.md)
    - .planning/spikes/006-curve-design/README.md (RECOMMENDED — calibration-provenance, not risk; informs `RSK-calibration-stale-stake-snapshot`)
    - .planning/spikes/007-chain-derived-controller/README.md (ADOPT — Family B; closed; cited but not a register input per D-04)
    Phase 1 design references (read for de-duplication discipline):
    - .planning/phases/01-register-inventory/01-CONTEXT.md (decisions D-01..D-10 and Claude's Discretion items — granularity, EXP-NN↔TEST-NN alignment, substrate-scope grouping, Wohlin borderline handling, disclosure-paragraph voice)
    - .planning/research/SUMMARY.md §"Phase 1: Register Inventory" (rationale) and §"Critical Pitfalls" (CRIT-1..CRIT-5 each produce or anchor an RSK-NN)
    - .planning/research/PITFALLS.md §"Critical Pitfalls" and §"Moderate Pitfalls" (each CRIT-* and MOD-* whose "Resolution phase" is "Realism-risks register" is a register entry; each whose resolution phase is a downstream phase is evidence-for / evidence-against context)
  </read_first>
  <action>
    Sweep every source document above and extract every risk-shaped statement. A "risk-shaped statement" is any text that asserts the simulator (a) does not faithfully model a Cardano-mainnet behaviour, (b) uses a calibration value with no deployed-system anchor, (c) carries an inherited limitation from upstream Leios, (d) might invalidate a CIP claim if challenged by a reviewer, or (e) is flagged in research/PITFALLS.md as a critical or moderate pitfall whose resolution phase is the register.

    For each risk-shaped statement, capture in context (do NOT write a transient file):
    1. A one-line summary of the risk
    2. The source document and approximate location (e.g. "cardano-realism-audit.md §'Un-anchored controller knobs'", "validity-threats.md §'phase-2-rb-scarcity LOW trust rating'", "REVIEW.md WR-7 row", "PITFALLS.md CRIT-1")
    3. Wohlin candidate category (construct / internal / external / conclusion) per decision D-07 — multi-category tagging is allowed for borderline cases per D-07 and CONTEXT.md Claude's Discretion "Wohlin borderline cases"

    Constraint: do NOT yet de-duplicate or cluster. The clustering pass is Task 2. The point of this task is to ensure no risk-shaped statement is silently dropped.

    Pitfalls to avoid:
    - Do NOT promote spike verdicts (VALIDATED / NEEDS-DISCLOSURE / RECOMMENDED / ADOPT) to RSK-NN verdicts per D-04 — spikes are evidence sources, not register inputs. They appear in evidence-for / evidence-against citations only.
    - Do NOT extract per-claim trust ratings from validity-threats.md — those go to Phase 2 coverage check per D-01. Only per-threat risk descriptions belong here.
    - Do NOT extract internal-process risk items (e.g. "regenerate goldens after intentional change") from CONCERNS.md / REVIEW.md — the register is CIP-facing per `<specifics>` in CONTEXT.md. Internal items stay in their original docs and can be referenced via evidence-for, but do not become RSK-NN entries.
  </action>
  <verify>
    <automated>test "$(ls -1 docs/phase-2/cardano-realism-audit.md docs/phase-2/validity-threats.md .planning/REVIEW.md .planning/codebase/CONCERNS.md .planning/mechanism-welfare-impact-2026-05-14.md 2>&amp;1 | grep -c '^docs\|^.planning')" -eq 5</automated>
  </verify>
  <done>
    Source documents are read; an inventory of risk-shaped statements is held in context covering: (a) cardano-realism-audit.md (NEEDS-DISCLOSURE sections + 4 un-anchored knobs), (b) validity-threats.md per-threat risk descriptions, (c) REVIEW.md LIVE/DEFERRED entries including WR-2 and WR-7, (d) CONCERNS.md CIP-facing risk-shaped items, (e) mechanism-welfare-impact-2026-05-14.md sign-flip-cell evidence, (f) seven spike READMEs as evidence-source context. Each item is tagged with source-location and Wohlin candidate category. Ready for Task 2's de-duplication.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cluster into ~20–30 thematic RSK-NN entries and draft the register file with full schema</name>
  <files>
    docs/phase-2/realism-risks-register.md (CREATE — new file)
  </files>
  <read_first>
    - .planning/phases/01-register-inventory/01-CONTEXT.md (decisions D-05 ID convention, D-07 Wohlin four-fold, D-08 required fields per RSK-NN entry, D-09 four mandatory LIVE entries, plus Claude's Discretion items "Entry granularity", "Substrate-scope grouping", "Wohlin borderline cases", "Per-CLAUDE.md convention" — abbreviation-on-first-use)
    - .planning/research/STACK.md §"Risk-register format" (Leios `RSK-NN` precedent and the Wohlin schema and disclosure-paragraph template)
    - .planning/research/ARCHITECTURE.md §"Pattern 1: Register entry as the unit of disclosure" (schema example using `RRR-` prefix — note: switched to `RSK-` per CONTEXT.md D-05, but the schema structure is otherwise reusable)
    - CLAUDE.md §"Conventions / gotchas" (abbreviation-on-first-use rule)
    - The inventory built in Task 1 (held in context)
  </read_first>
  <action>
    Cluster the inventory from Task 1 into ~20–30 thematic `RSK-NN` entries per CONTEXT.md Claude's Discretion "Entry granularity". A risk that spans multiple source statements (e.g. "single-seed claims" appears in PITFALLS CRIT-1, validity-threats §welfare-precision, family-b-results-table seed-diversity column) becomes one `RSK-NN`. Per-source-doc-statement granularity is reserved for cases where statements are genuinely different risks (rare).

    Then write `docs/phase-2/realism-risks-register.md` with the following structure (verbatim):

    1. **Header** (top of file):
       ```
       # Realism-Risks Register

       **Status:** Draft (Phase 1)
       **Scope:** Unresolved realism risks affecting the Cardano Improvement Proposal (CIP) responding to CPS-0023 ("Urgency Signaling")
       **Identifier convention:** `RSK-NN` for risks (this document) and `EXP-NN` for register-flagged cheap tests, per Leios precedent (`docs/ImpactAnalysis.md`). Append-only — identifiers never renumber.
       **Categorisation:** Wohlin four-fold (construct / internal / external / conclusion). Multi-tagging permitted where a risk genuinely straddles categories.
       **Verdict vocabulary:** LIVE / DORMANT / MITIGATED / DISCLOSED. Ambiguous cells default to LIVE (most-conservative).

       ## Reading guide

       This register is the single source of truth for unresolved realism risks affecting the CIP. Per-claim trust ratings live in `docs/phase-2/coverage-check.md` (Phase 2). Calibration-provenance triples `(value, source, date-retrieved)` live in the refreshed `docs/phase-2/cardano-realism-audit.md` (Phase 4). Spike READMEs in `.planning/spikes/` are the resolved-or-disclosed audit trail; this register cites them as evidence sources but does not re-express their verdicts.
       ```

    2. **Index table** (immediately after the header):
       ```
       ## Index

       | RSK | Title | Category | Verdict | EXP / Resolution |
       |-----|-------|----------|---------|------------------|
       | RSK-pool-count | Pool-count sensitivity above 100 pools | external | LIVE (TBD plan 02) | EXP-pool-number (→ TEST-05) |
       | RSK-single-seed-precision | Single-seed welfare claims at publication precision | conclusion | LIVE (TBD plan 02) | EXP-sign-flip-variance (→ TEST-03) |
       | ... | ... | ... | ... | ... |
       ```
       The "TBD plan 02" markers are placeholders; plan 02 finalises verdicts and EXP references. Every RSK-NN drafted in this task appears in the index table.

    3. **Per-entry sections** (one section per RSK-NN, ~20–30 sections total):
       ```
       ## RSK-NN: <title>

       **Category:** <construct | internal | external | conclusion> (multi-tag with comma if borderline per D-07)
       **Verdict:** TBD plan 02 (default LIVE if ambiguous per D-06)
       **Description:** <2–4 sentences describing the risk, what goes wrong if it lands, and the failure mode of concern>
       **Evidence-for** (citations that support the risk being real):
       - <source citation 1>
       - <source citation 2>
       **Evidence-against** (citations that bound or mitigate the risk):
       - <source citation 1, or "none yet">
       **Scope-of-resolution:** TBD plan 02
       **EXP / Resolution:** TBD plan 02 (placeholder `EXP-<slug>` if anticipated)
       **Disclosure-paragraph:** TBD plan 02 (required only for DISCLOSED entries per D-08)
       ```

    4. **Mandatory four LIVE entries** (D-09) — these MUST appear with the exact identifiers below:
       - `RSK-pool-count` — Pool-count sensitivity. Category: external. Description must reference CRIT-5 (PITFALLS.md) and the prototype pool-number sensitivity test from PROJECT.md Active item 2. Evidence-for: cardano-realism-audit.md topology section, spikes/006-curve-design/README.md (calibration provenance), CRIT-5. Provisional `EXP-pool-number` cross-referencing `TEST-05` per CONTEXT.md Claude's Discretion "EXP-NN ↔ TEST-NN alignment".
       - `RSK-single-seed-precision` — Single-seed welfare claims at publication-grade precision. Category: conclusion (also construct if multi-tagged per D-07). Description must reference CRIT-1 (PITFALLS.md), the four sign-flip cells (`d4_t50_w32`, `d8_t25_w32`, `x4_rb_quarter` under both rb-reserved-priority and partitioned arms — total 4 cells), and the Paired Seed Evaluation (PSE) plan. Evidence-for: family-b-results-table-2026-05-14.md, mechanism-welfare-impact-2026-05-14.md, CRIT-1. Provisional `EXP-sign-flip-variance` (→ TEST-03) and `EXP-canonical-variance` (→ TEST-04).
       - `RSK-un-anchored-controller-knobs` — Either one umbrella entry naming the four sub-points in the description (window-length 32, multiplier-floor 4, multiplier-floor 16, lane-signal-source), OR four sub-RSKs (`RSK-window-length-anchor`, `RSK-multiplier-floor-4-anchor`, `RSK-multiplier-floor-16-anchor`, `RSK-lane-signal-source-anchor`) — both are acceptable per D-09 and CONTEXT.md Claude's Discretion. **Recommendation:** use one umbrella entry whose description names the four sub-points and whose provisional `EXP / Resolution` field lists four placeholder EXP slugs (`EXP-window-length-anchor`, etc.). Rationale: matches the substrate-scope-grouping rationale (D-09 / CONTEXT.md "Substrate-scope grouping") — these knobs share a single resolution path (the two-hour literature search at Phase 4 open) and grouping them prevents the "looks anchored on 3 of 4" partial-resolution failure mode. Category: external (also construct if multi-tagged). Evidence-for: cardano-realism-audit.md un-anchored-controller-knobs section, spikes/003-pricing-controller-calibration/README.md, CRIT-3 (PITFALLS.md).
       - `RSK-substrate-scope` — One entry per CONTEXT.md Claude's Discretion "Substrate-scope grouping" whose description names three sub-points: (a) upstream `f64` in non-pricing hot paths (slot lottery, propagation, distribution sampling) per `validity-threats.md` CR-1 and CLAUDE.md "Numeric representation contract", (b) propagation-model fidelity (the simulator's RTT-driven topology vs Cardano mainnet's reality), (c) utility-maximising actor model (no adversarial / strategic bidders — Chung & Shi SODA 2023 (Symposium on Discrete Algorithms) impossibility result is the standard frame; deferred per PROJECT.md Out of Scope per CONTEXT.md `<deferred>`). Category: external (also construct if multi-tagged). Evidence-for: CLAUDE.md "Numeric representation contract" + "Determinism scope", spikes/004-topology-and-actor-model/README.md, CRIT-4 (PITFALLS.md), PROJECT.md Out of Scope items 2 and 3.

    5. **Additional thematic RSK-NN entries** to draft from the inventory (the list below is the planner's projection of what Task 1 will surface; the exact count and titles emerge from the inventory, target 20–30 total):
       - Risks sourced from cardano-realism-audit.md: fee-as-maxFee-envelope reinterpretation (`RSK-fee-as-maxFee-envelope`), calibration-stale stake snapshot at epoch 582 (`RSK-calibration-stale-stake-snapshot` — note: separate from `RSK-pool-count`; this is the freshness-of-snapshot risk per CRIT-5), demand-profile staleness (`RSK-demand-profile-staleness` per MIN-3), mempool revalidation cost modelling (`RSK-mempool-revalidation-cost-model` per ARCHITECTURE.md Pattern-1 example), RB-reserved anti-bribery formal-only-under-honest-producer-assumption (`RSK-rb-reserved-anti-bribery-honest-assumption`), un-reserved-variants-anti-bribery-absent (`RSK-un-reserved-anti-bribery-absent`).
       - Risks sourced from validity-threats.md: per-suite LOW trust ratings (cluster into thematic risks like `RSK-multiplier-floor-4-suite-coverage` per MOD-1), standard-user-fee-drift-exposure (`RSK-standard-user-fee-drift-exposure`), un-reserved-priority signal-source choice rationale (`RSK-signal-source-choice-rationale`).
       - Risks sourced from REVIEW.md / CONCERNS.md (per D-03 — these become RSK-NN entries only when CIP-facing): WR-7 (re-check exact CIP-facing form), hash-diversity-as-soft-vs-strict-gate (`RSK-hash-diversity-gate-policy` per MOD-4 — note the open question in STATE.md), determinism-scope-creep (`RSK-determinism-cross-arch-disclosure` per MOD-6 / CR-1).
       - Risks sourced from PITFALLS.md (CRIT-* and MOD-* whose resolution phase is the register or has a register handoff): MOD-2 steady-state-at-2000-slots (`RSK-steady-state-run-length` — links to `EXP-run-length` → TEST-06), MOD-3 adversarial-case-stress-not-exercised (subsumed by `RSK-substrate-scope` sub-point (c); do NOT duplicate), MOD-7 evidence-package-not-aligned-with-menu-structure (this is a Phase 2 coverage-check concern, NOT a register entry — exclude), MIN-1 slot-battle-disclosure-inconsistency (subsumed by WR-1-disclosure handling — likely covered by `RSK-determinism-cross-arch-disclosure` or its own short entry, planner's choice), MIN-2 welfare-as-f64-reporting-boundary (`RSK-welfare-reporting-f64-precision`).

    Target final count: 20–30 entries total (4 mandatory + ~16–26 thematic). If clustering produces fewer than 20 or more than 30, revisit the granularity calibration — Task 2 should land in the 20–30 band per CONTEXT.md Claude's Discretion. If clustering legitimately surfaces, say, 18 entries, that is acceptable; do NOT inflate to hit a count.

    Abbreviations on first use throughout the register prose per CONTEXT.md Claude's Discretion "Per-CLAUDE.md convention" — `Cardano Improvement Proposal (CIP)`, `Inter-Quartile Range (IQR)`, `Paired Seed Evaluation (PSE)`, `Bias-corrected and accelerated (BCa) bootstrap`, `Overview, Design concepts, Details (ODD) protocol`, `Symposium on Discrete Algorithms (SODA)`, `Conference on Computer and Communications Security (CCS)`, `Advances in Financial Technologies (AFT)` — all spelled out the first time they appear.

    Do NOT yet write final disclosure-paragraphs or finalise verdicts — those are plan 02's deliverable. Every `Verdict`, `Scope-of-resolution`, and `Disclosure-paragraph` field carries `TBD plan 02` as a placeholder so plan 02 can grep for them.
  </action>
  <verify>
    <automated>test -f docs/phase-2/realism-risks-register.md &amp;&amp; grep -q "^## RSK-pool-count" docs/phase-2/realism-risks-register.md &amp;&amp; grep -q "^## RSK-single-seed-precision" docs/phase-2/realism-risks-register.md &amp;&amp; grep -q "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md &amp;&amp; (grep -q "^## RSK-un-anchored-controller-knobs" docs/phase-2/realism-risks-register.md || grep -q "^## RSK-window-length-anchor" docs/phase-2/realism-risks-register.md)</automated>
  </verify>
  <acceptance_criteria>
    - docs/phase-2/realism-risks-register.md exists
    - File contains "# Realism-Risks Register" as the first heading
    - File contains "## Index" section with an index table
    - File contains at least 20 and at most 32 `^## RSK-` per-entry sections (count via `grep -c "^## RSK-" docs/phase-2/realism-risks-register.md`; the upper bound 32 is the discretion band with a +2 tolerance)
    - The four mandatory LIVE entries from D-09 appear:
      - `## RSK-pool-count` appears
      - `## RSK-single-seed-precision` appears
      - `## RSK-substrate-scope` appears
      - Either `## RSK-un-anchored-controller-knobs` OR all four sub-RSKs (`## RSK-window-length-anchor`, `## RSK-multiplier-floor-4-anchor`, `## RSK-multiplier-floor-16-anchor`, `## RSK-lane-signal-source-anchor`) appear
    - The `RSK-substrate-scope` entry's description mentions all three sub-points: `f64`, propagation, and actor model (verify via `grep -A 30 "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md | grep -i -E "f64|propagat|actor"` returns 3 distinct matches across the three terms)
    - Every per-entry section has the seven required fields per D-08: `Category`, `Verdict`, `Description`, `Evidence-for`, `Evidence-against`, `Scope-of-resolution`, `EXP / Resolution` (and `Disclosure-paragraph` placeholder for plan 02 to populate where required)
    - Every `Verdict`, `Scope-of-resolution`, and `Disclosure-paragraph` field carries `TBD plan 02` (verify via `grep -c "TBD plan 02" docs/phase-2/realism-risks-register.md` is at least `3 × entry-count`)
    - Abbreviations spelled out on first use: "Cardano Improvement Proposal (CIP)", "Inter-Quartile Range (IQR)", "Paired Seed Evaluation (PSE)" each appear in expanded form somewhere in the file (verify via `grep -E "Cardano Improvement Proposal \(CIP\)" docs/phase-2/realism-risks-register.md` returns at least one hit; same for IQR and PSE)
    - No spike verdict (`VALIDATED`, `RECOMMENDED`, `ADOPT`) is used as an `RSK-NN` `Verdict` value (verify via `grep -v "^#" docs/phase-2/realism-risks-register.md | grep -E "^\*\*Verdict:\*\* (VALIDATED|RECOMMENDED|ADOPT)$"` returns no hits — note grep excludes header lines; the spike vocabulary is `VALIDATED / NEEDS-DISCLOSURE / RECOMMENDED / ADOPT` per D-04, but the register's verdict vocabulary is `LIVE / DORMANT / MITIGATED / DISCLOSED` per D-06)
  </acceptance_criteria>
  <done>
    The register file exists with header, index table, and per-entry sections for every clustered RSK-NN. All four mandatory LIVE entries appear. All entries have the required fields populated; verdict / scope-of-resolution / disclosure-paragraph carry `TBD plan 02` placeholders. Entry count is in the 20–30 band (≤32 with tolerance). Abbreviations expanded on first use. The file is in a state where plan 02 can read it, grep for `TBD plan 02`, and finalise the judgement fields without further inventory work.
  </done>
</task>

</tasks>

<threat_model>
This plan modifies one Markdown file under `docs/phase-2/` and reads existing in-repo documents. There is no code change, no test suite execution, no schema migration, no new dependency, no external service interaction. No attack surface is added or modified.

The only failure modes are documentation-quality failures (missing fields, mis-categorised risks, dropped source statements), which the acceptance criteria in Task 2 cover via grep-style source assertions. STRIDE categories do not apply because there is no trust boundary being crossed by this change — the register is read by future planners and the user; it is not a runtime artefact.

No `<threat_model>` STRIDE register table is needed for this plan.
</threat_model>

<verification>
After Task 2 completes, run the following acceptance checks:

```bash
test -f docs/phase-2/realism-risks-register.md
grep -c "^## RSK-" docs/phase-2/realism-risks-register.md  # expect 20-32
grep -q "^## RSK-pool-count" docs/phase-2/realism-risks-register.md
grep -q "^## RSK-single-seed-precision" docs/phase-2/realism-risks-register.md
grep -q "^## RSK-substrate-scope" docs/phase-2/realism-risks-register.md
grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/realism-risks-register.md
grep -q "Inter-Quartile Range (IQR)" docs/phase-2/realism-risks-register.md
grep -c "TBD plan 02" docs/phase-2/realism-risks-register.md  # expect >= 3 × entry-count
```

All should pass before plan 02 begins.
</verification>

<success_criteria>
- `docs/phase-2/realism-risks-register.md` exists with header, index table, and per-entry sections
- 20–30 (tolerance up to 32) thematic RSK-NN entries cluster all risk-shaped statements from the six source docs + seven spikes
- The four mandatory LIVE entries from D-09 are present with their canonical identifiers
- Every entry has all required fields per D-08; judgement fields carry `TBD plan 02` placeholders
- Abbreviations expanded on first use per CLAUDE.md convention
- No spike verdict vocabulary leaks into the register's `Verdict` fields
</success_criteria>

<output>
After completion, create `.planning/phases/01-register-inventory/01-01-SUMMARY.md` per the standard summary template, recording:
- Final RSK-NN entry count
- The clustering decision for `RSK-un-anchored-controller-knobs` (umbrella vs four sub-RSKs)
- Any thematic risks that surfaced from the inventory pass beyond the planner's projection in Task 2's action (acceptable; the planner's list was a projection, not a contract)
- Any source-document risk-shaped statements that were considered and explicitly excluded (e.g. internal-process items per D-03; out-of-scope items from CONTEXT.md `<deferred>`) — record the exclusion rationale for plan 02 to honour
</output>
