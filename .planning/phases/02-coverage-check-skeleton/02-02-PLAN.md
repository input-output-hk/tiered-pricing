---
phase: 02-coverage-check-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - docs/phase-2/coverage-check.md
autonomous: true
requirements: [COV-02, COV-03, COV-04, COV-06]
tags: [coverage-check, non-welfare-columns, unpinned-suite-output-read, related-rsk-cross-references, consistency-verification]
must_haves:
  truths:
    - "docs/phase-2/coverage-check.md exists as a flat table with stable CLM-NN identifiers (append-only), one row per claim — finalised state (no TBD plan 02 markers remain in any cell)"
    - "Each CLM-NN row carries the full column set per CONTEXT.md D-17: id, claim, menu-option, status (BACKED / WEAK / UNBACKED / OUT-OF-SCOPE), confidence-method, backing-suite, backing-job, seeds-cited, golden-sha256, anti-bribery, signal-source-anchoring, standard-user-fee-drift-exposure, implementation-complexity, related-RSK-ids — with finalised content in every cell"
    - "Non-welfare property columns are present alongside welfare claims — anti-bribery, standard-user-fee-drift exposure, signal-source anchoring, implementation complexity — with each cell citing a spec section, a simulator measurement, or 'disclosed gap' per ROADMAP success criterion 3"
    - "Every row's `signal-source-anchoring` cell matches the format `<enum> (<citation>)` per D-14 — `mainnet-data-cited` / `spec-default` / `unanchored` followed by a citation or RSK-NN reference"
    - "The 12 unpinned demand-regime suites (`paper_like_*`, `sundaeswap_*` beyond the 7 goldens-pinned set, excluding the four UNRESOLVED suites already named) appear as `WEAK`-verdict rows where they cover claims not backed by goldens-pinned suites, per ROADMAP success criterion 4 and CONTEXT.md D-18"
    - "The four UNRESOLVED non-pinned suites from Phase 1 SUMMARY-2 (`phase-2-moderate-priority-only`, `phase-2-moderate-both-dynamic`, `phase-2-realistic-both-dynamic`, `phase-2-sundaeswap-both-dynamic`) have had their `sim-rs/output/` directories walked per Claude's Discretion UNRESOLVED-suite output-read scope: rows go `WEAK` if existing run data is found, `UNBACKED` if none exists; no re-runs are performed in Phase 2"
    - "The skeleton is committable before Phase 3 begins: rows for claims awaiting cheap-test results carry `status: UNBACKED` and surface compute priorities for Phase 3 task ordering per ROADMAP success criterion 5 and COV-06"
    - "Every BACKED row's `golden-sha256` cell either is `—` (structural / calibration claim) or matches a file under `sim-rs/parameters/phase-2-sweep/suites/.goldens/` whose first 12-16 hex characters of contents equals the cell value"
    - "Every `related-RSK-ids` value resolves to an `RSK-NN` heading in `docs/phase-2/realism-risks-register.md` (cross-reference consistency check)"
    - "The four un-anchored controller knobs each appear in at least one row's `signal-source-anchoring` cell as `unanchored (RSK-un-anchored-controller-knobs)` per CONTEXT.md D-14 (the register uses one umbrella entry per Phase 1 SUMMARY-2)"
    - "Abbreviations expanded on first use per D-21: Cardano Improvement Proposal (CIP), Lines-of-Code (LoC), Bias-corrected and accelerated (BCa) bootstrap, Ethereum Improvement Proposal 1559 (EIP-1559), Inter-Quartile Range (IQR), Paired Seed Evaluation (PSE)"
    - "CONTEXT.md decisions D-14, D-16, D-17, D-18, D-19, D-20 are honoured: D-14 (non-welfare cell format), D-16 (verdict vocabulary BACKED / WEAK / UNBACKED / OUT-OF-SCOPE), D-17 (column inventory), D-18 (unpinned-suite WEAK rule), D-19 (hash-diversity-gate strict rule documented in header but not applied in Phase 2), D-20 (committable-before-Phase-3 skeleton state)"
  artifacts:
    - path: "docs/phase-2/coverage-check.md"
      provides: "Finalised Phase 2 coverage-check skeleton: all four non-welfare property columns populated end-to-end; `related-RSK-ids` resolved against the register; unpinned-suite output-read pass complete; consistency-verification pass complete; no `TBD plan 02` markers remain"
      contains: "Hash-diversity gate"
    - path: "docs/phase-2/coverage-check.md"
      provides: "Per-row evidence: every BACKED row either is structural / calibration (backing-suite `—`) or has a non-empty `golden-sha256` prefix; every WEAK row cites an unpinned suite or single-seed evidence; every UNBACKED row names a Phase 3 EXP-NN slug"
      contains: "EXP-"
  key_links:
    - from: "docs/phase-2/coverage-check.md `related-RSK-ids` column"
      to: "docs/phase-2/realism-risks-register.md RSK-NN headings"
      via: "cross-reference resolution — every CLM-NN row's `related-RSK-ids` cell resolves to an RSK-NN heading"
      pattern: "RSK-"
    - from: "docs/phase-2/coverage-check.md `backing-suite` column"
      to: "sim-rs/parameters/phase-2-sweep/suites/<suite>.yaml"
      via: "path resolution — every non-`—` `backing-suite` path resolves to an existing suite YAML"
      pattern: "phase-2-"
    - from: "docs/phase-2/coverage-check.md `golden-sha256` column"
      to: "sim-rs/parameters/phase-2-sweep/suites/.goldens/<suite>.sha256"
      via: "hash-prefix verification — every non-`—` `golden-sha256` cell's hex prefix matches the corresponding `.sha256` file's contents"
      pattern: "pricing_event_stream"
---

<objective>
Finalise `docs/phase-2/coverage-check.md` by replacing every `TBD plan 02` placeholder with a real value, populating the four non-welfare property columns end-to-end, running the unpinned-suite output-read pass per Claude's Discretion (`EXP-unresolved-output-read`), and performing a cross-reference consistency verification against the register and the goldens directory.

Purpose: Plan 02-01 produced the skeleton with all CLM-NN rows enumerated and the 14-column structure laid down; many cells in the non-welfare property columns and the `related-RSK-ids` / `golden-sha256` columns carry `TBD plan 02` placeholders. This plan is the judgement-and-cross-reference pass on top of plan 02-01's enumeration skeleton:

1. Walk the four UNRESOLVED suite output directories per Claude's Discretion UNRESOLVED-suite output-read scope — promote rows from `UNBACKED` to `WEAK` where existing run data supports the row's claim (per D-18); rows with no data remain `UNBACKED`.
2. Walk the 12 unpinned demand-regime suites (which includes the four UNRESOLVED suites plus eight more from the `phase-2-*-singlelane.yaml`, `phase-2-*-both-dynamic.yaml`, `phase-2-*-priority-only.yaml` family) and ensure each appears as a `backing-suite` cell on at least one `WEAK` row covering a claim not already backed by a goldens-pinned suite (per D-18).
3. Populate the four non-welfare property columns end-to-end per CONTEXT.md `<specifics>` rules and CONTEXT.md D-14 cell format. Cells that plan 02-01 left as `TBD plan 02` are finalised here; cells already populated are reviewed for D-14 format compliance.
4. Resolve `related-RSK-ids` cells: every CLM-NN row gets at least one `RSK-NN` cross-reference where the underlying register entry bears on the row's evidence; multi-RSK rows are comma-separated per Claude's Discretion.
5. Populate `golden-sha256` cells with the actual prefix from the `.goldens/*.sha256` files for rows backed by goldens-pinned suites.
6. Run the consistency-verification pass: no dangling `RSK-NN` references; every menu-option enum value appears; every status enum value is from the four-value vocabulary; the four un-anchored controller knobs each surface in `signal-source-anchoring`; the 12 unpinned-suite rule is satisfied; calibration-claim rows have anchored content; abbreviations expanded on first use.

Output:
- `docs/phase-2/coverage-check.md` is the final v1 Phase-2 skeleton — no `TBD plan 02` markers remain (the table is committable before Phase 3 begins per COV-06); the four non-welfare property columns are populated end-to-end; the 12 unpinned demand-regime suites surface as `WEAK`-verdict rows where they cover claims not already backed by goldens-pinned suites; `related-RSK-ids` cross-references resolve against the register.
- Coverage for COV-02 (every row has the full column set — now including finalised non-welfare property cells), COV-03 (non-welfare property columns populated alongside welfare claims with each cell citing spec / measurement / "disclosed gap"), COV-04 (12 unpinned demand-regime suites appear as WEAK rows), COV-06 (skeleton is committable before Phase 3 begins).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-coverage-check-skeleton/02-CONTEXT.md
@.planning/phases/02-coverage-check-skeleton/02-01-SUMMARY.md
@.planning/phases/01-register-inventory/01-02-SUMMARY.md
@docs/phase-2/realism-risks-register.md
@CLAUDE.md

The coverage-check file itself is the primary input. Read the entire current state of `docs/phase-2/coverage-check.md` before beginning Task 1.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk UNRESOLVED suite output directories, populate non-welfare property columns end-to-end, resolve `related-RSK-ids` and `golden-sha256` cells, and promote UNBACKED rows to WEAK where unpinned-suite evidence supports it</name>
  <files>
    docs/phase-2/coverage-check.md (MODIFY — finalise non-welfare property cells; populate related-RSK-ids; populate golden-sha256 prefixes; promote rows from UNBACKED to WEAK per D-18 where evidence exists)
  </files>
  <read_first>
    - docs/phase-2/coverage-check.md (the full current state — produced by plan 02-01; contains all CLM-NN rows with the 14-column header; some non-welfare property cells and `related-RSK-ids` / `golden-sha256` cells carry `TBD plan 02` markers)
    - .planning/phases/02-coverage-check-skeleton/02-01-SUMMARY.md (plan 02-01's summary — records the final CLM-NN count, the claim-class breakdown, and which cells were left as `TBD plan 02`)
    - .planning/phases/02-coverage-check-skeleton/02-CONTEXT.md (decisions D-14 non-welfare cell format; D-16 verdict vocabulary; D-17 column inventory; D-18 unpinned-suite WEAK rule; D-19 hash-diversity-gate strict rule; D-20 committable-before-Phase-3; plus Claude's Discretion: column-ordering, status-conflict resolution, UNRESOLVED suites output-read scope, multi-RSK rows, EXP-NN forward references)
    - docs/phase-2/realism-risks-register.md (the 24 RSK-NN entries — the Index table at the top; the `related-RSK-ids` column on each CLM-NN row references these. Confirm whether the register used a single umbrella `RSK-un-anchored-controller-knobs` entry — it did, per Phase 1 SUMMARY-2 — so the `unanchored (RSK-...)` cell uses `RSK-un-anchored-controller-knobs` rather than per-knob sub-RSK identifiers)
    - .planning/phases/01-register-inventory/01-02-SUMMARY.md (names the four UNRESOLVED non-pinned suites and the three Phase-2-facing EXP-NN slugs `EXP-unresolved-output-read`, `EXP-coverage-non-welfare-columns`, `EXP-hash-diversity-policy-decision`)
    - CLAUDE.md §"Mechanism abstractions" (mechanism-design context for assessing the non-welfare property columns: validity rules per RB-reserved variants, partition-activated flag for partitioned variant, un-reserved priority signal source per CLAUDE.md "Calibration choices"); §"Calibration choices" (the four un-anchored controller knobs — window-length 32, multiplier-floor 4, multiplier-floor 16, lane-signal-source — and the rb-generation-probability anchoring, topology-realistic-100 anchoring); §"Running the suites" (the 7-suite table)
    - docs/phase-2/mechanism-design.md (cited by anti-bribery and standard-user-fee-drift-exposure cells; specific sections to cite for each menu option's anti-bribery property)
    - sim-rs/parameters/phase-2-sweep/suites/.goldens/ (read all seven `.sha256` files to populate `golden-sha256` column prefixes)
    - sim-rs/output/ (the existing run-data directory; walk each of the four UNRESOLVED suites' subdirectories per Claude's Discretion UNRESOLVED-suite output-read scope — if data exists, the corresponding rows go `WEAK`; if no data exists, the rows go `UNBACKED`; do NOT re-run any suite in this plan)
    - List of the 12 unpinned demand-regime suites (per CONTEXT.md D-18): `phase-2-congested-both-dynamic.yaml`, `phase-2-congested-priority-only.yaml`, `phase-2-congested-singlelane.yaml`, `phase-2-moderate-both-dynamic.yaml`, `phase-2-moderate-priority-only.yaml`, `phase-2-moderate-singlelane.yaml`, `phase-2-realistic-both-dynamic.yaml`, `phase-2-realistic-priority-only.yaml`, `phase-2-realistic-singlelane.yaml`, `phase-2-sundaeswap-both-dynamic.yaml`, `phase-2-sundaeswap-priority-only.yaml`, `phase-2-sundaeswap-singlelane.yaml` — 12 yaml files total under `sim-rs/parameters/phase-2-sweep/suites/`
  </read_first>
  <action>
    **Step 1: UNRESOLVED-suite output-read pass (D-18 + Claude's Discretion UNRESOLVED-suite output-read scope).**

    Walk `sim-rs/output/` and check whether subdirectories exist for the four UNRESOLVED non-pinned suites named in Phase 1 SUMMARY-2:
    - `phase-2-moderate-priority-only`
    - `phase-2-moderate-both-dynamic`
    - `phase-2-realistic-both-dynamic`
    - `phase-2-sundaeswap-both-dynamic`

    For each that has existing run data (the `output/<suite-name>/` directory contains `<job>/<seed>/` subdirectories with `metrics.json` or `pricing_event_stream.sha256` files), identify the (job, seed) entries that exist. For every CLM-NN row whose claim corresponds to a menu option exercised by one of these suites AND is currently `UNBACKED`, change `status` to `WEAK`, set `backing-suite` to the unpinned suite path (e.g. `sim-rs/parameters/phase-2-sweep/suites/phase-2-moderate-priority-only.yaml`), set `backing-job` to a specific job slug found in the output directory, set `seeds-cited` to the actual seed count, and set `confidence-method` to `single-seed point estimate from unpinned suite (no goldens-pinned; not under 3-layer determinism regime per D-18)` or similar. For each that has NO output data, the corresponding CLM-NN rows remain `UNBACKED` with `confidence-method: TBD Phase 3 (<EXP-slug> → TEST-NN)` per plan 02-01.

    Per Claude's Discretion "Status enum priority on conflicting evidence": if a CLM-NN row could be backed by either a goldens-pinned suite (BACKED-eligible) AND an unpinned suite (WEAK-eligible), prefer the goldens-pinned job and keep status `BACKED` (or `UNBACKED` if BACKED needs Phase 3 evidence to land); do NOT downgrade.

    **Step 2: Eight remaining unpinned demand-regime suites surface as WEAK rows (D-18).**

    The 12 unpinned demand-regime suites listed in `read_first` include the four UNRESOLVED suites plus eight more (`phase-2-congested-*` × 3, `phase-2-moderate-singlelane`, `phase-2-realistic-priority-only` and `phase-2-realistic-singlelane`, `phase-2-sundaeswap-priority-only` and `phase-2-sundaeswap-singlelane`). For each of the 12, ensure at least one CLM-NN row's `backing-suite` cell references that suite, on a `WEAK`-verdict row whose claim is not already covered by a goldens-pinned suite. If the existing CLM-NN row set already covers every unpinned-suite scope via goldens-pinned-suite rows, append additional CLM-NN rows at the end of the table to fill the gap; the CLM-NN identifier sequence continues append-only per D-15.

    For the eight non-UNRESOLVED unpinned suites: the executor walks `sim-rs/output/` for their subdirectories (if present) using the same rule as Step 1 — `WEAK` if data exists, `UNBACKED` if none. The four UNRESOLVED suites are simply the four whose status was flagged in Phase 1 SUMMARY-2; the rest are unpinned-but-not-flagged.

    **Step 3: Finalise non-welfare property column cells per CONTEXT.md D-14 and `<specifics>`.**

    For every CLM-NN row, ensure all four non-welfare property cells are populated with `<enum-value> (<citation>)` per D-14. Plan 02-01 may have left some cells as `TBD plan 02`; plan 02-02 finalises them. Cell-value rules per menu option (from CONTEXT.md `<specifics>` + CLAUDE.md mechanism abstractions):

    - **`anti-bribery`** per menu option:
      - `priority-only-RB-reserved`: `formal (mechanism-design.md §"RB priority-only validity rule")` — the RB-reserved validity rule excludes standard-fee txs by construction, providing a stronger anti-bribery property than the un-reserved variants. If `mechanism-design.md` does not have a section explicitly titled "anti-bribery" for this option, cite the closest applicable section (e.g. §"Lane partitioning").
      - `priority-only-un-reserved`: `informal (mechanism-design.md §"Un-reserved priority-only premium")` — the un-reserved arms have no validity-rule enforcement; the premium is empirically present but not load-bearing on a formal argument.
      - `both-dynamic-partitioned`: `informal (mechanism-design.md §"EB binary fullness trigger")` — partition activation gates priority-fee charging but does not formally prove anti-bribery.
      - `both-dynamic-un-partitioned`: `informal (mechanism-design.md §"Both-dynamic")` — same as priority-only-un-reserved.
      - `single-lane-EIP-1559-control`: `absent (no priority lane; bribery property does not apply)` — the EIP-1559 single-lane control has no priority lane so the formal/informal/absent property is `absent` by construction.

    - **`signal-source-anchoring`** per menu option:
      - Whenever the row's load-bearing parameter is one of the four un-anchored controller knobs (window-length 32; multiplier-floor 4 used in `phase-2-rb-scarcity` / `phase-2-urgency-inversion`; multiplier-floor 16 used in the other 5 goldens-pinned suites' `*_x16` jobs; lane-signal-source for both-dynamic-standard and un-reserved-priority), write `unanchored (RSK-un-anchored-controller-knobs)`. Confirm against `docs/phase-2/realism-risks-register.md` Index table that the umbrella entry is named exactly `RSK-un-anchored-controller-knobs`.
      - For calibration-claim rows backed by mainnet data (rb-generation-probability anchored to activeSlotsCoeff; topology-realistic-100 anchored to epoch-582), write `mainnet-data-cited (<value, source, date-retrieved>)`.
      - For rows whose value matches a documented EIP-1559 spec default, write `spec-default (<spec citation>)`.

    - **`standard-user-fee-drift-exposure`** per menu option (per CONTEXT.md `<specifics>` + CLAUDE.md "Calibration choices"):
      - `priority-only-RB-reserved`: `none (standard sample does not fire on RB-reserved RBs; CLAUDE.md §"Calibration choices" / Both-dynamic standard signal source)` — the standard quote is not perturbed by priority traffic.
      - `priority-only-un-reserved`: `none (un-reserved priority-only uses priority_paying_bytes/total_block_capacity signal; standard quote unaffected)` — same reason.
      - `both-dynamic-partitioned`: `bounded (partition_activated gates standard-lane drift; cite RSK-standard-user-fee-drift-exposure for Δfee/byte bound under heavy priority load)` — the partition activation flag provides a gate; the exact Δfee/byte bound is plan 02-02 reviewer judgement based on existing data; if unable to derive a bound, write `bounded (RSK-standard-user-fee-drift-exposure; bound unverified pending output-read pass per EXP-unresolved-output-read)`.
      - `both-dynamic-un-partitioned`: `exposed (un-bounded perturbation; cite RSK-standard-user-fee-drift-exposure)` — the un-partitioned variant has no gate; the disclosure-paragraph in the register covers the limit.
      - `single-lane-EIP-1559-control`: `bounded (EIP-1559 step bound; cite mechanism-design.md §"Single-lane EIP-1559")` — the EIP-1559 step caps per-block fee movement.

    - **`implementation-complexity`** per menu option (CONTEXT.md `<specifics>` + CLAUDE.md "Size sanity check"):
      - `priority-only-RB-reserved`: `medium (validity rule + RB-reserved priority capacity; sim-core/src/tx_pricing/two_lane.rs + linear_leios.rs RB-reserved variant; ~100-200 LoC delta over baseline single-lane)`.
      - `priority-only-un-reserved`: `low (un-reserved priority-only signal-source choice option 1 per CLAUDE.md "Calibration choices"; ~50-100 LoC delta)`.
      - `both-dynamic-partitioned`: `medium (partition_activated flag tracking on EB plus multiplier-floor invariant; ~150-250 LoC delta)`.
      - `both-dynamic-un-partitioned`: `medium (multiplier-floor invariant enforcement on two controllers; ~100-200 LoC delta)`.
      - `single-lane-EIP-1559-control`: `medium (single-lane EIP-1559 step; existing baseline in mechanism-design.md spike trail; ~100-150 LoC)`.

    For Lines-of-Code citations: the exact LoC delta is plan 02-02 judgement based on `wc -l sim-rs/sim-core/src/tx_pricing/*.rs` and the equivalent on the linear_leios.rs additions. If the executor cannot derive a tighter LoC estimate without re-counting the kernel, cite the CLAUDE.md "Size sanity check" table's range (1,437 lines total in `tx_pricing/`) and quote a fraction.

    EVERY CELL in the four non-welfare property columns must be populated by the end of Task 1 — no `TBD plan 02` markers remain in those four columns. If the citation specifically requires re-running an analysis, instead write the enum value plus `(disclosed gap — see RSK-NN)` per D-14's `disclosed gap` permission and choose the most relevant RSK-NN.

    **Step 4: Resolve `related-RSK-ids` for every CLM-NN row.**

    For every CLM-NN row, populate `related-RSK-ids` with the most directly relevant RSK-NN identifiers from `docs/phase-2/realism-risks-register.md`. The 24 RSK-NN identifiers available (confirmed via the register's Index table) are: `RSK-pool-count`, `RSK-single-seed-precision`, `RSK-un-anchored-controller-knobs`, `RSK-substrate-scope`, `RSK-fee-as-maxFee-envelope`, `RSK-mempool-cap-magnitude`, `RSK-max-fee-policy-default`, `RSK-calibration-stale-stake-snapshot`, `RSK-demand-mix-bit-calibration`, `RSK-demand-non-stationarity`, `RSK-target-inclusion-blocks-default`, `RSK-partition-activated-honest-producer`, `RSK-leios-spec-pre-deployment`, `RSK-multiplier-floor-4-suite-coverage`, `RSK-three-seed-statistical-power`, `RSK-unresolved-suite-claims`, `RSK-standard-user-fee-drift-exposure`, `RSK-cross-arch-determinism`, `RSK-admission-rejection-attribution`, `RSK-menu-collapse-to-advocacy`, `RSK-steady-state-run-length`, `RSK-hash-diversity-policy`, `RSK-welfare-as-f64-reporting`, `RSK-sundaeswap-demand-staleness`.

    Mapping rules (apply by claim class):
    - Welfare-claim rows whose backing is single-seed: cite `RSK-single-seed-precision` AND `RSK-three-seed-statistical-power`
    - Welfare-claim rows whose backing-suite is conditional on `multiplier_floor = 4`: ALSO cite `RSK-multiplier-floor-4-suite-coverage`
    - Welfare-claim rows whose backing-suite is one of the 12 unpinned demand-regime suites: ALSO cite `RSK-unresolved-suite-claims` AND `RSK-menu-collapse-to-advocacy`
    - Comparative-claim rows whose comparison hinges on a sign-flip cell (`d4_t50_w32`, `d8_t25_w32`, `x4_rb_quarter` × 2 arms): cite `RSK-single-seed-precision` (the resolution path)
    - Structural-claim rows about reorg-safety: cite `RSK-substrate-scope` (acknowledges substrate scope of the Family-B mechanism) — optional; structural claims may legitimately have `related-RSK-ids: —` if no register entry bears
    - Calibration-claim rows about topology stake snapshot: cite `RSK-calibration-stale-stake-snapshot` AND `RSK-pool-count`
    - Calibration-claim rows about rb-generation-probability: typically `related-RSK-ids: —` (no register entry bears on activeSlotsCoeff anchoring directly)
    - Anti-bribery claim rows: cite `RSK-partition-activated-honest-producer` if the row is the partitioned variant; otherwise `RSK-substrate-scope` (the actor-model sub-point covers strategic-bidder scope)
    - Standard-user-fee-drift-exposure claim rows: cite `RSK-standard-user-fee-drift-exposure`
    - Signal-source-anchoring claim rows backed by mainnet data: cite `RSK-calibration-stale-stake-snapshot`
    - Signal-source-anchoring claim rows backed by `unanchored`: cite `RSK-un-anchored-controller-knobs`
    - Implementation-complexity claim rows: typically `related-RSK-ids: —` (no register entry bears on complexity directly)
    - Rows referencing the EIP-1559 control: optionally cite `RSK-leios-spec-pre-deployment` if the claim depends on Leios deployment

    Multi-RSK rows comma-separate per Claude's Discretion "Multi-RSK rows". A single CLM-NN row may legitimately reference 1-3 RSK-NN entries.

    EVERY `related-RSK-ids` cell must either contain at least one valid RSK-NN identifier from the 24-entry namespace OR be `—` (with rationale that no register entry bears). No `TBD plan 02` markers remain.

    **Step 5: Populate `golden-sha256` cells for goldens-pinned-suite rows.**

    For every CLM-NN row whose `backing-suite` cell is one of the seven goldens-pinned-suite paths, read the corresponding `.sha256` file under `sim-rs/parameters/phase-2-sweep/suites/.goldens/` and populate the `golden-sha256` cell with the first 12-16 hex characters of the file's contents (the column legend in plan 02-01 already documents the truncation). The seven .sha256 files are: `phase-2-eip1559-robustness.sha256`, `phase-2-eip1559-smoothing.sha256`, `phase-2-priority-only-rb-reserved.sha256`, `phase-2-priority-only-unreserved.sha256`, `phase-2-rb-scarcity.sha256`, `phase-2-two-lane-both-dynamic.sha256`, `phase-2-urgency-inversion.sha256`.

    For rows backed by unpinned suites: `golden-sha256: —` (no .sha256 file exists for unpinned suites; the WEAK verdict carries the disclosure that the row is not under the 3-layer determinism regime per D-18).

    For structural / calibration rows whose backing is `—`: `golden-sha256: —`.

    Note: each `.sha256` file's contents may be a single hash if the suite has a single goldens-pinned (job, seed) entry, or a multi-line list. Use the row's `backing-job` slug to pick the correct hash within the file if multi-line; if the file is single-line, that hash applies to whatever (job, seed) the row references within the suite.

    Do NOT proceed to Task 2 (consistency verification) until every `TBD plan 02` marker in the file has been finalised in this task.
  </action>
  <verify>
    <automated>test -f docs/phase-2/coverage-check.md && ! grep -q "TBD plan 02" docs/phase-2/coverage-check.md && grep -q "^| CLM-" docs/phase-2/coverage-check.md && grep -c "unanchored (RSK-un-anchored-controller-knobs)" docs/phase-2/coverage-check.md | awk '{ if ($1 >= 4) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - No `TBD plan 02` markers remain anywhere in the file (verify via `grep -c "TBD plan 02" docs/phase-2/coverage-check.md` returns 0)
    - Every CLM-NN row's `anti-bribery` cell starts with one of `formal`, `informal`, `absent` and is followed by ` (` (citation parenthetical) per D-14 (verify via `grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $11 }' | grep -v -E "^\s*(formal|informal|absent) \(" | wc -l` returns 0)
    - Every CLM-NN row's `signal-source-anchoring` cell starts with one of `mainnet-data-cited`, `spec-default`, `unanchored` followed by ` (` per D-14 (verify via `grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $12 }' | grep -v -E "^\s*(mainnet-data-cited|spec-default|unanchored) \(" | wc -l` returns 0)
    - Every CLM-NN row's `standard-user-fee-drift-exposure` cell starts with one of `none`, `bounded`, `exposed` followed by ` (` per D-14 (verify via `grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $13 }' | grep -v -E "^\s*(none|bounded|exposed) \(" | wc -l` returns 0)
    - Every CLM-NN row's `implementation-complexity` cell starts with one of `low`, `medium`, `high` followed by ` (` per D-14 (verify via `grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $14 }' | grep -v -E "^\s*(low|medium|high) \(" | wc -l` returns 0)
    - The 12 unpinned demand-regime suites each appear in at least one row's `backing-suite` cell (verify via `for s in phase-2-congested-both-dynamic phase-2-congested-priority-only phase-2-congested-singlelane phase-2-moderate-both-dynamic phase-2-moderate-priority-only phase-2-moderate-singlelane phase-2-realistic-both-dynamic phase-2-realistic-priority-only phase-2-realistic-singlelane phase-2-sundaeswap-both-dynamic phase-2-sundaeswap-priority-only phase-2-sundaeswap-singlelane; do grep -q "$s" docs/phase-2/coverage-check.md || echo "missing: $s"; done` returns no "missing" lines)
    - The four un-anchored controller knobs surface in at least four rows: `grep -c "unanchored (RSK-un-anchored-controller-knobs)" docs/phase-2/coverage-check.md` returns at least 4
    - Every `related-RSK-ids` cell either contains an RSK-NN identifier that exists in the register OR is `—` (verify via extracting column 15 from CLM- rows and confirming each non-`—` value is one of the 24 register RSK-NN identifiers OR contains commas separating multiple such identifiers)
    - Every BACKED row whose `backing-suite` is not `—` has a non-empty `golden-sha256` cell (verify via walking BACKED rows: for any row where column 6 (`backing-suite`) is not `—`, column 9 (`golden-sha256`) must not be `—` and must not be empty)
    - The UNRESOLVED-suite output-read pass produced at least one outcome per UNRESOLVED suite — either the suite's rows were promoted to WEAK (if data exists) OR they remain UNBACKED (if no data exists), with the rationale captured in the SUMMARY's "Output-read findings" section
  </acceptance_criteria>
  <done>
    Every CLM-NN row has finalised content in all 14 cells. The four non-welfare property columns are populated end-to-end per D-14. The 12 unpinned demand-regime suites each appear as `backing-suite` on at least one WEAK or UNBACKED row. The four un-anchored controller knobs surface in `signal-source-anchoring` cells. `related-RSK-ids` cells cross-reference the register's 24 identifiers. `golden-sha256` cells are populated for goldens-pinned-suite rows. No `TBD plan 02` markers remain. Ready for Task 2's consistency verification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run consistency verification pass; confirm cross-references resolve; confirm enum vocabularies are honoured; confirm abbreviation rule is honoured</name>
  <files>
    docs/phase-2/coverage-check.md (MODIFY — only if consistency-verification surfaces issues that require row-level edits to fix)
  </files>
  <read_first>
    - docs/phase-2/coverage-check.md (the full current state — finalised by Task 1)
    - docs/phase-2/realism-risks-register.md (cross-reference target; the `related-RSK-ids` resolution check walks this file's section headings)
    - sim-rs/parameters/phase-2-sweep/suites/ (the 19 suite YAML files; the `backing-suite` resolution check confirms every cell value resolves to an existing suite YAML)
    - sim-rs/parameters/phase-2-sweep/suites/.goldens/ (the 7 `.sha256` files; the `golden-sha256` resolution check confirms every non-`—` cell value matches the corresponding file's contents)
    - .planning/phases/02-coverage-check-skeleton/02-CONTEXT.md (decisions D-13 five menu-option values; D-14 non-welfare cell format; D-16 verdict vocabulary; D-19 hash-diversity-gate strict rule; D-21 abbreviation-on-first-use)
  </read_first>
  <action>
    Run the following consistency-verification checks. If any check surfaces an issue, fix the row(s) in `docs/phase-2/coverage-check.md` and re-run the check before proceeding.

    **Check 1: Cross-reference resolution — `related-RSK-ids` against the register.**
    Extract every RSK-NN identifier referenced in the coverage-check's `related-RSK-ids` column and confirm each one exists as an `^## RSK-` heading in `docs/phase-2/realism-risks-register.md`. Command pattern: `comm -23 <(grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $15 }' | tr ',' '\n' | sed 's/^ *//;s/ *$//' | grep -oE "RSK-[a-zA-Z0-9-]+" | sort -u) <(grep -oE "^## (RSK-[a-zA-Z0-9-]+)" docs/phase-2/realism-risks-register.md | sed 's/^## //' | sort -u)` returns no lines (every RSK-NN cited in the coverage check exists in the register). If any line is returned, the referenced RSK-NN does not exist — fix the cell.

    **Check 2: `backing-suite` path resolution.**
    Extract every non-`—` `backing-suite` cell value and confirm each path resolves to an existing file under `sim-rs/parameters/phase-2-sweep/suites/`. Command pattern: walk column 6 of the CLM- rows, strip whitespace, skip `—`, and `test -f <path>` for each remaining value. If any cell points to a non-existent file, fix the cell.

    **Check 3: `golden-sha256` hash-prefix verification.**
    Extract every non-`—` `golden-sha256` cell value and confirm that the cell's hex prefix matches the corresponding `.sha256` file's contents. For rows whose `backing-suite` is `sim-rs/parameters/phase-2-sweep/suites/phase-2-X.yaml`, the corresponding goldens file is `sim-rs/parameters/phase-2-sweep/suites/.goldens/phase-2-X.sha256`. Read each .sha256 file and confirm the cell's prefix appears in the file's contents. If a row's `backing-job` slug is specific (multi-line .sha256 files have per-(job, seed) entries), confirm the prefix matches the row's specific (job, seed). If any mismatch, fix the cell.

    **Check 4: Menu-option enum vocabulary.**
    Confirm the menu-option column contains exactly the five values listed in D-13. Command pattern: `grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $4 }' | sed 's/^ *//;s/ *$//' | sort -u` must yield exactly five lines: `priority-only-RB-reserved`, `priority-only-un-reserved`, `both-dynamic-partitioned`, `both-dynamic-un-partitioned`, `single-lane-EIP-1559-control`. If any other value appears, fix it.

    **Check 5: Status enum vocabulary.**
    Confirm the status column contains only values from the four-value vocabulary per D-16. Command pattern: `grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $5 }' | sed 's/^ *//;s/ *$//' | sort -u` must yield a subset of `BACKED`, `WEAK`, `UNBACKED`, `OUT-OF-SCOPE`. If any other value appears, fix it. Per D-19 the BACKED count in Phase 2 should be limited to structural-by-construction and mainnet-anchored-calibration rows (the welfare/comparative-claim rows requiring Phase 3 evidence stay UNBACKED or WEAK).

    **Check 6: Non-welfare property column enum vocabularies.**
    - `anti-bribery` column 11: every cell starts with `formal` / `informal` / `absent` followed by ` (`
    - `signal-source-anchoring` column 12: every cell starts with `mainnet-data-cited` / `spec-default` / `unanchored` followed by ` (`
    - `standard-user-fee-drift-exposure` column 13: every cell starts with `none` / `bounded` / `exposed` followed by ` (`
    - `implementation-complexity` column 14: every cell starts with `low` / `medium` / `high` followed by ` (`

    **Check 7: The four un-anchored controller knobs surface in `signal-source-anchoring` cells.**
    Confirm `grep -c "unanchored (RSK-un-anchored-controller-knobs)" docs/phase-2/coverage-check.md` returns at least 4.

    **Check 8: The 12 unpinned demand-regime suites each surface as a `backing-suite` cell on at least one row.**
    For each of the 12 suites listed in Task 1's read_first, confirm the suite's basename appears at least once in the file. Command pattern: `for s in phase-2-congested-both-dynamic phase-2-congested-priority-only phase-2-congested-singlelane phase-2-moderate-both-dynamic phase-2-moderate-priority-only phase-2-moderate-singlelane phase-2-realistic-both-dynamic phase-2-realistic-priority-only phase-2-realistic-singlelane phase-2-sundaeswap-both-dynamic phase-2-sundaeswap-priority-only phase-2-sundaeswap-singlelane; do grep -q "$s" docs/phase-2/coverage-check.md || echo "missing: $s"; done` returns no "missing" lines.

    **Check 9: Abbreviation-on-first-use per D-21 / CLAUDE.md.**
    Confirm each of the following expanded forms appears at least once in the file:
    - `Cardano Improvement Proposal (CIP)`
    - `Lines-of-Code (LoC)`
    - `Bias-corrected and accelerated (BCa) bootstrap`
    - `Ethereum Improvement Proposal 1559 (EIP-1559)`
    - `Ranking Block (RB)` (per the menu option `priority-only-RB-reserved`)
    - `Inter-Quartile Range (IQR)` if referenced
    - `Paired Seed Evaluation (PSE)` if referenced

    **Check 10: The four sign-flip cells are referenced.**
    Confirm `grep -c "d4_t50_w32\|d8_t25_w32\|x4_rb_quarter" docs/phase-2/coverage-check.md` returns at least 4 — each of the four sign-flip cells (`d4_t50_w32`, `d8_t25_w32`, `x4_rb_quarter` under rb-reserved-priority arm, `x4_rb_quarter` under partitioned arm) appears at least once.

    **Check 11: COV-04 — the 12 unpinned demand-regime suites appear as WEAK-verdict rows (not BACKED).**
    Confirm no row whose `backing-suite` is one of the 12 unpinned suites carries `status: BACKED`. Command pattern: for each unpinned suite name, `grep "^| CLM-" docs/phase-2/coverage-check.md | grep "$suite_name" | awk -F'|' '{ print $5 }' | grep -c "BACKED"` returns 0. If any line returns >0, downgrade the row to `WEAK` and add a `confidence-method` annotation explaining why (the unpinned suite is not under the 3-layer determinism regime per D-18).

    **Check 12: COV-06 — the file is committable before Phase 3.**
    Confirm at least one CLM-NN row carries `status: UNBACKED` with `confidence-method` referencing a Phase 3 EXP-NN slug (e.g. `EXP-canonical-variance`, `EXP-sign-flip-variance`, `EXP-pool-number`, `EXP-run-length`). This surfaces Phase 3 compute priorities per COV-06 / D-20.

    If any check surfaces an issue, fix the file before declaring the task done. Do NOT silently leave a failing check.

    **Final update: footer / status header.**
    Update the file's status header line from "Skeleton (Phase 2; plan 01-01 enumeration; plan 01-02 finalisation pending)" to "v1 — Phase 2 skeleton committable for Phase 3 entry (per COV-06); Phase 3 will populate BACKED cells as cheap-test results arrive". Update the footer marker from "Phase 2 coverage-check skeleton; plan 02-02 finalises judgements..." to "Phase 2 coverage-check v1 finalised; Phase 3 will apply COV-05 hash-diversity gate and promote rows from UNBACKED to BACKED as multi-seed test results land".
  </action>
  <verify>
    <automated>test -f docs/phase-2/coverage-check.md && ! grep -q "TBD plan 02" docs/phase-2/coverage-check.md && comm -23 <(grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $15 }' | tr ',' '\n' | sed 's/^ *//;s/ *$//' | grep -oE "RSK-[a-zA-Z0-9-]+" | sort -u) <(grep -oE "^## (RSK-[a-zA-Z0-9-]+)" docs/phase-2/realism-risks-register.md | sed 's/^## //' | sort -u) | wc -l | awk '{ if ($1 == 0) exit 0; else exit 1 }' && grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/coverage-check.md && grep -q "Bias-corrected and accelerated (BCa) bootstrap" docs/phase-2/coverage-check.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TBD plan 02" docs/phase-2/coverage-check.md` returns 0
    - Cross-reference resolution (Check 1) returns no dangling RSK-NN: `comm -23 <(grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $15 }' | tr ',' '\n' | sed 's/^ *//;s/ *$//' | grep -oE "RSK-[a-zA-Z0-9-]+" | sort -u) <(grep -oE "^## (RSK-[a-zA-Z0-9-]+)" docs/phase-2/realism-risks-register.md | sed 's/^## //' | sort -u)` returns no lines
    - `backing-suite` path resolution (Check 2): every non-`—` cell value resolves to an existing file under `sim-rs/parameters/phase-2-sweep/suites/`
    - `golden-sha256` hash-prefix verification (Check 3): every non-`—` cell prefix matches the corresponding `.sha256` file's contents
    - Menu-option enum (Check 4): exactly 5 distinct values, matching D-13
    - Status enum (Check 5): subset of 4 values, matching D-16
    - Non-welfare property enum vocabularies (Check 6): all four columns pass the regex checks from Task 1's acceptance criteria
    - Four un-anchored controller knobs surface (Check 7): `grep -c "unanchored (RSK-un-anchored-controller-knobs)" docs/phase-2/coverage-check.md` returns at least 4
    - The 12 unpinned demand-regime suites each surface (Check 8): the for-loop check returns no "missing" lines
    - Abbreviations expanded on first use (Check 9): grep checks pass for CIP, LoC, BCa bootstrap, EIP-1559, RB
    - Four sign-flip cells referenced (Check 10): grep returns at least 4
    - COV-04 — no unpinned-suite row carries BACKED (Check 11)
    - COV-06 — at least one UNBACKED row references a Phase 3 EXP-NN slug (Check 12): `grep "UNBACKED" docs/phase-2/coverage-check.md | grep -c "EXP-"` returns at least 1
    - Status header and footer reflect v1-finalised state
  </acceptance_criteria>
  <done>
    All 12 consistency-verification checks pass. The file has no `TBD plan 02` markers. The status header and footer reflect v1-finalised state. The file is in its final v1 state and committable before Phase 3 begins.
  </done>
</task>

</tasks>

<threat_model>
No security surface — pure-documentation phase; no executable code, secrets, auth, or external inputs introduced. The file is read by future planners, the CIP author, and reviewers; it is not a runtime artefact. No `<threat_model>` STRIDE register table is needed.
</threat_model>

<verification>
After Task 2 completes, run the following final acceptance checks:

```bash
# No TBD markers remain
! grep -q "TBD plan 02" docs/phase-2/coverage-check.md

# Cross-reference resolution: every related-RSK-ids entry resolves to an RSK-NN in the register
comm -23 \
  <(grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $15 }' | tr ',' '\n' | sed 's/^ *//;s/ *$//' | grep -oE "RSK-[a-zA-Z0-9-]+" | sort -u) \
  <(grep -oE "^## (RSK-[a-zA-Z0-9-]+)" docs/phase-2/realism-risks-register.md | sed 's/^## //' | sort -u)

# Menu-option enum: exactly 5 distinct values
grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $4 }' | sed 's/^ *//;s/ *$//' | sort -u

# Status enum: subset of 4 values
grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $5 }' | sed 's/^ *//;s/ *$//' | sort -u

# The 12 unpinned demand-regime suites each surface
for s in phase-2-congested-both-dynamic phase-2-congested-priority-only phase-2-congested-singlelane \
         phase-2-moderate-both-dynamic phase-2-moderate-priority-only phase-2-moderate-singlelane \
         phase-2-realistic-both-dynamic phase-2-realistic-priority-only phase-2-realistic-singlelane \
         phase-2-sundaeswap-both-dynamic phase-2-sundaeswap-priority-only phase-2-sundaeswap-singlelane; do
    grep -q "$s" docs/phase-2/coverage-check.md || echo "missing: $s"
done

# Un-anchored controller knobs surface
grep -c "unanchored (RSK-un-anchored-controller-knobs)" docs/phase-2/coverage-check.md  # expect >= 4

# Sign-flip cells referenced
grep -c "d4_t50_w32\|d8_t25_w32\|x4_rb_quarter" docs/phase-2/coverage-check.md  # expect >= 4

# Abbreviations expanded
grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/coverage-check.md
grep -q "Lines-of-Code (LoC)" docs/phase-2/coverage-check.md
grep -q "Bias-corrected and accelerated (BCa) bootstrap" docs/phase-2/coverage-check.md
grep -q "Ethereum Improvement Proposal 1559 (EIP-1559)" docs/phase-2/coverage-check.md

# COV-06: at least one UNBACKED row names a Phase 3 EXP-NN
grep "UNBACKED" docs/phase-2/coverage-check.md | grep -c "EXP-"  # expect >= 1
```

All should pass before the plan is declared done.
</verification>

<success_criteria>
- `docs/phase-2/coverage-check.md` exists in v1 final state — committable before Phase 3 begins per COV-06
- No `TBD plan 02` markers remain anywhere in the file
- Every CLM-NN row has finalised content in all 14 cells per D-14 / D-17 / `<specifics>`
- Non-welfare property columns (anti-bribery, signal-source-anchoring, standard-user-fee-drift-exposure, implementation-complexity) are populated end-to-end per COV-03
- The 12 unpinned demand-regime suites each surface as `WEAK`-verdict rows per COV-04 / D-18
- The four un-anchored controller knobs surface in `signal-source-anchoring` cells per D-14
- The four sign-flip cells are referenced in CLM-NN rows
- Cross-reference resolution passes: every `related-RSK-ids` entry resolves to an RSK-NN in the register
- `backing-suite` path resolution passes: every non-`—` path resolves to an existing suite YAML
- `golden-sha256` hash-prefix verification passes for all rows backed by goldens-pinned suites
- Status header and footer reflect v1-finalised state
- Abbreviations expanded on first use per D-21
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-check-skeleton/02-02-SUMMARY.md` per the standard summary template, recording:
- Final CLM-NN distribution (counts of BACKED / WEAK / UNBACKED / OUT-OF-SCOPE)
- Output-read findings: for each of the four UNRESOLVED suites, what data was found in `sim-rs/output/` and which rows were promoted from UNBACKED to WEAK as a result
- For each of the 12 unpinned demand-regime suites, which CLM-NN row(s) cite it as `backing-suite` and what status those rows carry
- The four un-anchored controller knobs' coverage: which CLM-NN rows surface each knob's load-bearing parameter in `signal-source-anchoring`
- The four sign-flip cells' CLM-NN assignments (carried forward from plan 02-01's enumeration, now with finalised `related-RSK-ids` and confidence-method)
- Any RSK-NN entries from the register that were NOT cited by any CLM-NN row (acceptable — not every register entry must surface in the coverage check; record the rationale)
- Confirmation that the file is in v1 final state and Phase 3 may begin
</output>
