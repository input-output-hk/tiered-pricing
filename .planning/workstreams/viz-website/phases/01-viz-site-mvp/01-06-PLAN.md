---
phase: 01-viz-site-mvp
plan: 06
type: execute
wave: 4
depends_on: [04, 05]
files_modified:
  - sim-rs/scripts/viz/README.md
  - CLAUDE.md
autonomous: false
requirements: [VIZ-06]
must_haves:
  truths:
    - "`sim-rs/scripts/viz/README.md` documents the single command `python sim-rs/scripts/viz/build.py --serve` (D-07) and the flag set (`--source`, `--output`, `--include`, `--exclude`, `--serve`, `--port`). The script is Python stdlib-only (D-08)."
    - "The README maps the on-disk output layout (`<output>/index.html`, `<output>/static/`, `<output>/data/{index.json, <suite>.json, <suite>/<job>-<seed>.json}`) to the three-tier D-09 design."
    - "The README explicitly notes that `sim-rs/output/viz/` is gitignored transitively via the existing `sim-rs/.gitignore` `/output` rule (D-06) — and that no new gitignore entry is added by this phase (CRITICAL LANDMINE #4 / Pitfall 6)."
    - "`CLAUDE.md` references the viz site under a brief subsection so a future contributor finds the command without crawling `sim-rs/scripts/`."
    - "Abbreviations in the README and CLAUDE.md addition are expanded on first use per CLAUDE.md `## Conventions / gotchas` (Single-Page Application (SPA); Observable Plot; etc.)."
  artifacts:
    - path: sim-rs/scripts/viz/README.md
      provides: "Single documented command + flag reference + output layout + abbreviation-expanded prose"
      contains: "python sim-rs/scripts/viz/build.py --serve"
    - path: CLAUDE.md
      provides: "Updated to reference the viz site command under an existing or new short subsection"
      contains: "sim-rs/scripts/viz/build.py"
  key_links:
    - from: sim-rs/scripts/viz/README.md
      to: sim-rs/scripts/viz/build.py
      via: "Documented invocation"
      pattern: "python.*sim-rs/scripts/viz/build\\.py"
    - from: CLAUDE.md
      to: sim-rs/scripts/viz/README.md
      via: "Markdown link"
      pattern: "scripts/viz/README\\.md|scripts/viz/build\\.py"
---

<objective>
Docs + final end-to-end smoke: the README that satisfies VIZ-06's "single documented command" wording, the CLAUDE.md crumb that lets a future contributor find the viz site without crawling the tree, and a manual end-to-end checkpoint that confirms the full pipeline works against `sim-rs/output/`.

Purpose: Close the loop. After this plan, a fresh developer can clone the repo, run one command, and see the site. No tribal knowledge.

Output: `sim-rs/scripts/viz/README.md` (new), a small addition to `CLAUDE.md` (existing file), and a successful end-to-end checkpoint.

Autonomy: One checkpoint (end-to-end manual confirmation against the live `sim-rs/output/` tree).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-CONTEXT.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-02-PLAN.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-03-PLAN.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-04-PLAN.md
@.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-05-PLAN.md
@CLAUDE.md
@sim-rs/scripts/generate-realistic-100-topology.py
@sim-rs/.gitignore

<interfaces>
<!-- Final API surface (from Plans 01-02 and 01-04) — to document in the README. -->

CLI:
  python sim-rs/scripts/viz/build.py [OPTIONS]

OPTIONS:
  --source PATH       Root to walk for manifest.json (default: sim-rs/output)
  --output PATH       Build output directory (default: sim-rs/output/viz)
  --include GLOB      Include glob, repeatable, matched against suite path relative to --source
  --exclude GLOB      Exclude glob, repeatable, matched against suite path relative to --source
  --serve             After build, serve via http.server bound to 127.0.0.1
  --port INT          Port for --serve (default: 8000)

OUTPUT LAYOUT:
  <output>/index.html                       SPA shell
  <output>/static/                          Vendored assets (main.js, style.css, plot.min.js, PLOT_VERSION.txt)
  <output>/data/index.json                  Tier 1: list of every suite
  <output>/data/<suite_id>.json             Tier 2: per-suite headline metrics + manifest summary
  <output>/data/<suite_id>/<job>-<seed>.json   Tier 3: per-(job, seed) time-series + headline

GITIGNORE STATUS:
  `sim-rs/output/viz/` is gitignored transitively via `sim-rs/.gitignore` line `/output`.
  No new gitignore entry exists for this phase. (Pitfall 6 / CRITICAL LANDMINE #4.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `sim-rs/scripts/viz/README.md`</name>
  <files>sim-rs/scripts/viz/README.md</files>
  <read_first>
    sim-rs/scripts/generate-realistic-100-topology.py (lines 1-57 — module-docstring style, the structural template per PATTERNS.md `### sim-rs/scripts/viz/README.md` section),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-PATTERNS.md (`### sim-rs/scripts/viz/README.md` — landmine note that the gitignore is transitive),
    .planning/workstreams/viz-website/phases/01-viz-site-mvp/01-RESEARCH.md (Architecture Patterns / Pattern 1 — flag set; Common Pitfalls / Pitfall 6 — gitignore is already in place),
    sim-rs/.gitignore (one line; confirm `/output` rule still present)
  </read_first>
  <action>
    Author `sim-rs/scripts/viz/README.md`. Follow the structural pattern of `generate-realistic-100-topology.py`'s module docstring (PATTERNS.md `### sim-rs/scripts/viz/README.md` section): one-line summary, usage block with literal commands, flag table, output-layout map, then a short notes section.

    Suggested structure (this is the planner's recommendation; the executor can re-arrange but every section below must be present):

    `# phase-2 simulator visualisation site`

    Opening paragraph (2-3 sentences): "A local-first visualisation surface for the experiment artefacts produced under `sim-rs/output/`. The build script is a stdlib-only Python program that walks `sim-rs/output/` for `manifest.json` files, parses each suite's per-(job, seed) `run_summary.json` and `time_series.csv`, and emits a static HTML + JavaScript bundle that renders the headline metrics and time-series in the browser. Charts use Observable Plot (vendored under `static/plot.min.js`)." Abbreviation expansions on first use (CLAUDE.md `## Conventions / gotchas`): "Single-Page Application (SPA)", "Observable Plot", "Endorser Block (EB)" if it appears in the prose. Use abbreviations alone after first introduction.

    `## Quickstart`
        ```
        python sim-rs/scripts/viz/build.py --serve
        ```
    One paragraph: explain that this builds the bundle into `sim-rs/output/viz/` then serves it on `http://127.0.0.1:8000/`. Press Ctrl-C to stop. Re-run when new suites land under `sim-rs/output/`.

    `## Flags`
    A Markdown table mapping each flag from the interfaces block above to its type, default, and description. Match `argparse --help` exactly so the doc never drifts from the CLI.

    `## Output layout`
    A code-block tree showing the three-tier output (interfaces block above). One sentence per tier explaining its role:
    - Tier 1 (`index.json`): the suite-list used by the home page (VIZ-01).
    - Tier 2 (`<suite_id>.json`): per-suite manifest summary + (job, seed) table + cross-seed overlay (VIZ-02, VIZ-05).
    - Tier 3 (`<suite_id>/<job>-<seed>.json`): per-(job, seed) headline metrics + time-series (VIZ-03, VIZ-04).

    `## What gets rendered`
    Short bullets:
    - Home: sortable suite list (default sort: most-recent `started-at-utc` first).
    - Suite drill-down: manifest summary + sortable (job, seed) table + cross-seed time-series overlay.
    - Per-(job, seed): headline-metric strip + three Observable Plot panes (controller quote per lane, mempool bytes per lane, fees + refunds per slot).
    - Latency is reported per demand component (not per lane); see "Notes" below.

    `## Notes`
    Bullets covering the things a future contributor will trip over:
    - **`sim-rs/output/viz/` is gitignored.** This is transitive — `sim-rs/.gitignore` line `/output` catches everything under `sim-rs/output/`, including `viz/`. No new `.gitignore` entry was added. The bundle is regenerated on demand (per CONTEXT.md decision D-06).
    - **Stdlib-only.** Python's standard library is sufficient. No `requirements.txt`, no virtualenv, no `pip install`. PyYAML is NOT used by this script (matches D-08).
    - **Observable Plot is vendored** at `static/plot.min.js` (~150 KB). The version is recorded in `static/PLOT_VERSION.txt`. Refresh annually with a one-line PR; jsDelivr's `@0.6` major.minor tag auto-tracks patch releases. The bundle works fully offline.
    - **`metrics_comparison.txt` is human-only.** The build never parses it — every field it contains is also in `run_summary.json`. The per-suite "aggregates" section is reserved for a future enhancement; phase-2 suites today emit no suite-level `*.csv`, so the section is omitted entirely from the suite view (the historical `priority_only_fast_path_overall_comparison.csv` lives under `sim-rs/output/analysis/` from older work).
    - **Latency labels.** The simulator's `ComponentSummary.latency_blocks_observations` is a per-component list mixing priority and standard inclusions; there is no per-lane breakdown. The UI displays "Latency by demand component (blocks)" and shows each component's priority / standard inclusion counts in the same row so the user can see which lane dominates.
    - **Local-only network bind.** The server binds `127.0.0.1` exclusively, never `0.0.0.0`. Other devices on the LAN cannot reach the site.

    `## Tests`
    One paragraph + command block:
        ```
        python -m unittest discover -s sim-rs/scripts/viz/tests -t sim-rs/scripts/viz
        ```
    Note that the suite includes both unit tests (against synthetic fixtures under `tests/fixtures/`) and an HTTP smoke test that subprocess-spawns the serve helper.

    `## Updating the vendored Observable Plot bundle`
    Brief recipe for an annual refresh — the curl command from Plan 01-03, then update `PLOT_VERSION.txt`, run the test suite, commit.

    No more sections. The README is a developer reference, not marketing copy.

    Length target: 150-300 lines of Markdown (the analog `generate-realistic-100-topology.py` docstring is ~45 lines; a Markdown README naturally runs longer due to tables and code blocks). Stay under ~400 lines.
  </action>
  <verify>
    <automated>test -f sim-rs/scripts/viz/README.md && grep -q 'python sim-rs/scripts/viz/build.py --serve' sim-rs/scripts/viz/README.md && grep -q '127\\.0\\.0\\.1' sim-rs/scripts/viz/README.md && grep -q 'gitignored' sim-rs/scripts/viz/README.md && grep -q '/output' sim-rs/scripts/viz/README.md && grep -qE 'Single-Page Application \\(SPA\\)|SPA' sim-rs/scripts/viz/README.md && grep -q 'Observable Plot' sim-rs/scripts/viz/README.md && grep -q 'Latency by demand component' sim-rs/scripts/viz/README.md && ! grep -niE 'latency by lane' sim-rs/scripts/viz/README.md && grep -q 'metrics_comparison\\.txt' sim-rs/scripts/viz/README.md && grep -q 'priority_only_fast_path_overall_comparison\\.csv' sim-rs/scripts/viz/README.md && test "$(wc -l < sim-rs/scripts/viz/README.md)" -ge 80 && test "$(wc -l < sim-rs/scripts/viz/README.md)" -le 500 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `sim-rs/scripts/viz/README.md` exists; line count between 80 and 500.
    - Contains the literal invocation `python sim-rs/scripts/viz/build.py --serve` (the "single documented command" per VIZ-06 / D-07).
    - Contains the literal `127.0.0.1` (security bind statement).
    - Mentions both `gitignored` and `/output` in the same Notes section (CRITICAL LANDMINE #4 / Pitfall 6 surface).
    - Spells "Single-Page Application (SPA)" with abbreviation in parentheses on first use (CLAUDE.md abbreviation rule).
    - Spells "Observable Plot" in full on first use.
    - Contains the literal `Latency by demand component (blocks)` (Pitfall 5 surfaced).
    - Does NOT contain case-insensitive `latency by lane` anywhere (regression guard).
    - Mentions `metrics_comparison.txt` and explains that the build never parses it (Pitfall 4 surfaced).
    - Mentions `priority_only_fast_path_overall_comparison.csv` and explains it lives in `sim-rs/output/analysis/`, not in phase-2 suites (CRITICAL LANDMINE #2 / Pitfall 3 surfaced).
    - Documents all six argparse flags (`--source`, `--output`, `--include`, `--exclude`, `--serve`, `--port`).
  </acceptance_criteria>
  <done>
    README exists, passes all grep gates, and reads as a coherent developer reference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a viz-site crumb to `CLAUDE.md`</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (full file — to pick the right insertion location and match prose style),
    sim-rs/scripts/viz/README.md (just written — to link to it)
  </read_first>
  <action>
    Add a brief subsection to `CLAUDE.md` so a future contributor finds the viz site via the project's primary doc instead of via a directory walk. The cleanest insertion point is at the end of the `## Running the suites` section, immediately before `## Conventions / gotchas`. Use a short `### Visualising suite results` subsection (or similar; the executor can pick the exact heading).

    Content (rough shape — adapt the prose to fit CLAUDE.md's existing voice):

        ### Visualising suite results

        For browsing the contents of `sim-rs/output/` interactively, the
        phase-2 visualisation site lives at
        [`sim-rs/scripts/viz/`](sim-rs/scripts/viz/README.md). Build and
        serve via:

        ```sh
        python sim-rs/scripts/viz/build.py --serve
        ```

        The site renders the suite-list, per-suite drill-down, per-(job,
        seed) detail, and an in-suite cross-seed time-series overlay
        against the artefacts already on disk. Bundle output lands under
        `sim-rs/output/viz/` (gitignored transitively via the existing
        `/output` rule). See `sim-rs/scripts/viz/README.md` for the full
        flag reference.

    Two paragraphs total; no need for elaborate detail — the README is the deep doc, this is the breadcrumb. CLAUDE.md is a project-level file so the abbreviation-expansion rule still applies: spell "Single-Page Application (SPA)" if used here (it doesn't need to be — keep it simple).

    Do NOT modify any other section of CLAUDE.md. Do NOT introduce new top-level `##` headings. Do NOT add a 7th metric or change any existing convention.

    After editing, verify the file is still valid Markdown (no unbalanced backticks, no broken table rows) by inspection.
  </action>
  <verify>
    <automated>grep -q 'sim-rs/scripts/viz' CLAUDE.md && grep -q 'python sim-rs/scripts/viz/build.py' CLAUDE.md && grep -q 'Visualising suite results\\|viz-website\\|visualisation site' CLAUDE.md && grep -q 'sim-rs/output/viz' CLAUDE.md && (! grep -nE 'latency by lane' CLAUDE.md || echo 'WARN: case-insensitive latency by lane in CLAUDE.md — review') && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` contains the literal substring `sim-rs/scripts/viz`.
    - `CLAUDE.md` contains the literal invocation `python sim-rs/scripts/viz/build.py`.
    - `CLAUDE.md` mentions `sim-rs/output/viz` and notes it is gitignored / transitively under `/output`.
    - `CLAUDE.md` introduces the addition via a `### `-level subsection (not a new `## ` top heading) under an existing top-level section (`## Running the suites` or `## Repository layout` are the natural candidates).
    - No other section of `CLAUDE.md` is modified beyond whitespace touch-up adjacent to the new subsection. `git diff CLAUDE.md` shows changes localised to the inserted block.
    - Existing CLAUDE.md content (mechanism abstractions, calibration choices, etc.) is unchanged.
  </acceptance_criteria>
  <done>
    `CLAUDE.md` carries a discoverable breadcrumb to the viz site; the rest of the file is untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification against the live `sim-rs/output/` tree</name>
  <what-built>
    The full phase 1 deliverable: a documented, tested, served Single-Page Application that browses every suite under `sim-rs/output/`, renders headline metrics + three Plot chart panes per (job, seed), and an in-suite cross-seed overlay. Triggered by `python sim-rs/scripts/viz/build.py --serve`.
  </what-built>
  <how-to-verify>
    Fresh start: ensure no prior build artefacts skew the test.

    1. `cd /home/will/git/arc-tiered-pricing`
    2. `rm -rf sim-rs/output/viz` (clean the build directory).
    3. `python sim-rs/scripts/viz/build.py --serve` (from the repo root — the script uses default `--source sim-rs/output` and `--output sim-rs/output/viz`).
    4. Read the script's stderr summary. Expected:
        - "Wrote sim-rs/output/viz/data/index.json with <N> suites" where N is approximately equal to `find sim-rs/output -name manifest.json | wc -l`.
        - Warnings (if any) about malformed manifests or missing time-series CSVs — these are skip-and-warn cases (D-21).
        - "Serving sim-rs/output/viz at http://127.0.0.1:8000/  (Ctrl-C to stop)".
    5. Open `http://127.0.0.1:8000/` in a browser. DevTools open.
    6. Per-requirement spot-checks:
        - **VIZ-01:** Suite list visible; row count ≈ `find` result. Click a column header — sort flips.
        - **VIZ-02:** Click a suite. Manifest summary + (job, seed) table render.
        - **VIZ-03:** Click a (job, seed). Headline strip shows 5+ metrics. Cross-check one number (e.g. retained_value) against `python3 -c "import json; d=json.load(open('<path>/run_summary.json')); print(d['priority_retained_value_total']+d['standard_retained_value_total'])"`. The numbers must agree to integer precision.
        - **VIZ-04:** Three chart panes render with multi-line + legend + lane colouring. Hover a line — Plot's default tooltip shows the slot + value.
        - **VIZ-05:** Back in suite view, the cross-seed overlay select picks a job and renders one line per seed.
        - **VIZ-06:** A single command (`python sim-rs/scripts/viz/build.py --serve`) got you here. README documents it. CLAUDE.md links to the README.
    7. Spot-check the abbreviation rule on the README: read the first paragraph. "Single-Page Application (SPA)" or equivalent abbreviations should be expanded on first use.
    8. Spot-check a phase-3 suite (if any): the (job, seed) view should show "(no time-series available)" placeholder where charts would be, instead of crashing (Pitfall 8).
    9. Stop the server with Ctrl-C.
    10. Run the full test suite one more time:
        - `cd sim-rs && python3 -m unittest discover -s scripts/viz/tests -t scripts/viz -v`
        - Expected: all unit tests + smoke tests green; runtime under 15 seconds.

    If anything fails, describe the failure (URL, console error, mis-rendered metric, etc.). If everything passes, the phase is shippable.
  </how-to-verify>
  <resume-signal>Type "approved" if all six VIZ requirements pass + tests green, otherwise describe the failure.</resume-signal>
</task>

</tasks>

<verification>

```bash
cd /home/will/git/arc-tiered-pricing

# Docs in place
test -f sim-rs/scripts/viz/README.md
grep -q 'python sim-rs/scripts/viz/build.py' CLAUDE.md

# Tests all green
cd sim-rs && python3 -m unittest discover -s scripts/viz/tests -t scripts/viz -v 2>&1 | tail -5
```

</verification>

<success_criteria>
- `sim-rs/scripts/viz/README.md` exists and documents the single command + flags + output layout + gitignore-transitivity caveat + Pitfall 5 latency-label note.
- `CLAUDE.md` carries a breadcrumb subsection to the viz site.
- End-to-end manual checkpoint confirms all six VIZ requirements observable in the browser against the live `sim-rs/output/` tree.
- Full test suite green (unit + smoke) in under 15 seconds.
</success_criteria>

<output>
After completion, create `.planning/workstreams/viz-website/phases/01-viz-site-mvp/01-06-SUMMARY.md` per the standard summary template. Surface: the resolved README section structure, the line count, the inserted-CLAUDE.md-block location, and a one-line confirmation of each VIZ-NN being met (with the cross-checked numeric value from the VIZ-03 spot-check).
</output>
