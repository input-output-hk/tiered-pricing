---
phase: 05-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/phase-2/realism-risks-register.md
autonomous: true
requirements: [HAND-01]
tags: [handoff, register-edits, verdict-flips, disclosure-paragraph-promotion]
must_haves:
  truths:
    - "All six remaining LIVE register entries are flipped to DISCLOSED before the cip-author-summary is written"
    - "Each flipped entry's `disclosure-paragraph` field is load-bearing (no `(draft fallback;` prefix, no `TBD — drafted in Phase 4` markers)"
    - "Each flipped entry's `disclosure-paragraph` integrates the specific Phase 2 / Phase 3 / Phase 4 evidence named in D-47, D-48, D-49"
    - "The register's `## Index` table verdict column is updated to DISCLOSED for all six entries"
    - "The register's reading-guide preamble distribution sentence is updated from `6 LIVE + 18 DISCLOSED` to `0 LIVE + 24 DISCLOSED`"
    - "The register's footer prose carries the post-Plan-05-01 distribution narrative"
  artifacts:
    - path: "docs/phase-2/realism-risks-register.md"
      provides: "Register with all 24 entries at DISCLOSED verdict; load-bearing disclosure-paragraphs for the six newly-flipped entries"
      contains: "0 LIVE + 24 DISCLOSED"
  key_links:
    - from: "docs/phase-2/realism-risks-register.md `## Index` table"
      to: "per-entry `**Verdict:**` line of each of the six flipped entries"
      via: "verdict-column value `DISCLOSED`"
      pattern: "Index-table-verdict ≡ per-entry-verdict (all 24 entries DISCLOSED)"
    - from: "docs/phase-2/realism-risks-register.md `**Disclosure-paragraph:**` field on RSK-hash-diversity-policy"
      to: "Phase 2 D-19 strict-gate rule + Phase 3 hash-diversity-gate 17/17 BACKED-eligible cells pass"
      via: "in-paragraph citation of `.planning/realism-tests/hash-diversity-gate/results.md` and `docs/phase-2/coverage-check.md` §\"Hash-diversity gate (COV-05; Phase 3 enforces)\""
      pattern: "Disclosure-paragraph references both the Phase 2 policy decision and the Phase 3 17/17 result"
---

<objective>
Flip all six remaining LIVE register entries to DISCLOSED, promote each entry's `disclosure-paragraph` from draft fallback (or TBD forward-pointer) to load-bearing CIP-pasteable prose, integrate the specific Phase 2 / Phase 3 / Phase 4 evidence per D-47 / D-48 / D-49, and update the register's Index table + reading-guide preamble + footer distribution narrative.

Purpose: Phase 5 / HAND-01's cip-author-summary references the post-Phase-5 register distribution (24 DISCLOSED + 0 LIVE) and pastes from the six newly-flipped disclosure-paragraphs. The flips must land before HAND-01 (Plan 05-03) writes; HAND-02 (Plan 05-02) verifies the flipped state.

Output: Updated `docs/phase-2/realism-risks-register.md` with 0 LIVE + 24 DISCLOSED, six load-bearing disclosure-paragraphs, and consistent narrative across Index table / reading guide / footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-handoff/05-CONTEXT.md
@.planning/phases/04-refresh-and-anchor/04-SUMMARY.md
@.planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md
@docs/phase-2/realism-risks-register.md
@CLAUDE.md

<interfaces>
<!-- Register schema (per-entry field set) extracted from `docs/phase-2/realism-risks-register.md`. -->
<!-- Each `## RSK-<slug>:` entry carries these fields, in order: -->
<!-- - **Category:** <Wohlin category list> -->
<!-- - **Verdict:** LIVE | DISCLOSED | MITIGATED | DORMANT -->
<!-- - **Description:** <prose> -->
<!-- - **Evidence-for:** <bullet list> -->
<!-- - **Evidence-against:** <bullet list> -->
<!-- - **Scope-of-resolution:** <prose> -->
<!-- - **EXP / Resolution:** <EXP-<slug> reference or "DISCLOSED — see disclosure-paragraph below"> -->
<!-- - **Disclosure-paragraph:** <CIP-pasteable prose; first-use abbreviation expansion required> -->

<!-- Index-table column order: | RSK | Title | Category | Verdict | EXP / Resolution | -->

<!-- Distribution sentence in reading guide reads (pre-Plan-05-01): -->
<!-- "Post-Plan-04-07 verdict distribution: 6 LIVE + 18 DISCLOSED + 0 MITIGATED + 0 DORMANT" -->
<!-- Target (post-Plan-05-01): -->
<!-- "Post-Plan-05-01 verdict distribution: 0 LIVE + 24 DISCLOSED + 0 MITIGATED + 0 DORMANT" -->

<!-- The six LIVE entries to flip (locations within register; line numbers approximate per current file): -->
<!-- - RSK-single-seed-precision        (line ~64;  Index row line ~23) -->
<!-- - RSK-three-seed-statistical-power (line ~297; Index row line ~36) -->
<!-- - RSK-unresolved-suite-claims      (line ~313; Index row line ~37) -->
<!-- - RSK-standard-user-fee-drift-exposure (line ~327; Index row line ~38) -->
<!-- - RSK-menu-collapse-to-advocacy    (line ~375; Index row line ~41) -->
<!-- - RSK-hash-diversity-policy        (line ~406; Index row line ~43) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the five non-policy LIVE entries to DISCLOSED with prose-promoted disclosure-paragraphs</name>
  <files>docs/phase-2/realism-risks-register.md</files>
  <read_first>
    - docs/phase-2/realism-risks-register.md (full file; current LIVE entries at lines ~64, ~297, ~313, ~327, ~375)
    - .planning/phases/04-refresh-and-anchor/04-03-phase3-evidence-summary.md (full file; sourced for TEST-03 / TEST-04 / TEST-07a numerical findings)
    - .planning/phases/05-handoff/05-CONTEXT.md §"Six LIVE register entries → resolved to DISCLOSED at tag-time" (D-47, D-49 are the source-of-truth for the prose-promotion edits)
    - docs/phase-2/cardano-realism-audit.md §"Recommended disclosure statements" (the CIP-pasteable voice the disclosure-paragraphs match)
    - CLAUDE.md §"Conventions / gotchas" (abbreviation-on-first-use rule)
  </read_first>
  <action>
Edit `docs/phase-2/realism-risks-register.md` in place. For each of the five non-policy LIVE entries below, perform: (a) flip the per-entry `**Verdict:**` line from `LIVE` to `DISCLOSED`; (b) promote the `**Disclosure-paragraph:**` field per the per-entry directive below; (c) update the entry's Index-table row's Verdict cell from `LIVE` to `DISCLOSED`.

Per D-49, the edits are prose-promotion: remove the `*(draft fallback; ...)*` prefix from each `**Disclosure-paragraph:**` field; light-touch refine the prose to integrate Phase 3 / Phase 4 evidence as named below. Preserve abbreviation-on-first-use within each disclosure-paragraph (the register has a consolidated header expansion block, but each entry's disclosure-paragraph stands alone when pasted into the CIP, so first-use expansion within each disclosure-paragraph is required).

**Per-entry edits:**

1. **`RSK-single-seed-precision`** (currently line ~64):
   - Verdict line: LIVE → DISCLOSED.
   - EXP / Resolution line: keep `EXP-sign-flip-variance (→ TEST-03); EXP-canonical-variance (→ TEST-04)` but append ` — RESOLVED to DISCLOSED via Plan 05-01 prose-promotion after Phase 3 N=20 BCa CI evidence landed`.
   - Disclosure-paragraph: remove the `*(draft fallback; load-bearing only for cells whose multi-seed variance bands do not resolve via Paired Seed Evaluation (PSE))*` prefix. Light-touch refine the existing prose to integrate the Phase 3 TEST-03 / TEST-04 N=20 BCa findings per 04-03-phase3-evidence-summary.md §"TEST-03 sign-flip variance bands" and §"TEST-04 canonical menu-item variance bands": (i) name the four sign-flip cells' N=20 verdicts (2 BACKED, 2 WEAK with CI crossing zero); (ii) name the five canonical TEST-04 cells' N=20 verdicts (4 BACKED — the two un-reserved arms confirm welfare-positive, the two RB-reserved arms confirm welfare-negative — plus the EIP-1559 control as paired baseline); (iii) preserve the existing prose's "cells whose CI crosses zero are reported as ordering-level rather than magnitude-level" framing.

2. **`RSK-three-seed-statistical-power`** (currently line ~297):
   - Verdict line: LIVE → DISCLOSED.
   - EXP / Resolution line: keep `EXP-canonical-variance (→ TEST-04); overlap with EXP-sign-flip-variance (→ TEST-03)` but append ` — RESOLVED to DISCLOSED via Plan 05-01 prose-promotion`.
   - Disclosure-paragraph: replace the current `TBD — drafted in Phase 4 if test verdict lands as DISCLOSED. Expected Phase 3 path is MITIGATED via TEST-04 re-runs at N = 10 on the canonical menu-item cells; the residual disclosure (cells reported as ordering-level rather than magnitude-level claims) is folded into `RSK-single-seed-precision`'s disclosure-paragraph above.` placeholder with a load-bearing paragraph stating: (i) the original 3-seed suite default is sufficient for qualitative ordering claims (sign-coherence + median + IQR) but does not license tight 95% Bias-corrected and accelerated (BCa) bootstrap confidence intervals on magnitude; (ii) Phase 3 TEST-03 + TEST-04 promoted the 9 load-bearing cells (4 sign-flip + 5 canonical) to N=20 seeds, sufficient for nominal BCa coverage per `.planning/research/SUMMARY.md` §"Recommended Stack"; (iii) every other coverage-check row (CLM-NN beyond the 8 Phase-3-promoted rows) is reported at the 3-seed default with ordering-level framing; (iv) Cardano Improvement Proposal (CIP) reviewers should treat magnitude claims on non-promoted rows as ordering-level, not magnitude-level, per the framing folded into `RSK-single-seed-precision`'s disclosure-paragraph above. Ensure abbreviations expand on first use within this paragraph (CIP, BCa, IQR).

3. **`RSK-unresolved-suite-claims`** (currently line ~313):
   - Verdict line: LIVE → DISCLOSED.
   - EXP / Resolution line: keep `EXP-unresolved-output-read (Phase 2 coverage-check pass, not a cheap test; cross-reference REQ-COV-06)` but append ` — RESOLVED to DISCLOSED via Plan 05-01 prose-promotion after Phase 2 output-read pass produced definite verdicts`.
   - Disclosure-paragraph: replace the current `TBD — drafted in Phase 4 if test verdict lands as DISCLOSED. ...` placeholder with a load-bearing paragraph stating: (i) the four previously-UNRESOLVED suites (`phase-2-moderate-priority-only`, `phase-2-moderate-both-dynamic`, `phase-2-realistic-both-dynamic`, `phase-2-sundaeswap-both-dynamic`) were resolved by the Phase 2 output-read pass per Plan 02-02; (ii) post-Phase-4 aggregate validity-threats trust per `docs/phase-2/validity-threats.md` §"Aggregate trust summary" is 2 HIGH + 13 MEDIUM + 4 LOW + 0 UNRESOLVED (no UNRESOLVED suites remain); (iii) the four resolved suites each carry definite MEDIUM or LOW trust verdicts with per-suite Related-RSK and Related-CLM cross-references; (iv) standard-quote drift findings from the three both-dynamic suites are folded into `RSK-standard-user-fee-drift-exposure`'s disclosure-paragraph below; (v) the residual conclusion-validity caveat for the four resolved suites is that their three-seed default cannot license tight magnitude confidence intervals — see `RSK-three-seed-statistical-power`.

4. **`RSK-standard-user-fee-drift-exposure`** (currently line ~327):
   - Verdict line: LIVE → DISCLOSED.
   - EXP / Resolution line: keep `EXP-unresolved-output-read (overlap with RSK-unresolved-suite-claims); if drift unbounded after output-read, escalate to disclosure` but append ` — RESOLVED to DISCLOSED via Plan 05-01 prose-promotion`.
   - Disclosure-paragraph: remove the `*(draft fallback; load-bearing only if Phase 2 output-read surfaces unbounded or spike-amplified standard-quote drift)*` prefix. Light-touch refine the existing prose to integrate the Phase 2 output-read outcome: (i) the both-dynamic suites (`phase-2-moderate-both-dynamic`, `phase-2-realistic-both-dynamic`, `phase-2-sundaeswap-both-dynamic`) resolved with definite trust verdicts per Plan 02-02 and `docs/phase-2/validity-threats.md`; (ii) the observed standard-quote drift bound is reported per suite in `docs/phase-2/validity-threats.md` per-suite trust matrix and is bounded by Ethereum Improvement Proposal 1559 (EIP-1559) `±1/D` per-step on priority-saturated blocks (D=8 ≈ ±12.5% per priority-saturated block × partition-activated cadence) per `docs/phase-2/mechanism-design.md` §"EB binary fullness trigger"; (iii) preserve the existing framing that bounded-drift makes both-dynamic preferable to single-lane EIP-1559 on community-preference grounds; (iv) preserve the existing trade-off framing against the standard-lane fee-stability property that ranking-block-reserved priority-only mechanisms preserve by construction.

5. **`RSK-menu-collapse-to-advocacy`** (currently line ~375):
   - Verdict line: LIVE → DISCLOSED.
   - EXP / Resolution line: keep `EXP-coverage-non-welfare-columns (Phase 2 coverage-check design; cross-reference REQ-COV-03)` but append ` — RESOLVED to DISCLOSED via Plan 05-01 prose-promotion after Phase 2 coverage-check structural mitigation landed`.
   - Disclosure-paragraph: replace the current `TBD — drafted in Phase 4 if test verdict lands as DISCLOSED. Expected Phase 2 path is MITIGATED ...` placeholder with a load-bearing paragraph stating: (i) the Phase 2 coverage check (`docs/phase-2/coverage-check.md`) includes the four non-welfare property columns required by REQ-COV-03 (`anti-bribery`, `signal-source-anchoring`, `standard-user-fee-drift-exposure`, `implementation-complexity`); (ii) each column's cells carry cited evidence — spec section reference, simulator measurement, or "disclosed gap" per Realism Risk identifier (RSK)-NN cross-reference; (iii) the four columns surface real menu-trade-off differences across the five menu options (priority-only-RB-reserved / priority-only-un-reserved / both-dynamic-partitioned / both-dynamic-un-partitioned / single-lane-EIP-1559-control) rather than being uniform decoration; (iv) Cardano Improvement Proposal (CIP) reviewers asking "is this CIP a menu or a single-option recommendation in disguise" can resolve the question by inspecting the four non-welfare columns of `docs/phase-2/coverage-check.md`, which separate the welfare-dominant option from the option that wins on each non-welfare axis; (v) reference Chung and Shi's *Foundations of Transaction Fee Mechanism Design* (Symposium on Discrete Algorithms (SODA) 2023) only as support for the anti-bribery / incentive-compatibility axis, while stating that the remaining non-welfare axes are justified by CPS-0023 review needs and Cardano-specific implementation trade-offs.

After completing all five entry edits, also update the Index-table verdict cells for the five entries (the rows for `RSK-single-seed-precision`, `RSK-three-seed-statistical-power`, `RSK-unresolved-suite-claims`, `RSK-standard-user-fee-drift-exposure`, `RSK-menu-collapse-to-advocacy`) so each Verdict column reads `DISCLOSED`.

Preserve the existing footer prose and reading-guide preamble for this task — those are updated in Task 2 after the sixth entry flips and the distribution count finalises.
  </action>
  <verify>
    <automated>bash -c "set -e; cd /home/will/git/arc-tiered-pricing; grep -A1 '^## RSK-single-seed-precision' docs/phase-2/realism-risks-register.md | grep -q 'DISCLOSED' && echo 'single-seed-precision: DISCLOSED'; grep -A1 '^## RSK-three-seed-statistical-power' docs/phase-2/realism-risks-register.md | grep -q 'DISCLOSED' && echo 'three-seed: DISCLOSED'; grep -A1 '^## RSK-unresolved-suite-claims' docs/phase-2/realism-risks-register.md | grep -q 'DISCLOSED' && echo 'unresolved: DISCLOSED'; grep -A1 '^## RSK-standard-user-fee-drift-exposure' docs/phase-2/realism-risks-register.md | grep -q 'DISCLOSED' && echo 'fee-drift: DISCLOSED'; grep -A1 '^## RSK-menu-collapse-to-advocacy' docs/phase-2/realism-risks-register.md | grep -q 'DISCLOSED' && echo 'menu-collapse: DISCLOSED'; ! grep -q '(draft fallback;' docs/phase-2/realism-risks-register.md && echo 'no draft fallback markers remaining on the five entries (RSK-hash-diversity-policy unchanged until Task 2)' || echo 'WARN: draft fallback markers remain — verify in Task 2 that the only remaining one is RSK-hash-diversity-policy or none'"</automated>
  </verify>
  <acceptance_criteria>
    - Five entries' per-entry `**Verdict:**` line reads `DISCLOSED` (was `LIVE`).
    - Five entries' Index-table row Verdict cell reads `DISCLOSED` (was `LIVE`).
    - The `*(draft fallback; ...)*` prefix is removed from the two entries that previously carried it (`RSK-single-seed-precision`, `RSK-standard-user-fee-drift-exposure`).
    - The three placeholder disclosure-paragraphs (`RSK-three-seed-statistical-power`, `RSK-unresolved-suite-claims`, `RSK-menu-collapse-to-advocacy`) are replaced with load-bearing prose integrating the Phase 2 / Phase 3 / Phase 4 evidence named in the per-entry directive.
    - No `TBD — drafted in Phase 4 if test verdict lands as DISCLOSED.` markers remain on the five entries.
    - Each rewritten disclosure-paragraph expands first-use abbreviations (CIP, BCa, IQR, RSK, EIP-1559, SODA) within the paragraph itself.
    - The footer prose and reading-guide preamble are unchanged by this task — those are Task 2's edits.
  </acceptance_criteria>
  <done>Five register entries are flipped LIVE → DISCLOSED with load-bearing disclosure-paragraphs; the five Index-table rows show `DISCLOSED`.</done>
</task>

<task type="auto">
  <name>Task 2: Flip RSK-hash-diversity-policy + update Index header / reading guide / footer for 24 DISCLOSED distribution</name>
  <files>docs/phase-2/realism-risks-register.md</files>
  <read_first>
    - docs/phase-2/realism-risks-register.md (post-Task-1 state)
    - .planning/realism-tests/hash-diversity-gate/results.md (the Phase 3 17/17 BACKED-eligible cells pass result; cited verbatim in RSK-hash-diversity-policy's disclosure-paragraph)
    - .planning/phases/02-coverage-check-skeleton/02-CONTEXT.md (D-19 strict-gate rule)
    - docs/phase-2/coverage-check.md §"Hash-diversity gate (COV-05; Phase 3 enforces)" (the gate semantics the disclosure-paragraph cites)
    - .planning/phases/05-handoff/05-CONTEXT.md §"D-48" (the directive)
    - .planning/phases/04-refresh-and-anchor/04-07-consistency-report.md (Plan 04-07 reading-guide / footer rewrite as template for the Plan-05-01 update)
  </read_first>
  <action>
Edit `docs/phase-2/realism-risks-register.md` in place to complete the sixth flip and finalise the 24-DISCLOSED narrative.

**Step A — flip RSK-hash-diversity-policy** (currently line ~406):
- Verdict line: LIVE → DISCLOSED.
- EXP / Resolution line: replace `EXP-hash-diversity-policy-decision (Phase 3 COV-05 policy gate; cross-reference REQ-COV-05)` with `EXP-hash-diversity-policy-decision (RESOLVED: strict gate per Phase 2 D-19; Phase 3 hash-diversity-gate result: 17/17 BACKED-eligible cells pass — see .planning/realism-tests/hash-diversity-gate/results.md)`.
- Disclosure-paragraph: replace the current `TBD — drafted in Phase 4 if test verdict lands as DISCLOSED. ...` placeholder with a load-bearing paragraph stating (per D-48): (i) the strict-gate policy was decided in Phase 2 per CONTEXT.md D-19 — the gate text is "A row may only be cited as `BACKED` when the distinct `pricing_event_stream.sha256` count equals the seeds-cited value. Rows whose seed-set collapses to fewer distinct hashes are downgraded to `WEAK` with annotation, or are re-run with different seed values"; (ii) the Phase 3 gate-application pass reported in `.planning/realism-tests/hash-diversity-gate/results.md` confirms 17 of 17 BACKED-eligible cells across TEST-03 + TEST-04 + TEST-07a pass the gate (distinct `pricing_event_stream.sha256` count equals N seeds for each cell); 0 cells downgraded to WEAK from gate failure; 0 cells marked re-run-needed; (iii) the cross-cell SHA-256 identity observed in TEST-07a at `multiplier_floor = 16` between `rb_scarcity_x16_baseline` and `urgency_inversion_x16_correctly_priced` is an across-cell observation that does not violate the within-cell gate; (iv) every `BACKED` row in `docs/phase-2/coverage-check.md` meets this gate by construction; the Cardano Improvement Proposal (CIP) author can rely on `BACKED` cells without re-deriving the gate.
- Index-table row Verdict cell: `LIVE` → `DISCLOSED`.

**Step B — update the consolidated header reading guide** (lines ~14–16 currently):
Locate the sentence beginning `Post-Plan-04-07 verdict distribution: 6 LIVE + 18 DISCLOSED + 0 MITIGATED + 0 DORMANT`. Rewrite the paragraph to read (Plan-04-07-style narrative continuation): "This file is the register through Phase 5 / Wave 1 (Plan 05-01 verdict-flip edits). Post-Plan-05-01 verdict distribution: 0 LIVE + 24 DISCLOSED + 0 MITIGATED + 0 DORMANT (the v1 register's 12 LIVE + 12 DISCLOSED snapshot evolved across Phase 4 Waves 2 + 3 — six flips in Plans 04-06 and 04-07 — and across Phase 5 Wave 1: Plan 05-01 flipped the six remaining LIVE entries (RSK-single-seed-precision, RSK-three-seed-statistical-power, RSK-unresolved-suite-claims, RSK-standard-user-fee-drift-exposure, RSK-menu-collapse-to-advocacy, RSK-hash-diversity-policy) to DISCLOSED with load-bearing disclosure-paragraphs; the prose-promotion edits integrate Phase 2 D-19 strict-gate policy, Phase 3 hash-diversity-gate 17/17 BACKED-eligible pass, Phase 3 TEST-03 + TEST-04 N=20 Bias-corrected and accelerated (BCa) bootstrap confidence intervals, and Phase 2 output-read resolution of the four previously-UNRESOLVED suites and the four non-welfare property columns in `docs/phase-2/coverage-check.md`)."

Preserve the surrounding sentences (per-claim trust ratings live in `docs/phase-2/coverage-check.md`; calibration-provenance triples live in `docs/phase-2/cardano-realism-audit.md`; spike READMEs cited as evidence sources but not re-expressed).

**Step C — update the footer prose** (last paragraph before the file's closing `---` separator if one exists; alternatively the file may end without a horizontal rule — locate by tail-end prose):
Locate the existing "Register through Phase 4 Wave 3 (Plan 04-07)" framing and rewrite to "Register through Phase 5 Wave 1 (Plan 05-01). The six-flip evolution from Plan-04-07's 6 LIVE + 18 DISCLOSED to Plan-05-01's 0 LIVE + 24 DISCLOSED is the final verdict distribution at Phase 5 close; the post-Phase-5 tag `phase-2-cip-evidence-v1` references this distribution as the citable register state." Preserve any existing footer cross-references to spike READMEs, the family-b-decision-2026-05-14 memo, and the mechanism-welfare-impact-2026-05-14 memo.

**Step D — verify Index table totals.** After Step A's Index-table row edit, every row's Verdict column must read DISCLOSED. Count via the verify command below.
  </action>
  <verify>
    <automated>bash -c "set -e; cd /home/will/git/arc-tiered-pricing; verdict_count=$(grep -E '^\*\*Verdict:\*\*' docs/phase-2/realism-risks-register.md | grep -v '^#' | sort | uniq -c | awk '{print $2\"=\"$1}' | tr '\n' ' '); echo \"per-entry verdict counts: $verdict_count\"; index_count=$(awk '/^## Index/,/^## RSK-pool/' docs/phase-2/realism-risks-register.md | grep -oE '\| (LIVE|DISCLOSED|MITIGATED|DORMANT) \|' | sort | uniq -c | awk '{print $2$3$4\"=\"$1}' | tr '\n' ' '); echo \"Index-table verdict counts: $index_count\"; live_per_entry=$(grep -E '^\*\*Verdict:\*\*' docs/phase-2/realism-risks-register.md | grep -v '^#' | grep -c LIVE || true); test $live_per_entry -eq 0 && echo 'PASS: 0 LIVE per-entry'; disclosed_per_entry=$(grep -E '^\*\*Verdict:\*\*' docs/phase-2/realism-risks-register.md | grep -v '^#' | grep -c DISCLOSED); test $disclosed_per_entry -eq 24 && echo 'PASS: 24 DISCLOSED per-entry'; grep -q '0 LIVE + 24 DISCLOSED' docs/phase-2/realism-risks-register.md && echo 'PASS: reading-guide distribution sentence updated'; ! grep -q 'TBD — drafted in Phase 4 if test verdict lands as DISCLOSED' docs/phase-2/realism-risks-register.md && echo 'PASS: no TBD-drafted-in-Phase-4 markers remain'; ! grep -q '(draft fallback;' docs/phase-2/realism-risks-register.md && echo 'PASS: no (draft fallback; markers remain'"</automated>
  </verify>
  <acceptance_criteria>
    - Per-entry `**Verdict:**` line count: 24 DISCLOSED, 0 LIVE, 0 MITIGATED, 0 DORMANT.
    - Index-table Verdict column count: 24 DISCLOSED, 0 LIVE, 0 MITIGATED, 0 DORMANT.
    - The reading-guide preamble contains the string `0 LIVE + 24 DISCLOSED`.
    - No `TBD — drafted in Phase 4 if test verdict lands as DISCLOSED` placeholders remain on any entry.
    - No `(draft fallback;` markers remain on any entry.
    - `RSK-hash-diversity-policy`'s disclosure-paragraph cites the Phase 2 D-19 strict-gate rule verbatim AND the Phase 3 17/17 BACKED-eligible cells pass result AND the TEST-07a across-cell observation framing.
    - The footer prose names Plan 05-01 as the source of the six flips and references the `phase-2-cip-evidence-v1` tag as the citable post-Phase-5 register state.
  </acceptance_criteria>
  <done>Register is at 0 LIVE + 24 DISCLOSED; reading guide and footer reflect Plan-05-01 as the source of the final flip; Index-table and per-entry verdicts agree.</done>
</task>

</tasks>

<verification>
After both tasks complete, the register must satisfy:

```
$ grep -E '^\*\*Verdict:\*\*' docs/phase-2/realism-risks-register.md | sort | uniq -c
     24 **Verdict:** DISCLOSED

$ awk '/^## Index/,/^## RSK-pool/' docs/phase-2/realism-risks-register.md | grep -oE '\| (LIVE|DISCLOSED|MITIGATED|DORMANT) \|' | sort | uniq -c
     24 | DISCLOSED |
```

Plus: 0 occurrences of `(draft fallback;` and 0 occurrences of `TBD — drafted in Phase 4 if test verdict lands as DISCLOSED` across the file.
</verification>

<success_criteria>
1. All 24 `## RSK-<slug>:` entries have `**Verdict:** DISCLOSED`.
2. The `## Index` table's Verdict column reads `DISCLOSED` for all 24 rows.
3. The six previously-LIVE entries' disclosure-paragraphs are load-bearing CIP-pasteable prose integrating the specific Phase 2 / Phase 3 / Phase 4 evidence named in D-47 / D-48 / D-49.
4. The reading-guide preamble's distribution sentence reads `0 LIVE + 24 DISCLOSED + 0 MITIGATED + 0 DORMANT`.
5. The footer prose names Plan 05-01 and the `phase-2-cip-evidence-v1` tag.
6. Each rewritten disclosure-paragraph expands first-use abbreviations within the paragraph itself, per CLAUDE.md §"Conventions / gotchas".
</success_criteria>

<output>
After completion, create `.planning/phases/05-handoff/05-01-SUMMARY.md` per the GSD summary template.
</output>
