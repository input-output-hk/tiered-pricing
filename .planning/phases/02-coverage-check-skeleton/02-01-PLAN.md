---
phase: 02-coverage-check-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/phase-2/coverage-check.md
autonomous: true
requirements: [COV-01, COV-02, COV-06]
tags: [coverage-check, claim-enumeration, menu-options, cip-evidence]
must_haves:
  truths:
    - "docs/phase-2/coverage-check.md exists as a flat table with stable CLM-NN identifiers (append-only) and the file's first heading is '# Coverage Check'"
    - "The table contains at least 20 and at most 50 CLM-NN rows; each row is a per-(claim, menu-option) pair per CONTEXT.md D-13 (denormalised RTM shape)"
    - "Every CLM-NN row carries the full column set per CONTEXT.md D-17: id, claim, menu-option, status, confidence-method, backing-suite, backing-job, seeds-cited, golden-sha256, anti-bribery, signal-source-anchoring, standard-user-fee-drift-exposure, implementation-complexity, related-RSK-ids (14 columns total per Claude's Discretion column-ordering)"
    - "Exactly five distinct menu-option values appear in the menu-option column: priority-only-RB-reserved, priority-only-un-reserved, both-dynamic-partitioned, both-dynamic-un-partitioned, single-lane-EIP-1559-control (per CONTEXT.md D-13)"
    - "Every row's status cell is exactly one of BACKED / WEAK / UNBACKED / OUT-OF-SCOPE (CONTEXT.md D-16)"
    - "The four mandatory non-welfare property columns from CONTEXT.md D-14 are present in the table header: anti-bribery, signal-source-anchoring, standard-user-fee-drift-exposure, implementation-complexity"
    - "The table header includes a hash-diversity-gate semantics line stating COV-05's strict rule per CONTEXT.md D-19 (Phase 3 enforces; Phase 2 documents)"
    - "Claims are enumerated by walking the four claim-source documents in CONTEXT.md D-12 priority order (family-b-decision, family-b-full-sweep-analysis + family-b-results-table, mechanism-welfare-impact, then the seven goldens-pinned suite READMEs / suite YAML headers)"
    - "The user-seeded structural / calibration claims from CONTEXT.md <specifics> are enumerated as CLM-NN rows: mechanism-independence (Family A vs Family B robustness); reorg-safety by construction (Family B closes WR-1); rb-generation-probability = 0.05 anchored to mainnet activeSlotsCoeff; topology-realistic-100 stakes anchored to epoch-582; anti-bribery / standard-user-fee-drift-exposure / implementation-complexity per menu option"
    - "CONTEXT.md decisions D-11..D-15 are honoured: D-11 (four claim classes — welfare / comparative / structural / calibration — all share the CLM-NN namespace), D-12 (extract-then-augment ordering — source documents read first, user augmentation second), D-13 (per-(claim, menu-option) denormalised rows), D-14 (non-welfare cell format '<enum-value> (<citation>)'), D-15 (CLM-NN append-only identifiers)"
  artifacts:
    - path: "docs/phase-2/coverage-check.md"
      provides: "Coverage check skeleton: header with column legend and hash-diversity-gate semantics, table with 20-50 CLM-NN rows enumerated from the four source documents plus user-seeded structural / calibration claims; cells populated with what is available NOW from existing artefacts (many cells UNBACKED with confidence-method 'TBD Phase 3'); plan 02 finalises judgements, populates non-welfare property columns end-to-end, and runs consistency checks"
      contains: "# Coverage Check"
    - path: "docs/phase-2/coverage-check.md"
      provides: "Table header with all 14 columns including the four non-welfare property columns per CONTEXT.md D-14"
      contains: "| id | claim | menu-option |"
  key_links:
    - from: "docs/phase-2/coverage-check.md"
      to: "docs/phase-2/realism-risks-register.md"
      via: "related-RSK-ids column references the 24 stable RSK-NN identifiers from Phase 1"
      pattern: "RSK-"
    - from: "docs/phase-2/coverage-check.md"
      to: ".planning/family-b-decision-2026-05-14.md"
      via: "backing-suite / claim cells cite the headline welfare claims and the Family-B-vs-accumulator framing from the decision memo"
      pattern: "family-b-decision"
    - from: "docs/phase-2/coverage-check.md"
      to: "sim-rs/parameters/phase-2-sweep/suites/.goldens/"
      via: "golden-sha256 column values are read from the seven .sha256 files in the goldens directory"
      pattern: "pricing_event_stream"
---

<objective>
Build the inventory skeleton of `docs/phase-2/coverage-check.md` — a flat Requirements-Traceability-Matrix-style table mapping every menu-item trade-off claim the Cardano Improvement Proposal (CIP) will make to a specific backing simulator job. This plan does the **enumeration and table-skeleton** work: it reads the four claim-source documents in priority order, harvests every claim, enumerates the user-seeded structural / calibration claims that may not surface in those documents, lays out the 14-column header (including the hash-diversity-gate semantics line per COV-05), and writes one CLM-NN row per (claim, menu-option) pair. Cells are populated with what is available NOW from existing artefacts; cells that need Phase 3 cheap-test results carry `status: UNBACKED` with `confidence-method: TBD Phase 3`. Plan 02 finalises status verdicts on rows that have evidence-now (promoting UNBACKED to WEAK where unpinned-suite evidence supports it per D-18), populates the four non-welfare property columns end-to-end, populates `related-RSK-ids` from the register, and runs the consistency checks.

Purpose: Plan 02 cannot start its judgement work without this skeleton existing. The CLM-NN identifier namespace must be stable from this plan onward (append-only per D-15); plan 02 fills in non-welfare cells and verdicts against the already-laid CLM-NN rows. The 14-column header is also laid down by this plan so plan 02 has a fixed shape to populate.

Output:
- One new file: `docs/phase-2/coverage-check.md` with: a header containing the document's purpose, a reading-guide note, a column-legend block, a hash-diversity-gate semantics line per COV-05; a single flat table with 14 columns and 20-50 CLM-NN rows enumerated from the four source documents plus user-seeded augmentations.
- Coverage for COV-01 (single coverage-check file with stable CLM-NN identifiers), COV-02 (every row has the full column set), COV-06 (committable-before-Phase-3 skeleton state with `UNBACKED` rows surfacing Phase 3 compute priorities).
- Cells in the four non-welfare property columns (anti-bribery, signal-source-anchoring, standard-user-fee-drift-exposure, implementation-complexity) are populated where the answer is mechanically determinable from CLAUDE.md / mechanism-design.md / register; cells that require judgement-pass review may carry `TBD plan 02` as a placeholder, but the column itself MUST be present in the header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-coverage-check-skeleton/02-CONTEXT.md
@.planning/phases/01-register-inventory/01-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate claims from the four source documents (D-12 priority order), augment with user-seeded structural / calibration claims, and assign provisional CLM-NN identifiers</name>
  <files>
    No file is written in this task. The deliverable is a working set of (claim, menu-option) pairs held in context, ready for table-writing in Task 2. Do NOT write a transient inventory file — the coverage-check table itself is the output, and plan 02 must not have to read a transient intermediate doc.
  </files>
  <read_first>
    Source documents in CONTEXT.md D-12 priority order (the enumeration sweep covers every welfare, comparative, structural, and calibration claim in these):
    - .planning/family-b-decision-2026-05-14.md (the authoritative mechanism-commit memo with publication framing — primary source for headline welfare claims, the mechanism-independence claim "median |Δ%| ≈ 15% on un-reserved arms, 0/3 and 0/2 sign-flips", and structural claims like "reorg-safe by construction")
    - .planning/family-b-full-sweep-analysis-2026-05-14.md (full-sweep characterisation — primary source for numeric and comparative welfare claims by mechanism arm)
    - .planning/family-b-results-table-2026-05-14.md (numeric results table — primary backing-job source; many of its (mechanism arm × job × seed) cells lift directly into CLM-NN rows)
    - .planning/mechanism-welfare-impact-2026-05-14.md (the empirical Family-B-vs-accumulator characterisation; sources the four sign-flip cells `d4_t50_w32`, `d8_t25_w32`, and `x4_rb_quarter` × 2 arms; sources the "un-reserved arms are mechanism-robust" claim and the "single-lane EIP-1559 collapses by orders of magnitude" claim)
    Then the seven goldens-pinned suite framings:
    - sim-rs/parameters/phase-2-sweep/suites/phase-2-eip1559-robustness.yaml (single-lane EIP-1559 robustness across D and target)
    - sim-rs/parameters/phase-2-sweep/suites/phase-2-eip1559-smoothing.yaml (single-lane EIP-1559 window-length sweep)
    - sim-rs/parameters/phase-2-sweep/suites/phase-2-priority-only-rb-reserved.yaml (RB-reserved priority-only-static-standard at multiplier floors ×4 / ×8 / ×16)
    - sim-rs/parameters/phase-2-sweep/suites/phase-2-priority-only-unreserved.yaml (un-reserved priority-only premium at the same multiplier floors)
    - sim-rs/parameters/phase-2-sweep/suites/phase-2-two-lane-both-dynamic.yaml (both-dynamic in partitioned and un-partitioned forms)
    - sim-rs/parameters/phase-2-sweep/suites/phase-2-rb-scarcity.README.md and phase-2-rb-scarcity.yaml (RB-capacity scarcity as a two-lane experiment; multiplier_floor = 4 per CLAUDE.md "Calibration choices")
    - sim-rs/parameters/phase-2-sweep/suites/phase-2-urgency-inversion.README.md and phase-2-urgency-inversion.yaml (urgency inversion under mis-priced actors; multiplier_floor = 4)
    Phase 1 outputs (RSK-NN namespace plus three Phase-2-facing EXP-NN slugs):
    - docs/phase-2/realism-risks-register.md (the 24 stable RSK-NN entries; the related-RSK-ids column on each CLM-NN row references these; this task's enumeration uses the register's RSK-NN identifiers verbatim)
    - .planning/phases/01-register-inventory/01-02-SUMMARY.md (names the three Phase-2-facing EXP-NN slugs `EXP-unresolved-output-read`, `EXP-coverage-non-welfare-columns`, `EXP-hash-diversity-policy-decision`; names the four UNRESOLVED non-pinned suites `phase-2-moderate-priority-only`, `phase-2-moderate-both-dynamic`, `phase-2-realistic-both-dynamic`, `phase-2-sundaeswap-both-dynamic`; names the TEST-07a sub-requirement EXP-multiplier-floor-16-companion-run)
    CONTEXT.md and conventions (read in full before task starts; decisions D-11..D-21 are locked):
    - .planning/phases/02-coverage-check-skeleton/02-CONTEXT.md (decisions D-11 four-class claim taxonomy; D-12 extract-then-augment ordering; D-13 per-(claim, menu-option) row shape and the five menu-option values; D-14 non-welfare cell mixed-enum-plus-citation format; D-15 CLM-NN append-only convention; D-17 column inventory; D-18 unpinned-suite WEAK rule; D-19 hash-diversity-gate strict rule; D-21 abbreviation-on-first-use; plus Claude's Discretion items: column ordering, status-conflict resolution, UNRESOLVED suites output-read scope, multi-RSK rows, EXP-NN forward references in claim cells)
    - CLAUDE.md §"Mechanism abstractions" (mechanism design context for assessing anti-bribery and standard-user-fee-drift-exposure per menu option); §"Calibration choices" (rb-generation-probability anchoring, topology-realistic-100 stake snapshot, multiplier-floor 4 vs 16 cases, the four un-anchored controller knobs); §"Running the suites" (the 7-suite table the M3-vs-M4 framing comes from); §"Conventions / gotchas" (abbreviation-on-first-use rule per D-21)
  </read_first>
  <action>
    Sweep the four source documents in CONTEXT.md D-12 priority order, then the seven goldens-pinned suite framings, and extract every claim. A "claim" is any statement that asserts (a) a welfare metric value or ordering for a menu option (welfare or comparative claim), (b) a structural property holding by construction (structural claim — e.g. reorg-safety, formal anti-bribery, validity-rule presence), or (c) a calibration value anchored to mainnet data or to a spec default (calibration claim).

    **Phase 1: Source-document extraction (D-12 step 1-4).** For each source document, harvest every claim statement and record in context (do NOT write a transient file):
    1. A one-line summary of the claim (no jargon-only sentence; expand abbreviations on first use per D-21)
    2. The claim's class per D-11: welfare / comparative / structural / calibration
    3. Which menu option(s) the claim applies to — one of the five values in D-13 (`priority-only-RB-reserved`, `priority-only-un-reserved`, `both-dynamic-partitioned`, `both-dynamic-un-partitioned`, `single-lane-EIP-1559-control`) OR "all five" if it is a structural property that holds across all options OR "comparison: (option-A, option-B)" if it is a comparative claim
    4. The backing-suite path (e.g. `sim-rs/parameters/phase-2-sweep/suites/phase-2-two-lane-both-dynamic.yaml`) if the claim has empirical backing, OR `—` if the claim is structural / by-construction
    5. The backing-job slug (e.g. `d4_t50_w32`) if the claim has a specific job behind it, OR `—` for structural / aggregate claims
    6. Provisional confidence-method per D-11: e.g. `single-seed point estimate (N=1)`, `sign-coherence count from 33-job smoke`, `by-construction (spec §<heading>)`, `mainnet-anchored (value, source, date)`, `TBD Phase 3 (paired-bootstrap BCa N=20 awaiting TEST-03)`
    7. The four sign-flip cells (`d4_t50_w32`, `d8_t25_w32`, `x4_rb_quarter` under rb-reserved-priority, `x4_rb_quarter` under partitioned) from mechanism-welfare-impact-2026-05-14.md MUST surface as either a welfare claim per applicable menu option (with confidence-method noting the sign-flip and `EXP-sign-flip-variance (→ TEST-03)`) or a comparative claim, per the source-document framing.

    **Phase 2: User-seeded augmentation (D-12 step 2).** Ensure the following claims from CONTEXT.md `<specifics>` are enumerated even if the four source documents above do not surface them as named claims:
    - **Mechanism-independence (Family A vs Family B robustness):** one row per menu option. Un-reserved arms (priority-only-un-reserved, both-dynamic-un-partitioned) land WEAK at this stage with confidence-method `paired Family-A-vs-B delta from 33-job sundaeswap-smoke (median |Δ%| ≈ 15%, 0/3 and 0/2 sign-flips), single-seed evidence at N=1`. RB-reserved + partitioned arms land WEAK with disclosure of the four sign-flip cells. Single-lane EIP-1559 control row notes the "collapses by orders of magnitude" finding from mechanism-welfare-impact-2026-05-14.md.
    - **Reorg-safety by construction (Family B closes WR-1):** one row per menu option (all five rows BACKED via construction). Confidence-method: `by-construction (mechanism-design.md §"Chain-derived controller" + family-b-decision-2026-05-14.md §"What changed")`. Backing-suite: `—`. Backing-job: `—`.
    - **Calibration: rb-generation-probability = 0.05 anchored to mainnet activeSlotsCoeff:** one row per menu option (all five BACKED). Confidence-method: `mainnet-anchored (0.05, Cardano mainnet activeSlotsCoeff, 2026-05-14 retrieval)` per cardano-realism-audit.md §"RB cadence". Backing-suite: `—`. Backing-job: `—`.
    - **Calibration: topology-realistic-100.yaml stakes are mass-stratified epoch-582 mainnet snapshot:** one row per menu option (all five BACKED). Confidence-method: `mainnet-anchored (epoch 582 stake snapshot, Cardano mainnet on-chain state, 2026-05-14 retrieval)` per `.planning/spikes/006-curve-design/README.md` and `topology-realistic-100.yaml` header comment. Backing-suite: `—`. Backing-job: `—`.
    - **Anti-bribery property per menu option:** the four non-welfare property columns are populated row-by-row in Task 2 against every CLM-NN row, but the user-seeded enumeration in this task ensures at least one row per menu option exists whose claim is specifically the anti-bribery property. Per CONTEXT.md `<specifics>`: un-reserved arms get `informal`; RB-reserved arms get a stronger property (`formal` if mechanism-design.md proves it; otherwise `informal`); single-lane EIP-1559 control gets `absent`. Cite `mechanism-design.md` sections.
    - **Standard-user-fee-drift-exposure per menu option:** ensure at least one row per menu option exists whose claim is the standard-user-fee-drift-exposure property. Per CONTEXT.md `<specifics>`: both-dynamic arms (partitioned and un-partitioned) get `exposed` or `bounded` depending on whether mechanism-design.md / cardano-realism-audit.md provides a `Δfee/byte` bound; priority-only arms (RB-reserved and un-reserved) get `none` because the standard-lane fee is not perturbed by priority traffic per CLAUDE.md "Calibration choices" §"Both-dynamic standard signal source"; single-lane EIP-1559 control gets `bounded` (the EIP-1559 step bound).
    - **Implementation-complexity per menu option:** one row per menu option. Per CONTEXT.md `<specifics>`: all five options share most implementation cost; differences are validity-rule presence (RB-reserved variants), partition-activation tracking (partitioned variant), un-reserved priority-only signal-source choice (low LoC); EXP must estimate LoC delta from `sim-rs/sim-core/src/tx_pricing/` and the linear-leios block-production additions. Use enum `low` / `medium` / `high` per D-14 with a Lines-of-Code (LoC) estimate citation.

    **Phase 3: Calibration-claim enumeration from cardano-realism-audit.md.** In addition to the two user-seeded calibration claims above, walk `docs/phase-2/cardano-realism-audit.md` to identify any further calibration anchors that warrant a CLM-NN row (e.g. `rb-body-max-size-bytes` magnitude, `mempool-max-total-size-bytes` per CLAUDE.md "Calibration choices" §"Mempool cap"). For each: one row per menu option, all five BACKED, with `mainnet-anchored (value, source, date)` confidence-method. Do not exceed ~3-5 calibration claims total beyond the two user-seeded ones — over-enumerating calibration risks producing a flat audit-document mirror rather than a CIP-claim-shaped table.

    **Phase 4: Provisional CLM-NN assignment.** Once Phase 1-3 enumeration produces a working set of (claim, menu-option) pairs, group identical claims that span multiple menu options into a single distinct claim-text with one CLM-NN per (claim, menu-option) row per D-13. Assign sequential CLM-NN identifiers from `CLM-01` upward, append-only per D-15. The CLM-NN namespace is fixed by this task; plan 02 does not renumber.

    Target row count: ~25-40 rows per CONTEXT.md D-11 expected total (5-10 distinct claim types × 5 menu options, denormalised; with some claims applying to fewer than all five options — e.g. validity-rule presence applies only to RB-reserved variants — the count may land in the 20-50 range). Do not inflate to hit a target.

    Pitfalls to avoid:
    - Do NOT compress identical-claim-across-options rows into a single row. D-13 explicitly requires one row per (claim, menu-option) pair even when the claim text is identical.
    - Do NOT enumerate the 12 unpinned demand-regime suites as goldens-pinned. Per D-18 they are evidence sources for `WEAK` verdicts on rows whose claims they cover; the unpinned-suite evidence is folded into the `backing-suite` cell of the relevant row with confidence-method noting the unpinned-suite nature. Promoting an unpinned suite to goldens-pinned is out of scope per REQ-COV-04.
    - Do NOT skip the four sign-flip cells (`d4_t50_w32`, `d8_t25_w32`, `x4_rb_quarter` × 2 arms) — they are explicitly load-bearing for the `EXP-sign-flip-variance (→ TEST-03)` Phase 3 work and must each surface as either a welfare claim with that EXP cross-reference in the `claim` cell (Claude's Discretion: EXP-NN forward references) or as an explicit comparative claim.
    - Do NOT extract per-claim trust ratings from `docs/phase-2/validity-threats.md` and try to re-express them here — those are absorbed into the `confidence-method` cells of CLM-NN rows in plan 02's judgement pass; this task focuses on enumeration from family-B trio + suite framings.
  </action>
  <verify>
    <automated>test -f .planning/family-b-decision-2026-05-14.md && test -f .planning/family-b-full-sweep-analysis-2026-05-14.md && test -f .planning/family-b-results-table-2026-05-14.md && test -f .planning/mechanism-welfare-impact-2026-05-14.md && test -f sim-rs/parameters/phase-2-sweep/suites/phase-2-rb-scarcity.README.md && test -f sim-rs/parameters/phase-2-sweep/suites/phase-2-urgency-inversion.README.md && test -f docs/phase-2/realism-risks-register.md</automated>
  </verify>
  <acceptance_criteria>
    - All four claim-source documents in CONTEXT.md D-12 plus the two M4 suite READMEs and the realism-risks register are read (verified by the automated `test -f` check above)
    - A working set of (claim, menu-option) pairs covering all four claim classes (welfare / comparative / structural / calibration per D-11) is held in context, with one provisional CLM-NN identifier per (claim, menu-option) pair, ready for Task 2's table-writing
    - The five user-seeded structural / calibration claims from CONTEXT.md `<specifics>` (mechanism-independence; reorg-safety; rb-generation-probability anchoring; topology-realistic-100 anchoring; anti-bribery / standard-user-fee-drift-exposure / implementation-complexity per option) are enumerated even if not surfaced as named "claims" in the four source documents
    - The four sign-flip cells (`d4_t50_w32`, `d8_t25_w32`, `x4_rb_quarter` × 2 arms) each surface as at least one CLM-NN row with confidence-method noting `EXP-sign-flip-variance (→ TEST-03)` per Claude's Discretion EXP-NN forward-reference rule
    - The four UNRESOLVED non-pinned suites named in Phase 1 SUMMARY-2 (`phase-2-moderate-priority-only`, `phase-2-moderate-both-dynamic`, `phase-2-realistic-both-dynamic`, `phase-2-sundaeswap-both-dynamic`) are noted in context as targets for the Phase 2 output-read pass (`EXP-unresolved-output-read`); cell-level output-read is plan 02's work but Task 1 surfaces which claims may be supported by these suites
    - The provisional CLM-NN count lands in the 20-50 range per the D-11 expected total
  </acceptance_criteria>
  <done>
    Source documents are read; a working set of (claim, menu-option) pairs covering all four claim classes is held in context with provisional CLM-NN identifiers assigned. The five user-seeded claims from CONTEXT.md `<specifics>` are enumerated. The four sign-flip cells are surfaced with EXP-NN forward references. Ready for Task 2's table-writing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the coverage-check.md file with header, column legend, hash-diversity-gate semantics line, and one CLM-NN row per (claim, menu-option) pair enumerated in Task 1</name>
  <files>
    docs/phase-2/coverage-check.md (CREATE — new file)
  </files>
  <read_first>
    - .planning/phases/02-coverage-check-skeleton/02-CONTEXT.md (decisions D-13 five menu-option values; D-14 non-welfare cell format; D-15 CLM-NN append-only convention; D-16 verdict vocabulary; D-17 column inventory; D-19 hash-diversity-gate strict rule for the header; D-21 abbreviation-on-first-use; plus Claude's Discretion column-ordering)
    - docs/phase-2/realism-risks-register.md (the 24 RSK-NN identifiers — read the Index table or the section headings; the `related-RSK-ids` column on each CLM-NN row references these directly)
    - CLAUDE.md §"Conventions / gotchas" (abbreviation-on-first-use rule)
    - sim-rs/parameters/phase-2-sweep/suites/.goldens/*.sha256 (the seven .sha256 files; the `golden-sha256` column values come from these files for rows backed by goldens-pinned suites — read the files, copy the first 12-16 hex characters per row, document the truncation)
    - The working set from Task 1 (held in context)
  </read_first>
  <action>
    Write `docs/phase-2/coverage-check.md` with the following structure (verbatim).

    **1. Header (top of file):**

    ```
    # Coverage Check

    **Status:** Skeleton (Phase 2; plan 01-01 enumeration; plan 01-02 finalisation pending)
    **Scope:** Per-claim coverage map for the Cardano Improvement Proposal (CIP) responding to CPS-0023 ("Urgency Signaling"). Each row is one `(claim, menu-option)` pair; rows are atomic and greppable by claim, menu-option, status, or related-RSK reference.
    **Identifier convention:** `CLM-NN` (append-only; never renumbered) per Leios `docs/ImpactAnalysis.md` precedent. Cross-references to the realism-risks register use the `RSK-NN` identifiers from `docs/phase-2/realism-risks-register.md` directly.
    **Verdict vocabulary:** `BACKED` / `WEAK` / `UNBACKED` / `OUT-OF-SCOPE`. Different vocabulary from the register's `LIVE` / `DORMANT` / `MITIGATED` / `DISCLOSED`. `BACKED` requires the hash-diversity gate (see "Hash-diversity gate" section below).

    ## Reading guide

    This file is the single source of truth for menu-item-trade-off claim coverage. Each row maps one `(claim, menu-option)` pair to a specific backing simulator job (or to a by-construction citation, or to a mainnet calibration triple). Per-claim trust ratings on the upstream simulator substrate live in `docs/phase-2/validity-threats.md`; this file does not duplicate those — it absorbs them into per-row `confidence-method` cells.

    The four CIP menu options (`priority-only-RB-reserved`, `priority-only-un-reserved`, `both-dynamic-partitioned`, `both-dynamic-un-partitioned`) are evaluated against the single-lane `single-lane-EIP-1559-control` baseline. The control is retained as control evidence per `.planning/PROJECT.md`; it is not promoted as a CIP menu item.

    ## Column legend

    | Column | Type | Vocabulary / format |
    |--------|------|---------------------|
    | `id` | identifier | `CLM-NN` (append-only; never renumbered) |
    | `claim` | text | Plain prose; first-use abbreviations expanded; may include `EXP-<slug>` parenthetical forward-references to Phase 3 / Phase 4 work |
    | `menu-option` | enum (5 values) | `priority-only-RB-reserved` / `priority-only-un-reserved` / `both-dynamic-partitioned` / `both-dynamic-un-partitioned` / `single-lane-EIP-1559-control` |
    | `status` | enum (4 values) | `BACKED` / `WEAK` / `UNBACKED` / `OUT-OF-SCOPE` |
    | `confidence-method` | text | e.g. `paired-bootstrap Bias-corrected and accelerated (BCa) N=20`, `sign-coherence N=5`, `by-construction (spec §X)`, `mainnet-anchored (value, source, date)`, `TBD Phase 3` |
    | `backing-suite` | path or `—` | Suite YAML path under `sim-rs/parameters/phase-2-sweep/suites/`, or `—` for structural / calibration claims |
    | `backing-job` | slug or `—` | Job slug within the suite, or `—` |
    | `seeds-cited` | integer or `—` | Number of seeds whose results back this row, or `—` |
    | `golden-sha256` | hex (first 12-16 chars) or `—` | Truncated `pricing_event_stream.sha256` from `.goldens/<suite>.sha256`; full hash resolved by file lookup |
    | `anti-bribery` | enum + citation | `formal` / `informal` / `absent` followed by ` (<citation>)` per CONTEXT.md D-14 |
    | `signal-source-anchoring` | enum + citation | `mainnet-data-cited` / `spec-default` / `unanchored` followed by ` (<citation-or-RSK>)` per D-14; the four un-anchored controller knobs (window-length 32, multiplier-floor 4, multiplier-floor 16, lane-signal-source) MUST appear as `unanchored (RSK-NN)` in the rows where their value is load-bearing |
    | `standard-user-fee-drift-exposure` | enum + citation | `none` / `bounded` / `exposed` followed by ` (<citation-or-bound>)` per D-14 |
    | `implementation-complexity` | enum + LoC estimate | `low` / `medium` / `high` followed by ` (<LoC-or-modules-citation>)` per D-14 |
    | `related-RSK-ids` | comma-separated RSK-NN list | References to `docs/phase-2/realism-risks-register.md`; multiple RSK-NN per row are comma-separated; no per-RSK sub-rows per Claude's Discretion |

    ## Hash-diversity gate (COV-05; Phase 3 enforces)

    Per `.planning/REQUIREMENTS.md` REQ-COV-05 and CONTEXT.md D-19 (strict rule):

    > A row may only be cited as `BACKED` when the distinct `pricing_event_stream.sha256` count equals the seeds-cited value. Rows whose seed-set collapses to fewer distinct hashes are downgraded to `WEAK` with annotation, or are re-run with different seed values.

    The gate is enforced in Phase 3 after multi-seed runs land. Phase 2 documents the gate semantics but does not apply it (BACKED promotions are Phase 3 work).
    ```

    **2. The table.** Write a single Markdown table whose header row is the 14 columns from the column legend above (in the same order). Below the header, write one row per CLM-NN in the working set from Task 1.

    Cell-population rules for this enumeration plan (plan 02 finalises):

    - **`id`** — sequential `CLM-NN` starting from `CLM-01`. Append-only per D-15.
    - **`claim`** — prose; expand abbreviations on first use per D-21. Include `EXP-<slug>` forward references per Claude's Discretion (e.g. for sign-flip cells: `... (EXP-sign-flip-variance → TEST-03)`).
    - **`menu-option`** — exactly one of the five enum values per D-13.
    - **`status`** — for this plan, default to `UNBACKED` for any row whose claim requires multi-seed evidence (welfare claims, comparative claims) and to `BACKED` for any row whose claim is structural-by-construction or anchored to a mainnet calibration triple. Plan 02 promotes `UNBACKED` to `WEAK` where the unpinned-suite output-read pass surfaces single-seed evidence (per D-18).
    - **`confidence-method`** — for `UNBACKED` rows whose backing test exists as an `EXP-NN` in the register, write `TBD Phase 3 (<EXP-slug> → TEST-NN)` (e.g. `TBD Phase 3 (EXP-canonical-variance → TEST-04)`). For structural rows: `by-construction (<file path> §<heading>)`. For calibration rows: `mainnet-anchored (<value>, <source>, <date>)`.
    - **`backing-suite`** — for empirical rows that map onto a goldens-pinned suite, write the relative path under `sim-rs/parameters/phase-2-sweep/suites/`. For empirical rows backed only by an unpinned suite (D-18 case), write the unpinned suite's path. For structural / calibration rows, write `—`.
    - **`backing-job`** — for empirical rows, write the job slug (e.g. `d4_t50_w32`, `d8_t25_w32`, `x4_rb_quarter_rb_reserved_priority`, `x4_rb_quarter_partitioned`); for the canonical menu-item rows whose job slug is yet to be selected, write `TBD plan 02`. For structural / calibration rows, write `—`.
    - **`seeds-cited`** — for `UNBACKED` rows: `TBD Phase 3`. For structural rows backed by-construction: `—`. For calibration rows: `—`. For rows backed by an existing single-seed run (the 33-job sundaeswap-smoke or the seven goldens-pinned suite single-seed runs at seed=1), write the actual seed count (typically `1`).
    - **`golden-sha256`** — for rows backed by a goldens-pinned suite, read the suite's `.sha256` file at `sim-rs/parameters/phase-2-sweep/suites/.goldens/<suite>.sha256` and write the first 12-16 hex characters of the file's contents; document the truncation in the column legend. For structural / calibration / unpinned-suite rows: `—`.
    - **`anti-bribery`** — populate per CONTEXT.md `<specifics>` rules. Priority-only-RB-reserved: `formal` if `docs/phase-2/mechanism-design.md` proves it (RB-reserved validity rule excludes standard-fee txs by construction); otherwise `informal`. Priority-only-un-reserved: `informal` (the premium is empirically present but not load-bearing on a formal argument). Both-dynamic-partitioned: `informal` with the partition-activation flag caveat. Both-dynamic-un-partitioned: `informal`. Single-lane-EIP-1559-control: `absent`. Cite `mechanism-design.md` §<section> in every cell; if the citation requires plan 02 review, write `<enum> (TBD plan 02)`.
    - **`signal-source-anchoring`** — for rows where the load-bearing parameter is one of the four un-anchored controller knobs (window-length 32, multiplier-floor 4, multiplier-floor 16, lane-signal-source), write `unanchored (<RSK-NN>)` per D-14, where `<RSK-NN>` is `RSK-un-anchored-controller-knobs` (or a sub-RSK if the register used four sub-RSKs — confirm against `docs/phase-2/realism-risks-register.md`; the register uses a single umbrella entry `RSK-un-anchored-controller-knobs` per Phase 1 SUMMARY-2). For rows where the value matches an EIP-1559 spec default, write `spec-default (<citation>)`. For rows where the parameter is anchored to mainnet data per `cardano-realism-audit.md`, write `mainnet-data-cited (<citation>)`. EVERY ROW must have a non-empty `signal-source-anchoring` cell — if the rule is unclear, write `unanchored (RSK-un-anchored-controller-knobs)` as a conservative default and flag for plan 02 review.
    - **`standard-user-fee-drift-exposure`** — populate per CONTEXT.md `<specifics>` rules. Priority-only-RB-reserved: `none` (standard-lane fee is not perturbed by priority traffic per CLAUDE.md "Calibration choices" §"Both-dynamic standard signal source" — standard sample does not fire on RB-reserved RBs). Priority-only-un-reserved: `none` (same reason; un-reserved priority-only uses the priority-paying-bytes / total-block-capacity signal which does not perturb standard quote). Both-dynamic-partitioned: `bounded` (the partition activation gates standard-lane drift; cite the partition-activation flag); for the exact Δfee/byte bound, write `bounded (TBD plan 02)`. Both-dynamic-un-partitioned: `exposed` (un-bounded perturbation of standard-lane fee under heavy priority load; cite `RSK-standard-user-fee-drift-exposure`). Single-lane-EIP-1559-control: `bounded` (the EIP-1559 step bound; cite `mechanism-design.md` §<section>).
    - **`implementation-complexity`** — per CONTEXT.md `<specifics>`: priority-only-RB-reserved is `medium` (validity rule + RB-reserved priority capacity); priority-only-un-reserved is `low` (the un-reserved priority-only signal-source choice is option 1 per CLAUDE.md "Calibration choices"); both-dynamic-partitioned is `medium` (partition-activation flag tracking); both-dynamic-un-partitioned is `medium` (multiplier-floor invariant enforcement); single-lane-EIP-1559-control is `medium` (the existing EIP-1559 step). All five share the chain-derived controller machinery. Cite `sim-rs/sim-core/src/tx_pricing/` LoC delta; for the exact LoC estimate, write `<enum> (TBD plan 02 — measure LoC delta vs baseline)`.
    - **`related-RSK-ids`** — populate with the most directly relevant RSK-NN identifiers from `docs/phase-2/realism-risks-register.md`. Examples: rows about welfare claims at single-seed precision cite `RSK-single-seed-precision`; rows where the load-bearing knob is one of the four un-anchored controllers cite `RSK-un-anchored-controller-knobs`; rows referencing mainnet calibration values cite `RSK-calibration-stale-stake-snapshot` (when the snapshot freshness is the relevant risk) or `RSK-pool-count` (when the 100-pool topology is the relevant scope). Multi-RSK rows comma-separate per Claude's Discretion. If unsure, write `TBD plan 02` and plan 02's consistency-verification pass resolves them.

    **3. Footer:**

    ```
    ---

    *Phase 2 coverage-check skeleton; plan 02-02 finalises judgements, populates non-welfare property column cells where plan 02-01 left `TBD plan 02`, and runs cross-reference consistency checks against the register.*
    ```

    **Abbreviations to expand on first use throughout the file** per D-21 / CLAUDE.md "Conventions / gotchas":
    - `Cardano Improvement Proposal (CIP)` — in the Scope line
    - `Requirements Traceability Matrix (RTM)` — if the term is used in the reading guide
    - `Lines-of-Code (LoC)` — in the column legend's `implementation-complexity` row
    - `Bias-corrected and accelerated (BCa) bootstrap` — in the column legend's `confidence-method` row
    - `Paired Seed Evaluation (PSE)` — first use in a row whose confidence-method references it
    - `Inter-Quartile Range (IQR)` — first use in a row whose confidence-method references it
    - `Ethereum Improvement Proposal 1559 (EIP-1559)` — first use; the menu option `single-lane-EIP-1559-control` is the trigger
    - `Ranking Block (RB)` — first use; the menu option `priority-only-RB-reserved` is the trigger
    - `Endorser Block (EB)` — if referenced in any row
    - `Symposium on Discrete Algorithms (SODA)` — if referenced
    - `Conference on Computer and Communications Security (CCS)` — if referenced
    - `Advances in Financial Technologies (AFT)` — if referenced

    Do NOT write final BACKED-confidence text or paired-bootstrap BCa results in this task — those are Phase 3 work. Phase 2's job is the skeleton; UNBACKED rows are the deliverable for claims awaiting Phase 3 evidence.
  </action>
  <verify>
    <automated>test -f docs/phase-2/coverage-check.md && grep -q "^# Coverage Check" docs/phase-2/coverage-check.md && grep -c "^| CLM-" docs/phase-2/coverage-check.md | awk '{ if ($1 >= 20 && $1 <= 50) exit 0; else exit 1 }' && grep -q "Hash-diversity gate" docs/phase-2/coverage-check.md && grep -q "anti-bribery" docs/phase-2/coverage-check.md && grep -q "signal-source-anchoring" docs/phase-2/coverage-check.md && grep -q "standard-user-fee-drift-exposure" docs/phase-2/coverage-check.md && grep -q "implementation-complexity" docs/phase-2/coverage-check.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/phase-2/coverage-check.md` exists and its first heading is `# Coverage Check`
    - File contains `## Reading guide` section
    - File contains `## Column legend` section listing all 14 columns
    - File contains `## Hash-diversity gate (COV-05; Phase 3 enforces)` section quoting the strict rule per D-19
    - The table header row contains all 14 columns in the order specified by Claude's Discretion column-ordering: `id | claim | menu-option | status | confidence-method | backing-suite | backing-job | seeds-cited | golden-sha256 | anti-bribery | signal-source-anchoring | standard-user-fee-drift-exposure | implementation-complexity | related-RSK-ids` (verify via `grep -E '^\| id \| claim \| menu-option \| status \| confidence-method \| backing-suite \| backing-job \| seeds-cited \| golden-sha256 \| anti-bribery \| signal-source-anchoring \| standard-user-fee-drift-exposure \| implementation-complexity \| related-RSK-ids \|$' docs/phase-2/coverage-check.md` returns at least one hit, or the equivalent — leading-pipe / trailing-pipe tolerant)
    - The table contains at least 20 and at most 50 `^| CLM-` rows (verify via `grep -c "^| CLM-" docs/phase-2/coverage-check.md` in the 20-50 range)
    - The menu-option column contains exactly 5 distinct values: `priority-only-RB-reserved`, `priority-only-un-reserved`, `both-dynamic-partitioned`, `both-dynamic-un-partitioned`, `single-lane-EIP-1559-control` (verify via extracting column 3 from the CLM- rows and `sort -u` — must yield exactly those 5 strings)
    - Every row's status cell is one of `BACKED`, `WEAK`, `UNBACKED`, `OUT-OF-SCOPE` (verify via extracting column 4 from the CLM- rows and ensuring `sort -u` yields a subset of those 4 strings)
    - At least one row references each of the four sign-flip cells: `d4_t50_w32`, `d8_t25_w32`, `x4_rb_quarter` (twice — under rb-reserved-priority and partitioned); verify via `grep -c "d4_t50_w32\|d8_t25_w32\|x4_rb_quarter" docs/phase-2/coverage-check.md` returns at least 4
    - The four un-anchored controller knobs each appear in at least one row's `signal-source-anchoring` cell as `unanchored (RSK-un-anchored-controller-knobs)` per CONTEXT.md D-14 (verify via `grep -c "unanchored (RSK-un-anchored-controller-knobs)" docs/phase-2/coverage-check.md` returns at least 4; alternatively, if any of the four sub-RSK identifiers are used in the register, those substitute)
    - The user-seeded structural / calibration claims from CONTEXT.md `<specifics>` are represented: `grep -i "reorg-safe\|reorg safety\|Family B closes WR-1\|chain-derived"` returns at least 1 hit; `grep -i "rb-generation-probability\|activeSlotsCoeff"` returns at least 1; `grep -i "epoch-582\|epoch 582\|mass-stratified"` returns at least 1
    - Abbreviations expanded on first use: `grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/coverage-check.md`; `grep -q "Lines-of-Code (LoC)" docs/phase-2/coverage-check.md`; `grep -q "Bias-corrected and accelerated (BCa) bootstrap" docs/phase-2/coverage-check.md`; `grep -q "Ethereum Improvement Proposal 1559 (EIP-1559)" docs/phase-2/coverage-check.md`
    - For every row with `status: BACKED`, either (a) the `backing-suite` cell is `—` and the row is structural / calibration (confidence-method begins with `by-construction` or `mainnet-anchored`), OR (b) the `golden-sha256` cell is a hex-prefix non-empty value (verify by walking BACKED rows and checking the two cells; for plan 01 enumeration, this rule may admit `TBD plan 02` in `golden-sha256` if the row's BACKED status is from a structural claim — plan 02 will tighten)
  </acceptance_criteria>
  <done>
    `docs/phase-2/coverage-check.md` exists with header, reading guide, column legend, hash-diversity-gate semantics, and a single flat table with 20-50 CLM-NN rows. All 14 columns are present in the header. All five menu-option enum values are used; every status value is from the four-value vocabulary; the four sign-flip cells are referenced; the four un-anchored controller knobs surface in `signal-source-anchoring` cells; the user-seeded structural / calibration claims are enumerated. Cells that require plan 02 judgement carry `TBD plan 02` markers — plan 02 grep-finds and finalises them. Abbreviations expanded on first use. The file is in a state where plan 02 can read it, grep for `TBD plan 02`, and finalise the judgement fields without further enumeration work.
  </done>
</task>

</tasks>

<threat_model>
No security surface — pure-documentation phase; no executable code, secrets, auth, or external inputs introduced. The file is read by future planners, the CIP author, and reviewers; it is not a runtime artefact. No `<threat_model>` STRIDE register table is needed.
</threat_model>

<verification>
After Task 2 completes, run the following acceptance checks:

```bash
test -f docs/phase-2/coverage-check.md
grep -q "^# Coverage Check" docs/phase-2/coverage-check.md
grep -c "^| CLM-" docs/phase-2/coverage-check.md  # expect 20-50
grep -q "Hash-diversity gate" docs/phase-2/coverage-check.md
grep -q "anti-bribery" docs/phase-2/coverage-check.md
grep -q "signal-source-anchoring" docs/phase-2/coverage-check.md
grep -q "standard-user-fee-drift-exposure" docs/phase-2/coverage-check.md
grep -q "implementation-complexity" docs/phase-2/coverage-check.md

# Menu-option enum: exactly 5 distinct values
grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $4 }' | sed 's/^ *//;s/ *$//' | sort -u
# Expected output: 5 lines — priority-only-RB-reserved, priority-only-un-reserved, both-dynamic-partitioned, both-dynamic-un-partitioned, single-lane-EIP-1559-control

# Status enum: subset of 4 values
grep "^| CLM-" docs/phase-2/coverage-check.md | awk -F'|' '{ print $5 }' | sed 's/^ *//;s/ *$//' | sort -u
# Expected: subset of BACKED, WEAK, UNBACKED, OUT-OF-SCOPE

# Sign-flip cells appear
grep -c "d4_t50_w32\|d8_t25_w32\|x4_rb_quarter" docs/phase-2/coverage-check.md  # expect >= 4

# Four un-anchored controller knobs surface in signal-source-anchoring
grep -c "unanchored (RSK-un-anchored-controller-knobs)" docs/phase-2/coverage-check.md  # expect >= 4

# Abbreviations expanded on first use
grep -q "Cardano Improvement Proposal (CIP)" docs/phase-2/coverage-check.md
grep -q "Bias-corrected and accelerated (BCa) bootstrap" docs/phase-2/coverage-check.md
grep -q "Ethereum Improvement Proposal 1559 (EIP-1559)" docs/phase-2/coverage-check.md
```

All should pass before plan 02 begins.
</verification>

<success_criteria>
- `docs/phase-2/coverage-check.md` exists with header, reading guide, column legend, and hash-diversity-gate semantics section
- 20-50 CLM-NN rows enumerated from the four source documents plus user-seeded augmentations
- All 14 columns present in the table header per Claude's Discretion column ordering
- All five menu-option enum values used per D-13
- All four sign-flip cells referenced per CONTEXT.md `<specifics>` and Phase 1 SUMMARY-2's EXP-NN slugs
- The four un-anchored controller knobs surface in `signal-source-anchoring` cells per D-14
- The user-seeded structural / calibration claims from CONTEXT.md `<specifics>` are enumerated
- Abbreviations expanded on first use per D-21
- CLM-NN namespace is fixed (append-only per D-15) — plan 02 does not renumber
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-check-skeleton/02-01-SUMMARY.md` per the standard summary template, recording:
- Final CLM-NN entry count
- Breakdown by claim class (welfare / comparative / structural / calibration counts)
- Breakdown by initial status (UNBACKED count, BACKED count if any from structural / calibration rows, WEAK count if any from preliminary unpinned-suite recognition)
- Any (claim, menu-option) pairs that surfaced from the inventory pass beyond the planner's projection in Task 1's action (acceptable; the planner's list was a projection, not a contract)
- The four sign-flip cells' final CLM-NN assignments
- Any `TBD plan 02` markers in the file that plan 02 must finalise — counted by category (non-welfare property cells; backing-job slugs; related-RSK-ids; golden-sha256 prefixes)
</output>
