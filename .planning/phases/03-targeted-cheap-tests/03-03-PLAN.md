---
phase: 03-targeted-cheap-tests
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - .planning/realism-tests/hash-diversity-gate/results.md
  - docs/phase-2/coverage-check.md
autonomous: true
requirements: [COV-05]
must_haves:
  truths:
    - "Every BACKED-labelled row in docs/phase-2/coverage-check.md has been verified against the strict hash-diversity gate per CONTEXT.md D-19 + REQUIREMENTS.md COV-05 (distinct pricing_event_stream.sha256 count = seeds-cited)"
    - "Any BACKED row whose hash-diversity check failed (distinct count < seeds count) has been downgraded to WEAK with annotation `hash collision detected: distinct count = K < N` OR flagged for Phase 4 re-run with different seed values"
    - "A hash-diversity-gate report exists at .planning/realism-tests/hash-diversity-gate/results.md enumerating: per-row pass/fail status, per-row seed-set (truncated 12-char shas listed), per-row distinct count, downgrade list, re-run-recommendation list"
    - "Final coverage-check.md consistency review has been performed: every BACKED row has status + confidence-method + seeds-cited + backing-suite + backing-job + golden-sha256 populated (no row left half-filled); no row contradicts itself (UNBACKED rows have backing-suite = '—'; BACKED rows do not)"
    - "RSK cross-reference integrity check passes: every related-RSK-ids token in coverage-check.md resolves to a real RSK-NN entry in docs/phase-2/realism-risks-register.md"
    - "EXP-NN forward-reference integrity check: every EXP-<slug> parenthetical in coverage-check.md cell text either resolves to a registered EXP slug in the register OR points to a Phase 4 / Phase 5 work item documented in the relevant REQ-NN"
    - "All abbreviations expand on first use in the hash-diversity-gate results.md (BCa, IQR, MITIGATED, etc.) per CLAUDE.md"
  artifacts:
    - path: ".planning/realism-tests/hash-diversity-gate/results.md"
      provides: "COV-05 enforcement report — per-row pass/fail, downgrade list, re-run list, phase summary"
      min_lines: 60
    - path: "docs/phase-2/coverage-check.md"
      provides: "Final Phase 3 state of the coverage check — every BACKED row passes hash-diversity; every UNBACKED row points to a Phase 4 disclosure or Phase 5 carry-forward; every WEAK row has an annotation explaining why"
  key_links:
    - from: ".planning/realism-tests/hash-diversity-gate/results.md (per-row distinct-count entries)"
      to: ".planning/realism-tests/<sub-test>/<cell>.json (pricing_event_stream_sha256s arrays)"
      via: "per-cell sha256-array enumeration"
      pattern: "pricing_event_stream_sha256s"
    - from: "docs/phase-2/coverage-check.md (each downgraded row)"
      to: ".planning/realism-tests/hash-diversity-gate/results.md (the downgrade list section)"
      via: "annotation cell text"
      pattern: "hash collision detected"
---

<objective>
Wave 3 of Phase 3: apply the strict hash-diversity gate (COV-05, per
REQUIREMENTS.md + CONTEXT.md D-19) across every BACKED row in
docs/phase-2/coverage-check.md that was set by Wave 2's five sub-tasks;
downgrade rows that fail; perform a final consolidation pass to confirm
the coverage table is internally consistent and ready for Phase 4
(DOC-01 / DOC-02 / DOC-03) consumption.

This is the only sequential wave in Phase 3 — it runs AFTER all five
Wave 2 sub-tasks have landed (otherwise there is nothing to gate). One
task per CONTEXT.md D-31 ("Plan 03-03 (Wave 3, sequential)"); no
parallelism inside the wave.

Output: hash-diversity-gate report at `.planning/realism-tests/hash-diversity-gate/results.md`;
incremental in-place updates to `docs/phase-2/coverage-check.md`; `03-SUMMARY.md`
in the phase directory that VERIFY-PHASE consumes.

Cross-cutting truths re-stated (Wave 1 / Wave 2 set them; this wave enforces):
- The per-(job, seed) pricing_event_stream.sha256 is the input to the gate (RunSummary field).
- Strict gate semantics: distinct count must equal seeds-cited; any collapse → downgrade.
- Phase 3 suites are NOT goldens-pinned — the hash diversity gate is a Phase-3-specific
  per-row check, NOT a determinism golden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-targeted-cheap-tests/03-CONTEXT.md
@.planning/phases/03-targeted-cheap-tests/03-01-PLAN.md
@.planning/phases/03-targeted-cheap-tests/03-02-PLAN.md
@.planning/phases/02-coverage-check-skeleton/02-CONTEXT.md
@.planning/phases/01-register-inventory/01-02-SUMMARY.md
@docs/phase-2/realism-risks-register.md
@docs/phase-2/coverage-check.md

(interfaces — Wave 3 reads the artefact outputs of Wave 1 and Wave 2; produces a single new file plus edits to coverage-check.md.)

  From .planning/realism-tests/multi-seed-variance/sign-flip/CELL.json (Wave 2 Task A output):
    pricing_event_stream_sha256s — array of N hex strings.
    distinct_sha256_count — integer.
    n_seeds — integer.

  From .planning/realism-tests/multi-seed-variance/canonical/CELL.json (Wave 2 Task B):
    Same fields. Note: the control cell may have only samples, not paired output;
    still has pricing_event_stream_sha256s and distinct_sha256_count.

  From .planning/realism-tests/pool-number-sensitivity/PROFILE/JOB.json (Wave 2 Task C):
    pools100 sub-object — contains pricing_event_stream_sha256s and distinct_sha256_count.
    pools150 sub-object — same.
    Note: TEST-05 uses each profile's existing seed set (typically 3); the hash-diversity
    gate applies per (job, profile, pool-count). The gate failing on any (job, profile,
    pool-count) at either 100 or 150 pools downgrades the corresponding coverage row.

  From .planning/realism-tests/run-length-steady-state/MENU-OPTION/LENGTH.json (Wave 2 Task D):
    per_seed array — each element has seed + pricing_event_stream_sha256.
    Or equivalently a pricing_event_stream_sha256s array; both forms acceptable.

  From .planning/realism-tests/multiplier-floor-16-companion/CELL.json (Wave 2 Task E):
    pricing_event_stream_sha256s array; distinct_sha256_count.

  From docs/phase-2/coverage-check.md (existing Wave 2 output):
    Each BACKED row has populated columns including seeds-cited (integer)
    and golden-sha256 (truncated 12-char sha256 of seed=1). The full N-tuple
    of sha256 values is in the corresponding .planning/realism-tests/SUB-TEST/CELL.json.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply COV-05 hash-diversity gate + final coverage-check.md consolidation</name>
  <files>.planning/realism-tests/hash-diversity-gate/results.md, docs/phase-2/coverage-check.md</files>
  <read_first>
    - docs/phase-2/coverage-check.md (full file — every BACKED row's seeds-cited + backing-suite + backing-job are inputs to the gate)
    - docs/phase-2/realism-risks-register.md (RSK-NN entries — the RSK cross-reference integrity check reads this)
    - .planning/realism-tests/multi-seed-variance/sign-flip/*.json (Wave 2 Task A per-cell sha256 arrays)
    - .planning/realism-tests/multi-seed-variance/canonical/*.json (Wave 2 Task B per-cell sha256 arrays)
    - .planning/realism-tests/pool-number-sensitivity/**/*.json (Wave 2 Task C per (job, profile) sha256 arrays)
    - .planning/realism-tests/run-length-steady-state/**/*.json (Wave 2 Task D per (job, length) sha256 arrays)
    - .planning/realism-tests/multiplier-floor-16-companion/*.json AND results.md (Wave 2 Task E)
    - .planning/phases/03-targeted-cheap-tests/03-CONTEXT.md §"Plan wave decomposition" (D-31 wave-3 contract) and CLAUDE.md §"Conventions / gotchas" abbreviation rule
    - .planning/phases/02-coverage-check-skeleton/02-CONTEXT.md §"Hash-diversity gate (COV-05; Phase 3 enforces)" (D-19 strict rule)
  </read_first>
  <action>
    Step A — Enumerate every BACKED-labelled row in docs/phase-2/coverage-check.md. For each, identify the CLM-NN identifier, the seeds-cited value (integer N), the backing-suite (which Wave 2 sub-task it came from — TEST-03 / TEST-04 / TEST-05 / TEST-06 / TEST-07a), and the .planning/realism-tests/SUB-TEST/CELL.json path that holds the full sha256 array (the row's golden-sha256 column is only the seed=1 truncated hash; the full N-tuple is in the JSON artefact).

    Step B — For each BACKED row, run the strict hash-diversity gate (per CONTEXT.md D-19 + REQUIREMENTS.md COV-05):
      1. Open the corresponding .planning/realism-tests/SUB-TEST/CELL.json.
      2. Read the pricing_event_stream_sha256s array (and/or extract from per_seed entries for Task D's format).
      3. Compute distinct_count = len(set(array)).
      4. PASS iff distinct_count == seeds-cited. FAIL iff distinct_count is less than seeds-cited.

    For TEST-05 (pool-number-sensitivity), the gate applies per (job, profile, pool-count): a single coverage row may aggregate over multiple sub-cells; in that case the row PASSES iff ALL sub-cells pass. The hash-diversity gate is strict per CONTEXT.md D-19 ("a row can only be cited as BACKED when the distinct count = seed count" — no partial credit).

    Step C — Build downgrade list. For each FAIL row:
      - Default action per CONTEXT.md D-19 + CONTEXT.md §<specifics>: downgrade status from BACKED to WEAK with annotation `hash collision detected: distinct count = K less than N` in the confidence-method cell. Record the original sub-test + the offending cell's distinct count.
      - Alternative action per D-19: "re-run with different seed values" — flag for Phase 4 re-run. The hash-diversity-gate report records this alternative for the user / Phase 4 planner to choose between; the default is "downgrade to WEAK" because rerunning is Phase 4 work (the run-length default-slots raise pattern is the only Phase 3 work that triggers a re-run, and it's already handled in Wave 2 Task D).

    Step D — Apply downgrades to docs/phase-2/coverage-check.md in place. For each FAIL row:
      - Change status from BACKED to WEAK.
      - Append `; hash collision detected: distinct count = K, seeds-cited = N; see .planning/realism-tests/hash-diversity-gate/results.md` to the confidence-method cell.
      - Preserve all other columns (seeds-cited, backing-suite, backing-job, golden-sha256, non-welfare property columns, related-RSK-ids).

    Step E — Final coverage-check.md consistency review. Walk every row and assert:
      1. Internal consistency — every BACKED row has non-`—` values in status, confidence-method, seeds-cited, backing-suite, backing-job, golden-sha256. Every UNBACKED row has `—` in backing-suite + backing-job + seeds-cited + golden-sha256 (the row is awaiting evidence). Every WEAK row has at minimum status + confidence-method populated; if it has a backing-suite, the suite must exist.
      2. RSK cross-reference integrity — every token in related-RSK-ids must resolve to a real RSK-NN entry in docs/phase-2/realism-risks-register.md. Build the set of valid RSK-NN identifiers from the register's headings (`### RSK-` prefixed) and assert every coverage-check.md token is in that set.
      3. EXP-NN forward-reference integrity — every `EXP-` parenthetical in coverage-check.md cell text must either (a) resolve to an EXP slug in the register's RSK entries (per Phase 1's EXP-NN placeholders) OR (b) point to a Phase 4 / Phase 5 work item documented in REQUIREMENTS.md (e.g. EXP-window-length-anchor → Phase 4 / DOC-03).
      4. No CLM identifier renumbering — every CLM-NN that was present at Phase 2 close is still present at Phase 3 close with the same identifier (no removed rows; append-only per D-15).

    Step F — Write `.planning/realism-tests/hash-diversity-gate/results.md`. Header: title, date 2026-05-15, author "Phase 3 Plan 03-03 Task 1". Sections:
      1. Methodology — per CONTEXT.md D-19 strict gate semantics; per REQUIREMENTS.md COV-05. The gate reads the N-tuple of pricing_event_stream.sha256 values per BACKED row from .planning/realism-tests/SUB-TEST/CELL.json and asserts the distinct count equals seeds-cited.
      2. Per-row gate table — one row per BACKED coverage row. Columns: `CLM-NN | backing-suite | backing-job | seeds-cited (N) | distinct-sha256-count (K) | pass/fail | action-taken (none / downgrade-to-WEAK / re-run-recommendation)`. Include short-truncated 12-char sha256 array inline for traceability.
      3. Downgrade list — enumerated FAIL rows with their previous status, new status (WEAK), and the annotation added to confidence-method.
      4. Re-run recommendations — any FAIL row where the operator MAY prefer to re-run with different seed values rather than accept the WEAK downgrade. Phase 4 / disclosure-paragraph reviewer chooses.
      5. Consistency-check report — results of Steps E1–E4 above. Pass/fail per check.
      6. Phase 3 closeout statement — total BACKED rows after Wave 3, total WEAK (including post-downgrade), total UNBACKED, total OUT-OF-SCOPE. Pointer to Phase 4 for the disclosure-paragraph work needed on any LIVE→DISCLOSED entries surfaced by Wave 2 verdicts.
      7. Abbreviation glossary (per CLAUDE.md) — expand each acronym on first use within this document: BCa (Bias-corrected and accelerated), IQR (Inter-Quartile Range), CLM (Claim identifier), RSK (Risk identifier), EXP (Experiment slug), MITIGATED, DISCLOSED, REPLICATES, INVERTS.

    Step G — Verify the consistency-check passes by re-reading the updated coverage-check.md and re-running the RSK + EXP cross-reference walks. If any check fails, document in results.md and surface as a Phase 3 blocker rather than silently fix (some failures may require register changes that are out of Plan 03-03's scope).

    Implementation note: this task is mostly documentation work + small file edits + a few sha256-count assertions. The executor MAY implement it as a Python script under `sim-rs/scripts/hash-diversity-gate.py` that reads .planning/realism-tests/**/*.json, queries every BACKED row in coverage-check.md, applies the gate, writes the results.md, and emits the patched coverage-check.md — OR implement it as a hand-edited pass. Either is acceptable; if scripted, commit the script as a Phase 3 artefact.
  </action>
  <verify>
    <automated>test -f .planning/realism-tests/hash-diversity-gate/results.md && grep -qE "Per-row gate table|Downgrade list|Consistency-check report|Phase 3 closeout" .planning/realism-tests/hash-diversity-gate/results.md && python3 -c "import re,sys; rsk_set=set(re.findall(r'^### (RSK-[a-z0-9-]+)', open('docs/phase-2/realism-risks-register.md').read(), flags=re.MULTILINE)); cc=open('docs/phase-2/coverage-check.md').read(); cited=set(re.findall(r'(RSK-[a-z0-9-]+)', cc)); orphans=cited-rsk_set; sys.exit(0 if not orphans else (print('Orphan RSK refs:', orphans) or 1))"</automated>
  </verify>
  <acceptance_criteria>
    - File presence: `test -f .planning/realism-tests/hash-diversity-gate/results.md` exits 0.
    - Required sections present: `for s in "Methodology" "Per-row gate table" "Downgrade list" "Re-run recommendations" "Consistency-check report" "Phase 3 closeout" "Abbreviation glossary"; do grep -q "$s" .planning/realism-tests/hash-diversity-gate/results.md || exit 1; done` exits 0.
    - Per-row gate table has at least one row per BACKED-or-downgraded coverage row: `awk '/Per-row gate table/{flag=1} flag && /^\| CLM-/{c++} END{exit (c >= 1 ? 0 : 1)}' .planning/realism-tests/hash-diversity-gate/results.md` exits 0.
    - RSK integrity check passes: the Python one-liner in `<verify>` exits 0 (no orphan RSK references in coverage-check.md).
    - EXP integrity check passes: `python3 -c "import re,sys; reg=open('docs/phase-2/realism-risks-register.md').read(); req=open('.planning/REQUIREMENTS.md').read(); known_exp=set(re.findall(r'(EXP-[a-z0-9-]+)', reg)) | set(re.findall(r'(EXP-[a-z0-9-]+)', req)); cc=open('docs/phase-2/coverage-check.md').read(); cited=set(re.findall(r'(EXP-[a-z0-9-]+)', cc)); orphans=cited-known_exp-{'EXP-slug'}; sys.exit(0 if not orphans else (print('Orphan EXP refs:', orphans) or 1))"` exits 0 (where `EXP-slug` is the template placeholder allowed in the legend).
    - CLM-NN append-only check: every CLM-NN present at Phase 2 close (CLM-01..CLM-55 per coverage-check.md at end of Phase 2) is still present: `for n in $(seq -w 1 55); do grep -qE "^\| CLM-$n \|" docs/phase-2/coverage-check.md || { echo "missing CLM-$n"; exit 1; }; done` exits 0.
    - Every BACKED row in coverage-check.md has non-`—` golden-sha256 (after Wave 3): `python3 -c "import re,sys; r=re.findall(r'^\| (CLM-\d+) \| .* \| (BACKED) \|.*\| ([\w—]+) \|', open('docs/phase-2/coverage-check.md').read(), flags=re.MULTILINE); bad=[c for c,s,g in r if s=='BACKED' and g=='—']; sys.exit(0 if not bad else (print('BACKED with empty golden-sha256:', bad) or 1))"` exits 0.
    - Every UNBACKED row in coverage-check.md has `—` in backing-suite (no half-filled UNBACKED rows): see same regex pattern adjusted; spot-check via grep is acceptable if the strict regex is brittle.
    - No row left in BACKED state with `seeds-cited = —`: `grep -E "^\| CLM-[0-9]+ \|" docs/phase-2/coverage-check.md | grep -E "\| BACKED \|" | grep -E "\| — \|.*\| — \|" | wc -l | awk '{exit ($1 == 0 ? 0 : 1)}'` exits 0 (loose check; tighten if the executor's column layout permits).
    - Phase 3 SUMMARY pointer: `grep -q "03-SUMMARY.md\|Phase 3 closeout" .planning/realism-tests/hash-diversity-gate/results.md` exits 0.
  </acceptance_criteria>
  <done>
    Hash-diversity gate applied to every BACKED row in coverage-check.md; downgrades applied in place where the gate fails; .planning/realism-tests/hash-diversity-gate/results.md exists with all 7 required sections; final consistency review passes (RSK integrity, EXP integrity, CLM append-only, no half-filled rows). Phase 3 is ready for verify-phase consumption.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:

- `.planning/realism-tests/hash-diversity-gate/results.md` exists with all 7 required sections (Methodology, Per-row gate table, Downgrade list, Re-run recommendations, Consistency-check report, Phase 3 closeout, Abbreviation glossary).
- Every BACKED row in `docs/phase-2/coverage-check.md` has passed the strict hash-diversity gate (or has been downgraded to WEAK with an annotation).
- RSK cross-reference integrity check passes (no orphan RSK-NN tokens in coverage-check.md).
- EXP cross-reference integrity check passes (no orphan EXP-NN tokens beyond the legend's template `EXP-slug` placeholder).
- CLM-NN append-only check passes (CLM-01..CLM-55 from Phase 2 close all still present).
- `cd sim-rs && cargo test --workspace --release` exits 0 (M2/M3 goldens unaffected by documentation-only Wave 3 work).
- `cd sim-rs && cargo test --release -- --ignored determinism` exits 0 (M5 suite goldens unaffected; any Task-D-driven regenerations were committed in Wave 2 not Wave 3).
- Phase 3 deliverable list complete:
  * `sim-rs/sim-cli/src/metrics/paired_bootstrap.rs` (Wave 1)
  * `sim-rs/parameters/phase-2-sweep/topology-realistic-150.yaml` (Wave 1)
  * 6+ phase-3-*.yaml suite YAMLs (Wave 1 + Wave 2 — scoping + sign-flip + canonical + pool-number sub-suites + run-length + multiplier-floor-16-companion)
  * `.planning/realism-tests/multi-seed-variance/` (Wave 2 Tasks A + B)
  * `.planning/realism-tests/pool-number-sensitivity/` (Wave 2 Task C)
  * `.planning/realism-tests/run-length-steady-state/` (Wave 2 Task D)
  * `.planning/realism-tests/multiplier-floor-16-companion/` (Wave 2 Task E)
  * `.planning/realism-tests/hash-diversity-gate/` (Wave 3)
  * `docs/phase-2/coverage-check.md` updated incrementally across Waves 2 and 3 per D-27
</verification>

<success_criteria>
Wave 3 closeout — Phase 3 is complete:

- REQUIREMENTS.md COV-05 satisfied: strict hash-diversity gate applied to every BACKED row; failures downgraded; report at `.planning/realism-tests/hash-diversity-gate/results.md`.
- Coverage-check.md is internally consistent (RSK integrity, EXP integrity, CLM append-only, no half-filled rows).
- All 7 Phase 3 success criteria from ROADMAP.md Phase 3 are demonstrably met (TEST-01 lib exists + tested; TEST-02 scoping done + chose N; TEST-03 4 sign-flip cells have BCa CI + verdict; TEST-04 5 canonical cells have median + IQR + sign-coherence + BCa CI; TEST-05 pool-number sensitivity reports MITIGATED-or-LIVE-going-to-DISCLOSED; TEST-06 4 menu-option jobs at 3 slot lengths have STEADY verdict and any default-raise is committed; COV-05 hash-diversity gate applied across all BACKED rows).
- TEST-07a sub-requirement (Phase 1 plan-02 SUMMARY) resolved: 2 multiplier-floor-16 companion jobs run; qualitative REPLICATES / INVERTS verdict recorded.
- 03-SUMMARY.md exists in the phase directory and is ready for /gsd-verify-phase consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/03-targeted-cheap-tests/03-SUMMARY.md` (note: this is the phase-level SUMMARY for verify-phase consumption, NOT a per-plan SUMMARY) consolidating Plans 03-01, 03-02, 03-03. Sections:
- Phase deliverables list (each artefact with its path and a one-line description).
- Per-requirement verdict roll-up: TEST-01 (delivered), TEST-02 (scoping wall-clock + N chosen), TEST-03 (4-cell verdict roll-up), TEST-04 (5-cell verdict roll-up), TEST-05 (aggregation verdict + any (job, profile) failures), TEST-06 (per-(job, length) STEADY verdicts + default-raises), TEST-07a (REPLICATES / INVERTS), COV-05 (gate pass count + downgrade count).
- Coverage-check.md state at Phase 3 close: total BACKED / WEAK / UNBACKED / OUT-OF-SCOPE counts; per-RSK-NN coverage status (which LIVE entries are now MITIGATED-eligible, which are pending Phase 4 disclosure).
- Bootstrap-seed allocation table consolidated across all Wave 2 sub-tasks.
- Phase 4 / Phase 5 carry-forwards: every UNBACKED row's `EXP-slug → Phase X` mapping enumerated.
- Cross-cutting truth verification (D-23 + D-24 + abbreviation rule + Phase 3 suites not goldens-pinned).
- Tag/commit advisory (Phase 4 / DOC-01 will start from this commit).
</output>
